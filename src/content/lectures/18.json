{
  "id": "18",
  "title": {
    "en": "Stage 3 Capstone: Complete Attention Module",
    "ro": "Proiectul final al Etapei 3: Modulul complet de Attention",
    "el": "Ολοκληρωτικό έργο Σταδίου 3: Πλήρης μονάδα Attention"
  },
  "content": {
    "en": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">LECTURE 18 <span class=\"stage-badge\">Stage 3: Attention</span></div>\n  <h1 class=\"lecture-title\">Stage 3 Capstone: Complete Attention Module</h1>\n  <div class=\"lecture-subtitle\">Building a production-ready attention block with residual connections and layer normalization</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 50 min read</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Hands-on coding</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Advanced</span>\n  </div>\n</div>\n\n<h2>From Attention to the Attention Block</h2>\n\n<p>Over the past five lectures, we have built attention from the ground up: dot products, scaling, softmax, queries/keys/values, causal masking, and multi-head splitting. But raw attention alone is not how it appears inside an actual Transformer. In real architectures, attention is always wrapped in two critical components:</p>\n\n<ol>\n  <li><strong>Residual connections</strong> (also called skip connections) &mdash; which add the input directly to the attention output</li>\n  <li><strong>Layer normalization</strong> &mdash; which stabilizes the values flowing through the network</li>\n</ol>\n\n<p>Together, these form the <strong>Attention Block</strong>, the fundamental building block that is repeated dozens of times in a Transformer. GPT-2 Small has 12 of these blocks stacked; GPT-3 has 96; Llama 3 70B has 80.</p>\n\n<div class=\"info-box\"><strong>Key Insight:</strong> Without residual connections, deep Transformers cannot train &mdash; gradients vanish by the time they propagate through dozens of layers. Without layer normalization, the activations grow or shrink exponentially through the layers. These are not optional extras; they are essential to making deep attention networks work.</div>\n\n<h2>Residual Connections: Why They Matter</h2>\n\n<p>A residual connection is disarmingly simple: instead of computing <code>output = f(x)</code>, compute <code>output = x + f(x)</code>. The input \"skips\" around the transformation and is added back to the result.</p>\n\n<p>Why this matters for deep networks:</p>\n\n<ul>\n  <li><strong>Gradient highway:</strong> During backpropagation, the gradient flows directly through the addition, providing a clean path from the loss all the way back to early layers. Without it, gradients must pass through every layer's transformation, each potentially shrinking the signal.</li>\n  <li><strong>Easy identity:</strong> If a layer learns f(x) = 0, the block computes the identity function. This means adding more layers can never hurt (in theory) &mdash; the network can always learn to \"skip\" unnecessary layers.</li>\n  <li><strong>Incremental refinement:</strong> Each block learns a small <em>change</em> to the representation rather than a complete transformation. This is more stable and easier to optimize.</li>\n</ul>\n\n<p>Think of it like editing a document: instead of rewriting from scratch each time (which risks losing information), you start with the current version and apply edits. The residual connection ensures you always keep the current version.</p>\n\n<h2>Layer Normalization: Stabilizing Activations</h2>\n\n<p>Layer normalization normalizes the values across the feature dimension for each token independently. Given a vector x of dimension d_model:</p>\n\n<p style=\"text-align: center; font-size: 1.1em;\">\n  LayerNorm(x) = &gamma; &odot; (x - &mu;) / (&sigma; + &epsilon;) + &beta;\n</p>\n\n<p>Where &mu; and &sigma; are the mean and standard deviation computed across the d_model features, and &gamma; (scale) and &beta; (shift) are learned parameters.</p>\n\n<p>There are two common placements for layer normalization:</p>\n\n<ul>\n  <li><strong>Post-norm (original Transformer):</strong> <code>output = LayerNorm(x + Attention(x))</code></li>\n  <li><strong>Pre-norm (GPT-2, Llama, modern models):</strong> <code>output = x + Attention(LayerNorm(x))</code></li>\n</ul>\n\n<p>Pre-norm has become the standard because it provides more stable training, especially for very deep models. The intuition is that normalizing <em>before</em> the attention computation ensures the Q, K, V projections receive well-conditioned inputs.</p>\n\n<div class=\"tip-box\"><strong>Pro Tip:</strong> GPT-2 and most modern LLMs use pre-norm (also called \"Pre-LN Transformer\"). This was one of the key insights that made training very deep Transformers reliable. If you see \"Transformer\" in a modern paper, it almost certainly uses pre-norm.</div>\n\n<h2>Code: Complete AttentionBlock with LayerNorm + Residual</h2>\n\n<p>Let us build the complete attention block that you would find inside GPT-2, Llama, or any modern Transformer. This integrates everything we have learned in Stage 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    \"\"\"Layer Normalization as used in Transformers.\"\"\"\n\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)   # learnable scale\n        self.beta = np.zeros(d_model)   # learnable shift\n\n    def forward(self, x):\n        \"\"\"Normalize across the last dimension (features).\n\n        x shape: (batch, seq_len, d_model) or (batch, d_model)\n        \"\"\"\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        x_norm = (x - mean) / np.sqrt(var + self.eps)\n        return self.gamma * x_norm + self.beta\n\nclass MultiHeadAttention:\n    \"\"\"Multi-head self-attention.\"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None, return_attention=False):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn_weights = softmax(scores)\n        context = (attn_weights @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        output = context @ self.W_o\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\nclass AttentionBlock:\n    \"\"\"Complete attention block with pre-norm and residual connection.\n\n    This is the building block of GPT-2, Llama, and modern Transformers:\n        output = x + MultiHeadAttention(LayerNorm(x))\n\n    (Pre-norm variant, which is the modern standard.)\n    \"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.layer_norm = LayerNorm(d_model)\n        self.attention = MultiHeadAttention(d_model, n_heads, seed=seed)\n\n    def forward(self, x, mask=None, return_attention=False):\n        # Pre-norm: normalize before attention\n        x_norm = self.layer_norm.forward(x)\n\n        # Attention\n        if return_attention:\n            attn_out, attn_weights = self.attention.forward(\n                x_norm, mask=mask, return_attention=True\n            )\n        else:\n            attn_out = self.attention.forward(x_norm, mask=mask)\n\n        # Residual connection: add input to attention output\n        output = x + attn_out\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\n# Build and test the attention block\nd_model = 32\nn_heads = 4\nseq_len = 6\nbatch_size = 2\n\nnp.random.seed(42)\nx = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model=d_model, n_heads=n_heads, seed=0)\noutput, attn_weights = block.forward(x, mask=causal_mask, return_attention=True)\n\nprint(\"=== AttentionBlock Test ===\")\nprint(f\"Input shape:  {x.shape}\")\nprint(f\"Output shape: {output.shape}\")\nprint(f\"Attention weights shape: {attn_weights.shape}\")\nprint()\n\n# Verify residual connection: output should be close to input\n# (since weights are random and small, attention contribution is modest)\nresidual_magnitude = np.mean(np.abs(output - x))\ninput_magnitude = np.mean(np.abs(x))\nprint(f\"Input magnitude:    {input_magnitude:.4f}\")\nprint(f\"Residual change:    {residual_magnitude:.4f}\")\nprint(f\"Relative change:    {residual_magnitude/input_magnitude:.2%}\")\nprint(\"  (Small relative change shows residual connection preserves input)\")\nprint()\n\n# Verify layer norm: check that normalized values have mean~0, std~1\nx_norm = block.layer_norm.forward(x)\nprint(f\"Before LayerNorm - mean: {np.mean(x):.4f}, std: {np.std(x):.4f}\")\nprint(f\"After LayerNorm  - mean: {np.mean(x_norm):.6f}, std: {np.std(x_norm):.4f}\")\nprint()\n\n# Check per-token normalization\nfor t in range(min(3, seq_len)):\n    tok_norm = x_norm[0, t]\n    print(f\"  Token {t}: mean={np.mean(tok_norm):.6f}, std={np.std(tok_norm):.4f}\")\n</code></pre></div>\n\n<h2>Processing Batches Through the Attention Block</h2>\n\n<p>In practice, we process multiple sequences simultaneously (batched inference). Let us verify that our attention block handles batches correctly and that each sequence in the batch is processed independently.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n\n    def forward(self, x, mask=None):\n        return x + self.attn.forward(self.ln.forward(x), mask)\n\n# Test batch processing\nd_model = 32\nn_heads = 4\nseq_len = 8\nbatch_size = 4\n\nnp.random.seed(42)\nx_batch = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Process as a batch\noutput_batch = block.forward(x_batch, mask=causal_mask)\n\n# Process each sequence individually\noutputs_individual = []\nfor i in range(batch_size):\n    x_single = x_batch[i:i+1]  # (1, seq_len, d_model)\n    out_single = block.forward(x_single, mask=causal_mask)\n    outputs_individual.append(out_single)\n\noutput_individual = np.concatenate(outputs_individual, axis=0)\n\nprint(\"=== Batch Processing Verification ===\")\nprint(f\"Batch input shape:  {x_batch.shape}\")\nprint(f\"Batch output shape: {output_batch.shape}\")\nprint()\n\n# Verify batch == individual processing\nmatch = np.allclose(output_batch, output_individual, atol=1e-10)\nmax_diff = np.max(np.abs(output_batch - output_individual))\nprint(f\"Batch matches individual processing: {match}\")\nprint(f\"Maximum difference: {max_diff:.2e}\")\nprint()\n\n# Show that sequences in the batch are independent\nprint(\"Per-sequence output statistics:\")\nfor i in range(batch_size):\n    out = output_batch[i]\n    print(f\"  Sequence {i}: mean={np.mean(out):.4f}, \"\n          f\"std={np.std(out):.4f}, \"\n          f\"norm={np.linalg.norm(out):.4f}\")\n\nprint()\n# Stack multiple attention blocks (like a real Transformer)\nn_layers = 6\nblocks = [AttentionBlock(d_model, n_heads, seed=i*10) for i in range(n_layers)]\n\nx = x_batch.copy()\nprint(f\"=== Stacking {n_layers} Attention Blocks ===\")\nprint(f\"{'Layer':>6} | {'Mean':>8} | {'Std':>8} | {'Norm':>8} | {'Change':>8}\")\nprint(f\"{'-'*50}\")\nprint(f\"{'Input':>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | {np.linalg.norm(x):>8.2f} | {'---':>8}\")\n\nfor layer_idx, blk in enumerate(blocks):\n    x_prev = x.copy()\n    x = blk.forward(x, mask=causal_mask)\n    change = np.mean(np.abs(x - x_prev))\n    print(f\"{layer_idx:>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | \"\n          f\"{np.linalg.norm(x):>8.2f} | {change:>8.4f}\")\n\nprint()\nprint(\"Notice: LayerNorm + residual keeps values stable across layers.\")\nprint(\"Without them, values would explode or vanish in deep stacks.\")\n</code></pre></div>\n\n<h2>The Full Attention Pipeline</h2>\n\n<p>Let us now visualize the complete flow through the attention block, from raw input tokens all the way through to the output. This final code example ties together everything from Stage 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o, attn\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n        self.d_model = d_model\n        self.n_heads = n_heads\n\n    def forward(self, x, mask=None):\n        x_norm = self.ln.forward(x)\n        attn_out, attn_weights = self.attn.forward(x_norm, mask)\n        return x + attn_out, attn_weights\n\n# === Full Pipeline Visualization ===\ntokens = [\"The\", \"cat\", \"sat\", \"on\", \"the\", \"mat\"]\nd_model = 32\nn_heads = 4\nseq_len = len(tokens)\n\nnp.random.seed(42)\n\n# Step 1: Create token embeddings (simulated)\nvocab_size = 100\nembedding_matrix = np.random.randn(vocab_size, d_model) * 0.1\ntoken_ids = [10, 25, 60, 45, 10, 80]  # simulated token IDs\nx = embedding_matrix[token_ids].reshape(1, seq_len, d_model)\n\nprint(\"=\" * 60)\nprint(\"FULL ATTENTION PIPELINE VISUALIZATION\")\nprint(\"=\" * 60)\nprint(f\"Tokens: {tokens}\")\nprint(f\"d_model={d_model}, n_heads={n_heads}, d_k={d_model//n_heads}\")\nprint()\n\n# Step 2: Show the pipeline\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Trace each step\nprint(\"--- Step 1: Input Embeddings ---\")\nprint(f\"  Shape: {x.shape}\")\nprint(f\"  Token 'cat' embedding (first 8 dims): {x[0, 1, :8].round(4)}\")\nprint()\n\nx_norm = block.ln.forward(x)\nprint(\"--- Step 2: Layer Normalization ---\")\nprint(f\"  Shape: {x_norm.shape}\")\nfor t in range(seq_len):\n    m = np.mean(x_norm[0, t])\n    s = np.std(x_norm[0, t])\n    print(f\"  '{tokens[t]}': mean={m:.6f}, std={s:.4f}\")\nprint()\n\n# Show Q, K, V projections\nQ = x_norm @ block.attn.W_q\nK = x_norm @ block.attn.W_k\nV = x_norm @ block.attn.W_v\nprint(\"--- Step 3: Q, K, V Projections ---\")\nprint(f\"  Q shape: {Q.shape}, K shape: {K.shape}, V shape: {V.shape}\")\nprint(f\"  Q norm per token: {[f'{np.linalg.norm(Q[0,t]):.2f}' for t in range(seq_len)]}\")\nprint()\n\n# Show attention patterns\noutput, attn_weights = block.forward(x, mask=causal_mask)\n\nprint(\"--- Step 4: Attention Patterns (per head) ---\")\nfor h in range(n_heads):\n    print(f\"  Head {h} - What does 'mat' (last token) attend to?\")\n    last_attn = attn_weights[0, h, -1, :]\n    ranked = np.argsort(last_attn)[::-1]\n    for idx in ranked[:3]:\n        print(f\"    -> '{tokens[idx]}': {last_attn[idx]:.4f}\")\nprint()\n\nprint(\"--- Step 5: Residual Connection ---\")\nresidual_diff = output - x\nprint(f\"  Output = Input + Attention(LayerNorm(Input))\")\nprint(f\"  Mean absolute change per token:\")\nfor t in range(seq_len):\n    change = np.mean(np.abs(residual_diff[0, t]))\n    print(f\"    '{tokens[t]}': {change:.4f}\")\nprint()\n\n# Show that \"the\" appears twice and gets different representations\nprint(\"--- Step 6: Contextual Representations ---\")\nprint(f\"  'the' appears at positions 0 and 4.\")\nprint(f\"  Input embeddings are IDENTICAL (same token ID):\")\nprint(f\"    Position 0: {x[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {x[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(x[0, 0], x[0, 4])}\")\nprint()\nprint(f\"  After attention, they are DIFFERENT (context-aware):\")\nprint(f\"    Position 0: {output[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {output[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(output[0, 0], output[0, 4])}\")\ncos_sim = np.dot(output[0,0], output[0,4]) / (np.linalg.norm(output[0,0]) * np.linalg.norm(output[0,4]))\nprint(f\"    Cosine similarity: {cos_sim:.4f}\")\nprint()\nprint(\"This is the fundamental power of attention: identical tokens\")\nprint(\"become different based on their surrounding context.\")\n</code></pre></div>\n\n<div class=\"info-box\"><strong>Key Insight:</strong> The demonstration above shows the most important property of attention: identical input tokens (\"the\" at positions 0 and 4) produce different output representations because they attend to different contexts. This is what makes attention-based models so powerful for language &mdash; every token's representation is dynamically shaped by its context.</div>\n\n<h2>Stage 3 Summary: The Journey Through Attention</h2>\n\n<p>We have covered an enormous amount of ground in Stage 3. Let us review the complete journey from basic intuition to production-ready attention.</p>\n\n<h3>Lecture 13: Attention Intuition &amp; Dot-Product Scores</h3>\n<p>We started with the fundamental question: how can a model decide which parts of the input to focus on? We built intuition through the dictionary lookup analogy and implemented basic dot-product similarity as the foundation of attention scoring. We saw that the dot product naturally measures alignment between vectors, making it a perfect primitive for \"how related are these two tokens?\"</p>\n\n<h3>Lecture 14: Scaled Dot-Product Attention</h3>\n<p>We formalized attention with the complete equation: Attention(Q, K, V) = softmax(QK&#x1D40; / &radic;d_k) V. We explored why scaling by &radic;d_k is essential (preventing softmax saturation in high dimensions), implemented causal masking for autoregressive models, and saw how the softmax converts raw scores into a proper probability distribution over context tokens.</p>\n\n<h3>Lecture 15: Query, Key, Value Projections</h3>\n<p>We introduced learned linear projections that transform input embeddings into separate Query, Key, and Value representations. This was the crucial step from \"comparing raw embeddings\" to \"learning what to compare.\" The Q/K/V decomposition gives the model the ability to learn <em>what information to look for</em> (Q), <em>what information to advertise</em> (K), and <em>what information to transmit</em> (V) independently.</p>\n\n<h3>Lecture 16: Multi-Head Attention</h3>\n<p>We split attention into multiple parallel heads, each operating on a subspace of the full embedding dimension. This allows the model to simultaneously attend to different types of relationships &mdash; syntactic, semantic, positional &mdash; without interference. The parameter count stays the same; only the structure changes.</p>\n\n<h3>Lecture 17: Attention Variants &amp; Optimizations</h3>\n<p>We surveyed the practical innovations that make attention work at scale: FlashAttention for memory-efficient computation, Grouped-Query Attention for smaller KV-caches, KV-caching for faster inference, RoPE for relative position encoding, and sliding window attention for linear-cost long contexts.</p>\n\n<h3>Lecture 18: Complete Attention Module (This Lecture)</h3>\n<p>We wrapped attention in residual connections and layer normalization to create the complete Attention Block &mdash; the fundamental building unit of all Transformer architectures. We verified that this block maintains stable activations across deep stacks and produces context-dependent representations.</p>\n\n<div class=\"warning-box\"><strong>Important:</strong> Everything in Stage 3 is about a single component: the attention block. A complete Transformer layer also includes a feed-forward network (MLP) with its own residual connection and layer norm. That is where we are headed in Stage 4. The attention block handles \"communication\" between tokens; the feed-forward block handles \"computation\" within each token.</div>\n\n<h2>Bridge to Stage 4: The Transformer Architecture</h2>\n\n<p>With the attention block complete, we are ready to build the full Transformer. In Stage 4, we will:</p>\n\n<ol>\n  <li><strong>Add the feed-forward network:</strong> A two-layer MLP (with GELU activation) that processes each token independently after attention has mixed information between tokens</li>\n  <li><strong>Stack layers:</strong> Repeat the [Attention Block + FFN Block] pattern to build deep networks</li>\n  <li><strong>Add the final prediction head:</strong> Map from hidden states back to vocabulary probabilities</li>\n  <li><strong>Build GPT-2 from scratch:</strong> Assemble all components into a working language model</li>\n</ol>\n\n<p>The attention mechanism you now understand is the heart of every Transformer. Everything else is built around it. You have mastered the hardest part of the architecture &mdash; the rest is more straightforward engineering.</p>\n\n<div class=\"key-takeaways\">\n  <h3>Key Takeaways</h3>\n  <ul>\n    <li>The Attention Block wraps multi-head attention in layer normalization and a residual connection: <code>output = x + MHA(LayerNorm(x))</code> (pre-norm variant).</li>\n    <li>Residual connections provide a gradient highway through deep networks and let each layer learn incremental refinements rather than full transformations.</li>\n    <li>Layer normalization stabilizes activations by normalizing across features for each token, preventing values from exploding or vanishing through many layers.</li>\n    <li>Pre-norm (normalize before attention) is the modern standard, used in GPT-2, Llama, and most current architectures because it provides more stable training.</li>\n    <li>The attention block produces contextual representations: identical input tokens receive different outputs based on their surrounding context.</li>\n    <li>Stage 3 covered the complete attention story: dot-product scoring, scaling, softmax, Q/K/V projections, multi-head splitting, optimizations (GQA, KV-cache, RoPE), and the full attention block with residual + layer norm.</li>\n  </ul>\n</div>\n\n\n\n<div class=\"quiz-section\" data-quiz-id=\"l18-en\">\n<h3>&#x1F4DD; Lecture 18 Quiz</h3>\n<div class=\"quiz-instructions\">Select the best answer(s). Click \"Submit\" to see your score.</div>\n<div class=\"mcq-question\" data-correct='[1]' data-qidx=\"0\">\n  <div class=\"q-header\"><div class=\"q-number\">1</div><div class=\"q-text\">What is the purpose of a residual (skip) connection in a transformer block?</div></div>\n  <div class=\"mcq-options\">\n    <div class=\"mcq-option\" data-idx=\"0\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">A</div><div class=\"option-text\">To reduce the number of parameters</div></div>\n    <div class=\"mcq-option\" data-idx=\"1\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">B</div><div class=\"option-text\">To help gradients flow through deep networks by adding the input directly to the output: y = x + f(x)</div></div>\n    <div class=\"mcq-option\" data-idx=\"2\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">C</div><div class=\"option-text\">To replace the attention mechanism</div></div>\n    <div class=\"mcq-option\" data-idx=\"3\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">D</div><div class=\"option-text\">To increase the sequence length</div></div>\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explanation:</strong> Residual connections add the input to the sub-layer output: y = x + Attention(x). This provides a gradient highway, preventing vanishing gradients in deep networks.</div>\n</div>\n<div class=\"mcq-question\" data-correct='[0]' data-qidx=\"1\">\n  <div class=\"q-header\"><div class=\"q-number\">2</div><div class=\"q-text\">Where does Layer Normalization (pre-norm) go in a transformer block?</div></div>\n  <div class=\"mcq-options\">\n    <div class=\"mcq-option\" data-idx=\"0\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">A</div><div class=\"option-text\">Before the attention sub-layer: y = x + Attention(LayerNorm(x))</div></div>\n    <div class=\"mcq-option\" data-idx=\"1\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">B</div><div class=\"option-text\">After the entire transformer block</div></div>\n    <div class=\"mcq-option\" data-idx=\"2\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">C</div><div class=\"option-text\">Only during inference</div></div>\n    <div class=\"mcq-option\" data-idx=\"3\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">D</div><div class=\"option-text\">Between multi-head attention and dropout</div></div>\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explanation:</strong> Pre-norm applies LayerNorm before each sub-layer. GPT-2 and most modern LLMs use pre-norm because it provides more stable training dynamics.</div>\n</div>\n<div class=\"mcq-question\" data-correct='[2]' data-qidx=\"2\">\n  <div class=\"q-header\"><div class=\"q-number\">3</div><div class=\"q-text\">What does the complete AttentionBlock combine?</div></div>\n  <div class=\"mcq-options\">\n    <div class=\"mcq-option\" data-idx=\"0\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">A</div><div class=\"option-text\">Only multi-head attention</div></div>\n    <div class=\"mcq-option\" data-idx=\"1\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">B</div><div class=\"option-text\">Attention and feed-forward network only</div></div>\n    <div class=\"mcq-option\" data-idx=\"2\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">C</div><div class=\"option-text\">Layer normalization, multi-head causal self-attention, dropout, and residual connection</div></div>\n    <div class=\"mcq-option\" data-idx=\"3\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">D</div><div class=\"option-text\">Tokenizer and embedding layer</div></div>\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explanation:</strong> The AttentionBlock wraps: LayerNorm -> MultiHead Causal Self-Attention -> Dropout -> Residual Add. This is one half of a full transformer block (the other half is the feed-forward network).</div>\n</div>\n<div class=\"mcq-question\" data-correct='[0, 1, 3]' data-qidx=\"3\">\n  <div class=\"q-header\"><div class=\"q-number\">4</div><div class=\"q-text\">Which concepts from Stage 3 are combined in the final attention module?<span class=\"q-multi-hint\">Select all that apply</span></div></div>\n  <div class=\"mcq-options\">\n    <div class=\"mcq-option\" data-idx=\"0\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">A</div><div class=\"option-text\">Scaled dot-product attention (L13)</div></div>\n    <div class=\"mcq-option\" data-idx=\"1\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">B</div><div class=\"option-text\">Causal masking (L15)</div></div>\n    <div class=\"mcq-option\" data-idx=\"2\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">C</div><div class=\"option-text\">BPE tokenization (L8)</div></div>\n    <div class=\"mcq-option\" data-idx=\"3\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">D</div><div class=\"option-text\">Multi-head splitting (L16)</div></div>\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explanation:</strong> The final attention module combines scaled dot-product attention, QKV projections, causal masking, and multi-head splitting. Tokenization belongs to Stage 2.</div>\n</div>\n<div class=\"mcq-question\" data-correct='[3]' data-qidx=\"4\">\n  <div class=\"q-header\"><div class=\"q-number\">5</div><div class=\"q-text\">What comes after Stage 3 (Attention) in building a GPT model?</div></div>\n  <div class=\"mcq-options\">\n    <div class=\"mcq-option\" data-idx=\"0\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">A</div><div class=\"option-text\">Training the model immediately</div></div>\n    <div class=\"mcq-option\" data-idx=\"1\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">B</div><div class=\"option-text\">Deploying the model to production</div></div>\n    <div class=\"mcq-option\" data-idx=\"2\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">C</div><div class=\"option-text\">Adding more attention layers</div></div>\n    <div class=\"mcq-option\" data-idx=\"3\" tabindex=\"0\" role=\"checkbox\" aria-checked=\"false\"><div class=\"option-letter\">D</div><div class=\"option-text\">Stage 4: Building the full Transformer architecture — adding feed-forward networks, stacking blocks, and output head</div></div>\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explanation:</strong> Stage 4 adds the feed-forward network, stacks multiple transformer blocks, and adds the language model output head to complete the GPT architecture.</div>\n</div>\n<div class=\"quiz-controls\">\n  <button class=\"quiz-submit-btn\" onclick=\"submitQuiz(this)\">Submit Quiz</button>\n  <button class=\"quiz-reveal-btn\" onclick=\"revealQuiz(this)\">Show Answers</button>\n  <button class=\"quiz-reset-btn\" onclick=\"resetQuiz(this)\">Try Again</button>\n</div>\n<div class=\"quiz-results\"></div>\n</div>\n\n${navButtons('17', '19')}\n</div>",
    "ro": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">LECȚIA 18 <span class=\"stage-badge\">Etapa 3: Attention</span></div>\n  <h1 class=\"lecture-title\">Proiectul final al Etapei 3: Modulul complet de Attention</h1>\n  <div class=\"lecture-subtitle\">Construirea unui bloc de attention pregătit pentru producție cu conexiuni reziduale și normalizare pe straturi</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 50 min lectură</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Programare practică</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Avansat</span>\n  </div>\n</div>\n\n<h2>De la Attention la Blocul de Attention</h2>\n\n<p>Pe parcursul ultimelor cinci lecții, am construit mecanismul de attention de la zero: produse scalare, scalare, softmax, queries/keys/values, mascare cauzală și împărțire multi-head. Dar mecanismul de attention brut nu este modul în care apare în interiorul unui Transformer real. În arhitecturile reale, attention este întotdeauna înfășurat în două componente critice:</p>\n\n<ol>\n  <li><strong>Conexiuni reziduale</strong> (numite și skip connections) &mdash; care adaugă intrarea direct la ieșirea attention</li>\n  <li><strong>Normalizarea pe straturi</strong> &mdash; care stabilizează valorile care circulă prin rețea</li>\n</ol>\n\n<p>Împreună, acestea formează <strong>Blocul de Attention</strong>, elementul fundamental de construcție care se repetă de zeci de ori într-un Transformer. GPT-2 Small are 12 astfel de blocuri stivuite; GPT-3 are 96; Llama 3 70B are 80.</p>\n\n<div class=\"info-box\"><strong>Observație cheie:</strong> Fără conexiuni reziduale, Transformerele profunde nu pot fi antrenate &mdash; gradienții dispar până când se propagă prin zeci de straturi. Fără normalizarea pe straturi, activările cresc sau scad exponențial prin straturi. Acestea nu sunt opțiuni suplimentare; sunt esențiale pentru funcționarea rețelelor profunde de attention.</div>\n\n<h2>Conexiuni reziduale: De ce contează</h2>\n\n<p>O conexiune reziduală este dezarmant de simplă: în loc să calculăm <code>output = f(x)</code>, calculăm <code>output = x + f(x)</code>. Intrarea „sare\" peste transformare și este adăugată înapoi la rezultat.</p>\n\n<p>De ce contează acest lucru pentru rețelele profunde:</p>\n\n<ul>\n  <li><strong>Autostradă pentru gradienți:</strong> În timpul retropropagării, gradientul curge direct prin adunare, oferind o cale curată de la funcția de pierdere înapoi până la primele straturi. Fără aceasta, gradienții trebuie să treacă prin transformarea fiecărui strat, fiecare putând diminua semnalul.</li>\n  <li><strong>Identitate ușoară:</strong> Dacă un strat învață f(x) = 0, blocul calculează funcția identitate. Aceasta înseamnă că adăugarea mai multor straturi nu poate dăuna niciodată (în teorie) &mdash; rețeaua poate învăța întotdeauna să „sară peste\" straturile inutile.</li>\n  <li><strong>Rafinare incrementală:</strong> Fiecare bloc învață o mică <em>schimbare</em> a reprezentării, nu o transformare completă. Acest lucru este mai stabil și mai ușor de optimizat.</li>\n</ul>\n\n<p>Gândiți-vă la asta ca la editarea unui document: în loc să rescrieți de la zero de fiecare dată (ceea ce riscă pierderea informațiilor), porniți de la versiunea curentă și aplicați modificări. Conexiunea reziduală asigură că păstrați întotdeauna versiunea curentă.</p>\n\n<h2>Normalizarea pe straturi: Stabilizarea activărilor</h2>\n\n<p>Normalizarea pe straturi normalizează valorile pe dimensiunea trăsăturilor pentru fiecare token independent. Dat fiind un vector x de dimensiune d_model:</p>\n\n<p style=\"text-align: center; font-size: 1.1em;\">\n  LayerNorm(x) = &gamma; &odot; (x - &mu;) / (&sigma; + &epsilon;) + &beta;\n</p>\n\n<p>Unde &mu; și &sigma; sunt media și deviația standard calculate pe trăsăturile d_model, iar &gamma; (scală) și &beta; (deplasare) sunt parametri învățabili.</p>\n\n<p>Există două amplasări comune pentru normalizarea pe straturi:</p>\n\n<ul>\n  <li><strong>Post-norm (Transformerul original):</strong> <code>output = LayerNorm(x + Attention(x))</code></li>\n  <li><strong>Pre-norm (GPT-2, Llama, modele moderne):</strong> <code>output = x + Attention(LayerNorm(x))</code></li>\n</ul>\n\n<p>Pre-norm a devenit standardul deoarece oferă un antrenament mai stabil, mai ales pentru modele foarte profunde. Intuiția este că normalizarea <em>înainte de</em> calculul attention asigură că proiecțiile Q, K, V primesc intrări bine condiționate.</p>\n\n<div class=\"tip-box\"><strong>Sfat profesional:</strong> GPT-2 și majoritatea LLM-urilor moderne folosesc pre-norm (numit și „Pre-LN Transformer\"). Aceasta a fost una dintre observațiile cheie care a făcut antrenarea Transformerelor foarte profunde fiabilă. Dacă vedeți „Transformer\" într-o lucrare modernă, aproape sigur folosește pre-norm.</div>\n\n<h2>Cod: AttentionBlock complet cu LayerNorm + Rezidual</h2>\n\n<p>Să construim blocul complet de attention pe care l-ați găsi în GPT-2, Llama sau orice Transformer modern. Acesta integrează tot ce am învățat în Etapa 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    \"\"\"Layer Normalization as used in Transformers.\"\"\"\n\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)   # learnable scale\n        self.beta = np.zeros(d_model)   # learnable shift\n\n    def forward(self, x):\n        \"\"\"Normalize across the last dimension (features).\n\n        x shape: (batch, seq_len, d_model) or (batch, d_model)\n        \"\"\"\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        x_norm = (x - mean) / np.sqrt(var + self.eps)\n        return self.gamma * x_norm + self.beta\n\nclass MultiHeadAttention:\n    \"\"\"Multi-head self-attention.\"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None, return_attention=False):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn_weights = softmax(scores)\n        context = (attn_weights @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        output = context @ self.W_o\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\nclass AttentionBlock:\n    \"\"\"Complete attention block with pre-norm and residual connection.\n\n    This is the building block of GPT-2, Llama, and modern Transformers:\n        output = x + MultiHeadAttention(LayerNorm(x))\n\n    (Pre-norm variant, which is the modern standard.)\n    \"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.layer_norm = LayerNorm(d_model)\n        self.attention = MultiHeadAttention(d_model, n_heads, seed=seed)\n\n    def forward(self, x, mask=None, return_attention=False):\n        # Pre-norm: normalize before attention\n        x_norm = self.layer_norm.forward(x)\n\n        # Attention\n        if return_attention:\n            attn_out, attn_weights = self.attention.forward(\n                x_norm, mask=mask, return_attention=True\n            )\n        else:\n            attn_out = self.attention.forward(x_norm, mask=mask)\n\n        # Residual connection: add input to attention output\n        output = x + attn_out\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\n# Build and test the attention block\nd_model = 32\nn_heads = 4\nseq_len = 6\nbatch_size = 2\n\nnp.random.seed(42)\nx = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model=d_model, n_heads=n_heads, seed=0)\noutput, attn_weights = block.forward(x, mask=causal_mask, return_attention=True)\n\nprint(\"=== AttentionBlock Test ===\")\nprint(f\"Input shape:  {x.shape}\")\nprint(f\"Output shape: {output.shape}\")\nprint(f\"Attention weights shape: {attn_weights.shape}\")\nprint()\n\n# Verify residual connection: output should be close to input\n# (since weights are random and small, attention contribution is modest)\nresidual_magnitude = np.mean(np.abs(output - x))\ninput_magnitude = np.mean(np.abs(x))\nprint(f\"Input magnitude:    {input_magnitude:.4f}\")\nprint(f\"Residual change:    {residual_magnitude:.4f}\")\nprint(f\"Relative change:    {residual_magnitude/input_magnitude:.2%}\")\nprint(\"  (Small relative change shows residual connection preserves input)\")\nprint()\n\n# Verify layer norm: check that normalized values have mean~0, std~1\nx_norm = block.layer_norm.forward(x)\nprint(f\"Before LayerNorm - mean: {np.mean(x):.4f}, std: {np.std(x):.4f}\")\nprint(f\"After LayerNorm  - mean: {np.mean(x_norm):.6f}, std: {np.std(x_norm):.4f}\")\nprint()\n\n# Check per-token normalization\nfor t in range(min(3, seq_len)):\n    tok_norm = x_norm[0, t]\n    print(f\"  Token {t}: mean={np.mean(tok_norm):.6f}, std={np.std(tok_norm):.4f}\")\n</code></pre></div>\n\n<h2>Procesarea loturilor prin Blocul de Attention</h2>\n\n<p>În practică, procesăm mai multe secvențe simultan (inferență pe loturi). Să verificăm că blocul nostru de attention gestionează corect loturile și că fiecare secvență din lot este procesată independent.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n\n    def forward(self, x, mask=None):\n        return x + self.attn.forward(self.ln.forward(x), mask)\n\n# Test batch processing\nd_model = 32\nn_heads = 4\nseq_len = 8\nbatch_size = 4\n\nnp.random.seed(42)\nx_batch = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Process as a batch\noutput_batch = block.forward(x_batch, mask=causal_mask)\n\n# Process each sequence individually\noutputs_individual = []\nfor i in range(batch_size):\n    x_single = x_batch[i:i+1]  # (1, seq_len, d_model)\n    out_single = block.forward(x_single, mask=causal_mask)\n    outputs_individual.append(out_single)\n\noutput_individual = np.concatenate(outputs_individual, axis=0)\n\nprint(\"=== Batch Processing Verification ===\")\nprint(f\"Batch input shape:  {x_batch.shape}\")\nprint(f\"Batch output shape: {output_batch.shape}\")\nprint()\n\n# Verify batch == individual processing\nmatch = np.allclose(output_batch, output_individual, atol=1e-10)\nmax_diff = np.max(np.abs(output_batch - output_individual))\nprint(f\"Batch matches individual processing: {match}\")\nprint(f\"Maximum difference: {max_diff:.2e}\")\nprint()\n\n# Show that sequences in the batch are independent\nprint(\"Per-sequence output statistics:\")\nfor i in range(batch_size):\n    out = output_batch[i]\n    print(f\"  Sequence {i}: mean={np.mean(out):.4f}, \"\n          f\"std={np.std(out):.4f}, \"\n          f\"norm={np.linalg.norm(out):.4f}\")\n\nprint()\n# Stack multiple attention blocks (like a real Transformer)\nn_layers = 6\nblocks = [AttentionBlock(d_model, n_heads, seed=i*10) for i in range(n_layers)]\n\nx = x_batch.copy()\nprint(f\"=== Stacking {n_layers} Attention Blocks ===\")\nprint(f\"{'Layer':>6} | {'Mean':>8} | {'Std':>8} | {'Norm':>8} | {'Change':>8}\")\nprint(f\"{'-'*50}\")\nprint(f\"{'Input':>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | {np.linalg.norm(x):>8.2f} | {'---':>8}\")\n\nfor layer_idx, blk in enumerate(blocks):\n    x_prev = x.copy()\n    x = blk.forward(x, mask=causal_mask)\n    change = np.mean(np.abs(x - x_prev))\n    print(f\"{layer_idx:>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | \"\n          f\"{np.linalg.norm(x):>8.2f} | {change:>8.4f}\")\n\nprint()\nprint(\"Notice: LayerNorm + residual keeps values stable across layers.\")\nprint(\"Without them, values would explode or vanish in deep stacks.\")\n</code></pre></div>\n\n<h2>Fluxul complet de Attention</h2>\n\n<p>Să vizualizăm acum fluxul complet prin blocul de attention, de la tokenii de intrare brut până la ieșire. Acest ultim exemplu de cod leagă totul din Etapa 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o, attn\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n        self.d_model = d_model\n        self.n_heads = n_heads\n\n    def forward(self, x, mask=None):\n        x_norm = self.ln.forward(x)\n        attn_out, attn_weights = self.attn.forward(x_norm, mask)\n        return x + attn_out, attn_weights\n\n# === Full Pipeline Visualization ===\ntokens = [\"The\", \"cat\", \"sat\", \"on\", \"the\", \"mat\"]\nd_model = 32\nn_heads = 4\nseq_len = len(tokens)\n\nnp.random.seed(42)\n\n# Step 1: Create token embeddings (simulated)\nvocab_size = 100\nembedding_matrix = np.random.randn(vocab_size, d_model) * 0.1\ntoken_ids = [10, 25, 60, 45, 10, 80]  # simulated token IDs\nx = embedding_matrix[token_ids].reshape(1, seq_len, d_model)\n\nprint(\"=\" * 60)\nprint(\"FULL ATTENTION PIPELINE VISUALIZATION\")\nprint(\"=\" * 60)\nprint(f\"Tokens: {tokens}\")\nprint(f\"d_model={d_model}, n_heads={n_heads}, d_k={d_model//n_heads}\")\nprint()\n\n# Step 2: Show the pipeline\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Trace each step\nprint(\"--- Step 1: Input Embeddings ---\")\nprint(f\"  Shape: {x.shape}\")\nprint(f\"  Token 'cat' embedding (first 8 dims): {x[0, 1, :8].round(4)}\")\nprint()\n\nx_norm = block.ln.forward(x)\nprint(\"--- Step 2: Layer Normalization ---\")\nprint(f\"  Shape: {x_norm.shape}\")\nfor t in range(seq_len):\n    m = np.mean(x_norm[0, t])\n    s = np.std(x_norm[0, t])\n    print(f\"  '{tokens[t]}': mean={m:.6f}, std={s:.4f}\")\nprint()\n\n# Show Q, K, V projections\nQ = x_norm @ block.attn.W_q\nK = x_norm @ block.attn.W_k\nV = x_norm @ block.attn.W_v\nprint(\"--- Step 3: Q, K, V Projections ---\")\nprint(f\"  Q shape: {Q.shape}, K shape: {K.shape}, V shape: {V.shape}\")\nprint(f\"  Q norm per token: {[f'{np.linalg.norm(Q[0,t]):.2f}' for t in range(seq_len)]}\")\nprint()\n\n# Show attention patterns\noutput, attn_weights = block.forward(x, mask=causal_mask)\n\nprint(\"--- Step 4: Attention Patterns (per head) ---\")\nfor h in range(n_heads):\n    print(f\"  Head {h} - What does 'mat' (last token) attend to?\")\n    last_attn = attn_weights[0, h, -1, :]\n    ranked = np.argsort(last_attn)[::-1]\n    for idx in ranked[:3]:\n        print(f\"    -> '{tokens[idx]}': {last_attn[idx]:.4f}\")\nprint()\n\nprint(\"--- Step 5: Residual Connection ---\")\nresidual_diff = output - x\nprint(f\"  Output = Input + Attention(LayerNorm(Input))\")\nprint(f\"  Mean absolute change per token:\")\nfor t in range(seq_len):\n    change = np.mean(np.abs(residual_diff[0, t]))\n    print(f\"    '{tokens[t]}': {change:.4f}\")\nprint()\n\n# Show that \"the\" appears twice and gets different representations\nprint(\"--- Step 6: Contextual Representations ---\")\nprint(f\"  'the' appears at positions 0 and 4.\")\nprint(f\"  Input embeddings are IDENTICAL (same token ID):\")\nprint(f\"    Position 0: {x[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {x[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(x[0, 0], x[0, 4])}\")\nprint()\nprint(f\"  After attention, they are DIFFERENT (context-aware):\")\nprint(f\"    Position 0: {output[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {output[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(output[0, 0], output[0, 4])}\")\ncos_sim = np.dot(output[0,0], output[0,4]) / (np.linalg.norm(output[0,0]) * np.linalg.norm(output[0,4]))\nprint(f\"    Cosine similarity: {cos_sim:.4f}\")\nprint()\nprint(\"This is the fundamental power of attention: identical tokens\")\nprint(\"become different based on their surrounding context.\")\n</code></pre></div>\n\n<div class=\"info-box\"><strong>Observație cheie:</strong> Demonstrația de mai sus arată cea mai importantă proprietate a mecanismului de attention: tokenii de intrare identici („the\" la pozițiile 0 și 4) produc reprezentări de ieșire diferite deoarece atenționează la contexte diferite. Aceasta face modelele bazate pe attention atât de puternice pentru limbaj &mdash; reprezentarea fiecărui token este modelată dinamic de contextul său.</div>\n\n<h2>Rezumatul Etapei 3: Parcursul prin Attention</h2>\n\n<p>Am acoperit un volum enorm de material în Etapa 3. Să recapitulăm parcursul complet de la intuiția de bază la un modul de attention pregătit pentru producție.</p>\n\n<h3>Lecția 13: Intuiția Attention și scorurile produsului scalar</h3>\n<p>Am început cu întrebarea fundamentală: cum poate un model să decidă pe ce părți ale intrării să se concentreze? Am construit intuiția prin analogia cu căutarea în dicționar și am implementat similaritatea prin produs scalar ca fundament al scorurilor de attention. Am văzut că produsul scalar măsoară în mod natural alinierea între vectori, făcându-l o primitivă perfectă pentru „cât de legați sunt acești doi tokeni?\"</p>\n\n<h3>Lecția 14: Attention cu produs scalar scalat</h3>\n<p>Am formalizat mecanismul de attention cu ecuația completă: Attention(Q, K, V) = softmax(QK&#x1D40; / &radic;d_k) V. Am explorat de ce scalarea prin &radic;d_k este esențială (prevenind saturarea softmax în dimensiuni mari), am implementat mascarea cauzală pentru modelele autoregresive și am văzut cum softmax convertește scorurile brute într-o distribuție de probabilitate propriu-zisă peste tokenii de context.</p>\n\n<h3>Lecția 15: Proiecțiile Query, Key, Value</h3>\n<p>Am introdus proiecții liniare învățate care transformă embedding-urile de intrare în reprezentări separate Query, Key și Value. Aceasta a fost etapa crucială de la „compararea embedding-urilor brute\" la „învățarea a ce să comparăm\". Descompunerea Q/K/V oferă modelului capacitatea de a învăța independent <em>ce informații să caute</em> (Q), <em>ce informații să promoveze</em> (K) și <em>ce informații să transmită</em> (V).</p>\n\n<h3>Lecția 16: Attention multi-head</h3>\n<p>Am împărțit mecanismul de attention în mai multe head-uri paralele, fiecare operând pe un subspațiu al dimensiunii complete de embedding. Aceasta permite modelului să atenționeze simultan la diferite tipuri de relații &mdash; sintactice, semantice, poziționale &mdash; fără interferență. Numărul de parametri rămâne același; doar structura se schimbă.</p>\n\n<h3>Lecția 17: Variante și optimizări ale Attention</h3>\n<p>Am studiat inovațiile practice care fac attention-ul să funcționeze la scară: FlashAttention pentru calcul eficient din punct de vedere al memoriei, Grouped-Query Attention pentru cache-uri KV mai mici, KV-caching pentru inferență mai rapidă, RoPE pentru codificarea pozițiilor relative și attention cu fereastră glisantă pentru contexte lungi cu cost liniar.</p>\n\n<h3>Lecția 18: Modulul complet de Attention (această lecție)</h3>\n<p>Am înfășurat mecanismul de attention în conexiuni reziduale și normalizare pe straturi pentru a crea Blocul complet de Attention &mdash; unitatea fundamentală de construcție a tuturor arhitecturilor Transformer. Am verificat că acest bloc menține activări stabile pe stive profunde și produce reprezentări dependente de context.</p>\n\n<div class=\"warning-box\"><strong>Important:</strong> Tot ce am acoperit în Etapa 3 se referă la o singură componentă: blocul de attention. Un strat complet de Transformer include și o rețea feed-forward (MLP) cu propria conexiune reziduală și normalizare pe straturi. Acolo ne îndreptăm în Etapa 4. Blocul de attention gestionează „comunicarea\" între tokeni; blocul feed-forward gestionează „calculul\" în cadrul fiecărui token.</div>\n\n<h2>Punte către Etapa 4: Arhitectura Transformer</h2>\n\n<p>Cu blocul de attention complet, suntem gata să construim Transformerul complet. În Etapa 4, vom:</p>\n\n<ol>\n  <li><strong>Adăuga rețeaua feed-forward:</strong> Un MLP cu două straturi (cu activare GELU) care procesează fiecare token independent după ce attention a amestecat informațiile între tokeni</li>\n  <li><strong>Stivui straturi:</strong> Repetarea modelului [Bloc Attention + Bloc FFN] pentru a construi rețele profunde</li>\n  <li><strong>Adăuga capul final de predicție:</strong> Maparea de la stările ascunse înapoi la probabilitățile vocabularului</li>\n  <li><strong>Construi GPT-2 de la zero:</strong> Asamblarea tuturor componentelor într-un model de limbaj funcțional</li>\n</ol>\n\n<p>Mecanismul de attention pe care îl înțelegeți acum este inima fiecărui Transformer. Tot restul este construit în jurul lui. Ați stăpânit cea mai dificilă parte a arhitecturii &mdash; restul este inginerie mai directă.</p>\n\n<div class=\"key-takeaways\">\n  <h3>Concluzii cheie</h3>\n  <ul>\n    <li>Blocul de Attention înfășoară attention multi-head în normalizare pe straturi și o conexiune reziduală: <code>output = x + MHA(LayerNorm(x))</code> (varianta pre-norm).</li>\n    <li>Conexiunile reziduale oferă o autostradă pentru gradienți prin rețelele profunde și permit fiecărui strat să învețe rafinări incrementale în loc de transformări complete.</li>\n    <li>Normalizarea pe straturi stabilizează activările prin normalizare pe trăsături pentru fiecare token, prevenind explozia sau dispariția valorilor prin mai multe straturi.</li>\n    <li>Pre-norm (normalizare înainte de attention) este standardul modern, folosit în GPT-2, Llama și majoritatea arhitecturilor curente deoarece oferă un antrenament mai stabil.</li>\n    <li>Blocul de attention produce reprezentări contextuale: tokenii de intrare identici primesc ieșiri diferite pe baza contextului înconjurător.</li>\n    <li>Etapa 3 a acoperit povestea completă a mecanismului de attention: scoruri prin produs scalar, scalare, softmax, proiecții Q/K/V, împărțire multi-head, optimizări (GQA, KV-cache, RoPE) și blocul complet de attention cu rezidual + normalizare pe straturi.</li>\n  </ul>\n</div>\n\n<div class=\"quiz-section\" data-quiz-id=\"l18-ro\">\n<h3>&#x1F4DD; Verifică-ți Înțelegerea</h3>\n<div class=\"quiz-instructions\">Selectați cel mai bun răspuns (sau răspunsuri) pentru fiecare întrebare. Unele întrebări pot avea mai multe răspunsuri corecte (marcate cu un indiciu). Apăsați „Trimite Quizul\" pentru a vedea scorul, apoi „Arată Răspunsurile\" pentru explicații.</div>\n<div class=\"mcq-question\" data-correct='[1]' data-qidx=\"0\">\n  <div class=\"q-header\"><div class=\"q-number\">1</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explicație:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[0]' data-qidx=\"1\">\n  <div class=\"q-header\"><div class=\"q-number\">2</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explicație:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[2]' data-qidx=\"2\">\n  <div class=\"q-header\"><div class=\"q-number\">3</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explicație:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[0, 1, 3]' data-qidx=\"3\">\n  <div class=\"q-header\"><div class=\"q-number\">4</div><div class=\"q-text\"><span class=\"q-multi-hint\">Selectați toate răspunsurile corecte</span></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explicație:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[3]' data-qidx=\"4\">\n  <div class=\"q-header\"><div class=\"q-number\">5</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Explicație:</strong> </div>\n</div>\n<div class=\"quiz-controls\">\n  <button class=\"quiz-submit-btn\" onclick=\"submitQuiz(this)\">Trimite Quizul</button>\n  <button class=\"quiz-reveal-btn\" onclick=\"revealQuiz(this)\">Arată Răspunsurile</button>\n  <button class=\"quiz-reset-btn\" onclick=\"resetQuiz(this)\">Încearcă Din Nou</button>\n</div>\n<div class=\"quiz-results\"></div>\n</div>",
    "el": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">ΔΙΑΛΕΞΗ 18 <span class=\"stage-badge\">Στάδιο 3: Attention</span></div>\n  <h1 class=\"lecture-title\">Ολοκληρωτικό έργο Σταδίου 3: Πλήρης μονάδα Attention</h1>\n  <div class=\"lecture-subtitle\">Κατασκευή ενός μπλοκ attention έτοιμου για παραγωγή με υπολειμματικές συνδέσεις και κανονικοποίηση στρώματος</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 50 λεπτά ανάγνωσης</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Πρακτικός προγραμματισμός</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Προχωρημένο</span>\n  </div>\n</div>\n\n<h2>Από το Attention στο Μπλοκ Attention</h2>\n\n<p>Κατά τη διάρκεια των πέντε τελευταίων διαλέξεων, κατασκευάσαμε το attention από το μηδέν: εσωτερικά γινόμενα, κλιμάκωση, softmax, queries/keys/values, αιτιατή μάσκα και διαχωρισμό multi-head. Αλλά το ακατέργαστο attention δεν είναι ο τρόπος που εμφανίζεται μέσα σε έναν πραγματικό Transformer. Στις πραγματικές αρχιτεκτονικές, το attention περιβάλλεται πάντα από δύο κρίσιμα στοιχεία:</p>\n\n<ol>\n  <li><strong>Υπολειμματικές συνδέσεις</strong> (γνωστές και ως skip connections) &mdash; που προσθέτουν την είσοδο απευθείας στην έξοδο του attention</li>\n  <li><strong>Κανονικοποίηση στρώματος</strong> &mdash; που σταθεροποιεί τις τιμές που ρέουν μέσα στο δίκτυο</li>\n</ol>\n\n<p>Μαζί, αυτά σχηματίζουν το <strong>Μπλοκ Attention</strong>, το θεμελιώδες δομικό στοιχείο που επαναλαμβάνεται δεκάδες φορές σε έναν Transformer. Το GPT-2 Small έχει 12 τέτοια μπλοκ στοιβαγμένα, το GPT-3 έχει 96, το Llama 3 70B έχει 80.</p>\n\n<div class=\"info-box\"><strong>Βασική παρατήρηση:</strong> Χωρίς υπολειμματικές συνδέσεις, οι βαθιοί Transformers δεν μπορούν να εκπαιδευτούν &mdash; οι κλίσεις εξαφανίζονται μέχρι να διαδοθούν μέσα από δεκάδες στρώματα. Χωρίς κανονικοποίηση στρώματος, οι ενεργοποιήσεις αυξάνονται ή μειώνονται εκθετικά μέσα από τα στρώματα. Αυτά δεν είναι προαιρετικά πρόσθετα· είναι απαραίτητα για τη λειτουργία των βαθιών δικτύων attention.</div>\n\n<h2>Υπολειμματικές συνδέσεις: Γιατί έχουν σημασία</h2>\n\n<p>Μια υπολειμματική σύνδεση είναι αφοπλιστικά απλή: αντί να υπολογίσουμε <code>output = f(x)</code>, υπολογίζουμε <code>output = x + f(x)</code>. Η είσοδος «παρακάμπτει» τον μετασχηματισμό και προστίθεται πίσω στο αποτέλεσμα.</p>\n\n<p>Γιατί αυτό έχει σημασία για τα βαθιά δίκτυα:</p>\n\n<ul>\n  <li><strong>Αυτοκινητόδρομος κλίσεων:</strong> Κατά τη διάρκεια της οπισθοδιάδοσης, η κλίση ρέει απευθείας μέσω της πρόσθεσης, παρέχοντας ένα καθαρό μονοπάτι από τη συνάρτηση απώλειας μέχρι πίσω στα πρώτα στρώματα. Χωρίς αυτή, οι κλίσεις πρέπει να περάσουν μέσα από τον μετασχηματισμό κάθε στρώματος, καθένα από τα οποία μπορεί να εξασθενίσει το σήμα.</li>\n  <li><strong>Εύκολη ταυτότητα:</strong> Αν ένα στρώμα μάθει f(x) = 0, το μπλοκ υπολογίζει τη συνάρτηση ταυτότητας. Αυτό σημαίνει ότι η προσθήκη περισσότερων στρωμάτων δεν μπορεί ποτέ να βλάψει (θεωρητικά) &mdash; το δίκτυο μπορεί πάντα να μάθει να «παρακάμπτει» τα περιττά στρώματα.</li>\n  <li><strong>Σταδιακή βελτίωση:</strong> Κάθε μπλοκ μαθαίνει μια μικρή <em>αλλαγή</em> στην αναπαράσταση αντί για έναν πλήρη μετασχηματισμό. Αυτό είναι πιο σταθερό και ευκολότερο στη βελτιστοποίηση.</li>\n</ul>\n\n<p>Σκεφτείτε το σαν την επεξεργασία ενός εγγράφου: αντί να ξαναγράφετε από την αρχή κάθε φορά (κάτι που κινδυνεύει να χάσει πληροφορίες), ξεκινάτε με την τρέχουσα έκδοση και εφαρμόζετε αλλαγές. Η υπολειμματική σύνδεση εξασφαλίζει ότι διατηρείτε πάντα την τρέχουσα έκδοση.</p>\n\n<h2>Κανονικοποίηση στρώματος: Σταθεροποίηση ενεργοποιήσεων</h2>\n\n<p>Η κανονικοποίηση στρώματος κανονικοποιεί τις τιμές κατά τη διάσταση των χαρακτηριστικών για κάθε token ανεξάρτητα. Δεδομένου ενός διανύσματος x διάστασης d_model:</p>\n\n<p style=\"text-align: center; font-size: 1.1em;\">\n  LayerNorm(x) = &gamma; &odot; (x - &mu;) / (&sigma; + &epsilon;) + &beta;\n</p>\n\n<p>Όπου &mu; και &sigma; είναι ο μέσος όρος και η τυπική απόκλιση υπολογισμένα κατά μήκος των χαρακτηριστικών d_model, και &gamma; (κλίμακα) και &beta; (μετατόπιση) είναι εκπαιδεύσιμες παράμετροι.</p>\n\n<p>Υπάρχουν δύο κοινές τοποθετήσεις για την κανονικοποίηση στρώματος:</p>\n\n<ul>\n  <li><strong>Post-norm (αρχικός Transformer):</strong> <code>output = LayerNorm(x + Attention(x))</code></li>\n  <li><strong>Pre-norm (GPT-2, Llama, σύγχρονα μοντέλα):</strong> <code>output = x + Attention(LayerNorm(x))</code></li>\n</ul>\n\n<p>Το Pre-norm έχει γίνει το πρότυπο επειδή παρέχει πιο σταθερή εκπαίδευση, ειδικά για πολύ βαθιά μοντέλα. Η διαίσθηση είναι ότι η κανονικοποίηση <em>πριν</em> από τον υπολογισμό attention εξασφαλίζει ότι οι προβολές Q, K, V λαμβάνουν καλά προσαρμοσμένες εισόδους.</p>\n\n<div class=\"tip-box\"><strong>Επαγγελματική συμβουλή:</strong> Το GPT-2 και τα περισσότερα σύγχρονα LLMs χρησιμοποιούν pre-norm (γνωστό και ως «Pre-LN Transformer»). Αυτή ήταν μία από τις βασικές παρατηρήσεις που έκανε αξιόπιστη την εκπαίδευση πολύ βαθιών Transformers. Αν δείτε «Transformer» σε μια σύγχρονη δημοσίευση, σχεδόν σίγουρα χρησιμοποιεί pre-norm.</div>\n\n<h2>Κώδικας: Πλήρες AttentionBlock με LayerNorm + Υπολειμματική σύνδεση</h2>\n\n<p>Ας κατασκευάσουμε το πλήρες μπλοκ attention που θα βρίσκατε μέσα στο GPT-2, στο Llama ή σε οποιονδήποτε σύγχρονο Transformer. Αυτό ενσωματώνει όλα όσα μάθαμε στο Στάδιο 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    \"\"\"Layer Normalization as used in Transformers.\"\"\"\n\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)   # learnable scale\n        self.beta = np.zeros(d_model)   # learnable shift\n\n    def forward(self, x):\n        \"\"\"Normalize across the last dimension (features).\n\n        x shape: (batch, seq_len, d_model) or (batch, d_model)\n        \"\"\"\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        x_norm = (x - mean) / np.sqrt(var + self.eps)\n        return self.gamma * x_norm + self.beta\n\nclass MultiHeadAttention:\n    \"\"\"Multi-head self-attention.\"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None, return_attention=False):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn_weights = softmax(scores)\n        context = (attn_weights @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        output = context @ self.W_o\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\nclass AttentionBlock:\n    \"\"\"Complete attention block with pre-norm and residual connection.\n\n    This is the building block of GPT-2, Llama, and modern Transformers:\n        output = x + MultiHeadAttention(LayerNorm(x))\n\n    (Pre-norm variant, which is the modern standard.)\n    \"\"\"\n\n    def __init__(self, d_model, n_heads, seed=42):\n        self.layer_norm = LayerNorm(d_model)\n        self.attention = MultiHeadAttention(d_model, n_heads, seed=seed)\n\n    def forward(self, x, mask=None, return_attention=False):\n        # Pre-norm: normalize before attention\n        x_norm = self.layer_norm.forward(x)\n\n        # Attention\n        if return_attention:\n            attn_out, attn_weights = self.attention.forward(\n                x_norm, mask=mask, return_attention=True\n            )\n        else:\n            attn_out = self.attention.forward(x_norm, mask=mask)\n\n        # Residual connection: add input to attention output\n        output = x + attn_out\n\n        if return_attention:\n            return output, attn_weights\n        return output\n\n# Build and test the attention block\nd_model = 32\nn_heads = 4\nseq_len = 6\nbatch_size = 2\n\nnp.random.seed(42)\nx = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model=d_model, n_heads=n_heads, seed=0)\noutput, attn_weights = block.forward(x, mask=causal_mask, return_attention=True)\n\nprint(\"=== AttentionBlock Test ===\")\nprint(f\"Input shape:  {x.shape}\")\nprint(f\"Output shape: {output.shape}\")\nprint(f\"Attention weights shape: {attn_weights.shape}\")\nprint()\n\n# Verify residual connection: output should be close to input\n# (since weights are random and small, attention contribution is modest)\nresidual_magnitude = np.mean(np.abs(output - x))\ninput_magnitude = np.mean(np.abs(x))\nprint(f\"Input magnitude:    {input_magnitude:.4f}\")\nprint(f\"Residual change:    {residual_magnitude:.4f}\")\nprint(f\"Relative change:    {residual_magnitude/input_magnitude:.2%}\")\nprint(\"  (Small relative change shows residual connection preserves input)\")\nprint()\n\n# Verify layer norm: check that normalized values have mean~0, std~1\nx_norm = block.layer_norm.forward(x)\nprint(f\"Before LayerNorm - mean: {np.mean(x):.4f}, std: {np.std(x):.4f}\")\nprint(f\"After LayerNorm  - mean: {np.mean(x_norm):.6f}, std: {np.std(x_norm):.4f}\")\nprint()\n\n# Check per-token normalization\nfor t in range(min(3, seq_len)):\n    tok_norm = x_norm[0, t]\n    print(f\"  Token {t}: mean={np.mean(tok_norm):.6f}, std={np.std(tok_norm):.4f}\")\n</code></pre></div>\n\n<h2>Επεξεργασία παρτίδων μέσω του Μπλοκ Attention</h2>\n\n<p>Στην πράξη, επεξεργαζόμαστε πολλαπλές ακολουθίες ταυτόχρονα (εξαγωγή συμπερασμάτων σε παρτίδες). Ας επαληθεύσουμε ότι το μπλοκ attention μας χειρίζεται σωστά τις παρτίδες και ότι κάθε ακολουθία στην παρτίδα επεξεργάζεται ανεξάρτητα.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n\n    def forward(self, x, mask=None):\n        return x + self.attn.forward(self.ln.forward(x), mask)\n\n# Test batch processing\nd_model = 32\nn_heads = 4\nseq_len = 8\nbatch_size = 4\n\nnp.random.seed(42)\nx_batch = np.random.randn(batch_size, seq_len, d_model)\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\n\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Process as a batch\noutput_batch = block.forward(x_batch, mask=causal_mask)\n\n# Process each sequence individually\noutputs_individual = []\nfor i in range(batch_size):\n    x_single = x_batch[i:i+1]  # (1, seq_len, d_model)\n    out_single = block.forward(x_single, mask=causal_mask)\n    outputs_individual.append(out_single)\n\noutput_individual = np.concatenate(outputs_individual, axis=0)\n\nprint(\"=== Batch Processing Verification ===\")\nprint(f\"Batch input shape:  {x_batch.shape}\")\nprint(f\"Batch output shape: {output_batch.shape}\")\nprint()\n\n# Verify batch == individual processing\nmatch = np.allclose(output_batch, output_individual, atol=1e-10)\nmax_diff = np.max(np.abs(output_batch - output_individual))\nprint(f\"Batch matches individual processing: {match}\")\nprint(f\"Maximum difference: {max_diff:.2e}\")\nprint()\n\n# Show that sequences in the batch are independent\nprint(\"Per-sequence output statistics:\")\nfor i in range(batch_size):\n    out = output_batch[i]\n    print(f\"  Sequence {i}: mean={np.mean(out):.4f}, \"\n          f\"std={np.std(out):.4f}, \"\n          f\"norm={np.linalg.norm(out):.4f}\")\n\nprint()\n# Stack multiple attention blocks (like a real Transformer)\nn_layers = 6\nblocks = [AttentionBlock(d_model, n_heads, seed=i*10) for i in range(n_layers)]\n\nx = x_batch.copy()\nprint(f\"=== Stacking {n_layers} Attention Blocks ===\")\nprint(f\"{'Layer':>6} | {'Mean':>8} | {'Std':>8} | {'Norm':>8} | {'Change':>8}\")\nprint(f\"{'-'*50}\")\nprint(f\"{'Input':>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | {np.linalg.norm(x):>8.2f} | {'---':>8}\")\n\nfor layer_idx, blk in enumerate(blocks):\n    x_prev = x.copy()\n    x = blk.forward(x, mask=causal_mask)\n    change = np.mean(np.abs(x - x_prev))\n    print(f\"{layer_idx:>6} | {np.mean(x):>8.4f} | {np.std(x):>8.4f} | \"\n          f\"{np.linalg.norm(x):>8.2f} | {change:>8.4f}\")\n\nprint()\nprint(\"Notice: LayerNorm + residual keeps values stable across layers.\")\nprint(\"Without them, values would explode or vanish in deep stacks.\")\n</code></pre></div>\n\n<h2>Η πλήρης ροή Attention</h2>\n\n<p>Ας οπτικοποιήσουμε τώρα την πλήρη ροή μέσα από το μπλοκ attention, από τα ακατέργαστα tokens εισόδου μέχρι την έξοδο. Αυτό το τελευταίο παράδειγμα κώδικα ενώνει τα πάντα από το Στάδιο 3.</p>\n\n<div class=\"code-block\"><div class=\"code-header\"><span class=\"code-lang\">Python</span><div class=\"code-actions\"><button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button></div></div><pre><code class=\"language-python\">\nimport numpy as np\n\ndef softmax(x, axis=-1):\n    x_max = np.max(x, axis=axis, keepdims=True)\n    e_x = np.exp(x - x_max)\n    return e_x / np.sum(e_x, axis=axis, keepdims=True)\n\nclass LayerNorm:\n    def __init__(self, d_model, eps=1e-5):\n        self.eps = eps\n        self.gamma = np.ones(d_model)\n        self.beta = np.zeros(d_model)\n\n    def forward(self, x):\n        mean = np.mean(x, axis=-1, keepdims=True)\n        var = np.var(x, axis=-1, keepdims=True)\n        return self.gamma * (x - mean) / np.sqrt(var + self.eps) + self.beta\n\nclass MultiHeadAttention:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.d_model = d_model\n        self.n_heads = n_heads\n        self.d_k = d_model // n_heads\n        rng = np.random.RandomState(seed)\n        scale = np.sqrt(2.0 / d_model)\n        self.W_q = rng.randn(d_model, d_model) * scale\n        self.W_k = rng.randn(d_model, d_model) * scale\n        self.W_v = rng.randn(d_model, d_model) * scale\n        self.W_o = rng.randn(d_model, d_model) * scale\n\n    def forward(self, x, mask=None):\n        batch, seq_len, _ = x.shape\n        Q = (x @ self.W_q).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        K = (x @ self.W_k).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        V = (x @ self.W_v).reshape(batch, seq_len, self.n_heads, self.d_k).transpose(0,2,1,3)\n        scores = Q @ K.transpose(0,1,3,2) / np.sqrt(self.d_k)\n        if mask is not None:\n            scores = scores + mask * (-1e9)\n        attn = softmax(scores)\n        context = (attn @ V).transpose(0,2,1,3).reshape(batch, seq_len, self.d_model)\n        return context @ self.W_o, attn\n\nclass AttentionBlock:\n    def __init__(self, d_model, n_heads, seed=42):\n        self.ln = LayerNorm(d_model)\n        self.attn = MultiHeadAttention(d_model, n_heads, seed)\n        self.d_model = d_model\n        self.n_heads = n_heads\n\n    def forward(self, x, mask=None):\n        x_norm = self.ln.forward(x)\n        attn_out, attn_weights = self.attn.forward(x_norm, mask)\n        return x + attn_out, attn_weights\n\n# === Full Pipeline Visualization ===\ntokens = [\"The\", \"cat\", \"sat\", \"on\", \"the\", \"mat\"]\nd_model = 32\nn_heads = 4\nseq_len = len(tokens)\n\nnp.random.seed(42)\n\n# Step 1: Create token embeddings (simulated)\nvocab_size = 100\nembedding_matrix = np.random.randn(vocab_size, d_model) * 0.1\ntoken_ids = [10, 25, 60, 45, 10, 80]  # simulated token IDs\nx = embedding_matrix[token_ids].reshape(1, seq_len, d_model)\n\nprint(\"=\" * 60)\nprint(\"FULL ATTENTION PIPELINE VISUALIZATION\")\nprint(\"=\" * 60)\nprint(f\"Tokens: {tokens}\")\nprint(f\"d_model={d_model}, n_heads={n_heads}, d_k={d_model//n_heads}\")\nprint()\n\n# Step 2: Show the pipeline\ncausal_mask = np.triu(np.ones((seq_len, seq_len)), k=1)\nblock = AttentionBlock(d_model, n_heads, seed=0)\n\n# Trace each step\nprint(\"--- Step 1: Input Embeddings ---\")\nprint(f\"  Shape: {x.shape}\")\nprint(f\"  Token 'cat' embedding (first 8 dims): {x[0, 1, :8].round(4)}\")\nprint()\n\nx_norm = block.ln.forward(x)\nprint(\"--- Step 2: Layer Normalization ---\")\nprint(f\"  Shape: {x_norm.shape}\")\nfor t in range(seq_len):\n    m = np.mean(x_norm[0, t])\n    s = np.std(x_norm[0, t])\n    print(f\"  '{tokens[t]}': mean={m:.6f}, std={s:.4f}\")\nprint()\n\n# Show Q, K, V projections\nQ = x_norm @ block.attn.W_q\nK = x_norm @ block.attn.W_k\nV = x_norm @ block.attn.W_v\nprint(\"--- Step 3: Q, K, V Projections ---\")\nprint(f\"  Q shape: {Q.shape}, K shape: {K.shape}, V shape: {V.shape}\")\nprint(f\"  Q norm per token: {[f'{np.linalg.norm(Q[0,t]):.2f}' for t in range(seq_len)]}\")\nprint()\n\n# Show attention patterns\noutput, attn_weights = block.forward(x, mask=causal_mask)\n\nprint(\"--- Step 4: Attention Patterns (per head) ---\")\nfor h in range(n_heads):\n    print(f\"  Head {h} - What does 'mat' (last token) attend to?\")\n    last_attn = attn_weights[0, h, -1, :]\n    ranked = np.argsort(last_attn)[::-1]\n    for idx in ranked[:3]:\n        print(f\"    -> '{tokens[idx]}': {last_attn[idx]:.4f}\")\nprint()\n\nprint(\"--- Step 5: Residual Connection ---\")\nresidual_diff = output - x\nprint(f\"  Output = Input + Attention(LayerNorm(Input))\")\nprint(f\"  Mean absolute change per token:\")\nfor t in range(seq_len):\n    change = np.mean(np.abs(residual_diff[0, t]))\n    print(f\"    '{tokens[t]}': {change:.4f}\")\nprint()\n\n# Show that \"the\" appears twice and gets different representations\nprint(\"--- Step 6: Contextual Representations ---\")\nprint(f\"  'the' appears at positions 0 and 4.\")\nprint(f\"  Input embeddings are IDENTICAL (same token ID):\")\nprint(f\"    Position 0: {x[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {x[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(x[0, 0], x[0, 4])}\")\nprint()\nprint(f\"  After attention, they are DIFFERENT (context-aware):\")\nprint(f\"    Position 0: {output[0, 0, :6].round(4)}\")\nprint(f\"    Position 4: {output[0, 4, :6].round(4)}\")\nprint(f\"    Match: {np.allclose(output[0, 0], output[0, 4])}\")\ncos_sim = np.dot(output[0,0], output[0,4]) / (np.linalg.norm(output[0,0]) * np.linalg.norm(output[0,4]))\nprint(f\"    Cosine similarity: {cos_sim:.4f}\")\nprint()\nprint(\"This is the fundamental power of attention: identical tokens\")\nprint(\"become different based on their surrounding context.\")\n</code></pre></div>\n\n<div class=\"info-box\"><strong>Βασική παρατήρηση:</strong> Η παραπάνω επίδειξη δείχνει την πιο σημαντική ιδιότητα του attention: πανομοιότυπα tokens εισόδου («the» στις θέσεις 0 και 4) παράγουν διαφορετικές αναπαραστάσεις εξόδου επειδή δίνουν προσοχή σε διαφορετικά συμφραζόμενα. Αυτό κάνει τα μοντέλα βασισμένα σε attention τόσο ισχυρά για τη γλώσσα &mdash; η αναπαράσταση κάθε token διαμορφώνεται δυναμικά από τα συμφραζόμενά του.</div>\n\n<h2>Περίληψη Σταδίου 3: Το ταξίδι μέσα από το Attention</h2>\n\n<p>Καλύψαμε τεράστιο έδαφος στο Στάδιο 3. Ας ανασκοπήσουμε το πλήρες ταξίδι από τη βασική διαίσθηση ως το attention έτοιμο για παραγωγή.</p>\n\n<h3>Διάλεξη 13: Διαίσθηση Attention και βαθμολογίες εσωτερικού γινομένου</h3>\n<p>Ξεκινήσαμε με το θεμελιώδες ερώτημα: πώς μπορεί ένα μοντέλο να αποφασίσει σε ποια μέρη της εισόδου να εστιάσει; Χτίσαμε τη διαίσθηση μέσω της αναλογίας αναζήτησης σε λεξικό και υλοποιήσαμε τη βασική ομοιότητα εσωτερικού γινομένου ως θεμέλιο της βαθμολόγησης attention. Είδαμε ότι το εσωτερικό γινόμενο μετρά φυσικά την ευθυγράμμιση μεταξύ διανυσμάτων, καθιστώντας το ιδανικό πρωτόγονο στοιχείο για «πόσο σχετικά είναι αυτά τα δύο tokens;»</p>\n\n<h3>Διάλεξη 14: Κλιμακούμενο Attention εσωτερικού γινομένου</h3>\n<p>Τυποποιήσαμε το attention με την πλήρη εξίσωση: Attention(Q, K, V) = softmax(QK&#x1D40; / &radic;d_k) V. Εξερευνήσαμε γιατί η κλιμάκωση κατά &radic;d_k είναι ουσιαστική (αποτρέπει τον κορεσμό του softmax σε υψηλές διαστάσεις), υλοποιήσαμε αιτιατή μάσκα για αυτοπαλίνδρομα μοντέλα και είδαμε πώς το softmax μετατρέπει τις ακατέργαστες βαθμολογίες σε κανονική κατανομή πιθανοτήτων πάνω στα tokens συμφραζομένων.</p>\n\n<h3>Διάλεξη 15: Προβολές Query, Key, Value</h3>\n<p>Εισαγάγαμε εκπαιδεύσιμες γραμμικές προβολές που μετασχηματίζουν τα embeddings εισόδου σε ξεχωριστές αναπαραστάσεις Query, Key και Value. Αυτό ήταν το κρίσιμο βήμα από «σύγκριση ακατέργαστων embeddings» σε «εκμάθηση του τι να συγκρίνουμε». Η αποσύνθεση Q/K/V δίνει στο μοντέλο τη δυνατότητα να μάθει ανεξάρτητα <em>ποιες πληροφορίες να αναζητά</em> (Q), <em>ποιες πληροφορίες να διαφημίζει</em> (K) και <em>ποιες πληροφορίες να μεταδίδει</em> (V).</p>\n\n<h3>Διάλεξη 16: Multi-Head Attention</h3>\n<p>Χωρίσαμε το attention σε πολλαπλά παράλληλα heads, καθένα λειτουργώντας σε έναν υποχώρο της πλήρους διάστασης embedding. Αυτό επιτρέπει στο μοντέλο να δίνει ταυτόχρονα προσοχή σε διαφορετικούς τύπους σχέσεων &mdash; συντακτικές, σημασιολογικές, θεσιακές &mdash; χωρίς παρεμβολές. Ο αριθμός παραμέτρων παραμένει ίδιος· μόνο η δομή αλλάζει.</p>\n\n<h3>Διάλεξη 17: Παραλλαγές και βελτιστοποιήσεις Attention</h3>\n<p>Επισκοπήσαμε τις πρακτικές καινοτομίες που κάνουν το attention να λειτουργεί σε κλίμακα: FlashAttention για αποδοτικό υπολογισμό μνήμης, Grouped-Query Attention για μικρότερες KV-caches, KV-caching για ταχύτερη εξαγωγή συμπερασμάτων, RoPE για σχετική κωδικοποίηση θέσης και attention κυλιόμενου παραθύρου για μακρά συμφραζόμενα με γραμμικό κόστος.</p>\n\n<h3>Διάλεξη 18: Πλήρης μονάδα Attention (αυτή η διάλεξη)</h3>\n<p>Περιβάλαμε το attention με υπολειμματικές συνδέσεις και κανονικοποίηση στρώματος για να δημιουργήσουμε το πλήρες Μπλοκ Attention &mdash; τη θεμελιώδη δομική μονάδα όλων των αρχιτεκτονικών Transformer. Επαληθεύσαμε ότι αυτό το μπλοκ διατηρεί σταθερές ενεργοποιήσεις σε βαθιές στοίβες και παράγει αναπαραστάσεις εξαρτώμενες από τα συμφραζόμενα.</p>\n\n<div class=\"warning-box\"><strong>Σημαντικό:</strong> Ό,τι καλύψαμε στο Στάδιο 3 αφορά ένα μόνο στοιχείο: το μπλοκ attention. Ένα πλήρες στρώμα Transformer περιλαμβάνει επίσης ένα δίκτυο πρόσθιας τροφοδότησης (MLP) με τη δική του υπολειμματική σύνδεση και κανονικοποίηση στρώματος. Εκεί κατευθυνόμαστε στο Στάδιο 4. Το μπλοκ attention χειρίζεται την «επικοινωνία» μεταξύ tokens· το μπλοκ πρόσθιας τροφοδότησης χειρίζεται τον «υπολογισμό» εντός κάθε token.</div>\n\n<h2>Γέφυρα προς το Στάδιο 4: Η αρχιτεκτονική Transformer</h2>\n\n<p>Με το μπλοκ attention ολοκληρωμένο, είμαστε έτοιμοι να κατασκευάσουμε τον πλήρη Transformer. Στο Στάδιο 4, θα:</p>\n\n<ol>\n  <li><strong>Προσθέσουμε το δίκτυο πρόσθιας τροφοδότησης:</strong> Ένα MLP δύο στρωμάτων (με ενεργοποίηση GELU) που επεξεργάζεται κάθε token ανεξάρτητα αφού το attention έχει αναμείξει πληροφορίες μεταξύ των tokens</li>\n  <li><strong>Στοιβάξουμε στρώματα:</strong> Επανάληψη του μοτίβου [Μπλοκ Attention + Μπλοκ FFN] για την κατασκευή βαθιών δικτύων</li>\n  <li><strong>Προσθέσουμε την τελική κεφαλή πρόβλεψης:</strong> Αντιστοίχιση από κρυφές καταστάσεις πίσω σε πιθανότητες λεξιλογίου</li>\n  <li><strong>Κατασκευάσουμε το GPT-2 από το μηδέν:</strong> Συναρμολόγηση όλων των στοιχείων σε ένα λειτουργικό γλωσσικό μοντέλο</li>\n</ol>\n\n<p>Ο μηχανισμός attention που κατανοείτε τώρα είναι η καρδιά κάθε Transformer. Ό,τι άλλο κατασκευάζεται γύρω του. Έχετε κατακτήσει το πιο δύσκολο μέρος της αρχιτεκτονικής &mdash; τα υπόλοιπα είναι πιο απλή μηχανική.</p>\n\n<div class=\"key-takeaways\">\n  <h3>Βασικά συμπεράσματα</h3>\n  <ul>\n    <li>Το Μπλοκ Attention περιβάλλει το multi-head attention με κανονικοποίηση στρώματος και υπολειμματική σύνδεση: <code>output = x + MHA(LayerNorm(x))</code> (παραλλαγή pre-norm).</li>\n    <li>Οι υπολειμματικές συνδέσεις παρέχουν αυτοκινητόδρομο κλίσεων μέσα σε βαθιά δίκτυα και επιτρέπουν σε κάθε στρώμα να μαθαίνει σταδιακές βελτιώσεις αντί για πλήρεις μετασχηματισμούς.</li>\n    <li>Η κανονικοποίηση στρώματος σταθεροποιεί τις ενεργοποιήσεις μέσω κανονικοποίησης κατά τα χαρακτηριστικά για κάθε token, αποτρέποντας τις τιμές από το να εκραγούν ή να εξαφανιστούν μέσα σε πολλά στρώματα.</li>\n    <li>Η pre-norm (κανονικοποίηση πριν από το attention) είναι το σύγχρονο πρότυπο, που χρησιμοποιείται στο GPT-2, το Llama και τις περισσότερες τρέχουσες αρχιτεκτονικές επειδή παρέχει πιο σταθερή εκπαίδευση.</li>\n    <li>Το μπλοκ attention παράγει συμφραζομενικές αναπαραστάσεις: πανομοιότυπα tokens εισόδου λαμβάνουν διαφορετικές εξόδους ανάλογα με τα περιβάλλοντα συμφραζόμενα.</li>\n    <li>Το Στάδιο 3 κάλυψε την πλήρη ιστορία του attention: βαθμολόγηση εσωτερικού γινομένου, κλιμάκωση, softmax, προβολές Q/K/V, διαχωρισμός multi-head, βελτιστοποιήσεις (GQA, KV-cache, RoPE) και το πλήρες μπλοκ attention με υπολειμματική σύνδεση + κανονικοποίηση στρώματος.</li>\n  </ul>\n</div>\n\n<div class=\"quiz-section\" data-quiz-id=\"l18-el\">\n<h3>&#x1F4DD; Ελέγξτε την Κατανόησή σας</h3>\n<div class=\"quiz-instructions\">Επιλέξτε την καλύτερη απάντηση (ή απαντήσεις) για κάθε ερώτηση. Ορισμένες ερωτήσεις μπορεί να έχουν πολλαπλές σωστές απαντήσεις (με σχετική ένδειξη). Κάντε κλικ στο «Υποβολή» για να δείτε τη βαθμολογία σας, μετά στο «Εμφάνιση Απαντήσεων» για εξηγήσεις.</div>\n<div class=\"mcq-question\" data-correct='[1]' data-qidx=\"0\">\n  <div class=\"q-header\"><div class=\"q-number\">1</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Εξήγηση:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[0]' data-qidx=\"1\">\n  <div class=\"q-header\"><div class=\"q-number\">2</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Εξήγηση:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[2]' data-qidx=\"2\">\n  <div class=\"q-header\"><div class=\"q-number\">3</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Εξήγηση:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[0, 1, 3]' data-qidx=\"3\">\n  <div class=\"q-header\"><div class=\"q-number\">4</div><div class=\"q-text\"><span class=\"q-multi-hint\">Επιλέξτε όλες τις σωστές</span></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Εξήγηση:</strong> </div>\n</div>\n<div class=\"mcq-question\" data-correct='[3]' data-qidx=\"4\">\n  <div class=\"q-header\"><div class=\"q-number\">5</div><div class=\"q-text\"></div></div>\n  <div class=\"mcq-options\">\n  </div>\n  <div class=\"mcq-explanation\"><strong>&#x2705; Εξήγηση:</strong> </div>\n</div>\n<div class=\"quiz-controls\">\n  <button class=\"quiz-submit-btn\" onclick=\"submitQuiz(this)\">Υποβολή Quiz</button>\n  <button class=\"quiz-reveal-btn\" onclick=\"revealQuiz(this)\">Εμφάνιση Απαντήσεων</button>\n  <button class=\"quiz-reset-btn\" onclick=\"resetQuiz(this)\">Δοκιμάστε Ξανά</button>\n</div>\n<div class=\"quiz-results\"></div>\n</div>"
  },
  "codeBlocks": []
}