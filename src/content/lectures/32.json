{
  "id": "32",
  "title": {
    "en": "Stage 5 Capstone — Pretrained Text Generator",
    "ro": "Proiectul Final al Etapei 5 — Generator de Text Preantrenat",
    "el": "Τελικό Έργο Σταδίου 5 — Γεννήτρια Κειμένου με Προεκπαίδευση"
  },
  "content": {
    "en": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"lecture-label\">LECTURE 32 <span class=\"stage-badge\">Stage 5: Pretraining</span></div>\n<h1 class=\"lecture-title\">Stage 5 Capstone &mdash; Pretrained Text Generator</h1>\n<div class=\"lecture-subtitle\">A Complete System: Pretrained Model, Multiple Generation Strategies, Quality Evaluation, and Interactive Playground</div>\n<div class=\"lecture-meta\">\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>50 min read</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Capstone</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>You built a language model from scratch. Now watch it speak.</span>\n</div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polygon points=\"16.24 7.76 14.12 14.12 7.76 16.24 9.88 9.88 16.24 7.76\"/></svg>Introduction: The Culmination</h2>\n\n<p>In the Stage 4 Capstone (Lecture 24), you built the Mini GPT-2 <em>architecture</em> &mdash; but with random weights, it could only produce gibberish. Now, after building the entire training machinery, you will see <strong>a model you built and trained from scratch generating text that resembles real language patterns.</strong> This is how ChatGPT, Claude, and every LLM works. The difference is only scale.</p>\n\n<div class=\"info-box\">\n<strong>What You Will Build:</strong> A pretrained Mini GPT-2 with four generation strategies (greedy, temperature, top-k, top-p), quality evaluation metrics, and an interactive generation playground.\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z\"/><path d=\"m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65\"/><path d=\"m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65\"/></svg>Part 1: Pretraining with Extended Corpus</h2>\n\n<p>We use a corpus with varied sentence structures and enough repetition for our small model to learn meaningful patterns.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-1')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# STAGE 5 CAPSTONE: PRETRAINED TEXT GENERATOR\n# ============================================================\n\nclass CharTok:\n    def __init__(self, text):\n        ch = sorted(set(text))\n        self.c2i={c:i for i,c in enumerate(ch)}\n        self.i2c={i:c for i,c in enumerate(ch)}\n        self.V=len(ch)\n    def encode(self,t): return [self.c2i.get(c,0) for c in t]\n    def decode(self,ids): return ''.join(self.i2c.get(i,'?') for i in ids)\n\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):\n        mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True)\n        return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass Attn:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h\n        self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3)\n        K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3)\n        sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9)\n        e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FFN:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df)\n        self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):\n        z=x@self.W1+self.b1\n        return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Blk:\n    def __init__(self,d,h,s):\n        self.ln1=LN(d);self.at=Attn(d,h,s);self.ln2=LN(d);self.ff=FFN(d,s)\n    def forward(self,x):\n        x=x+self.at.forward(self.ln1.forward(x))\n        x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L)\n        self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Blk(d,h,rs) for _ in range(L)]\n        self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ndef cross_entropy(logits,targets):\n    B,T,V=logits.shape;lf=logits.reshape(B*T,V);tf=targets.reshape(B*T)\n    s=lf-lf.max(-1,keepdims=True)\n    lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    return -lp[np.arange(B*T),tf].mean()\n\ndef train_step(model,inp,tgt,lr):\n    logits=model.forward(inp);loss=cross_entropy(logits,tgt)\n    B,T,V=logits.shape\n    s=logits-logits.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(T):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*T)\n    x=model.tok[inp]+model.pos[:T]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x)\n    dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    model.head-=lr*dh\n    de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(T):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n    return loss\n\n# === TRAINING CORPUS ===\ncorpus = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(corpus)\nids = tok.encode(corpus)\nT = 32\ndata = []\nfor i in range(0,len(ids)-T,12):\n    ch=ids[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\n\nmodel = GPT(tok.V, d=64, h=4, L=4, T=T)\nprint(\"=\" * 58)\nprint(\"  CAPSTONE: PRETRAINING MINI GPT-2\")\nprint(\"=\" * 58)\nprint(f\"  Vocab: {tok.V} | Tokens: {len(ids)} | Examples: {len(data)}\")\n\nmax_steps=100; bs=8; max_lr=0.005; min_lr=0.0005; warmup=15\nlosses=[]\nfor step in range(max_steps):\n    idx=np.random.choice(len(data),bs)\n    inp=np.stack([data[i][0] for i in idx])\n    tgt=np.stack([data[i][1] for i in idx])\n    if step<warmup: lr=max_lr*(step+1)/warmup\n    else:\n        prog=(step-warmup)/max(1,max_steps-warmup)\n        lr=min_lr+0.5*(max_lr-min_lr)*(1+np.cos(np.pi*prog))\n    loss=train_step(model,inp,tgt,lr)\n    losses.append(float(loss))\n    if step%20==0 or step==max_steps-1:\n        print(f\"  Step {step:3d} | Loss: {loss:.4f} | LR: {lr:.6f}\")\n\ndef gen(prompt,n=60,temp=0.7):\n    ids=tok.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]])\n        logits=model.forward(ctx)[0,-1]/temp\n        p=np.exp(logits-logits.max());p/=p.sum()\n        ids.append(int(np.random.choice(len(p),p=p)))\n    return tok.decode(ids[len(tok.encode(prompt)):])\n\nprint(f\"\\\n  Loss: {losses[0]:.4f} -> {losses[-1]:.4f} ({(1-losses[-1]/losses[0])*100:.1f}% improvement)\")\nprint(\"\\\n  Sample generations:\")\nfor prompt in [\"the \", \"a \", \"the model \"]:\n    print(f\"  '{prompt}' -> '{prompt}{gen(prompt,60,0.6)}'\")\nprint(f\"\\\n  Model ready for generation experiments!\")\n  </code></pre>\n  <div id=\"output-l32-1\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z\"/><path d=\"m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65\"/><path d=\"m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65\"/></svg>Part 2: Generation Strategies</h2>\n\n<p>A language model produces a probability distribution over the vocabulary at each step. How we <em>choose</em> a token dramatically affects output quality. The four main strategies:</p>\n\n<ul>\n<li><strong>Greedy:</strong> Always pick the highest-probability token. Deterministic but often repetitive.</li>\n<li><strong>Temperature:</strong> Divide logits by T before softmax. T&lt;1 = sharper (confident); T&gt;1 = flatter (random).</li>\n<li><strong>Top-k:</strong> Only consider the k most probable tokens, renormalize, sample. Prevents long-tail noise. GPT-2 default: k=40.</li>\n<li><strong>Top-p (Nucleus):</strong> Include smallest token set whose cumulative probability exceeds p. Adapts dynamically to confidence. Default in Claude, GPT-4, and most modern APIs.</li>\n</ul>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-2')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# FOUR GENERATION STRATEGIES - Implementation & Comparison\n# ============================================================\n\ndef sm(logits):\n    s=logits-logits.max();e=np.exp(s);return e/e.sum()\ndef greedy(lo):return int(np.argmax(lo))\ndef temp_sample(lo,T=0.8):p=sm(lo/max(T,1e-8));return int(np.random.choice(len(p),p=p))\ndef topk_sample(lo,k=10,T=0.8):\n    sc=lo/max(T,1e-8);top=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[top]=sc[top]\n    p=sm(f);return int(np.random.choice(len(p),p=p))\ndef topp_sample(lo,p_th=0.9,T=0.8):\n    p=sm(lo/max(T,1e-8));si=np.argsort(p)[::-1];cs=np.cumsum(p[si])\n    cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=p[ni];np_/=np_.sum()\n    return int(np.random.choice(ni,p=np_))\n\n# Simulate realistic logits\nV=50;logits=np.random.randn(V)*0.5\nlogits[10]=3.0;logits[25]=2.5;logits[30]=2.0;logits[5]=1.5\nprobs=sm(logits);top5=np.argsort(probs)[-5:][::-1]\n\nprint(\"=\"*58)\nprint(\"  GENERATION STRATEGIES COMPARISON\")\nprint(\"=\"*58)\nprint(\"\\\n  Top 5 tokens by probability:\")\nfor i,idx in enumerate(top5):\n    print(f\"    {i+1}. Token {idx}: {probs[idx]:.4f} ({probs[idx]*100:.1f}%)\")\n\nfrom collections import Counter\nstrats={'Greedy':lambda l:greedy(l),'Temp=0.5':lambda l:temp_sample(l,0.5),\n    'Temp=1.0':lambda l:temp_sample(l,1.0),'Temp=1.5':lambda l:temp_sample(l,1.5),\n    'Top-k(5)':lambda l:topk_sample(l,5),'Top-k(20)':lambda l:topk_sample(l,20),\n    'Top-p(.8)':lambda l:topp_sample(l,0.8),'Top-p(.95)':lambda l:topp_sample(l,0.95)}\nprint(f\"\\\n  {'Strategy':<14} {'Unique':>7} {'Most common':>14}\")\nprint(\"  \"+\"-\"*40)\nfor name,fn in strats.items():\n    samp=[fn(logits.copy())for _ in range(100)];u=len(set(samp))\n    mc=Counter(samp).most_common(1)[0]\n    print(f\"  {name:<14} {u:>5}     Token {mc[0]}:{mc[1]:>3}x\")\n\nprint(f\"\\\n  TEMPERATURE EFFECT ON TOP TOKENS\")\nprint(f\"  {'Token':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:print(f\"{'T='+str(t):>8}\",end=\"\")\nprint()\nfor idx in top5[:4]:\n    print(f\"  #{idx:<5}\",end=\"\")\n    for t in[0.3,0.7,1.0,1.5]:print(f\"{sm(logits/t)[idx]:>8.3f}\",end=\"\")\n    print()\nprint(f\"\\\n  {'Entropy':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:\n    p=sm(logits/t);H=-np.sum(p*np.log(p+1e-10));print(f\"{H:>8.3f}\",end=\"\")\nprint(\"  (higher = more random)\")\n  </code></pre>\n  <div id=\"output-l32-2\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z\"/><path d=\"m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65\"/><path d=\"m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65\"/></svg>Part 3: Evaluating Generation Quality</h2>\n\n<p>How do we know if our model is any good? Three key metrics: <strong>Perplexity</strong> (exp of average cross-entropy loss &mdash; lower is better, 1.0 = perfect), <strong>Token Diversity</strong> (fraction of unique unigrams and bigrams), and <strong>Repetition Rate</strong> (how often n-grams repeat &mdash; a common LLM failure mode).</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-3')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# GENERATION QUALITY EVALUATION\n# Reuses model components defined in previous blocks\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(80):\n    idx=np.random.choice(len(data),6);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True);p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.004 if step<15 else 0.002*(1+np.cos(np.pi*(step-15)/65))/2\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef gen(prompt,n=80,temp=0.7,strat='temp',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temp':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/temp;ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nppl_total=0.0\nfor i in range(min(20,len(data))):\n    inp,tgt=data[i];lo=model.forward(inp.reshape(1,-1))[0]\n    s=lo-lo.max(-1,keepdims=True);lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    ppl_total-=lp[np.arange(T),tgt].mean()\nppl=float(np.exp(ppl_total/min(20,len(data))))\n\nprint(\"=\"*58)\nprint(\"  GENERATION QUALITY EVALUATION\")\nprint(\"=\"*58)\nprint(f\"\\\n  1. PERPLEXITY: {ppl:.1f} (random: {tk.V})\")\nif ppl<tk.V:print(f\"     {(1-ppl/tk.V)*100:.0f}% better than random!\")\nprint(f\"\\\n  2. STRATEGY COMPARISON\")\nprint(f\"     {'Strategy':<14} {'Div-1':>7} {'Div-2':>7} {'3g-Rep':>7}\")\nprint(\"     \"+\"-\"*38)\nfor name,strat in[('Greedy','greedy'),('Temp 0.7','temp'),('Top-k 10','top_k'),('Top-p 0.9','top_p')]:\n    d1s,d2s,rs=[],[],[]\n    for _ in range(5):\n        t=gen(\"the \",80,0.7,strat)\n        d1=len(set(t))/max(1,len(t));bg=[t[i:i+2]for i in range(len(t)-1)];d2=len(set(bg))/max(1,len(bg))\n        ng=[t[i:i+3]for i in range(len(t)-2)];r=1-len(set(ng))/max(1,len(ng))\n        d1s.append(d1);d2s.append(d2);rs.append(r)\n    print(f\"     {name:<14} {np.mean(d1s):>5.3f}   {np.mean(d2s):>5.3f}   {np.mean(rs):>5.3f}\")\nprint(f\"\\\n  3. SAMPLE OUTPUTS\")\nfor name,strat in[('Greedy','greedy'),('Temp','temp'),('Top-k','top_k'),('Top-p','top_p')]:\n    t=gen(\"the \",50,0.7,strat)\n    print(f\"     {name:<7} 'the {t[:48].replace(chr(10),chr(32))}'\")\n  </code></pre>\n  <div id=\"output-l32-3\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z\"/><path d=\"m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65\"/><path d=\"m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65\"/></svg>Part 4: Interactive Generation Playground</h2>\n\n<p>Experiment with all generation parameters: change temperature, try different prompts, switch strategies.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-4')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# INTERACTIVE TEXT GENERATION PLAYGROUND\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True))\n        return(e/e.sum(-1,keepdims=True)@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(100):\n    idx=np.random.choice(len(data),8);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True);oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.005*(step+1)/15 if step<15 else 0.0025*(1+np.cos(np.pi*(step-15)/85))/2+0.0005\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef go(prompt,n=80,temp=0.7,strat='top_p',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temperature':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/max(temp,0.01);ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nprint(\"=\"*58)\nprint(\"  INTERACTIVE TEXT GENERATION PLAYGROUND\")\nprint(\"=\"*58)\nprint(\"\\\n  EXP 1: Temperature Sweep\")\nfor temp in [0.2, 0.5, 0.7, 1.0, 1.5]:\n    t=go(\"the \",55,temp,'temperature');print(f\"  T={temp:<4} '{t[:50].replace(chr(10),chr(32))}'\")\nprint(f\"\\\n  EXP 2: Strategy Comparison\")\nfor strat,name in[('greedy','Greedy'),('temperature','Temp'),('top_k','Top-k'),('top_p','Top-p')]:\n    t=go(\"the model \",55,0.7,strat,10,0.9);print(f\"  {name:<7} '{t[:50].replace(chr(10),chr(32))}'\")\nprint(f\"\\\n  EXP 3: Various Prompts\")\nfor prompt in[\"the cat \",\"a neural \",\"training \",\"the sun \",\"each layer \"]:\n    t=go(prompt,50,0.6,'top_p');print(f\"  '{prompt}' -> '{t[:45].replace(chr(10),chr(32))}'\")\nprint(f\"\\\n  EXP 4: Long Generation\")\nlong=go(\"the \",200,0.7,'top_p');disp=long.replace(chr(10),chr(32))\nprint(f\"  '{disp[:80]}'\")\nwords=disp.split()\nprint(f\"  Words:{len(words)} Unique:{len(set(words))} Rep:{1-len(set(words))/max(1,len(words)):.0%}\")\n\nprint(\"\\\n\"+\"=\"*58)\nprint(\"  CONGRATULATIONS!\")\nprint(\"=\"*58)\nprint(\"  You built a complete language model from scratch:\")\nprint(\"  Tokenizer, Embeddings, Attention, FFN, LayerNorm,\")\nprint(\"  Residual Connections, Transformer Blocks, GPT-2,\")\nprint(\"  Cross-Entropy Loss, Backprop, AdamW, LR Scheduling,\")\nprint(\"  Gradient Clipping, Training Loop, Checkpointing,\")\nprint(\"  and 4 Generation Strategies with Evaluation.\")\nprint()\nprint(\"  Scale from 75K to 175B params, KB to TB of data,\")\nprint(\"  seconds to months of compute -- and you get GPT-4.\")\nprint(\"  Same architecture. Same math. You understand it all.\")\n  </code></pre>\n  <div id=\"output-l32-4\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"m12.83 2.18a2 2 0 0 0-1.66 0L2.6 6.08a1 1 0 0 0 0 1.83l8.58 3.91a2 2 0 0 0 1.66 0l8.58-3.9a1 1 0 0 0 0-1.83Z\"/><path d=\"m22 17.65-9.17 4.16a2 2 0 0 1-1.66 0L2 17.65\"/><path d=\"m22 12.65-9.17 4.16a2 2 0 0 1-1.66 0L2 12.65\"/></svg>Part 5: What Our Model Learned and What Comes Next</h2>\n\n<p>Despite ~75,000 parameters on a few kilobytes, our model learned character frequencies, character sequences (\"th\" is common), word boundaries (spaces at correct intervals), and common phrases. Scale this up and capabilities grow dramatically:</p>\n\n<table class=\"comparison-table\">\n<thead>\n<tr><th>Scale</th><th>Parameters</th><th>Data</th><th>Capabilities</th></tr>\n</thead>\n<tbody>\n<tr><td>Our model</td><td>75K</td><td>~5KB</td><td>Character patterns, word fragments</td></tr>\n<tr><td>GPT-2</td><td>1.5B</td><td>~40GB</td><td>Coherent paragraphs, basic reasoning</td></tr>\n<tr><td>GPT-3</td><td>175B</td><td>~570GB</td><td>In-context learning, translation, code</td></tr>\n<tr><td>GPT-4</td><td>~1.8T</td><td>~13TB</td><td>Complex reasoning, multimodal</td></tr>\n</tbody>\n</table>\n\n<p>What comes next beyond pretraining: <strong>Supervised Fine-Tuning</strong> (instruction-response pairs), <strong>Alignment</strong> (RLHF / Constitutional AI), and <strong>Deployment</strong> (quantization, KV-caching, model parallelism).</p>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M6 9H4.5a2.5 2.5 0 0 1 0-5H6\"/><path d=\"M18 9h1.5a2.5 2.5 0 0 0 0-5H18\"/><path d=\"M4 22h16\"/><path d=\"M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22\"/><path d=\"M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22\"/><path d=\"M18 2H6v7a6 6 0 0 0 12 0V2Z\"/></svg>Final Reflection</h2>\n\n<p>Over 32 lectures, you built every component from scratch: tokenization, embeddings, attention, feed-forward networks, normalization, residual connections, Transformer blocks, the GPT-2 architecture, cross-entropy loss, backpropagation, AdamW, LR scheduling, gradient clipping, and four generation strategies. No black box remains.</p>\n\n<div class=\"info-box\">\n<strong>The Key Insight:</strong> An LLM is not magic. It is linear algebra, calculus, and statistics in the Transformer architecture, trained to predict the next token. The capabilities of ChatGPT and Claude emerge from this architecture, this objective, and <em>massive</em> scale. You understand all of it.\n</div>\n\n<p>You built a real, working language model from scratch. <strong>That is a remarkable achievement.</strong></p>\n\n${navButtons('31','33')}",
    "ro": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"lecture-label\">LECTURE 32 <span class=\"stage-badge\">Etapa 5: Preantrenare</span></div>\n<h1 class=\"lecture-title\">Lecția 32: Proiectul Final al Etapei 5 &mdash; Generator de Text Preantrenat</h1>\n<div class=\"lecture-subtitle\">Un Sistem Complet: Model Preantrenat, Strategii Multiple de Generare, Evaluarea Calității și Playground Interactiv</div>\n<div class=\"lecture-meta\">\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>50 min de citit</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Proiect Final</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Ați construit un model de limbaj de la zero. Acum priviți-l cum vorbește.</span>\n</div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Introducere: Punctul Culminant</h2>\n\n<p>În Proiectul Final al Etapei 4 (Lecția 24), ați construit <em>arhitectura</em> Mini GPT-2 &mdash; dar cu ponderi aleatorii, putea produce doar text fără sens. Acum, după ce ați construit întreaga mașinărie de antrenament, veți vedea <strong>un model pe care l-ați construit și antrenat de la zero generând text care seamănă cu tipare reale de limbaj.</strong> Așa funcționează ChatGPT, Claude și fiecare LLM. Diferența este doar de scară.</p>\n\n<div class=\"info-box\">\n<strong>Ce Veți Construi:</strong> Un Mini GPT-2 preantrenat cu patru strategii de generare (greedy, temperature, top-k, top-p), metrici de evaluare a calității și un playground interactiv de generare.\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Partea 1: Preantrenare cu Corpus Extins</h2>\n\n<p>Folosim un corpus cu structuri variate de propoziții și suficientă repetare pentru ca modelul nostru mic să învețe tipare semnificative.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-1')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# STAGE 5 CAPSTONE: PRETRAINED TEXT GENERATOR\n# ============================================================\n\nclass CharTok:\n    def __init__(self, text):\n        ch = sorted(set(text))\n        self.c2i={c:i for i,c in enumerate(ch)}\n        self.i2c={i:c for i,c in enumerate(ch)}\n        self.V=len(ch)\n    def encode(self,t): return [self.c2i.get(c,0) for c in t]\n    def decode(self,ids): return ''.join(self.i2c.get(i,'?') for i in ids)\n\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):\n        mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True)\n        return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass Attn:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h\n        self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3)\n        K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3)\n        sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9)\n        e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FFN:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df)\n        self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):\n        z=x@self.W1+self.b1\n        return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Blk:\n    def __init__(self,d,h,s):\n        self.ln1=LN(d);self.at=Attn(d,h,s);self.ln2=LN(d);self.ff=FFN(d,s)\n    def forward(self,x):\n        x=x+self.at.forward(self.ln1.forward(x))\n        x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L)\n        self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Blk(d,h,rs) for _ in range(L)]\n        self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ndef cross_entropy(logits,targets):\n    B,T,V=logits.shape;lf=logits.reshape(B*T,V);tf=targets.reshape(B*T)\n    s=lf-lf.max(-1,keepdims=True)\n    lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    return -lp[np.arange(B*T),tf].mean()\n\ndef train_step(model,inp,tgt,lr):\n    logits=model.forward(inp);loss=cross_entropy(logits,tgt)\n    B,T,V=logits.shape\n    s=logits-logits.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(T):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*T)\n    x=model.tok[inp]+model.pos[:T]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x)\n    dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    model.head-=lr*dh\n    de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(T):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n    return loss\n\n# === TRAINING CORPUS ===\ncorpus = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(corpus)\nids = tok.encode(corpus)\nT = 32\ndata = []\nfor i in range(0,len(ids)-T,12):\n    ch=ids[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\n\nmodel = GPT(tok.V, d=64, h=4, L=4, T=T)\nprint(\"=\" * 58)\nprint(\"  CAPSTONE: PRETRAINING MINI GPT-2\")\nprint(\"=\" * 58)\nprint(f\"  Vocab: {tok.V} | Tokens: {len(ids)} | Examples: {len(data)}\")\n\nmax_steps=100; bs=8; max_lr=0.005; min_lr=0.0005; warmup=15\nlosses=[]\nfor step in range(max_steps):\n    idx=np.random.choice(len(data),bs)\n    inp=np.stack([data[i][0] for i in idx])\n    tgt=np.stack([data[i][1] for i in idx])\n    if step<warmup: lr=max_lr*(step+1)/warmup\n    else:\n        prog=(step-warmup)/max(1,max_steps-warmup)\n        lr=min_lr+0.5*(max_lr-min_lr)*(1+np.cos(np.pi*prog))\n    loss=train_step(model,inp,tgt,lr)\n    losses.append(float(loss))\n    if step%20==0 or step==max_steps-1:\n        print(f\"  Step {step:3d} | Loss: {loss:.4f} | LR: {lr:.6f}\")\n\ndef gen(prompt,n=60,temp=0.7):\n    ids=tok.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]])\n        logits=model.forward(ctx)[0,-1]/temp\n        p=np.exp(logits-logits.max());p/=p.sum()\n        ids.append(int(np.random.choice(len(p),p=p)))\n    return tok.decode(ids[len(tok.encode(prompt)):])\n\nprint(f\"\\\n  Loss: {losses[0]:.4f} -> {losses[-1]:.4f} ({(1-losses[-1]/losses[0])*100:.1f}% improvement)\")\nprint(\"\\\n  Sample generations:\")\nfor prompt in [\"the \", \"a \", \"the model \"]:\n    print(f\"  '{prompt}' -> '{prompt}{gen(prompt,60,0.6)}'\")\nprint(f\"\\\n  Model ready for generation experiments!\")\n  </code></pre>\n  <div id=\"output-l32-1\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Partea 2: Strategii de Generare</h2>\n\n<p>Un model de limbaj produce o distribuție de probabilitate peste vocabular la fiecare pas. Modul în care <em>alegem</em> un token afectează dramatic calitatea rezultatului. Cele patru strategii principale:</p>\n\n<ul>\n<li><strong>Greedy:</strong> Alege întotdeauna token-ul cu probabilitatea cea mai mare. Determinist dar adesea repetitiv.</li>\n<li><strong>Temperature:</strong> Împarte logits-urile la T înainte de softmax. T&lt;1 = mai ascuțit (încrezător); T&gt;1 = mai plat (aleatoriu).</li>\n<li><strong>Top-k:</strong> Consideră doar cele mai probabile k token-uri, renormalizează, eșantionează. Previne zgomotul de coadă lungă. Valoare implicită GPT-2: k=40.</li>\n<li><strong>Top-p (Nucleus):</strong> Include cel mai mic set de token-uri a căror probabilitate cumulativă depășește p. Se adaptează dinamic la nivelul de încredere. Implicit în Claude, GPT-4 și majoritatea API-urilor moderne.</li>\n</ul>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-2')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# FOUR GENERATION STRATEGIES - Implementation & Comparison\n# ============================================================\n\ndef sm(logits):\n    s=logits-logits.max();e=np.exp(s);return e/e.sum()\ndef greedy(lo):return int(np.argmax(lo))\ndef temp_sample(lo,T=0.8):p=sm(lo/max(T,1e-8));return int(np.random.choice(len(p),p=p))\ndef topk_sample(lo,k=10,T=0.8):\n    sc=lo/max(T,1e-8);top=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[top]=sc[top]\n    p=sm(f);return int(np.random.choice(len(p),p=p))\ndef topp_sample(lo,p_th=0.9,T=0.8):\n    p=sm(lo/max(T,1e-8));si=np.argsort(p)[::-1];cs=np.cumsum(p[si])\n    cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=p[ni];np_/=np_.sum()\n    return int(np.random.choice(ni,p=np_))\n\n# Simulate realistic logits\nV=50;logits=np.random.randn(V)*0.5\nlogits[10]=3.0;logits[25]=2.5;logits[30]=2.0;logits[5]=1.5\nprobs=sm(logits);top5=np.argsort(probs)[-5:][::-1]\n\nprint(\"=\"*58)\nprint(\"  GENERATION STRATEGIES COMPARISON\")\nprint(\"=\"*58)\nprint(\"\\\n  Top 5 tokens by probability:\")\nfor i,idx in enumerate(top5):\n    print(f\"    {i+1}. Token {idx}: {probs[idx]:.4f} ({probs[idx]*100:.1f}%)\")\n\nfrom collections import Counter\nstrats={'Greedy':lambda l:greedy(l),'Temp=0.5':lambda l:temp_sample(l,0.5),\n    'Temp=1.0':lambda l:temp_sample(l,1.0),'Temp=1.5':lambda l:temp_sample(l,1.5),\n    'Top-k(5)':lambda l:topk_sample(l,5),'Top-k(20)':lambda l:topk_sample(l,20),\n    'Top-p(.8)':lambda l:topp_sample(l,0.8),'Top-p(.95)':lambda l:topp_sample(l,0.95)}\nprint(f\"\\\n  {'Strategy':<14} {'Unique':>7} {'Most common':>14}\")\nprint(\"  \"+\"-\"*40)\nfor name,fn in strats.items():\n    samp=[fn(logits.copy())for _ in range(100)];u=len(set(samp))\n    mc=Counter(samp).most_common(1)[0]\n    print(f\"  {name:<14} {u:>5}     Token {mc[0]}:{mc[1]:>3}x\")\n\nprint(f\"\\\n  TEMPERATURE EFFECT ON TOP TOKENS\")\nprint(f\"  {'Token':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:print(f\"{'T='+str(t):>8}\",end=\"\")\nprint()\nfor idx in top5[:4]:\n    print(f\"  #{idx:<5}\",end=\"\")\n    for t in[0.3,0.7,1.0,1.5]:print(f\"{sm(logits/t)[idx]:>8.3f}\",end=\"\")\n    print()\nprint(f\"\\\n  {'Entropy':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:\n    p=sm(logits/t);H=-np.sum(p*np.log(p+1e-10));print(f\"{H:>8.3f}\",end=\"\")\nprint(\"  (higher = more random)\")\n  </code></pre>\n  <div id=\"output-l32-2\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Partea 3: Evaluarea Calității Generării</h2>\n\n<p>Cum știm dacă modelul nostru este bun? Trei metrici cheie: <strong>Perplexitate</strong> (exp din media cross-entropy loss &mdash; mai mic este mai bine, 1.0 = perfect), <strong>Diversitatea Token-urilor</strong> (fracțiunea de unigrame și bigrame unice) și <strong>Rata de Repetiție</strong> (cât de des se repetă n-gramele &mdash; un mod comun de eșec al LLM-urilor).</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-3')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# GENERATION QUALITY EVALUATION\n# Reuses model components defined in previous blocks\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(80):\n    idx=np.random.choice(len(data),6);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True);p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.004 if step<15 else 0.002*(1+np.cos(np.pi*(step-15)/65))/2\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef gen(prompt,n=80,temp=0.7,strat='temp',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temp':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/temp;ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nppl_total=0.0\nfor i in range(min(20,len(data))):\n    inp,tgt=data[i];lo=model.forward(inp.reshape(1,-1))[0]\n    s=lo-lo.max(-1,keepdims=True);lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    ppl_total-=lp[np.arange(T),tgt].mean()\nppl=float(np.exp(ppl_total/min(20,len(data))))\n\nprint(\"=\"*58)\nprint(\"  GENERATION QUALITY EVALUATION\")\nprint(\"=\"*58)\nprint(f\"\\\n  1. PERPLEXITY: {ppl:.1f} (random: {tk.V})\")\nif ppl<tk.V:print(f\"     {(1-ppl/tk.V)*100:.0f}% better than random!\")\nprint(f\"\\\n  2. STRATEGY COMPARISON\")\nprint(f\"     {'Strategy':<14} {'Div-1':>7} {'Div-2':>7} {'3g-Rep':>7}\")\nprint(\"     \"+\"-\"*38)\nfor name,strat in[('Greedy','greedy'),('Temp 0.7','temp'),('Top-k 10','top_k'),('Top-p 0.9','top_p')]:\n    d1s,d2s,rs=[],[],[]\n    for _ in range(5):\n        t=gen(\"the \",80,0.7,strat)\n        d1=len(set(t))/max(1,len(t));bg=[t[i:i+2]for i in range(len(t)-1)];d2=len(set(bg))/max(1,len(bg))\n        ng=[t[i:i+3]for i in range(len(t)-2)];r=1-len(set(ng))/max(1,len(ng))\n        d1s.append(d1);d2s.append(d2);rs.append(r)\n    print(f\"     {name:<14} {np.mean(d1s):>5.3f}   {np.mean(d2s):>5.3f}   {np.mean(rs):>5.3f}\")\nprint(f\"\\\n  3. SAMPLE OUTPUTS\")\nfor name,strat in[('Greedy','greedy'),('Temp','temp'),('Top-k','top_k'),('Top-p','top_p')]:\n    t=gen(\"the \",50,0.7,strat)\n    print(f\"     {name:<7} 'the {t[:48].replace(chr(10),' ')}'\")\n  </code></pre>\n  <div id=\"output-l32-3\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Partea 4: Playground Interactiv de Generare</h2>\n\n<p>Experimentați cu toți parametrii de generare: schimbați temperatura, încercați diferite prompt-uri, comutați între strategii.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-4')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# INTERACTIVE TEXT GENERATION PLAYGROUND\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True))\n        return(e/e.sum(-1,keepdims=True)@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(100):\n    idx=np.random.choice(len(data),8);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True);oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.005*(step+1)/15 if step<15 else 0.0025*(1+np.cos(np.pi*(step-15)/85))/2+0.0005\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef go(prompt,n=80,temp=0.7,strat='top_p',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temperature':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/max(temp,0.01);ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nprint(\"=\"*58)\nprint(\"  INTERACTIVE TEXT GENERATION PLAYGROUND\")\nprint(\"=\"*58)\nprint(\"\\\n  EXP 1: Temperature Sweep\")\nfor temp in [0.2, 0.5, 0.7, 1.0, 1.5]:\n    t=go(\"the \",55,temp,'temperature');print(f\"  T={temp:<4} '{t[:50].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 2: Strategy Comparison\")\nfor strat,name in[('greedy','Greedy'),('temperature','Temp'),('top_k','Top-k'),('top_p','Top-p')]:\n    t=go(\"the model \",55,0.7,strat,10,0.9);print(f\"  {name:<7} '{t[:50].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 3: Various Prompts\")\nfor prompt in[\"the cat \",\"a neural \",\"training \",\"the sun \",\"each layer \"]:\n    t=go(prompt,50,0.6,'top_p');print(f\"  '{prompt}' -> '{t[:45].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 4: Long Generation\")\nlong=go(\"the \",200,0.7,'top_p');disp=long.replace(chr(10),' ')\nprint(f\"  '{disp[:80]}'\")\nwords=disp.split()\nprint(f\"  Words:{len(words)} Unique:{len(set(words))} Rep:{1-len(set(words))/max(1,len(words)):.0%}\")\n\nprint(\"\\\n\"+\"=\"*58)\nprint(\"  CONGRATULATIONS!\")\nprint(\"=\"*58)\nprint(\"  You built a complete language model from scratch:\")\nprint(\"  Tokenizer, Embeddings, Attention, FFN, LayerNorm,\")\nprint(\"  Residual Connections, Transformer Blocks, GPT-2,\")\nprint(\"  Cross-Entropy Loss, Backprop, AdamW, LR Scheduling,\")\nprint(\"  Gradient Clipping, Training Loop, Checkpointing,\")\nprint(\"  and 4 Generation Strategies with Evaluation.\")\nprint()\nprint(\"  Scale from 75K to 175B params, KB to TB of data,\")\nprint(\"  seconds to months of compute -- and you get GPT-4.\")\nprint(\"  Same architecture. Same math. You understand it all.\")\n  </code></pre>\n  <div id=\"output-l32-4\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><rect width=\"16\" height=\"16\" x=\"4\" y=\"4\" rx=\"2\"/><rect width=\"6\" height=\"6\" x=\"9\" y=\"9\" rx=\"1\"/><path d=\"M15 2v2\"/><path d=\"M15 20v2\"/><path d=\"M2 15h2\"/><path d=\"M2 9h2\"/><path d=\"M20 15h2\"/><path d=\"M20 9h2\"/><path d=\"M9 2v2\"/><path d=\"M9 20v2\"/></svg>Partea 5: Ce a Învățat Modelul Nostru și Ce Urmează</h2>\n\n<p>În ciuda a ~75.000 de parametri pe câțiva kilobytes, modelul nostru a învățat frecvențele caracterelor, secvențele de caractere („th\" este frecvent), limitele cuvintelor (spații la intervale corecte) și fraze comune. Măriți scara și capacitățile cresc dramatic:</p>\n\n<table class=\"comparison-table\">\n<thead>\n<tr><th>Scară</th><th>Parametri</th><th>Date</th><th>Capacități</th></tr>\n</thead>\n<tbody>\n<tr><td>Modelul nostru</td><td>75K</td><td>~5KB</td><td>Tipare de caractere, fragmente de cuvinte</td></tr>\n<tr><td>GPT-2</td><td>1.5B</td><td>~40GB</td><td>Paragrafe coerente, raționament de bază</td></tr>\n<tr><td>GPT-3</td><td>175B</td><td>~570GB</td><td>Învățare în context, traducere, cod</td></tr>\n<tr><td>GPT-4</td><td>~1.8T</td><td>~13TB</td><td>Raționament complex, multimodal</td></tr>\n</tbody>\n</table>\n\n<p>Ce urmează dincolo de preantrenare: <strong>Supervised Fine-Tuning</strong> (perechi instrucțiune-răspuns), <strong>Aliniere</strong> (RLHF / Constitutional AI) și <strong>Deployment</strong> (quantization, KV-caching, paralelism de model).</p>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M6 9H4.5a2.5 2.5 0 0 1 0-5H6\"/><path d=\"M18 9h1.5a2.5 2.5 0 0 0 0-5H18\"/><path d=\"M4 22h16\"/><path d=\"M10 14.66V17c0 .55-.47.98-.97 1.21C7.85 18.75 7 20.24 7 22\"/><path d=\"M14 14.66V17c0 .55.47.98.97 1.21C16.15 18.75 17 20.24 17 22\"/><path d=\"M18 2H6v7a6 6 0 0 0 12 0V2Z\"/></svg>Reflecție Finală</h2>\n\n<p>De-a lungul a 32 de lecții, ați construit fiecare componentă de la zero: tokenizare, embedding-uri, attention, rețele feed-forward, normalizare, conexiuni reziduale, blocuri Transformer, arhitectura GPT-2, cross-entropy loss, backpropagation, AdamW, programarea LR, gradient clipping și patru strategii de generare. Nicio cutie neagră nu a rămas.</p>\n\n<div class=\"info-box\">\n<strong>Descoperirea Cheie:</strong> Un LLM nu este magie. Este algebră liniară, calcul diferențial și statistică în arhitectura Transformer, antrenat să prezică următorul token. Capacitățile ChatGPT și Claude apar din această arhitectură, acest obiectiv și o scară <em>masivă</em>. Înțelegeți totul.\n</div>\n\n<p>Ați construit un model de limbaj real și funcțional de la zero. <strong>Aceasta este o realizare remarcabilă.</strong></p>",
    "el": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"lecture-label\">LECTURE 32 <span class=\"stage-badge\">Στάδιο 5: Προεκπαίδευση</span></div>\n<h1 class=\"lecture-title\">Διάλεξη 32: Τελικό Έργο Σταδίου 5 &mdash; Γεννήτρια Κειμένου με Προεκπαίδευση</h1>\n<div class=\"lecture-subtitle\">Ένα Πλήρες Σύστημα: Προεκπαιδευμένο Μοντέλο, Πολλαπλές Στρατηγικές Παραγωγής, Αξιολόγηση Ποιότητας και Διαδραστικό Playground</div>\n<div class=\"lecture-meta\">\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>50 λεπτά ανάγνωση</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Τελικό Έργο</span>\n<span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Χτίσατε ένα γλωσσικό μοντέλο από το μηδέν. Τώρα δείτε το να μιλάει.</span>\n</div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Εισαγωγή: Η Κορύφωση</h2>\n\n<p>Στο Τελικό Έργο του Σταδίου 4 (Διάλεξη 24), χτίσατε την <em>αρχιτεκτονική</em> Mini GPT-2 &mdash; αλλά με τυχαία βάρη, μπορούσε να παράγει μόνο ασυναρτησίες. Τώρα, αφού χτίσατε ολόκληρο τον μηχανισμό εκπαίδευσης, θα δείτε <strong>ένα μοντέλο που χτίσατε και εκπαιδεύσατε από το μηδέν να παράγει κείμενο που μοιάζει με πραγματικά γλωσσικά μοτίβα.</strong> Έτσι λειτουργούν το ChatGPT, το Claude και κάθε LLM. Η διαφορά είναι μόνο η κλίμακα.</p>\n\n<div class=\"info-box\">\n<strong>Τι Θα Χτίσετε:</strong> Ένα προεκπαιδευμένο Mini GPT-2 με τέσσερις στρατηγικές παραγωγής (greedy, temperature, top-k, top-p), μετρικές αξιολόγησης ποιότητας και ένα διαδραστικό playground παραγωγής.\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Μέρος 1: Προεκπαίδευση με Εκτεταμένο Corpus</h2>\n\n<p>Χρησιμοποιούμε ένα corpus με ποικίλες δομές προτάσεων και αρκετή επανάληψη ώστε το μικρό μας μοντέλο να μάθει σημαντικά μοτίβα.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-1')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# STAGE 5 CAPSTONE: PRETRAINED TEXT GENERATOR\n# ============================================================\n\nclass CharTok:\n    def __init__(self, text):\n        ch = sorted(set(text))\n        self.c2i={c:i for i,c in enumerate(ch)}\n        self.i2c={i:c for i,c in enumerate(ch)}\n        self.V=len(ch)\n    def encode(self,t): return [self.c2i.get(c,0) for c in t]\n    def decode(self,ids): return ''.join(self.i2c.get(i,'?') for i in ids)\n\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):\n        mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True)\n        return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass Attn:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h\n        self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3)\n        K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3)\n        sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9)\n        e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FFN:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df)\n        self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):\n        z=x@self.W1+self.b1\n        return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Blk:\n    def __init__(self,d,h,s):\n        self.ln1=LN(d);self.at=Attn(d,h,s);self.ln2=LN(d);self.ff=FFN(d,s)\n    def forward(self,x):\n        x=x+self.at.forward(self.ln1.forward(x))\n        x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L)\n        self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Blk(d,h,rs) for _ in range(L)]\n        self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ndef cross_entropy(logits,targets):\n    B,T,V=logits.shape;lf=logits.reshape(B*T,V);tf=targets.reshape(B*T)\n    s=lf-lf.max(-1,keepdims=True)\n    lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    return -lp[np.arange(B*T),tf].mean()\n\ndef train_step(model,inp,tgt,lr):\n    logits=model.forward(inp);loss=cross_entropy(logits,tgt)\n    B,T,V=logits.shape\n    s=logits-logits.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(T):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*T)\n    x=model.tok[inp]+model.pos[:T]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x)\n    dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    model.head-=lr*dh\n    de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(T):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n    return loss\n\n# === TRAINING CORPUS ===\ncorpus = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(corpus)\nids = tok.encode(corpus)\nT = 32\ndata = []\nfor i in range(0,len(ids)-T,12):\n    ch=ids[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\n\nmodel = GPT(tok.V, d=64, h=4, L=4, T=T)\nprint(\"=\" * 58)\nprint(\"  CAPSTONE: PRETRAINING MINI GPT-2\")\nprint(\"=\" * 58)\nprint(f\"  Vocab: {tok.V} | Tokens: {len(ids)} | Examples: {len(data)}\")\n\nmax_steps=100; bs=8; max_lr=0.005; min_lr=0.0005; warmup=15\nlosses=[]\nfor step in range(max_steps):\n    idx=np.random.choice(len(data),bs)\n    inp=np.stack([data[i][0] for i in idx])\n    tgt=np.stack([data[i][1] for i in idx])\n    if step<warmup: lr=max_lr*(step+1)/warmup\n    else:\n        prog=(step-warmup)/max(1,max_steps-warmup)\n        lr=min_lr+0.5*(max_lr-min_lr)*(1+np.cos(np.pi*prog))\n    loss=train_step(model,inp,tgt,lr)\n    losses.append(float(loss))\n    if step%20==0 or step==max_steps-1:\n        print(f\"  Step {step:3d} | Loss: {loss:.4f} | LR: {lr:.6f}\")\n\ndef gen(prompt,n=60,temp=0.7):\n    ids=tok.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]])\n        logits=model.forward(ctx)[0,-1]/temp\n        p=np.exp(logits-logits.max());p/=p.sum()\n        ids.append(int(np.random.choice(len(p),p=p)))\n    return tok.decode(ids[len(tok.encode(prompt)):])\n\nprint(f\"\\\n  Loss: {losses[0]:.4f} -> {losses[-1]:.4f} ({(1-losses[-1]/losses[0])*100:.1f}% improvement)\")\nprint(\"\\\n  Sample generations:\")\nfor prompt in [\"the \", \"a \", \"the model \"]:\n    print(f\"  '{prompt}' -> '{prompt}{gen(prompt,60,0.6)}'\")\nprint(f\"\\\n  Model ready for generation experiments!\")\n  </code></pre>\n  <div id=\"output-l32-1\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Μέρος 2: Στρατηγικές Παραγωγής</h2>\n\n<p>Ένα γλωσσικό μοντέλο παράγει μια κατανομή πιθανοτήτων πάνω στο λεξιλόγιο σε κάθε βήμα. Ο τρόπος που <em>επιλέγουμε</em> ένα token επηρεάζει δραματικά την ποιότητα της εξόδου. Οι τέσσερις κύριες στρατηγικές:</p>\n\n<ul>\n<li><strong>Greedy:</strong> Πάντα επιλέγει το token με την υψηλότερη πιθανότητα. Ντετερμινιστικό αλλά συχνά επαναληπτικό.</li>\n<li><strong>Temperature:</strong> Διαιρεί τα logits με T πριν το softmax. T&lt;1 = πιο οξύ (σίγουρο)· T&gt;1 = πιο επίπεδο (τυχαίο).</li>\n<li><strong>Top-k:</strong> Εξετάζει μόνο τα k πιο πιθανά tokens, επανακανονικοποιεί, δειγματοληπτεί. Αποτρέπει τον θόρυβο μακριάς ουράς. Προεπιλογή GPT-2: k=40.</li>\n<li><strong>Top-p (Nucleus):</strong> Περιλαμβάνει το μικρότερο σύνολο tokens του οποίου η αθροιστική πιθανότητα υπερβαίνει το p. Προσαρμόζεται δυναμικά στο επίπεδο εμπιστοσύνης. Προεπιλογή σε Claude, GPT-4 και τα περισσότερα σύγχρονα APIs.</li>\n</ul>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-2')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# FOUR GENERATION STRATEGIES - Implementation & Comparison\n# ============================================================\n\ndef sm(logits):\n    s=logits-logits.max();e=np.exp(s);return e/e.sum()\ndef greedy(lo):return int(np.argmax(lo))\ndef temp_sample(lo,T=0.8):p=sm(lo/max(T,1e-8));return int(np.random.choice(len(p),p=p))\ndef topk_sample(lo,k=10,T=0.8):\n    sc=lo/max(T,1e-8);top=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[top]=sc[top]\n    p=sm(f);return int(np.random.choice(len(p),p=p))\ndef topp_sample(lo,p_th=0.9,T=0.8):\n    p=sm(lo/max(T,1e-8));si=np.argsort(p)[::-1];cs=np.cumsum(p[si])\n    cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=p[ni];np_/=np_.sum()\n    return int(np.random.choice(ni,p=np_))\n\n# Simulate realistic logits\nV=50;logits=np.random.randn(V)*0.5\nlogits[10]=3.0;logits[25]=2.5;logits[30]=2.0;logits[5]=1.5\nprobs=sm(logits);top5=np.argsort(probs)[-5:][::-1]\n\nprint(\"=\"*58)\nprint(\"  GENERATION STRATEGIES COMPARISON\")\nprint(\"=\"*58)\nprint(\"\\\n  Top 5 tokens by probability:\")\nfor i,idx in enumerate(top5):\n    print(f\"    {i+1}. Token {idx}: {probs[idx]:.4f} ({probs[idx]*100:.1f}%)\")\n\nfrom collections import Counter\nstrats={'Greedy':lambda l:greedy(l),'Temp=0.5':lambda l:temp_sample(l,0.5),\n    'Temp=1.0':lambda l:temp_sample(l,1.0),'Temp=1.5':lambda l:temp_sample(l,1.5),\n    'Top-k(5)':lambda l:topk_sample(l,5),'Top-k(20)':lambda l:topk_sample(l,20),\n    'Top-p(.8)':lambda l:topp_sample(l,0.8),'Top-p(.95)':lambda l:topp_sample(l,0.95)}\nprint(f\"\\\n  {'Strategy':<14} {'Unique':>7} {'Most common':>14}\")\nprint(\"  \"+\"-\"*40)\nfor name,fn in strats.items():\n    samp=[fn(logits.copy())for _ in range(100)];u=len(set(samp))\n    mc=Counter(samp).most_common(1)[0]\n    print(f\"  {name:<14} {u:>5}     Token {mc[0]}:{mc[1]:>3}x\")\n\nprint(f\"\\\n  TEMPERATURE EFFECT ON TOP TOKENS\")\nprint(f\"  {'Token':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:print(f\"{'T='+str(t):>8}\",end=\"\")\nprint()\nfor idx in top5[:4]:\n    print(f\"  #{idx:<5}\",end=\"\")\n    for t in[0.3,0.7,1.0,1.5]:print(f\"{sm(logits/t)[idx]:>8.3f}\",end=\"\")\n    print()\nprint(f\"\\\n  {'Entropy':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:\n    p=sm(logits/t);H=-np.sum(p*np.log(p+1e-10));print(f\"{H:>8.3f}\",end=\"\")\nprint(\"  (higher = more random)\")\n  </code></pre>\n  <div id=\"output-l32-2\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Μέρος 3: Αξιολόγηση Ποιότητας Παραγωγής</h2>\n\n<p>Πώς ξέρουμε αν το μοντέλο μας είναι καλό; Τρεις βασικές μετρικές: <strong>Perplexity</strong> (exp του μέσου cross-entropy loss &mdash; χαμηλότερο είναι καλύτερο, 1.0 = τέλειο), <strong>Ποικιλομορφία Tokens</strong> (κλάσμα μοναδικών unigrams και bigrams) και <strong>Ρυθμός Επανάληψης</strong> (πόσο συχνά επαναλαμβάνονται τα n-grams &mdash; ένας συνηθισμένος τρόπος αποτυχίας των LLM).</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-3')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# GENERATION QUALITY EVALUATION\n# Reuses model components defined in previous blocks\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True));a=e/e.sum(-1,keepdims=True)\n        return(a@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(80):\n    idx=np.random.choice(len(data),6);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True);p=np.exp(s)/np.exp(s).sum(-1,keepdims=True)\n    oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.004 if step<15 else 0.002*(1+np.cos(np.pi*(step-15)/65))/2\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef gen(prompt,n=80,temp=0.7,strat='temp',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temp':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/temp;ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/temp;pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nppl_total=0.0\nfor i in range(min(20,len(data))):\n    inp,tgt=data[i];lo=model.forward(inp.reshape(1,-1))[0]\n    s=lo-lo.max(-1,keepdims=True);lp=s-np.log(np.exp(s).sum(-1,keepdims=True))\n    ppl_total-=lp[np.arange(T),tgt].mean()\nppl=float(np.exp(ppl_total/min(20,len(data))))\n\nprint(\"=\"*58)\nprint(\"  GENERATION QUALITY EVALUATION\")\nprint(\"=\"*58)\nprint(f\"\\\n  1. PERPLEXITY: {ppl:.1f} (random: {tk.V})\")\nif ppl<tk.V:print(f\"     {(1-ppl/tk.V)*100:.0f}% better than random!\")\nprint(f\"\\\n  2. STRATEGY COMPARISON\")\nprint(f\"     {'Strategy':<14} {'Div-1':>7} {'Div-2':>7} {'3g-Rep':>7}\")\nprint(\"     \"+\"-\"*38)\nfor name,strat in[('Greedy','greedy'),('Temp 0.7','temp'),('Top-k 10','top_k'),('Top-p 0.9','top_p')]:\n    d1s,d2s,rs=[],[],[]\n    for _ in range(5):\n        t=gen(\"the \",80,0.7,strat)\n        d1=len(set(t))/max(1,len(t));bg=[t[i:i+2]for i in range(len(t)-1)];d2=len(set(bg))/max(1,len(bg))\n        ng=[t[i:i+3]for i in range(len(t)-2)];r=1-len(set(ng))/max(1,len(ng))\n        d1s.append(d1);d2s.append(d2);rs.append(r)\n    print(f\"     {name:<14} {np.mean(d1s):>5.3f}   {np.mean(d2s):>5.3f}   {np.mean(rs):>5.3f}\")\nprint(f\"\\\n  3. SAMPLE OUTPUTS\")\nfor name,strat in[('Greedy','greedy'),('Temp','temp'),('Top-k','top_k'),('Top-p','top_p')]:\n    t=gen(\"the \",50,0.7,strat)\n    print(f\"     {name:<7} 'the {t[:48].replace(chr(10),' ')}'\")\n  </code></pre>\n  <div id=\"output-l32-3\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Μέρος 4: Διαδραστικό Playground Παραγωγής</h2>\n\n<p>Πειραματιστείτε με όλες τις παραμέτρους παραγωγής: αλλάξτε τη θερμοκρασία, δοκιμάστε διαφορετικά prompts, εναλλάξτε στρατηγικές.</p>\n\n<div class=\"code-block-wrapper\">\n  <div class=\"code-block-header\">\n    <span>Python</span>\n    <button class=\"run-btn\" onclick=\"runPyodideCode(this, 'output-l32-4')\">&#9654; Run</button>\n  </div>\n  <pre><code class=\"language-python\">\nimport numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# INTERACTIVE TEXT GENERATION PLAYGROUND\n# ============================================================\n\n# --- Compact model rebuild + training ---\nclass CT:\n    def __init__(self,text):\n        ch=sorted(set(text));self.c2i={c:i for i,c in enumerate(ch)};self.i2c={i:c for i,c in enumerate(ch)};self.V=len(ch)\n    def encode(self,t):return[self.c2i.get(c,0)for c in t]\n    def decode(self,ids):return''.join(self.i2c.get(i,'?')for i in ids)\nclass LN:\n    def __init__(self,d):self.g=np.ones(d);self.b=np.zeros(d)\n    def forward(self,x):mu=x.mean(-1,keepdims=True);v=x.var(-1,keepdims=True);return self.g*((x-mu)/np.sqrt(v+1e-5))+self.b\nclass At:\n    def __init__(self,d,h,s):\n        self.h=h;self.hd=d//h;self.Wq=np.random.randn(d,d)*s;self.Wk=np.random.randn(d,d)*s\n        self.Wv=np.random.randn(d,d)*s;self.Wo=np.random.randn(d,d)*s\n    def forward(self,x):\n        B,T,C=x.shape;h=self.h;hd=self.hd\n        Q=(x@self.Wq).reshape(B,T,h,hd).transpose(0,2,1,3);K=(x@self.Wk).reshape(B,T,h,hd).transpose(0,2,1,3)\n        V=(x@self.Wv).reshape(B,T,h,hd).transpose(0,2,1,3);sc=Q@K.transpose(0,1,3,2)/np.sqrt(hd)\n        sc+=np.triu(np.ones((T,T)),k=1)*(-1e9);e=np.exp(sc-sc.max(-1,keepdims=True))\n        return(e/e.sum(-1,keepdims=True)@V).transpose(0,2,1,3).reshape(B,T,C)@self.Wo\nclass FF:\n    def __init__(self,d,s):\n        df=4*d;self.W1=np.random.randn(d,df)*s;self.b1=np.zeros(df);self.W2=np.random.randn(df,d)*s;self.b2=np.zeros(d)\n    def forward(self,x):z=x@self.W1+self.b1;return(0.5*z*(1+np.tanh(np.sqrt(2/np.pi)*(z+0.044715*z**3))))@self.W2+self.b2\nclass Bk:\n    def __init__(self,d,h,s):self.ln1=LN(d);self.at=At(d,h,s);self.ln2=LN(d);self.ff=FF(d,s)\n    def forward(self,x):x=x+self.at.forward(self.ln1.forward(x));x=x+self.ff.forward(self.ln2.forward(x));return x\nclass GPT:\n    def __init__(self,V,d,h,L,T):\n        s=0.02;rs=s/np.sqrt(2*L);self.tok=np.random.randn(V,d)*s;self.pos=np.random.randn(T,d)*s\n        self.blocks=[Bk(d,h,rs)for _ in range(L)];self.ln_f=LN(d);self.head=np.random.randn(d,V)*s;self.V=V;self.T=T\n    def forward(self,ids):\n        B,T=ids.shape;x=self.tok[ids]+self.pos[:T]\n        for bl in self.blocks:x=bl.forward(x)\n        return self.ln_f.forward(x)@self.head\n\ncorpus=\"the cat sat on the mat.\\\nthe dog ran in the park.\\\nthe model learns to predict.\\\nattention helps focus on context.\\\ntraining requires data and loss.\\\neach layer transforms input.\\\npretraining teaches understanding.\\\n\"*6\ntk=CT(corpus);ids_all=tk.encode(corpus);T=32;data=[]\nfor i in range(0,len(ids_all)-T,10):\n    ch=ids_all[i:i+T+1]\n    if len(ch)==T+1:data.append((np.array(ch[:-1]),np.array(ch[1:])))\nmodel=GPT(tk.V,64,4,4,T)\nfor step in range(100):\n    idx=np.random.choice(len(data),8);inp=np.stack([data[i][0]for i in idx]);tgt=np.stack([data[i][1]for i in idx])\n    lo=model.forward(inp);B,Tl,V=lo.shape;s=lo-lo.max(-1,keepdims=True)\n    p=np.exp(s)/np.exp(s).sum(-1,keepdims=True);oh=np.zeros_like(p)\n    for b in range(B):\n        for t in range(Tl):oh[b,t,tgt[b,t]]=1.0\n    dl=(p-oh)/(B*Tl);x=model.tok[inp]+model.pos[:Tl]\n    for bl in model.blocks:x=bl.forward(x)\n    x=model.ln_f.forward(x);dh=np.zeros_like(model.head)\n    for b in range(B):dh+=x[b].T@dl[b]\n    gn=np.sqrt((dh**2).sum())\n    if gn>1:dh/=gn\n    lr=0.005*(step+1)/15 if step<15 else 0.0025*(1+np.cos(np.pi*(step-15)/85))/2+0.0005\n    model.head-=lr*dh;de=np.zeros_like(model.tok);dx=dl@model.head.T\n    for b in range(B):\n        for t in range(Tl):de[inp[b,t]]+=dx[b,t]\n    en=np.sqrt((de**2).sum())\n    if en>1:de/=en\n    model.tok-=lr*de\n\ndef go(prompt,n=80,temp=0.7,strat='top_p',k=10,p_th=0.9):\n    ids=tk.encode(prompt)\n    for _ in range(n):\n        ctx=np.array([ids[-T:]]);lo=model.forward(ctx)[0,-1]\n        if strat=='greedy':nxt=int(np.argmax(lo))\n        elif strat=='temperature':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_k':\n            sc=lo/max(temp,0.01);ti=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[ti]=sc[ti]\n            pr=np.exp(f-f.max());pr/=pr.sum();nxt=int(np.random.choice(len(pr),p=pr))\n        elif strat=='top_p':\n            sc=lo/max(temp,0.01);pr=np.exp(sc-sc.max());pr/=pr.sum();si=np.argsort(pr)[::-1]\n            cs=np.cumsum(pr[si]);cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=pr[ni];np_/=np_.sum()\n            nxt=int(np.random.choice(ni,p=np_))\n        ids.append(nxt)\n    return tk.decode(ids[len(tk.encode(prompt)):])\n\nprint(\"=\"*58)\nprint(\"  INTERACTIVE TEXT GENERATION PLAYGROUND\")\nprint(\"=\"*58)\nprint(\"\\\n  EXP 1: Temperature Sweep\")\nfor temp in [0.2, 0.5, 0.7, 1.0, 1.5]:\n    t=go(\"the \",55,temp,'temperature');print(f\"  T={temp:<4} '{t[:50].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 2: Strategy Comparison\")\nfor strat,name in[('greedy','Greedy'),('temperature','Temp'),('top_k','Top-k'),('top_p','Top-p')]:\n    t=go(\"the model \",55,0.7,strat,10,0.9);print(f\"  {name:<7} '{t[:50].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 3: Various Prompts\")\nfor prompt in[\"the cat \",\"a neural \",\"training \",\"the sun \",\"each layer \"]:\n    t=go(prompt,50,0.6,'top_p');print(f\"  '{prompt}' -> '{t[:45].replace(chr(10),' ')}'\")\nprint(f\"\\\n  EXP 4: Long Generation\")\nlong=go(\"the \",200,0.7,'top_p');disp=long.replace(chr(10),' ')\nprint(f\"  '{disp[:80]}'\")\nwords=disp.split()\nprint(f\"  Words:{len(words)} Unique:{len(set(words))} Rep:{1-len(set(words))/max(1,len(words)):.0%}\")\n\nprint(\"\\\n\"+\"=\"*58)\nprint(\"  CONGRATULATIONS!\")\nprint(\"=\"*58)\nprint(\"  You built a complete language model from scratch:\")\nprint(\"  Tokenizer, Embeddings, Attention, FFN, LayerNorm,\")\nprint(\"  Residual Connections, Transformer Blocks, GPT-2,\")\nprint(\"  Cross-Entropy Loss, Backprop, AdamW, LR Scheduling,\")\nprint(\"  Gradient Clipping, Training Loop, Checkpointing,\")\nprint(\"  and 4 Generation Strategies with Evaluation.\")\nprint()\nprint(\"  Scale from 75K to 175B params, KB to TB of data,\")\nprint(\"  seconds to months of compute -- and you get GPT-4.\")\nprint(\"  Same architecture. Same math. You understand it all.\")\n  </code></pre>\n  <div id=\"output-l32-4\" class=\"output\"></div>\n</div>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Μέρος 5: Τι Έμαθε το Μοντέλο μας και Τι Ακολουθεί</h2>\n\n<p>Παρά τις ~75.000 παραμέτρους σε μερικά kilobytes, το μοντέλο μας έμαθε συχνότητες χαρακτήρων, ακολουθίες χαρακτήρων (το «th» είναι συχνό), όρια λέξεων (κενά σε σωστά διαστήματα) και συνηθισμένες φράσεις. Αυξήστε την κλίμακα και οι δυνατότητες αυξάνονται δραματικά:</p>\n\n<table class=\"comparison-table\">\n<thead>\n<tr><th>Κλίμακα</th><th>Παράμετροι</th><th>Δεδομένα</th><th>Δυνατότητες</th></tr>\n</thead>\n<tbody>\n<tr><td>Το μοντέλο μας</td><td>75K</td><td>~5KB</td><td>Μοτίβα χαρακτήρων, τμήματα λέξεων</td></tr>\n<tr><td>GPT-2</td><td>1.5B</td><td>~40GB</td><td>Συνεκτικές παράγραφοι, βασική λογική</td></tr>\n<tr><td>GPT-3</td><td>175B</td><td>~570GB</td><td>Μάθηση εντός context, μετάφραση, κώδικας</td></tr>\n<tr><td>GPT-4</td><td>~1.8T</td><td>~13TB</td><td>Σύνθετη λογική, πολυτροπικό</td></tr>\n</tbody>\n</table>\n\n<p>Τι ακολουθεί πέρα από την προεκπαίδευση: <strong>Supervised Fine-Tuning</strong> (ζεύγη οδηγία-απάντηση), <strong>Ευθυγράμμιση</strong> (RLHF / Constitutional AI) και <strong>Deployment</strong> (quantization, KV-caching, παραλληλισμός μοντέλου).</p>\n\n<h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Τελική Αναστοχασμός</h2>\n\n<p>Σε 32 διαλέξεις, χτίσατε κάθε στοιχείο από το μηδέν: tokenization, embeddings, attention, δίκτυα feed-forward, normalization, residual connections, Transformer blocks, αρχιτεκτονική GPT-2, cross-entropy loss, backpropagation, AdamW, προγραμματισμό LR, gradient clipping και τέσσερις στρατηγικές παραγωγής. Κανένα μαύρο κουτί δεν απέμεινε.</p>\n\n<div class=\"info-box\">\n<strong>Η Βασική Διαπίστωση:</strong> Ένα LLM δεν είναι μαγεία. Είναι γραμμική άλγεβρα, λογισμός και στατιστική στην αρχιτεκτονική Transformer, εκπαιδευμένο να προβλέπει το επόμενο token. Οι δυνατότητες του ChatGPT και του Claude αναδύονται από αυτή την αρχιτεκτονική, αυτόν τον στόχο και <em>τεράστια</em> κλίμακα. Τα κατανοείτε όλα.\n</div>\n\n<p>Χτίσατε ένα πραγματικό, λειτουργικό γλωσσικό μοντέλο από το μηδέν. <strong>Αυτό είναι ένα αξιοσημείωτο επίτευγμα.</strong></p>"
  },
  "codeBlocks": [
    {
      "id": "block-0",
      "title": "Python",
      "code": "import numpy as np\nnp.random.seed(42)\n\n# ============================================================\n#  A COMPLETE, TRAINABLE GPT IN NUMPY\n#\n#  Every layer below has BOTH a forward() and a backward().\n#  The backward passes are exactly the derivations from\n#  Lecture 24b. Nothing here is frozen; nothing is faked.\n#  If you change a weight, the loss changes -- and the\n#  gradient check at the bottom proves it.\n# ============================================================\n\n\nclass CharTok:\n    \"\"\"Character-level tokenizer (Lecture 7).\"\"\"\n    def __init__(self, text):\n        chars = sorted(set(text))\n        self.c2i = {c: i for i, c in enumerate(chars)}\n        self.i2c = {i: c for i, c in enumerate(chars)}\n        self.V = len(chars)\n\n    def encode(self, t):\n        return [self.c2i.get(c, 0) for c in t]\n\n    def decode(self, ids):\n        return ''.join(self.i2c.get(int(i), '?') for i in ids)\n\n\nclass LN:\n    \"\"\"LayerNorm (Lecture 20) with its backward pass (Lecture 24b).\"\"\"\n    def __init__(self, d):\n        self.g = np.ones(d)\n        self.b = np.zeros(d)\n        self.dg = np.zeros(d)\n        self.db = np.zeros(d)\n\n    def forward(self, x):\n        mu = x.mean(-1, keepdims=True)\n        var = x.var(-1, keepdims=True)\n        self.inv = 1.0 / np.sqrt(var + 1e-5)\n        self.xhat = (x - mu) * self.inv\n        return self.g * self.xhat + self.b\n\n    def backward(self, dy):\n        D = dy.shape[-1]\n        self.dg = (dy * self.xhat).reshape(-1, D).sum(0)\n        self.db = dy.reshape(-1, D).sum(0)\n        dxhat = dy * self.g\n        # Two correction terms: nudging one feature also moves the row's\n        # mean and variance, so both effects flow back.\n        return self.inv * (\n            dxhat\n            - dxhat.mean(-1, keepdims=True)\n            - self.xhat * (dxhat * self.xhat).mean(-1, keepdims=True)\n        )\n\n    def params(self):\n        return [('g', self), ('b', self)]\n\n\nGK = np.sqrt(2.0 / np.pi)\n\n\ndef gelu(z):\n    \"\"\"GELU, tanh approximation (Lecture 19).\"\"\"\n    return 0.5 * z * (1.0 + np.tanh(GK * (z + 0.044715 * z ** 3)))\n\n\ndef dgelu(z):\n    u = GK * (z + 0.044715 * z ** 3)\n    t = np.tanh(u)\n    return 0.5 * (1.0 + t) + 0.5 * z * (1.0 - t * t) * GK * (1.0 + 3 * 0.044715 * z * z)\n\n\nclass Attn:\n    \"\"\"Causal multi-head self-attention (Lectures 13-16) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.h, self.hd, self.d = h, d // h, d\n        self.Wq = np.random.randn(d, d) * s\n        self.Wk = np.random.randn(d, d) * s\n        self.Wv = np.random.randn(d, d) * s\n        self.Wo = np.random.randn(d, d) * s\n        for n in ('Wq', 'Wk', 'Wv', 'Wo'):\n            setattr(self, 'd' + n, np.zeros((d, d)))\n\n    def _split(self, m, B, T):\n        return m.reshape(B, T, self.h, self.hd).transpose(0, 2, 1, 3)\n\n    def _merge(self, m, B, T):\n        return m.transpose(0, 2, 1, 3).reshape(B, T, self.d)\n\n    def forward(self, x):\n        B, T, C = x.shape\n        self.x = x\n        Q = self._split(x @ self.Wq, B, T)\n        Kk = self._split(x @ self.Wk, B, T)\n        V = self._split(x @ self.Wv, B, T)\n        sc = Q @ Kk.transpose(0, 1, 3, 2) / np.sqrt(self.hd)\n        sc = sc + np.triu(np.ones((T, T)), k=1) * (-1e9)   # causal mask\n        e = np.exp(sc - sc.max(-1, keepdims=True))\n        a = e / e.sum(-1, keepdims=True)\n        self.Q, self.Kk, self.V, self.a = Q, Kk, V, a\n        self.o = self._merge(a @ V, B, T)\n        return self.o @ self.Wo\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        self.dWo = self.o.reshape(-1, C).T @ dy.reshape(-1, C)\n        do = self._split(dy @ self.Wo.T, B, T)\n\n        dV = self.a.transpose(0, 1, 3, 2) @ do\n        da = do @ self.V.transpose(0, 1, 3, 2)\n        # softmax Jacobian, applied row-wise\n        dsc = self.a * (da - (da * self.a).sum(-1, keepdims=True)) / np.sqrt(self.hd)\n\n        dQ = dsc @ self.Kk\n        dKk = dsc.transpose(0, 1, 3, 2) @ self.Q\n\n        dQf = self._merge(dQ, B, T).reshape(-1, C)\n        dKf = self._merge(dKk, B, T).reshape(-1, C)\n        dVf = self._merge(dV, B, T).reshape(-1, C)\n        xf = self.x.reshape(-1, C)\n\n        self.dWq = xf.T @ dQf\n        self.dWk = xf.T @ dKf\n        self.dWv = xf.T @ dVf\n        return (dQf @ self.Wq.T + dKf @ self.Wk.T + dVf @ self.Wv.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('Wq', 'Wk', 'Wv', 'Wo')]\n\n\nclass FFN:\n    \"\"\"Position-wise feed-forward network (Lecture 19) + backward.\"\"\"\n    def __init__(self, d, s):\n        df = 4 * d\n        self.W1 = np.random.randn(d, df) * s\n        self.b1 = np.zeros(df)\n        self.W2 = np.random.randn(df, d) * s\n        self.b2 = np.zeros(d)\n        self.dW1 = np.zeros((d, df)); self.db1 = np.zeros(df)\n        self.dW2 = np.zeros((df, d)); self.db2 = np.zeros(d)\n\n    def forward(self, x):\n        self.x = x\n        self.z = x @ self.W1 + self.b1\n        self.a = gelu(self.z)\n        return self.a @ self.W2 + self.b2\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        dyf = dy.reshape(-1, C)\n        self.dW2 = self.a.reshape(-1, self.a.shape[-1]).T @ dyf\n        self.db2 = dyf.sum(0)\n        dz = (dy @ self.W2.T) * dgelu(self.z)\n        dzf = dz.reshape(-1, dz.shape[-1])\n        self.dW1 = self.x.reshape(-1, C).T @ dzf\n        self.db1 = dzf.sum(0)\n        return (dzf @ self.W1.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('W1', 'b1', 'W2', 'b2')]\n\n\nclass Blk:\n    \"\"\"Pre-norm transformer block (Lectures 21-22) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.ln1, self.at = LN(d), Attn(d, h, s)\n        self.ln2, self.ff = LN(d), FFN(d, s)\n\n    def forward(self, x):\n        x = x + self.at.forward(self.ln1.forward(x))\n        x = x + self.ff.forward(self.ln2.forward(x))\n        return x\n\n    def backward(self, d):\n        # A residual connection adds the gradient to BOTH paths: through the\n        # sublayer, and straight past it. That second path is why deep stacks\n        # train at all (Lecture 21).\n        d = d + self.ln2.backward(self.ff.backward(d))\n        d = d + self.ln1.backward(self.at.backward(d))\n        return d\n\n    def params(self):\n        return self.ln1.params() + self.at.params() + self.ln2.params() + self.ff.params()\n\n\nclass GPT:\n    \"\"\"The whole model (Lecture 23) — now with a real backward pass.\"\"\"\n    def __init__(self, V, d, h, L, T):\n        s = 0.02\n        rs = s / np.sqrt(2 * L)          # scaled init for residual depth\n        self.tok = np.random.randn(V, d) * s\n        self.pos = np.random.randn(T, d) * s\n        self.dtok = np.zeros((V, d)); self.dpos = np.zeros((T, d))\n        self.blocks = [Blk(d, h, rs) for _ in range(L)]\n        self.ln_f = LN(d)\n        self.head = np.random.randn(d, V) * s\n        self.dhead = np.zeros((d, V))\n        self.V, self.T = V, T\n\n    def forward(self, ids):\n        B, T = ids.shape\n        self.ids = ids\n        x = self.tok[ids] + self.pos[:T]\n        for bl in self.blocks:\n            x = bl.forward(x)\n        self.xf = self.ln_f.forward(x)\n        return self.xf @ self.head\n\n    def backward(self, dlogits):\n        B, T, V = dlogits.shape\n        C = self.head.shape[0]\n        self.dhead = self.xf.reshape(-1, C).T @ dlogits.reshape(-1, V)\n        d = self.ln_f.backward(dlogits @ self.head.T)\n        for bl in reversed(self.blocks):     # <-- every block now gets a gradient\n            d = bl.backward(d)\n        self.dpos = np.zeros_like(self.pos)\n        self.dpos[:T] = d.sum(0)             # positions are shared across the batch\n        self.dtok = np.zeros_like(self.tok)\n        np.add.at(self.dtok, self.ids.reshape(-1), d.reshape(-1, C))  # scatter-add\n\n    def params(self):\n        p = [('tok', self), ('pos', self), ('head', self)]\n        for bl in self.blocks:\n            p += bl.params()\n        return p + self.ln_f.params()\n\n\ndef cross_entropy(logits, targets):\n    \"\"\"Loss (Lecture 25). Returns (loss, dL/dlogits).\"\"\"\n    B, T, V = logits.shape\n    lf = logits.reshape(B * T, V)\n    tf = targets.reshape(B * T)\n    s = lf - lf.max(-1, keepdims=True)\n    lp = s - np.log(np.exp(s).sum(-1, keepdims=True))\n    loss = -lp[np.arange(B * T), tf].mean()\n    p = np.exp(lp)\n    p[np.arange(B * T), tf] -= 1.0\n    return loss, (p / (B * T)).reshape(B, T, V)\n\n\nclass AdamW:\n    \"\"\"AdamW + global grad-norm clipping (Lectures 27-28).\"\"\"\n    def __init__(self, params, betas=(0.9, 0.95), eps=1e-8, wd=0.01):\n        self.p = params\n        self.b1, self.b2, self.eps, self.wd = betas[0], betas[1], eps, wd\n        self.m = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.v = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.t = 0\n\n    def step(self, lr, clip=1.0):\n        self.t += 1\n        total = 0.0\n        for n, o in self.p:\n            g = getattr(o, 'd' + n)\n            total += float((g * g).sum())\n        norm = np.sqrt(total)\n        scale = min(1.0, clip / (norm + 1e-6))\n        for i, (n, o) in enumerate(self.p):\n            g = getattr(o, 'd' + n) * scale\n            self.m[i] = self.b1 * self.m[i] + (1 - self.b1) * g\n            self.v[i] = self.b2 * self.v[i] + (1 - self.b2) * (g * g)\n            mh = self.m[i] / (1 - self.b1 ** self.t)\n            vh = self.v[i] / (1 - self.b2 ** self.t)\n            w = getattr(o, n)\n            if w.ndim > 1:                      # decoupled decay, not on biases/gains\n                w = w - lr * self.wd * w\n            setattr(o, n, w - lr * mh / (np.sqrt(vh) + self.eps))\n        return norm\n\n\ndef lr_at(step, warmup, total, max_lr, min_lr):\n    \"\"\"Linear warmup then cosine decay (Lecture 27).\"\"\"\n    if step < warmup:\n        return max_lr * (step + 1) / warmup\n    r = (step - warmup) / max(1, total - warmup)\n    return min_lr + 0.5 * (max_lr - min_lr) * (1 + np.cos(np.pi * r))\n\n\nCORPUS = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(CORPUS)\ntoken_ids = tok.encode(CORPUS)\nT = 24\ndata = []\nfor i in range(0, len(token_ids) - T, 4):\n    ch = token_ids[i:i + T + 1]\n    if len(ch) == T + 1:\n        data.append((np.array(ch[:-1]), np.array(ch[1:])))\n\nBASELINE = float(np.log(tok.V))   # loss of a model that guesses uniformly\nrng = np.random.default_rng(0)\n\n\ndef train(model, steps, bs=12, max_lr=3e-3, min_lr=3e-4, warmup=60, log_every=0):\n    \"\"\"Full training loop (Lecture 26). Returns the loss history.\"\"\"\n    opt = AdamW(model.params())\n    hist = []\n    for step in range(steps):\n        idx = rng.choice(len(data), bs)\n        inp = np.stack([data[i][0] for i in idx])\n        tgt = np.stack([data[i][1] for i in idx])\n        lr = lr_at(step, warmup, steps, max_lr, min_lr)\n        loss, dl = cross_entropy(model.forward(inp), tgt)\n        model.backward(dl)\n        opt.step(lr)\n        hist.append(float(loss))\n        if log_every and (step % log_every == 0 or step == steps - 1):\n            print(f\"  step {step:4d} | loss {loss:.4f} | ppl {np.exp(loss):6.2f} | lr {lr:.5f}\")\n    return hist\n\n\ndef sample_next(logits, strategy='top_k', temp=0.8, k=10, p=0.9):\n    \"\"\"Sampling strategies (Lecture 30).\"\"\"\n    if strategy == 'greedy':\n        return int(np.argmax(logits))\n    lg = logits / max(1e-6, temp)\n    if strategy == 'top_k':\n        thr = np.sort(lg)[-k]\n        lg = np.where(lg < thr, -1e9, lg)\n    elif strategy == 'top_p':\n        order = np.argsort(lg)[::-1]\n        pr = np.exp(lg[order] - lg[order].max()); pr /= pr.sum()\n        cut = np.searchsorted(np.cumsum(pr), p) + 1\n        keep = order[:cut]\n        mask = np.full_like(lg, -1e9); mask[keep] = lg[keep]; lg = mask\n    pr = np.exp(lg - lg.max()); pr /= pr.sum()\n    return int(rng.choice(len(pr), p=pr))\n\n\ndef generate(model, prompt, n=100, strategy='top_k', temp=0.8, k=10, p=0.9):\n    out = tok.encode(prompt)\n    for _ in range(n):\n        w = out[-T:]\n        padded = [0] * (T - len(w)) + w\n        logits = model.forward(np.array([padded]))[0, -1]\n        out.append(sample_next(logits, strategy, temp, k, p))\n    return tok.decode(out[len(tok.encode(prompt)):])\n\n\ndef gradient_check(model):\n    \"\"\"Lecture 24b, made executable: compare analytic vs numerical gradients.\"\"\"\n    gi = np.stack([data[i][0] for i in range(2)])\n    gt = np.stack([data[i][1] for i in range(2)])\n    _, dl = cross_entropy(model.forward(gi), gt)\n    model.backward(dl)\n    W, G = model.blocks[0].at.Wq, model.blocks[0].at.dWq\n    worst = 0.0\n    for r, c in [(0, 0), (3, 7), (5, 2), (11, 19)]:\n        eps, old = 1e-5, W[r, c]\n        W[r, c] = old + eps\n        lp, _ = cross_entropy(model.forward(gi), gt)\n        W[r, c] = old - eps\n        lm, _ = cross_entropy(model.forward(gi), gt)\n        W[r, c] = old\n        num, ana = (lp - lm) / (2 * eps), G[r, c]\n        worst = max(worst, abs(num - ana) / max(1e-8, abs(num) + abs(ana)))\n    return worst\n\n\n# ============================================================\n#  STAGE 5 CAPSTONE — PRETRAIN YOUR OWN GPT\n# ============================================================\nmodel = GPT(tok.V, d=48, h=4, L=2, T=T)\n\nprint(\"=\" * 58)\nprint(\"  CAPSTONE: PRETRAINING A MINI GPT\")\nprint(\"=\" * 58)\nprint(f\"  Vocab {tok.V} | Tokens {len(token_ids)} | Examples {len(data)}\")\nn_params = sum(getattr(o, n).size for n, o in model.params())\nprint(f\"  Parameters: {n_params:,}\")\nprint(f\"  Random-guess loss (ln V): {BASELINE:.4f}  <- we must beat this\")\nprint(\"-\" * 58)\n\nworst = gradient_check(model)\nprint(f\"  Gradient check (block 0, Wq): worst rel. error {worst:.2e}\")\nprint(\"  -> analytic gradients match numerical ones. The math is right.\")\nprint(\"-\" * 58)\nprint(\"  Training (this takes about a minute in the browser)...\")\n\nhist = train(model, steps=900, bs=12, log_every=100)\n\nprint(\"-\" * 58)\nprint(f\"  Loss {hist[0]:.4f} -> {hist[-1]:.4f}   (random = {BASELINE:.4f})\")\nprint(f\"  Perplexity {np.exp(hist[0]):.1f} -> {np.exp(hist[-1]):.2f}\")\nif hist[-1] < BASELINE:\n    print(f\"  The model is {(1 - hist[-1] / BASELINE) * 100:.0f}% better than guessing.\")\nprint(\"-\" * 58)\nprint(\"  SAMPLE (top-k 10, temperature 0.8):\")\nprint(\"  'the \" + generate(model, \"the \", 110).replace(chr(10), ' ') + \"'\")\nprint(\"=\" * 58)\nprint(\"  You just trained every weight in a transformer -- attention,\")\nprint(\"  feed-forward, layer norms, embeddings -- with gradients you\")\nprint(\"  derived by hand. This is the real thing, only small.\")\nprint()\nprint(\"  It MEMORISES this tiny corpus (that is what the very low loss\")\nprint(\"  means). Lecture 31 explains why: at this scale, memorising is\")\nprint(\"  the optimum. Real generalisation needs more data and more\")\nprint(\"  parameters -- which is exactly what you will do next on a GPU.\")\nprint(\"=\" * 58)\n",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-1",
      "title": "Python",
      "code": "import numpy as np\nnp.random.seed(42)\n\n# ============================================================\n# FOUR GENERATION STRATEGIES - Implementation & Comparison\n# ============================================================\n\ndef sm(logits):\n    s=logits-logits.max();e=np.exp(s);return e/e.sum()\ndef greedy(lo):return int(np.argmax(lo))\ndef temp_sample(lo,T=0.8):p=sm(lo/max(T,1e-8));return int(np.random.choice(len(p),p=p))\ndef topk_sample(lo,k=10,T=0.8):\n    sc=lo/max(T,1e-8);top=np.argsort(sc)[-k:];f=np.full_like(sc,-1e9);f[top]=sc[top]\n    p=sm(f);return int(np.random.choice(len(p),p=p))\ndef topp_sample(lo,p_th=0.9,T=0.8):\n    p=sm(lo/max(T,1e-8));si=np.argsort(p)[::-1];cs=np.cumsum(p[si])\n    cut=np.searchsorted(cs,p_th)+1;ni=si[:cut];np_=p[ni];np_/=np_.sum()\n    return int(np.random.choice(ni,p=np_))\n\n# Simulate realistic logits\nV=50;logits=np.random.randn(V)*0.5\nlogits[10]=3.0;logits[25]=2.5;logits[30]=2.0;logits[5]=1.5\nprobs=sm(logits);top5=np.argsort(probs)[-5:][::-1]\n\nprint(\"=\"*58)\nprint(\"  GENERATION STRATEGIES COMPARISON\")\nprint(\"=\"*58)\nprint(\"\\\\n  Top 5 tokens by probability:\")\nfor i,idx in enumerate(top5):\n    print(f\"    {i+1}. Token {idx}: {probs[idx]:.4f} ({probs[idx]*100:.1f}%)\")\n\nfrom collections import Counter\nstrats={'Greedy':lambda l:greedy(l),'Temp=0.5':lambda l:temp_sample(l,0.5),\n    'Temp=1.0':lambda l:temp_sample(l,1.0),'Temp=1.5':lambda l:temp_sample(l,1.5),\n    'Top-k(5)':lambda l:topk_sample(l,5),'Top-k(20)':lambda l:topk_sample(l,20),\n    'Top-p(.8)':lambda l:topp_sample(l,0.8),'Top-p(.95)':lambda l:topp_sample(l,0.95)}\nprint(f\"\\\\n  {'Strategy':<14} {'Unique':>7} {'Most common':>14}\")\nprint(\"  \"+\"-\"*40)\nfor name,fn in strats.items():\n    samp=[fn(logits.copy())for _ in range(100)];u=len(set(samp))\n    mc=Counter(samp).most_common(1)[0]\n    print(f\"  {name:<14} {u:>5}     Token {mc[0]}:{mc[1]:>3}x\")\n\nprint(f\"\\\\n  TEMPERATURE EFFECT ON TOP TOKENS\")\nprint(f\"  {'Token':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:print(f\"{'T='+str(t):>8}\",end=\"\")\nprint()\nfor idx in top5[:4]:\n    print(f\"  #{idx:<5}\",end=\"\")\n    for t in[0.3,0.7,1.0,1.5]:print(f\"{sm(logits/t)[idx]:>8.3f}\",end=\"\")\n    print()\nprint(f\"\\\\n  {'Entropy':<7}\",end=\"\")\nfor t in[0.3,0.7,1.0,1.5]:\n    p=sm(logits/t);H=-np.sum(p*np.log(p+1e-10));print(f\"{H:>8.3f}\",end=\"\")\nprint(\"  (higher = more random)\")",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-2",
      "title": "Python",
      "code": "import numpy as np\nnp.random.seed(42)\n\n# ============================================================\n#  A COMPLETE, TRAINABLE GPT IN NUMPY\n#\n#  Every layer below has BOTH a forward() and a backward().\n#  The backward passes are exactly the derivations from\n#  Lecture 24b. Nothing here is frozen; nothing is faked.\n#  If you change a weight, the loss changes -- and the\n#  gradient check at the bottom proves it.\n# ============================================================\n\n\nclass CharTok:\n    \"\"\"Character-level tokenizer (Lecture 7).\"\"\"\n    def __init__(self, text):\n        chars = sorted(set(text))\n        self.c2i = {c: i for i, c in enumerate(chars)}\n        self.i2c = {i: c for i, c in enumerate(chars)}\n        self.V = len(chars)\n\n    def encode(self, t):\n        return [self.c2i.get(c, 0) for c in t]\n\n    def decode(self, ids):\n        return ''.join(self.i2c.get(int(i), '?') for i in ids)\n\n\nclass LN:\n    \"\"\"LayerNorm (Lecture 20) with its backward pass (Lecture 24b).\"\"\"\n    def __init__(self, d):\n        self.g = np.ones(d)\n        self.b = np.zeros(d)\n        self.dg = np.zeros(d)\n        self.db = np.zeros(d)\n\n    def forward(self, x):\n        mu = x.mean(-1, keepdims=True)\n        var = x.var(-1, keepdims=True)\n        self.inv = 1.0 / np.sqrt(var + 1e-5)\n        self.xhat = (x - mu) * self.inv\n        return self.g * self.xhat + self.b\n\n    def backward(self, dy):\n        D = dy.shape[-1]\n        self.dg = (dy * self.xhat).reshape(-1, D).sum(0)\n        self.db = dy.reshape(-1, D).sum(0)\n        dxhat = dy * self.g\n        # Two correction terms: nudging one feature also moves the row's\n        # mean and variance, so both effects flow back.\n        return self.inv * (\n            dxhat\n            - dxhat.mean(-1, keepdims=True)\n            - self.xhat * (dxhat * self.xhat).mean(-1, keepdims=True)\n        )\n\n    def params(self):\n        return [('g', self), ('b', self)]\n\n\nGK = np.sqrt(2.0 / np.pi)\n\n\ndef gelu(z):\n    \"\"\"GELU, tanh approximation (Lecture 19).\"\"\"\n    return 0.5 * z * (1.0 + np.tanh(GK * (z + 0.044715 * z ** 3)))\n\n\ndef dgelu(z):\n    u = GK * (z + 0.044715 * z ** 3)\n    t = np.tanh(u)\n    return 0.5 * (1.0 + t) + 0.5 * z * (1.0 - t * t) * GK * (1.0 + 3 * 0.044715 * z * z)\n\n\nclass Attn:\n    \"\"\"Causal multi-head self-attention (Lectures 13-16) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.h, self.hd, self.d = h, d // h, d\n        self.Wq = np.random.randn(d, d) * s\n        self.Wk = np.random.randn(d, d) * s\n        self.Wv = np.random.randn(d, d) * s\n        self.Wo = np.random.randn(d, d) * s\n        for n in ('Wq', 'Wk', 'Wv', 'Wo'):\n            setattr(self, 'd' + n, np.zeros((d, d)))\n\n    def _split(self, m, B, T):\n        return m.reshape(B, T, self.h, self.hd).transpose(0, 2, 1, 3)\n\n    def _merge(self, m, B, T):\n        return m.transpose(0, 2, 1, 3).reshape(B, T, self.d)\n\n    def forward(self, x):\n        B, T, C = x.shape\n        self.x = x\n        Q = self._split(x @ self.Wq, B, T)\n        Kk = self._split(x @ self.Wk, B, T)\n        V = self._split(x @ self.Wv, B, T)\n        sc = Q @ Kk.transpose(0, 1, 3, 2) / np.sqrt(self.hd)\n        sc = sc + np.triu(np.ones((T, T)), k=1) * (-1e9)   # causal mask\n        e = np.exp(sc - sc.max(-1, keepdims=True))\n        a = e / e.sum(-1, keepdims=True)\n        self.Q, self.Kk, self.V, self.a = Q, Kk, V, a\n        self.o = self._merge(a @ V, B, T)\n        return self.o @ self.Wo\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        self.dWo = self.o.reshape(-1, C).T @ dy.reshape(-1, C)\n        do = self._split(dy @ self.Wo.T, B, T)\n\n        dV = self.a.transpose(0, 1, 3, 2) @ do\n        da = do @ self.V.transpose(0, 1, 3, 2)\n        # softmax Jacobian, applied row-wise\n        dsc = self.a * (da - (da * self.a).sum(-1, keepdims=True)) / np.sqrt(self.hd)\n\n        dQ = dsc @ self.Kk\n        dKk = dsc.transpose(0, 1, 3, 2) @ self.Q\n\n        dQf = self._merge(dQ, B, T).reshape(-1, C)\n        dKf = self._merge(dKk, B, T).reshape(-1, C)\n        dVf = self._merge(dV, B, T).reshape(-1, C)\n        xf = self.x.reshape(-1, C)\n\n        self.dWq = xf.T @ dQf\n        self.dWk = xf.T @ dKf\n        self.dWv = xf.T @ dVf\n        return (dQf @ self.Wq.T + dKf @ self.Wk.T + dVf @ self.Wv.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('Wq', 'Wk', 'Wv', 'Wo')]\n\n\nclass FFN:\n    \"\"\"Position-wise feed-forward network (Lecture 19) + backward.\"\"\"\n    def __init__(self, d, s):\n        df = 4 * d\n        self.W1 = np.random.randn(d, df) * s\n        self.b1 = np.zeros(df)\n        self.W2 = np.random.randn(df, d) * s\n        self.b2 = np.zeros(d)\n        self.dW1 = np.zeros((d, df)); self.db1 = np.zeros(df)\n        self.dW2 = np.zeros((df, d)); self.db2 = np.zeros(d)\n\n    def forward(self, x):\n        self.x = x\n        self.z = x @ self.W1 + self.b1\n        self.a = gelu(self.z)\n        return self.a @ self.W2 + self.b2\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        dyf = dy.reshape(-1, C)\n        self.dW2 = self.a.reshape(-1, self.a.shape[-1]).T @ dyf\n        self.db2 = dyf.sum(0)\n        dz = (dy @ self.W2.T) * dgelu(self.z)\n        dzf = dz.reshape(-1, dz.shape[-1])\n        self.dW1 = self.x.reshape(-1, C).T @ dzf\n        self.db1 = dzf.sum(0)\n        return (dzf @ self.W1.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('W1', 'b1', 'W2', 'b2')]\n\n\nclass Blk:\n    \"\"\"Pre-norm transformer block (Lectures 21-22) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.ln1, self.at = LN(d), Attn(d, h, s)\n        self.ln2, self.ff = LN(d), FFN(d, s)\n\n    def forward(self, x):\n        x = x + self.at.forward(self.ln1.forward(x))\n        x = x + self.ff.forward(self.ln2.forward(x))\n        return x\n\n    def backward(self, d):\n        # A residual connection adds the gradient to BOTH paths: through the\n        # sublayer, and straight past it. That second path is why deep stacks\n        # train at all (Lecture 21).\n        d = d + self.ln2.backward(self.ff.backward(d))\n        d = d + self.ln1.backward(self.at.backward(d))\n        return d\n\n    def params(self):\n        return self.ln1.params() + self.at.params() + self.ln2.params() + self.ff.params()\n\n\nclass GPT:\n    \"\"\"The whole model (Lecture 23) — now with a real backward pass.\"\"\"\n    def __init__(self, V, d, h, L, T):\n        s = 0.02\n        rs = s / np.sqrt(2 * L)          # scaled init for residual depth\n        self.tok = np.random.randn(V, d) * s\n        self.pos = np.random.randn(T, d) * s\n        self.dtok = np.zeros((V, d)); self.dpos = np.zeros((T, d))\n        self.blocks = [Blk(d, h, rs) for _ in range(L)]\n        self.ln_f = LN(d)\n        self.head = np.random.randn(d, V) * s\n        self.dhead = np.zeros((d, V))\n        self.V, self.T = V, T\n\n    def forward(self, ids):\n        B, T = ids.shape\n        self.ids = ids\n        x = self.tok[ids] + self.pos[:T]\n        for bl in self.blocks:\n            x = bl.forward(x)\n        self.xf = self.ln_f.forward(x)\n        return self.xf @ self.head\n\n    def backward(self, dlogits):\n        B, T, V = dlogits.shape\n        C = self.head.shape[0]\n        self.dhead = self.xf.reshape(-1, C).T @ dlogits.reshape(-1, V)\n        d = self.ln_f.backward(dlogits @ self.head.T)\n        for bl in reversed(self.blocks):     # <-- every block now gets a gradient\n            d = bl.backward(d)\n        self.dpos = np.zeros_like(self.pos)\n        self.dpos[:T] = d.sum(0)             # positions are shared across the batch\n        self.dtok = np.zeros_like(self.tok)\n        np.add.at(self.dtok, self.ids.reshape(-1), d.reshape(-1, C))  # scatter-add\n\n    def params(self):\n        p = [('tok', self), ('pos', self), ('head', self)]\n        for bl in self.blocks:\n            p += bl.params()\n        return p + self.ln_f.params()\n\n\ndef cross_entropy(logits, targets):\n    \"\"\"Loss (Lecture 25). Returns (loss, dL/dlogits).\"\"\"\n    B, T, V = logits.shape\n    lf = logits.reshape(B * T, V)\n    tf = targets.reshape(B * T)\n    s = lf - lf.max(-1, keepdims=True)\n    lp = s - np.log(np.exp(s).sum(-1, keepdims=True))\n    loss = -lp[np.arange(B * T), tf].mean()\n    p = np.exp(lp)\n    p[np.arange(B * T), tf] -= 1.0\n    return loss, (p / (B * T)).reshape(B, T, V)\n\n\nclass AdamW:\n    \"\"\"AdamW + global grad-norm clipping (Lectures 27-28).\"\"\"\n    def __init__(self, params, betas=(0.9, 0.95), eps=1e-8, wd=0.01):\n        self.p = params\n        self.b1, self.b2, self.eps, self.wd = betas[0], betas[1], eps, wd\n        self.m = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.v = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.t = 0\n\n    def step(self, lr, clip=1.0):\n        self.t += 1\n        total = 0.0\n        for n, o in self.p:\n            g = getattr(o, 'd' + n)\n            total += float((g * g).sum())\n        norm = np.sqrt(total)\n        scale = min(1.0, clip / (norm + 1e-6))\n        for i, (n, o) in enumerate(self.p):\n            g = getattr(o, 'd' + n) * scale\n            self.m[i] = self.b1 * self.m[i] + (1 - self.b1) * g\n            self.v[i] = self.b2 * self.v[i] + (1 - self.b2) * (g * g)\n            mh = self.m[i] / (1 - self.b1 ** self.t)\n            vh = self.v[i] / (1 - self.b2 ** self.t)\n            w = getattr(o, n)\n            if w.ndim > 1:                      # decoupled decay, not on biases/gains\n                w = w - lr * self.wd * w\n            setattr(o, n, w - lr * mh / (np.sqrt(vh) + self.eps))\n        return norm\n\n\ndef lr_at(step, warmup, total, max_lr, min_lr):\n    \"\"\"Linear warmup then cosine decay (Lecture 27).\"\"\"\n    if step < warmup:\n        return max_lr * (step + 1) / warmup\n    r = (step - warmup) / max(1, total - warmup)\n    return min_lr + 0.5 * (max_lr - min_lr) * (1 + np.cos(np.pi * r))\n\n\nCORPUS = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(CORPUS)\ntoken_ids = tok.encode(CORPUS)\nT = 24\ndata = []\nfor i in range(0, len(token_ids) - T, 4):\n    ch = token_ids[i:i + T + 1]\n    if len(ch) == T + 1:\n        data.append((np.array(ch[:-1]), np.array(ch[1:])))\n\nBASELINE = float(np.log(tok.V))   # loss of a model that guesses uniformly\nrng = np.random.default_rng(0)\n\n\ndef train(model, steps, bs=12, max_lr=3e-3, min_lr=3e-4, warmup=60, log_every=0):\n    \"\"\"Full training loop (Lecture 26). Returns the loss history.\"\"\"\n    opt = AdamW(model.params())\n    hist = []\n    for step in range(steps):\n        idx = rng.choice(len(data), bs)\n        inp = np.stack([data[i][0] for i in idx])\n        tgt = np.stack([data[i][1] for i in idx])\n        lr = lr_at(step, warmup, steps, max_lr, min_lr)\n        loss, dl = cross_entropy(model.forward(inp), tgt)\n        model.backward(dl)\n        opt.step(lr)\n        hist.append(float(loss))\n        if log_every and (step % log_every == 0 or step == steps - 1):\n            print(f\"  step {step:4d} | loss {loss:.4f} | ppl {np.exp(loss):6.2f} | lr {lr:.5f}\")\n    return hist\n\n\ndef sample_next(logits, strategy='top_k', temp=0.8, k=10, p=0.9):\n    \"\"\"Sampling strategies (Lecture 30).\"\"\"\n    if strategy == 'greedy':\n        return int(np.argmax(logits))\n    lg = logits / max(1e-6, temp)\n    if strategy == 'top_k':\n        thr = np.sort(lg)[-k]\n        lg = np.where(lg < thr, -1e9, lg)\n    elif strategy == 'top_p':\n        order = np.argsort(lg)[::-1]\n        pr = np.exp(lg[order] - lg[order].max()); pr /= pr.sum()\n        cut = np.searchsorted(np.cumsum(pr), p) + 1\n        keep = order[:cut]\n        mask = np.full_like(lg, -1e9); mask[keep] = lg[keep]; lg = mask\n    pr = np.exp(lg - lg.max()); pr /= pr.sum()\n    return int(rng.choice(len(pr), p=pr))\n\n\ndef generate(model, prompt, n=100, strategy='top_k', temp=0.8, k=10, p=0.9):\n    out = tok.encode(prompt)\n    for _ in range(n):\n        w = out[-T:]\n        padded = [0] * (T - len(w)) + w\n        logits = model.forward(np.array([padded]))[0, -1]\n        out.append(sample_next(logits, strategy, temp, k, p))\n    return tok.decode(out[len(tok.encode(prompt)):])\n\n\n# ============================================================\n#  GENERATION QUALITY EVALUATION (Lectures 25 + 30)\n# ============================================================\nmodel = GPT(tok.V, d=48, h=4, L=2, T=T)\nprint(\"=\" * 58)\nprint(\"  GENERATION QUALITY EVALUATION\")\nprint(\"=\" * 58)\nprint(\"  Training a model to evaluate (about 30 seconds)...\")\nhist = train(model, steps=450, bs=12, warmup=50)\nprint(f\"  Trained: loss {hist[0]:.4f} -> {hist[-1]:.4f} (random {BASELINE:.4f})\")\n\n# ---- 1. Perplexity on held-out windows -----------------------------\ntotal = 0.0\nn_eval = min(20, len(data))\nfor i in range(n_eval):\n    inp, tgt = data[len(data) - 1 - i]\n    lo = model.forward(inp.reshape(1, -1))[0]\n    s = lo - lo.max(-1, keepdims=True)\n    lp = s - np.log(np.exp(s).sum(-1, keepdims=True))\n    total -= lp[np.arange(T), tgt].mean()\nppl = float(np.exp(total / n_eval))\n\nprint(\"-\" * 58)\nprint(f\"  1. PERPLEXITY: {ppl:.2f}   (random guessing = {tok.V})\")\nprint(f\"     {(1 - ppl / tok.V) * 100:.0f}% better than random.\")\n\n# ---- 2. Sampling strategies compared -------------------------------\nprint(\"\\n  2. STRATEGY COMPARISON (higher Div = more varied text,\")\nprint(\"     higher 3g-Rep = more repetition)\")\nprint(f\"     {'Strategy':<14}{'Div-1':>8}{'Div-2':>8}{'3g-Rep':>9}\")\nprint(\"     \" + \"-\" * 39)\nfor label, strat in [('Greedy', 'greedy'), ('Temp 0.7', 'temperature'),\n                     ('Top-k 10', 'top_k'), ('Top-p 0.9', 'top_p')]:\n    d1s, d2s, reps = [], [], []\n    for _ in range(4):\n        t = generate(model, \"the \", 80, strategy=strat, temp=0.7)\n        d1s.append(len(set(t)) / max(1, len(t)))\n        bg = [t[i:i + 2] for i in range(len(t) - 1)]\n        d2s.append(len(set(bg)) / max(1, len(bg)))\n        tg = [t[i:i + 3] for i in range(len(t) - 2)]\n        reps.append(1 - len(set(tg)) / max(1, len(tg)))\n    print(f\"     {label:<14}{np.mean(d1s):>8.3f}{np.mean(d2s):>8.3f}{np.mean(reps):>9.3f}\")\n\nprint(\"\\n  3. SAMPLE OUTPUTS\")\nfor label, strat in [('Greedy', 'greedy'), ('Temp', 'temperature'),\n                     ('Top-k', 'top_k'), ('Top-p', 'top_p')]:\n    t = generate(model, \"the \", 55, strategy=strat, temp=0.7)\n    print(f\"     {label:<7} 'the {t[:52].replace(chr(10), ' ')}'\")\n\nprint(\"\\n  Greedy repeats itself: it always takes the single most likely\")\nprint(\"  character, so it falls into loops. Sampling trades a little\")\nprint(\"  accuracy for variety -- exactly the trade-off from Lecture 30.\")\nprint(\"=\" * 58)\n",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-3",
      "title": "Python",
      "code": "import numpy as np\nnp.random.seed(42)\n\n# ============================================================\n#  A COMPLETE, TRAINABLE GPT IN NUMPY\n#\n#  Every layer below has BOTH a forward() and a backward().\n#  The backward passes are exactly the derivations from\n#  Lecture 24b. Nothing here is frozen; nothing is faked.\n#  If you change a weight, the loss changes -- and the\n#  gradient check at the bottom proves it.\n# ============================================================\n\n\nclass CharTok:\n    \"\"\"Character-level tokenizer (Lecture 7).\"\"\"\n    def __init__(self, text):\n        chars = sorted(set(text))\n        self.c2i = {c: i for i, c in enumerate(chars)}\n        self.i2c = {i: c for i, c in enumerate(chars)}\n        self.V = len(chars)\n\n    def encode(self, t):\n        return [self.c2i.get(c, 0) for c in t]\n\n    def decode(self, ids):\n        return ''.join(self.i2c.get(int(i), '?') for i in ids)\n\n\nclass LN:\n    \"\"\"LayerNorm (Lecture 20) with its backward pass (Lecture 24b).\"\"\"\n    def __init__(self, d):\n        self.g = np.ones(d)\n        self.b = np.zeros(d)\n        self.dg = np.zeros(d)\n        self.db = np.zeros(d)\n\n    def forward(self, x):\n        mu = x.mean(-1, keepdims=True)\n        var = x.var(-1, keepdims=True)\n        self.inv = 1.0 / np.sqrt(var + 1e-5)\n        self.xhat = (x - mu) * self.inv\n        return self.g * self.xhat + self.b\n\n    def backward(self, dy):\n        D = dy.shape[-1]\n        self.dg = (dy * self.xhat).reshape(-1, D).sum(0)\n        self.db = dy.reshape(-1, D).sum(0)\n        dxhat = dy * self.g\n        # Two correction terms: nudging one feature also moves the row's\n        # mean and variance, so both effects flow back.\n        return self.inv * (\n            dxhat\n            - dxhat.mean(-1, keepdims=True)\n            - self.xhat * (dxhat * self.xhat).mean(-1, keepdims=True)\n        )\n\n    def params(self):\n        return [('g', self), ('b', self)]\n\n\nGK = np.sqrt(2.0 / np.pi)\n\n\ndef gelu(z):\n    \"\"\"GELU, tanh approximation (Lecture 19).\"\"\"\n    return 0.5 * z * (1.0 + np.tanh(GK * (z + 0.044715 * z ** 3)))\n\n\ndef dgelu(z):\n    u = GK * (z + 0.044715 * z ** 3)\n    t = np.tanh(u)\n    return 0.5 * (1.0 + t) + 0.5 * z * (1.0 - t * t) * GK * (1.0 + 3 * 0.044715 * z * z)\n\n\nclass Attn:\n    \"\"\"Causal multi-head self-attention (Lectures 13-16) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.h, self.hd, self.d = h, d // h, d\n        self.Wq = np.random.randn(d, d) * s\n        self.Wk = np.random.randn(d, d) * s\n        self.Wv = np.random.randn(d, d) * s\n        self.Wo = np.random.randn(d, d) * s\n        for n in ('Wq', 'Wk', 'Wv', 'Wo'):\n            setattr(self, 'd' + n, np.zeros((d, d)))\n\n    def _split(self, m, B, T):\n        return m.reshape(B, T, self.h, self.hd).transpose(0, 2, 1, 3)\n\n    def _merge(self, m, B, T):\n        return m.transpose(0, 2, 1, 3).reshape(B, T, self.d)\n\n    def forward(self, x):\n        B, T, C = x.shape\n        self.x = x\n        Q = self._split(x @ self.Wq, B, T)\n        Kk = self._split(x @ self.Wk, B, T)\n        V = self._split(x @ self.Wv, B, T)\n        sc = Q @ Kk.transpose(0, 1, 3, 2) / np.sqrt(self.hd)\n        sc = sc + np.triu(np.ones((T, T)), k=1) * (-1e9)   # causal mask\n        e = np.exp(sc - sc.max(-1, keepdims=True))\n        a = e / e.sum(-1, keepdims=True)\n        self.Q, self.Kk, self.V, self.a = Q, Kk, V, a\n        self.o = self._merge(a @ V, B, T)\n        return self.o @ self.Wo\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        self.dWo = self.o.reshape(-1, C).T @ dy.reshape(-1, C)\n        do = self._split(dy @ self.Wo.T, B, T)\n\n        dV = self.a.transpose(0, 1, 3, 2) @ do\n        da = do @ self.V.transpose(0, 1, 3, 2)\n        # softmax Jacobian, applied row-wise\n        dsc = self.a * (da - (da * self.a).sum(-1, keepdims=True)) / np.sqrt(self.hd)\n\n        dQ = dsc @ self.Kk\n        dKk = dsc.transpose(0, 1, 3, 2) @ self.Q\n\n        dQf = self._merge(dQ, B, T).reshape(-1, C)\n        dKf = self._merge(dKk, B, T).reshape(-1, C)\n        dVf = self._merge(dV, B, T).reshape(-1, C)\n        xf = self.x.reshape(-1, C)\n\n        self.dWq = xf.T @ dQf\n        self.dWk = xf.T @ dKf\n        self.dWv = xf.T @ dVf\n        return (dQf @ self.Wq.T + dKf @ self.Wk.T + dVf @ self.Wv.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('Wq', 'Wk', 'Wv', 'Wo')]\n\n\nclass FFN:\n    \"\"\"Position-wise feed-forward network (Lecture 19) + backward.\"\"\"\n    def __init__(self, d, s):\n        df = 4 * d\n        self.W1 = np.random.randn(d, df) * s\n        self.b1 = np.zeros(df)\n        self.W2 = np.random.randn(df, d) * s\n        self.b2 = np.zeros(d)\n        self.dW1 = np.zeros((d, df)); self.db1 = np.zeros(df)\n        self.dW2 = np.zeros((df, d)); self.db2 = np.zeros(d)\n\n    def forward(self, x):\n        self.x = x\n        self.z = x @ self.W1 + self.b1\n        self.a = gelu(self.z)\n        return self.a @ self.W2 + self.b2\n\n    def backward(self, dy):\n        B, T, C = dy.shape\n        dyf = dy.reshape(-1, C)\n        self.dW2 = self.a.reshape(-1, self.a.shape[-1]).T @ dyf\n        self.db2 = dyf.sum(0)\n        dz = (dy @ self.W2.T) * dgelu(self.z)\n        dzf = dz.reshape(-1, dz.shape[-1])\n        self.dW1 = self.x.reshape(-1, C).T @ dzf\n        self.db1 = dzf.sum(0)\n        return (dzf @ self.W1.T).reshape(B, T, C)\n\n    def params(self):\n        return [(n, self) for n in ('W1', 'b1', 'W2', 'b2')]\n\n\nclass Blk:\n    \"\"\"Pre-norm transformer block (Lectures 21-22) + backward.\"\"\"\n    def __init__(self, d, h, s):\n        self.ln1, self.at = LN(d), Attn(d, h, s)\n        self.ln2, self.ff = LN(d), FFN(d, s)\n\n    def forward(self, x):\n        x = x + self.at.forward(self.ln1.forward(x))\n        x = x + self.ff.forward(self.ln2.forward(x))\n        return x\n\n    def backward(self, d):\n        # A residual connection adds the gradient to BOTH paths: through the\n        # sublayer, and straight past it. That second path is why deep stacks\n        # train at all (Lecture 21).\n        d = d + self.ln2.backward(self.ff.backward(d))\n        d = d + self.ln1.backward(self.at.backward(d))\n        return d\n\n    def params(self):\n        return self.ln1.params() + self.at.params() + self.ln2.params() + self.ff.params()\n\n\nclass GPT:\n    \"\"\"The whole model (Lecture 23) — now with a real backward pass.\"\"\"\n    def __init__(self, V, d, h, L, T):\n        s = 0.02\n        rs = s / np.sqrt(2 * L)          # scaled init for residual depth\n        self.tok = np.random.randn(V, d) * s\n        self.pos = np.random.randn(T, d) * s\n        self.dtok = np.zeros((V, d)); self.dpos = np.zeros((T, d))\n        self.blocks = [Blk(d, h, rs) for _ in range(L)]\n        self.ln_f = LN(d)\n        self.head = np.random.randn(d, V) * s\n        self.dhead = np.zeros((d, V))\n        self.V, self.T = V, T\n\n    def forward(self, ids):\n        B, T = ids.shape\n        self.ids = ids\n        x = self.tok[ids] + self.pos[:T]\n        for bl in self.blocks:\n            x = bl.forward(x)\n        self.xf = self.ln_f.forward(x)\n        return self.xf @ self.head\n\n    def backward(self, dlogits):\n        B, T, V = dlogits.shape\n        C = self.head.shape[0]\n        self.dhead = self.xf.reshape(-1, C).T @ dlogits.reshape(-1, V)\n        d = self.ln_f.backward(dlogits @ self.head.T)\n        for bl in reversed(self.blocks):     # <-- every block now gets a gradient\n            d = bl.backward(d)\n        self.dpos = np.zeros_like(self.pos)\n        self.dpos[:T] = d.sum(0)             # positions are shared across the batch\n        self.dtok = np.zeros_like(self.tok)\n        np.add.at(self.dtok, self.ids.reshape(-1), d.reshape(-1, C))  # scatter-add\n\n    def params(self):\n        p = [('tok', self), ('pos', self), ('head', self)]\n        for bl in self.blocks:\n            p += bl.params()\n        return p + self.ln_f.params()\n\n\ndef cross_entropy(logits, targets):\n    \"\"\"Loss (Lecture 25). Returns (loss, dL/dlogits).\"\"\"\n    B, T, V = logits.shape\n    lf = logits.reshape(B * T, V)\n    tf = targets.reshape(B * T)\n    s = lf - lf.max(-1, keepdims=True)\n    lp = s - np.log(np.exp(s).sum(-1, keepdims=True))\n    loss = -lp[np.arange(B * T), tf].mean()\n    p = np.exp(lp)\n    p[np.arange(B * T), tf] -= 1.0\n    return loss, (p / (B * T)).reshape(B, T, V)\n\n\nclass AdamW:\n    \"\"\"AdamW + global grad-norm clipping (Lectures 27-28).\"\"\"\n    def __init__(self, params, betas=(0.9, 0.95), eps=1e-8, wd=0.01):\n        self.p = params\n        self.b1, self.b2, self.eps, self.wd = betas[0], betas[1], eps, wd\n        self.m = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.v = [np.zeros_like(getattr(o, n)) for n, o in params]\n        self.t = 0\n\n    def step(self, lr, clip=1.0):\n        self.t += 1\n        total = 0.0\n        for n, o in self.p:\n            g = getattr(o, 'd' + n)\n            total += float((g * g).sum())\n        norm = np.sqrt(total)\n        scale = min(1.0, clip / (norm + 1e-6))\n        for i, (n, o) in enumerate(self.p):\n            g = getattr(o, 'd' + n) * scale\n            self.m[i] = self.b1 * self.m[i] + (1 - self.b1) * g\n            self.v[i] = self.b2 * self.v[i] + (1 - self.b2) * (g * g)\n            mh = self.m[i] / (1 - self.b1 ** self.t)\n            vh = self.v[i] / (1 - self.b2 ** self.t)\n            w = getattr(o, n)\n            if w.ndim > 1:                      # decoupled decay, not on biases/gains\n                w = w - lr * self.wd * w\n            setattr(o, n, w - lr * mh / (np.sqrt(vh) + self.eps))\n        return norm\n\n\ndef lr_at(step, warmup, total, max_lr, min_lr):\n    \"\"\"Linear warmup then cosine decay (Lecture 27).\"\"\"\n    if step < warmup:\n        return max_lr * (step + 1) / warmup\n    r = (step - warmup) / max(1, total - warmup)\n    return min_lr + 0.5 * (max_lr - min_lr) * (1 + np.cos(np.pi * r))\n\n\nCORPUS = \"\"\"the cat sat on the mat and looked around the room.\nthe dog ran in the park and played with the ball.\na bird flew over the tree and sang a beautiful song.\nthe sun shone brightly in the clear blue sky today.\nthe model learns to predict the next word in text.\na neural network processes data through many layers.\nthe attention mechanism helps focus on important parts.\ntraining a language model requires lots of text data.\nthe transformer block combines attention and feed forward.\neach layer builds on the output of the previous one.\nthe loss function measures how well the model predicts.\ngradient descent updates weights to reduce the total loss.\nthe learning rate controls how big each update step is.\npretraining teaches the model to understand language patterns.\nthe cat sat on the mat and watched the birds outside.\nthe dog ran in the park and chased the other dogs.\n\"\"\" * 4\n\ntok = CharTok(CORPUS)\ntoken_ids = tok.encode(CORPUS)\nT = 24\ndata = []\nfor i in range(0, len(token_ids) - T, 4):\n    ch = token_ids[i:i + T + 1]\n    if len(ch) == T + 1:\n        data.append((np.array(ch[:-1]), np.array(ch[1:])))\n\nBASELINE = float(np.log(tok.V))   # loss of a model that guesses uniformly\nrng = np.random.default_rng(0)\n\n\ndef train(model, steps, bs=12, max_lr=3e-3, min_lr=3e-4, warmup=60, log_every=0):\n    \"\"\"Full training loop (Lecture 26). Returns the loss history.\"\"\"\n    opt = AdamW(model.params())\n    hist = []\n    for step in range(steps):\n        idx = rng.choice(len(data), bs)\n        inp = np.stack([data[i][0] for i in idx])\n        tgt = np.stack([data[i][1] for i in idx])\n        lr = lr_at(step, warmup, steps, max_lr, min_lr)\n        loss, dl = cross_entropy(model.forward(inp), tgt)\n        model.backward(dl)\n        opt.step(lr)\n        hist.append(float(loss))\n        if log_every and (step % log_every == 0 or step == steps - 1):\n            print(f\"  step {step:4d} | loss {loss:.4f} | ppl {np.exp(loss):6.2f} | lr {lr:.5f}\")\n    return hist\n\n\ndef sample_next(logits, strategy='top_k', temp=0.8, k=10, p=0.9):\n    \"\"\"Sampling strategies (Lecture 30).\"\"\"\n    if strategy == 'greedy':\n        return int(np.argmax(logits))\n    lg = logits / max(1e-6, temp)\n    if strategy == 'top_k':\n        thr = np.sort(lg)[-k]\n        lg = np.where(lg < thr, -1e9, lg)\n    elif strategy == 'top_p':\n        order = np.argsort(lg)[::-1]\n        pr = np.exp(lg[order] - lg[order].max()); pr /= pr.sum()\n        cut = np.searchsorted(np.cumsum(pr), p) + 1\n        keep = order[:cut]\n        mask = np.full_like(lg, -1e9); mask[keep] = lg[keep]; lg = mask\n    pr = np.exp(lg - lg.max()); pr /= pr.sum()\n    return int(rng.choice(len(pr), p=pr))\n\n\ndef generate(model, prompt, n=100, strategy='top_k', temp=0.8, k=10, p=0.9):\n    out = tok.encode(prompt)\n    for _ in range(n):\n        w = out[-T:]\n        padded = [0] * (T - len(w)) + w\n        logits = model.forward(np.array([padded]))[0, -1]\n        out.append(sample_next(logits, strategy, temp, k, p))\n    return tok.decode(out[len(tok.encode(prompt)):])\n\n\n# ============================================================\n#  INTERACTIVE GENERATION PLAYGROUND\n#  Change any number below and re-run.\n# ============================================================\nmodel = GPT(tok.V, d=48, h=4, L=2, T=T)\nprint(\"=\" * 58)\nprint(\"  INTERACTIVE TEXT GENERATION PLAYGROUND\")\nprint(\"=\" * 58)\nprint(\"  Training your model (about 30 seconds)...\")\nhist = train(model, steps=450, bs=12, warmup=50)\nprint(f\"  Ready. Loss {hist[0]:.4f} -> {hist[-1]:.4f} (random {BASELINE:.4f})\")\n\nprint(\"\\n  EXPERIMENT 1 - Temperature sweep (low = safe, high = wild)\")\nfor temp in [0.2, 0.5, 0.7, 1.0, 1.5]:\n    t = generate(model, \"the \", 55, strategy='temperature', temp=temp)\n    print(f\"    T={temp:<4} '{t[:52].replace(chr(10), ' ')}'\")\n\nprint(\"\\n  EXPERIMENT 2 - Strategy comparison\")\nfor strat, label in [('greedy', 'Greedy'), ('temperature', 'Temp'),\n                     ('top_k', 'Top-k'), ('top_p', 'Top-p')]:\n    t = generate(model, \"the model \", 55, strategy=strat, temp=0.7)\n    print(f\"    {label:<7} '{t[:52].replace(chr(10), ' ')}'\")\n\nprint(\"\\n  EXPERIMENT 3 - Different prompts\")\nfor prompt in [\"the cat \", \"a neural \", \"training \", \"the sun \", \"each layer \"]:\n    t = generate(model, prompt, 48, strategy='top_p', temp=0.6)\n    print(f\"    '{prompt}' -> '{t[:45].replace(chr(10), ' ')}'\")\n\nprint(\"\\n  EXPERIMENT 4 - Long generation\")\nlong = generate(model, \"the \", 200, strategy='top_p', temp=0.7).replace(chr(10), ' ')\nprint(f\"    '{long[:76]}'\")\nwords = long.split()\nprint(f\"    words {len(words)} | unique {len(set(words))} | repetition \"\n      f\"{1 - len(set(words)) / max(1, len(words)):.0%}\")\n\nprint(\"\\n\" + \"=\" * 58)\nprint(\"  YOU BUILT AND TRAINED A LANGUAGE MODEL\")\nprint(\"=\" * 58)\nprint(\"  Tokenizer, embeddings, positional encodings, causal\")\nprint(\"  self-attention, feed-forward layers, layer norm, residual\")\nprint(\"  connections, transformer blocks, cross-entropy loss, a full\")\nprint(\"  backward pass, AdamW, LR scheduling, gradient clipping,\")\nprint(\"  a training loop, and four sampling strategies.\")\nprint()\nprint(\"  Every gradient here is one you derived in Lecture 24b, and\")\nprint(\"  the gradient check in the capstone proves they are correct.\")\nprint()\nprint(\"  Same architecture and same maths as GPT-4. The difference is\")\nprint(\"  scale: 75K parameters instead of a trillion, kilobytes instead\")\nprint(\"  of terabytes, one minute instead of months.\")\nprint(\"=\" * 58)\n",
      "language": "python",
      "runnable": true
    }
  ]
}