{
  "id": "50",
  "title": {
    "en": "Connecting Your LLM to GenAI SaaS Products",
    "ro": "Conectarea LLM-ului la Produsele GenAI SaaS",
    "el": "Σύνδεση του LLM σας με τα Προϊόντα GenAI SaaS"
  },
  "content": {
    "en": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\">Stage 7: NeuralForge Platform</div>\n<h1>Lecture 50: Connecting Your LLM to GenAI SaaS Products</h1>\n<p class=\"lecture-subtitle\">Wiring Your Custom Language Model into Five Real-World AI Applications</p>\n<div class=\"lecture-meta\">\n<span>~40 min read</span> | <span>Intermediate&ndash;Advanced</span> | <span>Hands-on integration guide</span>\n</div>\n</div>\n\n<h2>1. The Five GenAI SaaS Products You Built</h2>\n\n<p>Throughout this course, you built five GenAI SaaS products. Each one solves a real problem using AI. Now it is time to connect them all to <strong>your own LLM</strong> &mdash; the language model you trained and fine-tuned in NeuralForge.</p>\n\n<p>Here is a summary of each product and how your LLM enhances it:</p>\n\n<table>\n<thead><tr><th>Product</th><th>Purpose</th><th>How Your LLM Helps</th></tr></thead>\n<tbody>\n<tr><td><strong>PixelForge</strong></td><td>AI Image Generation</td><td>Enhances image prompts, generates descriptions, suggests styles</td></tr>\n<tr><td><strong>ClipCraft</strong></td><td>AI Video Generation</td><td>Writes video scripts, scene descriptions, storyboards</td></tr>\n<tr><td><strong>ProseAI</strong></td><td>Text Rewriting</td><td>Powers text transformation across multiple styles and modes</td></tr>\n<tr><td><strong>TruthLens</strong></td><td>Plagiarism/AI Detection</td><td>Analyzes text patterns, writing consistency, vocabulary metrics</td></tr>\n<tr><td><strong>DocMind</strong></td><td>Document Intelligence</td><td>Structures documents, answers questions, extracts key information</td></tr>\n</tbody>\n</table>\n\n<div class=\"info-box\">\n<strong>The Big Picture:</strong> Your LLM is the engine. Each SaaS product is a specialized vehicle built around that engine. The system prompt (from Lecture 49) determines how the engine behaves in each vehicle.\n</div>\n\n<h2>2. How the Connection Works</h2>\n\n<p>The architecture is straightforward. Every SaaS product communicates with your LLM through HTTP API calls. Here is the flow:</p>\n\n<h3>The Request Flow</h3>\n\n<ol>\n<li><strong>User</strong> opens your SaaS product (e.g., ProseAI) in their browser</li>\n<li><strong>SaaS Frontend</strong> sends the user&rsquo;s request to the SaaS backend API</li>\n<li><strong>SaaS API</strong> constructs a prompt using the product&rsquo;s system prompt + user input</li>\n<li><strong>SaaS API</strong> sends the prompt to your LLM&rsquo;s API endpoint</li>\n<li><strong>Your LLM</strong> processes the prompt and returns a response</li>\n<li><strong>SaaS API</strong> formats the response and sends it back to the frontend</li>\n<li><strong>User</strong> sees the result in the SaaS product&rsquo;s UI</li>\n</ol>\n\n<h3>Architecture Diagram</h3>\n\n<pre><code>User Browser\n    |\n    v\nSaaS Frontend (React/Next.js on Vercel)\n    |\n    v\nSaaS API (FastAPI / Next.js API routes)\n    |         \\\n    v          v\nYour LLM    Commercial API (fallback)\n(NeuralForge  (OpenAI / Claude /\n localhost    Gemini as backup)\n or Vercel)\n    |\n    v\nResponse back to user</code></pre>\n\n<h3>Where Your LLM Runs</h3>\n\n<p>Your LLM can run in two places:</p>\n\n<ul>\n<li><strong>Locally:</strong> <code>http://localhost:8000</code> &mdash; for development and testing. Start your NeuralForge server and your SaaS products can reach it on your machine.</li>\n<li><strong>Deployed:</strong> <code>https://your-name-neuralforge.vercel.app</code> &mdash; for production. After deploying to Vercel (covered in Lecture 51), your SaaS products can reach it from anywhere.</li>\n</ul>\n\n<h3>The Connection Is Just a URL</h3>\n\n<p>Each SaaS product needs exactly one piece of information: <strong>the URL of your LLM</strong>. This is stored as an environment variable:</p>\n\n<pre><code># In your SaaS product's .env file:\nLLM_URL=http://localhost:8000\n\n# Or, for production:\nLLM_URL=https://your-name-neuralforge.vercel.app</code></pre>\n\n<p>That is it. Change the URL, and your SaaS product switches between your local LLM and the deployed version.</p>\n\n<h2>3. What the NeuralForge API Generator Produces</h2>\n\n<p>The NeuralForge platform includes an <strong>API Generator</strong> that creates ready-to-use integration code for each SaaS product. When you select a product and click &ldquo;Generate,&rdquo; it produces:</p>\n\n<ul>\n<li>A complete FastAPI application with all necessary endpoints</li>\n<li>The system prompt for that product (from Lecture 49)</li>\n<li>Request/response models with proper validation</li>\n<li>Error handling and fallback logic</li>\n<li>CORS configuration for frontend communication</li>\n<li>Health check endpoints</li>\n</ul>\n\n<h3>How to Use the API Generator</h3>\n\n<ol>\n<li>Open NeuralForge in your browser</li>\n<li>Navigate to the &ldquo;API Generator&rdquo; section</li>\n<li>Select the SaaS product you want to connect (e.g., ProseAI)</li>\n<li>Click &ldquo;Generate API Code&rdquo;</li>\n<li>The generated code appears in the code editor</li>\n<li>Click &ldquo;Copy&rdquo; to copy it to your clipboard</li>\n<li>Paste into a new Python file in your SaaS project</li>\n</ol>\n\n<h3>What Gets Generated</h3>\n\n<p>For each product, the generator creates endpoints specific to that product&rsquo;s functionality. The generated code includes:</p>\n\n<ul>\n<li>Import statements and dependencies</li>\n<li>FastAPI app initialization with CORS middleware</li>\n<li>Pydantic models for request/response validation</li>\n<li>The product-specific system prompt</li>\n<li>Main endpoint(s) for the product&rsquo;s core functionality</li>\n<li>Fallback to commercial APIs when the custom LLM is unavailable</li>\n<li>Error handling with meaningful error messages</li>\n</ul>\n\n<h2>4. Product-by-Product Integration Guide</h2>\n\n<p>Let&rsquo;s walk through each product and understand exactly what endpoints are generated, what the system prompt does, and how to customize the behavior.</p>\n\n<h3>PixelForge (Image Generation)</h3>\n\n<p><strong>Generated endpoints:</strong></p>\n<ul>\n<li><code>POST /api/enhance-prompt</code> &mdash; Takes a simple image description, returns an enhanced prompt optimized for image generation</li>\n<li><code>POST /api/describe-image</code> &mdash; Takes an image URL, returns a text description of the image</li>\n<li><code>POST /api/suggest-styles</code> &mdash; Takes a concept, returns suggested art styles and variations</li>\n</ul>\n\n<p><strong>System prompt role:</strong> The LLM acts as an image prompt engineer. It takes simple descriptions like &ldquo;a cat on a roof&rdquo; and transforms them into detailed, AI-image-generation-optimized prompts with lighting, style, composition, and quality modifiers.</p>\n\n<p><strong>Example request and response:</strong></p>\n\n<pre><code># Request:\nPOST /api/enhance-prompt\n{\n  \"prompt\": \"a cat sitting on a roof at sunset\",\n  \"style\": \"photograph\"\n}\n\n# Response:\n{\n  \"enhanced_prompt\": \"A domestic tabby cat sitting gracefully\n    on weathered terracotta roof tiles, golden hour sunset\n    lighting casting long warm shadows, shot from below\n    at a slight angle, shallow depth of field with bokeh\n    city lights in background, professional photography,\n    8K resolution, warm color palette with orange and\n    amber tones, National Geographic style\",\n  \"tokens_used\": 89\n}</code></pre>\n\n<h3>ClipCraft (Video Generation)</h3>\n\n<p><strong>Generated endpoints:</strong></p>\n<ul>\n<li><code>POST /api/generate-script</code> &mdash; Creates a structured video script with scene descriptions</li>\n<li><code>POST /api/describe-scene</code> &mdash; Generates detailed scene descriptions for video generation</li>\n</ul>\n\n<p><strong>System prompt role:</strong> The LLM acts as a video script writer and scene director. It breaks video concepts into individual scenes with camera movements, timing, and audio cues.</p>\n\n<h3>ProseAI (Text Rewriting)</h3>\n\n<p><strong>Generated endpoints:</strong></p>\n<ul>\n<li><code>POST /api/rewrite</code> &mdash; Rewrites text in the specified mode (formal, casual, concise, expand, simplify)</li>\n<li><code>POST /api/analyze-style</code> &mdash; Analyzes the writing style of the input text</li>\n<li><code>POST /api/suggest-improvements</code> &mdash; Suggests specific improvements to the text</li>\n</ul>\n\n<p><strong>System prompt role:</strong> The LLM acts as a professional text editor. Each rewriting mode transforms the text differently while preserving all factual content.</p>\n\n<p><strong>Example request and response:</strong></p>\n\n<pre><code># Request:\nPOST /api/rewrite\n{\n  \"text\": \"The quarterly financial results have demonstrated\n    a significant improvement in our revenue streams,\n    which can be attributed to the strategic initiatives\n    implemented during the previous fiscal period.\",\n  \"mode\": \"casual\"\n}\n\n# Response:\n{\n  \"rewritten\": \"Great news — we made a lot more money this\n    quarter! Those changes we made last quarter really\n    paid off.\",\n  \"original_word_count\": 28,\n  \"rewritten_word_count\": 20,\n  \"mode\": \"casual\"\n}</code></pre>\n\n<h3>TruthLens (Plagiarism/AI Detection)</h3>\n\n<p><strong>Generated endpoints:</strong></p>\n<ul>\n<li><code>POST /api/analyze</code> &mdash; Performs comprehensive text analysis with pattern detection</li>\n<li><code>POST /api/compare</code> &mdash; Compares two texts for similarity</li>\n</ul>\n\n<p><strong>System prompt role:</strong> The LLM acts as a text analysis expert. It evaluates writing consistency, statistical patterns, structural organization, and vocabulary complexity, returning structured JSON results.</p>\n\n<h3>DocMind (Document Intelligence)</h3>\n\n<p><strong>Generated endpoints:</strong></p>\n<ul>\n<li><code>POST /api/ask</code> &mdash; Answer questions about a provided document</li>\n<li><code>POST /api/summarize</code> &mdash; Summarize a document at different detail levels</li>\n<li><code>POST /api/extract</code> &mdash; Extract specific data points from a document</li>\n</ul>\n\n<p><strong>System prompt role:</strong> The LLM acts as a document analysis assistant. It strictly answers based on the provided document content, quotes relevant passages, and never fabricates information.</p>\n\n<p><strong>Example request and response:</strong></p>\n\n<pre><code># Request:\nPOST /api/ask\n{\n  \"document\": \"Annual Report 2024: Total revenue was $4.2M,\n    up 35% from $3.1M in 2023. Operating expenses\n    decreased by 12% to $2.8M. Net profit margin\n    improved from 8% to 15%.\",\n  \"question\": \"What was the revenue growth?\"\n}\n\n# Response:\n{\n  \"answer\": \"Revenue grew by 35%, from $3.1M in 2023 to\n    $4.2M in 2024.\",\n  \"confidence\": \"high\",\n  \"source_quote\": \"Total revenue was $4.2M, up 35% from\n    $3.1M in 2023.\"\n}</code></pre>\n\n<h2>5. Deploying the Integration API</h2>\n\n<p>The generated API code runs as a separate service alongside your LLM. Here is how to deploy it:</p>\n\n<h3>Local Development</h3>\n\n<pre><code># 1. Save the generated code as main.py\n# 2. Install dependencies:\npip install fastapi uvicorn httpx openai\n\n# 3. Set environment variables:\nexport LLM_URL=http://localhost:8000\nexport OPENAI_API_KEY=sk-...  # Optional fallback\n\n# 4. Run the integration API:\nuvicorn main:app --port 8001\n\n# Your integration API is now at http://localhost:8001\n# Your LLM is at http://localhost:8000\n# Your SaaS frontend calls http://localhost:8001</code></pre>\n\n<h3>Production Deployment</h3>\n\n<p>For production, deploy the integration API to Vercel alongside your SaaS frontend, or run it as a separate Vercel project. Set the <code>LLM_URL</code> environment variable to your deployed NeuralForge URL.</p>\n\n<h2>6. Fallback to Commercial APIs</h2>\n\n<p>Your Mini GPT-2 model is impressive for what it is &mdash; a language model you built from scratch. But it is a small model (around 85M parameters), and it may not produce production-quality text for every use case. This is where <strong>commercial API fallback</strong> becomes essential.</p>\n\n<h3>When to Use Fallback</h3>\n\n<ul>\n<li>Your LLM server is down or unreachable</li>\n<li>The response quality from your model is below a threshold</li>\n<li>The task requires capabilities your model was not trained for (e.g., complex reasoning)</li>\n<li>You need the quality of a commercial model for production users</li>\n</ul>\n\n<h3>How Fallback Works</h3>\n\n<p>The generated code automatically handles fallback. Here is the logic:</p>\n\n<ol>\n<li>Try to call your custom LLM first</li>\n<li>If the call fails (timeout, error, or server unreachable), fall back to a commercial API</li>\n<li>The commercial API uses the exact same system prompt, so the behavior is consistent</li>\n<li>The response indicates which model was actually used</li>\n</ol>\n\n<pre><code># The fallback logic in the generated code:\ntry:\n    # Try your custom LLM first\n    response = await call_custom_llm(prompt)\n    return {\"response\": response, \"model\": \"custom-llm\"}\nexcept Exception:\n    # Fall back to commercial API\n    response = await call_openai(prompt)\n    return {\"response\": response, \"model\": \"gpt-4o-mini\"}</code></pre>\n\n<h3>Setting Up Fallback</h3>\n\n<p>To enable commercial API fallback, set the API key as an environment variable:</p>\n\n<pre><code># In your .env file:\nLLM_URL=http://localhost:8000\nOPENAI_API_KEY=sk-...           # OpenAI fallback\nANTHROPIC_API_KEY=sk-ant-...    # Or Anthropic fallback\nGOOGLE_API_KEY=AI...            # Or Google fallback</code></pre>\n\n<p>The generated code checks for these keys in order and uses the first available one as the fallback provider.</p>\n\n<div class=\"info-box\">\n<strong>Cost-Saving Tip:</strong> Use Google Gemini Flash as your fallback provider. It is the cheapest commercial option at $0.10/$0.40 per 1M tokens, and the free tier (15 req/min) is often sufficient for fallback scenarios where your custom LLM handles most traffic.\n</div>\n\n<h2>7. Testing Your Integrations</h2>\n\n<p>Before deploying to production, thoroughly test each integration. Here is a systematic approach:</p>\n\n<h3>Step 1: Test with the NeuralForge Playground</h3>\n\n<p>The NeuralForge platform includes a Playground where you can test prompts interactively:</p>\n\n<ol>\n<li>Open the Playground in NeuralForge</li>\n<li>Paste the system prompt for the product you want to test</li>\n<li>Type sample user inputs</li>\n<li>Check that the responses are appropriate for the product</li>\n<li>Adjust the system prompt if needed</li>\n</ol>\n\n<h3>Step 2: Test the Integration API Directly</h3>\n\n<p>Use the testing script (code block 2 below) to call each endpoint and verify responses:</p>\n\n<pre><code># Quick test with curl:\ncurl -X POST http://localhost:8001/api/rewrite \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"text\": \"Hello world\", \"mode\": \"formal\"}'</code></pre>\n\n<h3>Step 3: Compare Custom LLM vs. Commercial API</h3>\n\n<p>Send the same prompt to both your custom LLM and a commercial API, then compare the results side by side. This helps you understand where your model excels and where you might want to use fallback.</p>\n\n<h3>Step 4: Test Edge Cases</h3>\n\n<ul>\n<li>Empty inputs &mdash; should return a helpful error message</li>\n<li>Very long inputs &mdash; should handle gracefully or truncate</li>\n<li>Non-English inputs &mdash; should respond appropriately</li>\n<li>Malformed requests &mdash; should return proper validation errors</li>\n<li>LLM server down &mdash; should fall back to commercial API</li>\n</ul>\n\n<div class=\"warning-box\">\n<strong>Before Going Live:</strong> Test every endpoint with at least 10 different inputs. Pay special attention to the fallback behavior &mdash; deliberately stop your LLM server and verify that the commercial API takes over seamlessly.\n</div>\n\n<h2>8. Customizing the Behavior</h2>\n\n<p>Each integration can be customized in several ways:</p>\n\n<h3>Modify the System Prompt</h3>\n\n<p>The system prompt is the primary control mechanism. Edit it to change how the LLM behaves for that product. Refer to Lecture 49 for prompt engineering techniques.</p>\n\n<h3>Adjust Temperature and Top-K</h3>\n\n<ul>\n<li><strong>PixelForge:</strong> Higher temperature (0.8&ndash;1.0) for creative prompt enhancement</li>\n<li><strong>ProseAI:</strong> Medium temperature (0.5&ndash;0.7) for balanced rewriting</li>\n<li><strong>TruthLens:</strong> Low temperature (0.0&ndash;0.2) for consistent, factual analysis</li>\n<li><strong>DocMind:</strong> Low temperature (0.0&ndash;0.3) for accurate document Q&A</li>\n<li><strong>ClipCraft:</strong> Medium-high temperature (0.7&ndash;0.9) for creative scripts</li>\n</ul>\n\n<h3>Add Custom Endpoints</h3>\n\n<p>The generated code is a starting point. You can add new endpoints to handle additional use cases specific to your SaaS product. For example, add a <code>/api/batch-rewrite</code> endpoint to ProseAI that processes multiple texts at once.</p>\n\n<h3>Change the Fallback Provider</h3>\n\n<p>The default fallback is OpenAI, but you can switch to any provider from Lecture 48 by modifying the fallback function in the generated code.</p>\n\n<h2>Summary</h2>\n\n<p>You now understand how to connect your NeuralForge LLM to all five GenAI SaaS products. The key concepts are:</p>\n\n<ul>\n<li>Each product communicates with your LLM through a simple HTTP API</li>\n<li>The system prompt determines how your LLM behaves for each product</li>\n<li>The NeuralForge API Generator creates ready-to-use integration code</li>\n<li>Commercial API fallback ensures reliability when your custom LLM is unavailable</li>\n<li>Testing is essential &mdash; test each endpoint with multiple inputs before deploying</li>\n</ul>\n\n<p>In the next and final lecture, you will deploy your complete LLM system to the internet, making it accessible from anywhere in the world.</p>\n\n</div>",
    "ro": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\">Etapa 7: Platforma NeuralForge</div>\n<h1>Lecția 50: Conectarea LLM-ului la Produsele GenAI SaaS</h1>\n<p class=\"lecture-subtitle\">Integrarea Modelului Tău de Limbaj Personalizat în Cinci Aplicații AI din Lumea Reală</p>\n<div class=\"lecture-meta\">\n<span>~40 min de citit</span> | <span>Intermediar&ndash;Avansat</span> | <span>Ghid practic de integrare</span>\n</div>\n</div>\n\n<h2>1. Cele Cinci Produse GenAI SaaS pe Care Le-ai Construit</h2>\n\n<p>Pe parcursul acestui curs, ai construit cinci produse GenAI SaaS. Fiecare rezolvă o problemă reală folosind AI. Acum este momentul să le conectezi pe toate la <strong>propriul tău LLM</strong> &mdash; modelul de limbaj pe care l-ai antrenat și l-ai perfecționat în NeuralForge.</p>\n\n<p>Iată un rezumat al fiecărui produs și modul în care LLM-ul tău îl îmbunătățește:</p>\n\n<table>\n<thead><tr><th>Produs</th><th>Scop</th><th>Cum Ajută LLM-ul Tău</th></tr></thead>\n<tbody>\n<tr><td><strong>PixelForge</strong></td><td>Generare de Imagini AI</td><td>Îmbunătățește prompturi de imagine, generează descrieri, sugerează stiluri</td></tr>\n<tr><td><strong>ClipCraft</strong></td><td>Generare de Video AI</td><td>Scrie scenarii video, descrieri de scene, storyboard-uri</td></tr>\n<tr><td><strong>ProseAI</strong></td><td>Rescrierea Textului</td><td>Alimentează transformarea textului în mai multe stiluri și moduri</td></tr>\n<tr><td><strong>TruthLens</strong></td><td>Detectarea Plagiatului/AI</td><td>Analizează tipare de text, consistența scrierii, metrici de vocabular</td></tr>\n<tr><td><strong>DocMind</strong></td><td>Inteligența Documentelor</td><td>Structurează documente, răspunde la întrebări, extrage informații cheie</td></tr>\n</tbody>\n</table>\n\n<div class=\"info-box\">\n<strong>Imaginea de Ansamblu:</strong> LLM-ul tău este motorul. Fiecare produs SaaS este un vehicul specializat construit în jurul acelui motor. Promptul de sistem (din Lecția 49) determină cum se comportă motorul în fiecare vehicul.\n</div>\n\n<h2>2. Cum Funcționează Conexiunea</h2>\n\n<p>Arhitectura este simplă. Fiecare produs SaaS comunică cu LLM-ul tău prin apeluri HTTP API. Iată fluxul:</p>\n\n<h3>Fluxul Cererii</h3>\n\n<ol>\n<li><strong>Utilizatorul</strong> deschide produsul tău SaaS (de ex., ProseAI) în browser</li>\n<li><strong>Frontend-ul SaaS</strong> trimite cererea utilizatorului către API-ul backend SaaS</li>\n<li><strong>API-ul SaaS</strong> construiește un prompt folosind promptul de sistem al produsului + intrarea utilizatorului</li>\n<li><strong>API-ul SaaS</strong> trimite promptul către endpoint-ul API al LLM-ului tău</li>\n<li><strong>LLM-ul tău</strong> procesează promptul și returnează un răspuns</li>\n<li><strong>API-ul SaaS</strong> formatează răspunsul și îl trimite înapoi la frontend</li>\n<li><strong>Utilizatorul</strong> vede rezultatul în interfața produsului SaaS</li>\n</ol>\n\n<h3>Diagrama Arhitecturii</h3>\n\n<pre><code>User Browser\n    |\n    v\nSaaS Frontend (React/Next.js on Vercel)\n    |\n    v\nSaaS API (FastAPI / Next.js API routes)\n    |         \\\n    v          v\nYour LLM    Commercial API (fallback)\n(NeuralForge  (OpenAI / Claude /\n localhost    Gemini as backup)\n or Vercel)\n    |\n    v\nResponse back to user</code></pre>\n\n<h3>Unde Rulează LLM-ul Tău</h3>\n\n<p>LLM-ul tău poate rula în două locuri:</p>\n\n<ul>\n<li><strong>Local:</strong> <code>http://localhost:8000</code> &mdash; pentru dezvoltare și testare. Pornește serverul NeuralForge și produsele tale SaaS îl pot accesa pe mașina ta.</li>\n<li><strong>Implementat:</strong> <code>https://your-name-neuralforge.vercel.app</code> &mdash; pentru producție. După implementarea pe Vercel (acoperită în Lecția 51), produsele tale SaaS îl pot accesa de oriunde.</li>\n</ul>\n\n<h3>Conexiunea Este Doar un URL</h3>\n\n<p>Fiecare produs SaaS are nevoie de exact o singură informație: <strong>URL-ul LLM-ului tău</strong>. Acesta este stocat ca variabilă de mediu:</p>\n\n<pre><code># In your SaaS product's .env file:\nLLM_URL=http://localhost:8000\n\n# Or, for production:\nLLM_URL=https://your-name-neuralforge.vercel.app</code></pre>\n\n<p>Atât. Schimbă URL-ul și produsul tău SaaS comută între LLM-ul local și versiunea implementată.</p>\n\n<h2>3. Ce Produce Generatorul de API NeuralForge</h2>\n\n<p>Platforma NeuralForge include un <strong>Generator de API</strong> care creează cod de integrare gata de utilizare pentru fiecare produs SaaS. Când selectezi un produs și dai clic pe &ldquo;Generate,&rdquo; produce:</p>\n\n<ul>\n<li>O aplicație FastAPI completă cu toate endpoint-urile necesare</li>\n<li>Promptul de sistem pentru acel produs (din Lecția 49)</li>\n<li>Modele de cerere/răspuns cu validare corespunzătoare</li>\n<li>Gestionarea erorilor și logica de fallback</li>\n<li>Configurarea CORS pentru comunicarea cu frontend-ul</li>\n<li>Endpoint-uri de verificare a stării de sănătate</li>\n</ul>\n\n<h3>Cum Se Utilizează Generatorul de API</h3>\n\n<ol>\n<li>Deschide NeuralForge în browser</li>\n<li>Navighează la secțiunea &ldquo;API Generator&rdquo;</li>\n<li>Selectează produsul SaaS pe care vrei să-l conectezi (de ex., ProseAI)</li>\n<li>Dă clic pe &ldquo;Generate API Code&rdquo;</li>\n<li>Codul generat apare în editorul de cod</li>\n<li>Dă clic pe &ldquo;Copy&rdquo; pentru a-l copia în clipboard</li>\n<li>Lipește-l într-un fișier Python nou în proiectul tău SaaS</li>\n</ol>\n\n<h3>Ce Se Generează</h3>\n\n<p>Pentru fiecare produs, generatorul creează endpoint-uri specifice funcționalității acelui produs. Codul generat include:</p>\n\n<ul>\n<li>Declarații de import și dependențe</li>\n<li>Inițializarea aplicației FastAPI cu middleware CORS</li>\n<li>Modele Pydantic pentru validarea cererii/răspunsului</li>\n<li>Promptul de sistem specific produsului</li>\n<li>Endpoint-urile principale pentru funcționalitatea de bază a produsului</li>\n<li>Fallback la API-uri comerciale când LLM-ul personalizat nu este disponibil</li>\n<li>Gestionarea erorilor cu mesaje de eroare relevante</li>\n</ul>\n\n<h2>4. Ghid de Integrare Produs cu Produs</h2>\n\n<p>Să parcurgem fiecare produs și să înțelegem exact ce endpoint-uri sunt generate, ce face promptul de sistem și cum să personalizăm comportamentul.</p>\n\n<h3>PixelForge (Generare de Imagini)</h3>\n\n<p><strong>Endpoint-uri generate:</strong></p>\n<ul>\n<li><code>POST /api/enhance-prompt</code> &mdash; Primește o descriere simplă a imaginii, returnează un prompt îmbunătățit optimizat pentru generarea de imagini</li>\n<li><code>POST /api/describe-image</code> &mdash; Primește un URL de imagine, returnează o descriere text a imaginii</li>\n<li><code>POST /api/suggest-styles</code> &mdash; Primește un concept, returnează stiluri artistice sugerate și variații</li>\n</ul>\n\n<p><strong>Rolul promptului de sistem:</strong> LLM-ul acționează ca un inginer de prompturi pentru imagini. Preia descrieri simple precum &ldquo;o pisică pe un acoperiș&rdquo; și le transformă în prompturi detaliate, optimizate pentru generarea de imagini AI cu iluminare, stil, compoziție și modificatori de calitate.</p>\n\n<p><strong>Exemplu de cerere și răspuns:</strong></p>\n\n<pre><code># Request:\nPOST /api/enhance-prompt\n{\n  \"prompt\": \"a cat sitting on a roof at sunset\",\n  \"style\": \"photograph\"\n}\n\n# Response:\n{\n  \"enhanced_prompt\": \"A domestic tabby cat sitting gracefully\n    on weathered terracotta roof tiles, golden hour sunset\n    lighting casting long warm shadows, shot from below\n    at a slight angle, shallow depth of field with bokeh\n    city lights in background, professional photography,\n    8K resolution, warm color palette with orange and\n    amber tones, National Geographic style\",\n  \"tokens_used\": 89\n}</code></pre>\n\n<h3>ClipCraft (Generare de Video)</h3>\n\n<p><strong>Endpoint-uri generate:</strong></p>\n<ul>\n<li><code>POST /api/generate-script</code> &mdash; Creează un scenariu video structurat cu descrieri de scene</li>\n<li><code>POST /api/describe-scene</code> &mdash; Generează descrieri detaliate de scene pentru generarea de video</li>\n</ul>\n\n<p><strong>Rolul promptului de sistem:</strong> LLM-ul acționează ca un scenarist video și regizor de scene. Descompune conceptele video în scene individuale cu mișcări de cameră, sincronizare și indicii audio.</p>\n\n<h3>ProseAI (Rescrierea Textului)</h3>\n\n<p><strong>Endpoint-uri generate:</strong></p>\n<ul>\n<li><code>POST /api/rewrite</code> &mdash; Rescrie textul în modul specificat (formal, casual, concis, extins, simplificat)</li>\n<li><code>POST /api/analyze-style</code> &mdash; Analizează stilul de scriere al textului de intrare</li>\n<li><code>POST /api/suggest-improvements</code> &mdash; Sugerează îmbunătățiri specifice textului</li>\n</ul>\n\n<p><strong>Rolul promptului de sistem:</strong> LLM-ul acționează ca un editor de text profesional. Fiecare mod de rescriere transformă textul diferit, păstrând în același timp tot conținutul factual.</p>\n\n<p><strong>Exemplu de cerere și răspuns:</strong></p>\n\n<pre><code># Request:\nPOST /api/rewrite\n{\n  \"text\": \"The quarterly financial results have demonstrated\n    a significant improvement in our revenue streams,\n    which can be attributed to the strategic initiatives\n    implemented during the previous fiscal period.\",\n  \"mode\": \"casual\"\n}\n\n# Response:\n{\n  \"rewritten\": \"Great news — we made a lot more money this\n    quarter! Those changes we made last quarter really\n    paid off.\",\n  \"original_word_count\": 28,\n  \"rewritten_word_count\": 20,\n  \"mode\": \"casual\"\n}</code></pre>\n\n<h3>TruthLens (Detectarea Plagiatului/AI)</h3>\n\n<p><strong>Endpoint-uri generate:</strong></p>\n<ul>\n<li><code>POST /api/analyze</code> &mdash; Efectuează analiză cuprinzătoare de text cu detectarea tiparelor</li>\n<li><code>POST /api/compare</code> &mdash; Compară două texte pentru similaritate</li>\n</ul>\n\n<p><strong>Rolul promptului de sistem:</strong> LLM-ul acționează ca un expert în analiza textului. Evaluează consistența scrierii, tiparele statistice, organizarea structurală și complexitatea vocabularului, returnând rezultate JSON structurate.</p>\n\n<h3>DocMind (Inteligența Documentelor)</h3>\n\n<p><strong>Endpoint-uri generate:</strong></p>\n<ul>\n<li><code>POST /api/ask</code> &mdash; Răspunde la întrebări despre un document furnizat</li>\n<li><code>POST /api/summarize</code> &mdash; Rezumă un document la diferite niveluri de detaliu</li>\n<li><code>POST /api/extract</code> &mdash; Extrage puncte de date specifice dintr-un document</li>\n</ul>\n\n<p><strong>Rolul promptului de sistem:</strong> LLM-ul acționează ca un asistent de analiză a documentelor. Răspunde strict pe baza conținutului documentului furnizat, citează pasaje relevante și nu inventează niciodată informații.</p>\n\n<p><strong>Exemplu de cerere și răspuns:</strong></p>\n\n<pre><code># Request:\nPOST /api/ask\n{\n  \"document\": \"Annual Report 2024: Total revenue was $4.2M,\n    up 35% from $3.1M in 2023. Operating expenses\n    decreased by 12% to $2.8M. Net profit margin\n    improved from 8% to 15%.\",\n  \"question\": \"What was the revenue growth?\"\n}\n\n# Response:\n{\n  \"answer\": \"Revenue grew by 35%, from $3.1M in 2023 to\n    $4.2M in 2024.\",\n  \"confidence\": \"high\",\n  \"source_quote\": \"Total revenue was $4.2M, up 35% from\n    $3.1M in 2023.\"\n}</code></pre>\n\n<h2>5. Implementarea API-ului de Integrare</h2>\n\n<p>Codul API generat rulează ca un serviciu separat alături de LLM-ul tău. Iată cum se implementează:</p>\n\n<h3>Dezvoltare Locală</h3>\n\n<pre><code># 1. Save the generated code as main.py\n# 2. Install dependencies:\npip install fastapi uvicorn httpx openai\n\n# 3. Set environment variables:\nexport LLM_URL=http://localhost:8000\nexport OPENAI_API_KEY=sk-...  # Optional fallback\n\n# 4. Run the integration API:\nuvicorn main:app --port 8001\n\n# Your integration API is now at http://localhost:8001\n# Your LLM is at http://localhost:8000\n# Your SaaS frontend calls http://localhost:8001</code></pre>\n\n<h3>Implementare în Producție</h3>\n\n<p>Pentru producție, implementează API-ul de integrare pe Vercel alături de frontend-ul SaaS sau rulează-l ca un proiect Vercel separat. Setează variabila de mediu <code>LLM_URL</code> la URL-ul NeuralForge implementat.</p>\n\n<h2>6. Fallback la API-uri Comerciale</h2>\n\n<p>Modelul tău Mini GPT-2 este impresionant pentru ce este &mdash; un model de limbaj construit de la zero. Dar este un model mic (aproximativ 85M parametri) și s-ar putea să nu producă text de calitate pentru producție în fiecare caz de utilizare. Aici <strong>fallback-ul la API-uri comerciale</strong> devine esențial.</p>\n\n<h3>Când Se Folosește Fallback-ul</h3>\n\n<ul>\n<li>Serverul LLM-ului tău este oprit sau inaccesibil</li>\n<li>Calitatea răspunsului de la modelul tău este sub un prag</li>\n<li>Sarcina necesită capabilități pentru care modelul tău nu a fost antrenat (de ex., raționament complex)</li>\n<li>Ai nevoie de calitatea unui model comercial pentru utilizatorii din producție</li>\n</ul>\n\n<h3>Cum Funcționează Fallback-ul</h3>\n\n<p>Codul generat gestionează automat fallback-ul. Iată logica:</p>\n\n<ol>\n<li>Încearcă să apeleze mai întâi LLM-ul tău personalizat</li>\n<li>Dacă apelul eșuează (timeout, eroare sau server inaccesibil), trece la un API comercial</li>\n<li>API-ul comercial folosește exact același prompt de sistem, deci comportamentul este consistent</li>\n<li>Răspunsul indică ce model a fost efectiv utilizat</li>\n</ol>\n\n<pre><code># The fallback logic in the generated code:\ntry:\n    # Try your custom LLM first\n    response = await call_custom_llm(prompt)\n    return {\"response\": response, \"model\": \"custom-llm\"}\nexcept Exception:\n    # Fall back to commercial API\n    response = await call_openai(prompt)\n    return {\"response\": response, \"model\": \"gpt-4o-mini\"}</code></pre>\n\n<h3>Configurarea Fallback-ului</h3>\n\n<p>Pentru a activa fallback-ul la API-uri comerciale, setează cheia API ca variabilă de mediu:</p>\n\n<pre><code># In your .env file:\nLLM_URL=http://localhost:8000\nOPENAI_API_KEY=sk-...           # OpenAI fallback\nANTHROPIC_API_KEY=sk-ant-...    # Or Anthropic fallback\nGOOGLE_API_KEY=AI...            # Or Google fallback</code></pre>\n\n<p>Codul generat verifică aceste chei în ordine și folosește prima disponibilă ca furnizor de fallback.</p>\n\n<div class=\"info-box\">\n<strong>Sfat pentru Economisirea Costurilor:</strong> Folosește Google Gemini Flash ca furnizor de fallback. Este cea mai ieftină opțiune comercială la $0.10/$0.40 per 1M tokeni, iar nivelul gratuit (15 cereri/min) este adesea suficient pentru scenariile de fallback unde LLM-ul tău personalizat gestionează majoritatea traficului.\n</div>\n\n<h2>7. Testarea Integrărilor Tale</h2>\n\n<p>Înainte de implementarea în producție, testează temeinic fiecare integrare. Iată o abordare sistematică:</p>\n\n<h3>Pasul 1: Testează cu NeuralForge Playground</h3>\n\n<p>Platforma NeuralForge include un Playground unde poți testa prompturi interactiv:</p>\n\n<ol>\n<li>Deschide Playground-ul în NeuralForge</li>\n<li>Lipește promptul de sistem pentru produsul pe care vrei să-l testezi</li>\n<li>Tastează intrări de exemplu ale utilizatorului</li>\n<li>Verifică dacă răspunsurile sunt adecvate pentru produs</li>\n<li>Ajustează promptul de sistem dacă este necesar</li>\n</ol>\n\n<h3>Pasul 2: Testează API-ul de Integrare Direct</h3>\n\n<p>Folosește scriptul de testare (blocul de cod 2 de mai jos) pentru a apela fiecare endpoint și a verifica răspunsurile:</p>\n\n<pre><code># Quick test with curl:\ncurl -X POST http://localhost:8001/api/rewrite \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"text\": \"Hello world\", \"mode\": \"formal\"}'</code></pre>\n\n<h3>Pasul 3: Compară LLM-ul Personalizat vs. API-ul Comercial</h3>\n\n<p>Trimite același prompt atât la LLM-ul tău personalizat cât și la un API comercial, apoi compară rezultatele una lângă alta. Aceasta te ajută să înțelegi unde excelează modelul tău și unde ai putea dori să folosești fallback-ul.</p>\n\n<h3>Pasul 4: Testează Cazuri Limită</h3>\n\n<ul>\n<li>Intrări goale &mdash; ar trebui să returneze un mesaj de eroare util</li>\n<li>Intrări foarte lungi &mdash; ar trebui să le gestioneze cu grație sau să le trunchieze</li>\n<li>Intrări în alte limbi &mdash; ar trebui să răspundă corespunzător</li>\n<li>Cereri malformate &mdash; ar trebui să returneze erori de validare corespunzătoare</li>\n<li>Serverul LLM oprit &mdash; ar trebui să treacă la API-ul comercial</li>\n</ul>\n\n<div class=\"warning-box\">\n<strong>Înainte de Lansare:</strong> Testează fiecare endpoint cu cel puțin 10 intrări diferite. Acordă atenție specială comportamentului de fallback &mdash; oprește deliberat serverul LLM și verifică dacă API-ul comercial preia fără probleme.\n</div>\n\n<h2>8. Personalizarea Comportamentului</h2>\n\n<p>Fiecare integrare poate fi personalizată în mai multe moduri:</p>\n\n<h3>Modificarea Promptului de Sistem</h3>\n\n<p>Promptul de sistem este mecanismul principal de control. Editează-l pentru a schimba modul în care LLM-ul se comportă pentru acel produs. Consultă Lecția 49 pentru tehnici de inginerie a prompturilor.</p>\n\n<h3>Ajustarea Temperaturii și Top-K</h3>\n\n<ul>\n<li><strong>PixelForge:</strong> Temperatură mai mare (0.8&ndash;1.0) pentru îmbunătățire creativă a prompturilor</li>\n<li><strong>ProseAI:</strong> Temperatură medie (0.5&ndash;0.7) pentru rescriere echilibrată</li>\n<li><strong>TruthLens:</strong> Temperatură scăzută (0.0&ndash;0.2) pentru analiză consistentă, factuală</li>\n<li><strong>DocMind:</strong> Temperatură scăzută (0.0&ndash;0.3) pentru Q&A precis pe documente</li>\n<li><strong>ClipCraft:</strong> Temperatură mediu-mare (0.7&ndash;0.9) pentru scenarii creative</li>\n</ul>\n\n<h3>Adaugarea de Endpoint-uri Personalizate</h3>\n\n<p>Codul generat este un punct de plecare. Poți adăuga endpoint-uri noi pentru a gestiona cazuri de utilizare suplimentare specifice produsului tău SaaS. De exemplu, adaugă un endpoint <code>/api/batch-rewrite</code> la ProseAI care procesează mai multe texte simultan.</p>\n\n<h3>Schimbarea Furnizorului de Fallback</h3>\n\n<p>Fallback-ul implicit este OpenAI, dar poți comuta la orice furnizor din Lecția 48 modificând funcția de fallback din codul generat.</p>\n\n<h2>Rezumat</h2>\n\n<p>Acum înțelegi cum să conectezi LLM-ul tău NeuralForge la toate cele cinci produse GenAI SaaS. Conceptele cheie sunt:</p>\n\n<ul>\n<li>Fiecare produs comunică cu LLM-ul tău printr-un simplu API HTTP</li>\n<li>Promptul de sistem determină cum se comportă LLM-ul tău pentru fiecare produs</li>\n<li>Generatorul de API NeuralForge creează cod de integrare gata de utilizare</li>\n<li>Fallback-ul la API-uri comerciale asigură fiabilitatea când LLM-ul tău personalizat nu este disponibil</li>\n<li>Testarea este esențială &mdash; testează fiecare endpoint cu mai multe intrări înainte de implementare</li>\n</ul>\n\n<p>În lecția următoare și ultima, vei implementa sistemul complet LLM pe internet, făcându-l accesibil de oriunde din lume.</p>\n\n</div>",
    "el": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\">Στάδιο 7: Πλατφόρμα NeuralForge</div>\n<h1>Μάθημα 50: Σύνδεση του LLM σας με τα Προϊόντα GenAI SaaS</h1>\n<p class=\"lecture-subtitle\">Ενσωμάτωση του Προσαρμοσμένου Γλωσσικού Μοντέλου σας σε Πέντε Πραγματικές Εφαρμογές AI</p>\n<div class=\"lecture-meta\">\n<span>~40 λεπτά ανάγνωση</span> | <span>Ενδιάμεσο&ndash;Προχωρημένο</span> | <span>Πρακτικός οδηγός ενσωμάτωσης</span>\n</div>\n</div>\n\n<h2>1. Τα Πέντε Προϊόντα GenAI SaaS που Δημιουργήσατε</h2>\n\n<p>Καθ' όλη τη διάρκεια αυτού του μαθήματος, δημιουργήσατε πέντε προϊόντα GenAI SaaS. Το καθένα λύνει ένα πραγματικό πρόβλημα χρησιμοποιώντας AI. Τώρα είναι η ώρα να τα συνδέσετε όλα με <strong>το δικό σας LLM</strong> &mdash; το γλωσσικό μοντέλο που εκπαιδεύσατε και βελτιστοποιήσατε στο NeuralForge.</p>\n\n<p>Ορίστε μια σύνοψη κάθε προϊόντος και πώς το LLM σας το ενισχύει:</p>\n\n<table>\n<thead><tr><th>Προϊόν</th><th>Σκοπός</th><th>Πώς Βοηθάει το LLM σας</th></tr></thead>\n<tbody>\n<tr><td><strong>PixelForge</strong></td><td>Δημιουργία Εικόνων AI</td><td>Βελτιώνει prompts εικόνων, δημιουργεί περιγραφές, προτείνει στυλ</td></tr>\n<tr><td><strong>ClipCraft</strong></td><td>Δημιουργία Βίντεο AI</td><td>Γράφει σενάρια βίντεο, περιγραφές σκηνών, storyboards</td></tr>\n<tr><td><strong>ProseAI</strong></td><td>Επαναγραφή Κειμένου</td><td>Τροφοδοτεί τη μετατροπή κειμένου σε πολλαπλά στυλ και τρόπους</td></tr>\n<tr><td><strong>TruthLens</strong></td><td>Ανίχνευση Λογοκλοπής/AI</td><td>Αναλύει μοτίβα κειμένου, συνέπεια γραφής, μετρικές λεξιλογίου</td></tr>\n<tr><td><strong>DocMind</strong></td><td>Νοημοσύνη Εγγράφων</td><td>Δομεί έγγραφα, απαντάει ερωτήσεις, εξάγει βασικές πληροφορίες</td></tr>\n</tbody>\n</table>\n\n<div class=\"info-box\">\n<strong>Η Μεγάλη Εικόνα:</strong> Το LLM σας είναι ο κινητήρας. Κάθε προϊόν SaaS είναι ένα εξειδικευμένο όχημα χτισμένο γύρω από αυτόν τον κινητήρα. Το system prompt (από το Μάθημα 49) καθορίζει πώς συμπεριφέρεται ο κινητήρας σε κάθε όχημα.\n</div>\n\n<h2>2. Πώς Λειτουργεί η Σύνδεση</h2>\n\n<p>Η αρχιτεκτονική είναι απλή. Κάθε προϊόν SaaS επικοινωνεί με το LLM σας μέσω κλήσεων HTTP API. Ορίστε η ροή:</p>\n\n<h3>Η Ροή Αιτήματος</h3>\n\n<ol>\n<li><strong>Ο χρήστης</strong> ανοίγει το προϊόν SaaS σας (π.χ., ProseAI) στο πρόγραμμα περιήγησης</li>\n<li><strong>Το Frontend SaaS</strong> στέλνει το αίτημα του χρήστη στο backend API του SaaS</li>\n<li><strong>Το API SaaS</strong> κατασκευάζει ένα prompt χρησιμοποιώντας το system prompt του προϊόντος + την είσοδο του χρήστη</li>\n<li><strong>Το API SaaS</strong> στέλνει το prompt στο endpoint API του LLM σας</li>\n<li><strong>Το LLM σας</strong> επεξεργάζεται το prompt και επιστρέφει απάντηση</li>\n<li><strong>Το API SaaS</strong> μορφοποιεί την απάντηση και τη στέλνει πίσω στο frontend</li>\n<li><strong>Ο χρήστης</strong> βλέπει το αποτέλεσμα στη διεπαφή του προϊόντος SaaS</li>\n</ol>\n\n<h3>Διάγραμμα Αρχιτεκτονικής</h3>\n\n<pre><code>User Browser\n    |\n    v\nSaaS Frontend (React/Next.js on Vercel)\n    |\n    v\nSaaS API (FastAPI / Next.js API routes)\n    |         \\\n    v          v\nYour LLM    Commercial API (fallback)\n(NeuralForge  (OpenAI / Claude /\n localhost    Gemini as backup)\n or Vercel)\n    |\n    v\nResponse back to user</code></pre>\n\n<h3>Πού Τρέχει το LLM σας</h3>\n\n<p>Το LLM σας μπορεί να τρέχει σε δύο μέρη:</p>\n\n<ul>\n<li><strong>Τοπικά:</strong> <code>http://localhost:8000</code> &mdash; για ανάπτυξη και δοκιμές. Ξεκινήστε τον server NeuralForge και τα προϊόντα SaaS σας μπορούν να τον προσεγγίσουν στη μηχανή σας.</li>\n<li><strong>Αναπτυγμένο:</strong> <code>https://your-name-neuralforge.vercel.app</code> &mdash; για παραγωγή. Μετά την ανάπτυξη στο Vercel (καλύπτεται στο Μάθημα 51), τα προϊόντα SaaS σας μπορούν να τον προσεγγίσουν από οπουδήποτε.</li>\n</ul>\n\n<h3>Η Σύνδεση Είναι Απλά ένα URL</h3>\n\n<p>Κάθε προϊόν SaaS χρειάζεται ακριβώς μία πληροφορία: <strong>το URL του LLM σας</strong>. Αυτό αποθηκεύεται ως μεταβλητή περιβάλλοντος:</p>\n\n<pre><code># In your SaaS product's .env file:\nLLM_URL=http://localhost:8000\n\n# Or, for production:\nLLM_URL=https://your-name-neuralforge.vercel.app</code></pre>\n\n<p>Αυτό είναι. Αλλάξτε το URL και το προϊόν SaaS σας εναλλάσσεται μεταξύ του τοπικού LLM και της αναπτυγμένης έκδοσης.</p>\n\n<h2>3. Τι Παράγει ο Γεννήτρια API του NeuralForge</h2>\n\n<p>Η πλατφόρμα NeuralForge περιλαμβάνει μια <strong>Γεννήτρια API</strong> που δημιουργεί κώδικα ενσωμάτωσης έτοιμο προς χρήση για κάθε προϊόν SaaS. Όταν επιλέξετε ένα προϊόν και κάνετε κλικ στο &ldquo;Generate,&rdquo; παράγει:</p>\n\n<ul>\n<li>Μια πλήρη εφαρμογή FastAPI με όλα τα απαραίτητα endpoints</li>\n<li>Το system prompt για αυτό το προϊόν (από το Μάθημα 49)</li>\n<li>Μοντέλα αιτήματος/απάντησης με κατάλληλη επικύρωση</li>\n<li>Διαχείριση σφαλμάτων και λογική fallback</li>\n<li>Ρύθμιση CORS για επικοινωνία με το frontend</li>\n<li>Endpoints ελέγχου υγείας</li>\n</ul>\n\n<h3>Πώς να Χρησιμοποιήσετε τη Γεννήτρια API</h3>\n\n<ol>\n<li>Ανοίξτε το NeuralForge στο πρόγραμμα περιήγησής σας</li>\n<li>Πλοηγηθείτε στην ενότητα &ldquo;API Generator&rdquo;</li>\n<li>Επιλέξτε το προϊόν SaaS που θέλετε να συνδέσετε (π.χ., ProseAI)</li>\n<li>Κάντε κλικ στο &ldquo;Generate API Code&rdquo;</li>\n<li>Ο παραγόμενος κώδικας εμφανίζεται στον επεξεργαστή κώδικα</li>\n<li>Κάντε κλικ στο &ldquo;Copy&rdquo; για να τον αντιγράψετε στο πρόχειρο</li>\n<li>Επικολλήστε σε ένα νέο αρχείο Python στο έργο SaaS σας</li>\n</ol>\n\n<h3>Τι Δημιουργείται</h3>\n\n<p>Για κάθε προϊόν, η γεννήτρια δημιουργεί endpoints ειδικά για τη λειτουργικότητα αυτού του προϊόντος. Ο παραγόμενος κώδικας περιλαμβάνει:</p>\n\n<ul>\n<li>Δηλώσεις εισαγωγής και εξαρτήσεις</li>\n<li>Αρχικοποίηση εφαρμογής FastAPI με middleware CORS</li>\n<li>Μοντέλα Pydantic για επικύρωση αιτήματος/απάντησης</li>\n<li>Το system prompt ειδικό για το προϊόν</li>\n<li>Κύρια endpoints για τη βασική λειτουργικότητα του προϊόντος</li>\n<li>Fallback σε εμπορικά APIs όταν το προσαρμοσμένο LLM δεν είναι διαθέσιμο</li>\n<li>Διαχείριση σφαλμάτων με ουσιαστικά μηνύματα σφάλματος</li>\n</ul>\n\n<h2>4. Οδηγός Ενσωμάτωσης Ανά Προϊόν</h2>\n\n<p>Ας δούμε κάθε προϊόν και ας κατανοήσουμε ακριβώς τι endpoints δημιουργούνται, τι κάνει το system prompt και πώς να προσαρμόσουμε τη συμπεριφορά.</p>\n\n<h3>PixelForge (Δημιουργία Εικόνων)</h3>\n\n<p><strong>Παραγόμενα endpoints:</strong></p>\n<ul>\n<li><code>POST /api/enhance-prompt</code> &mdash; Δέχεται μια απλή περιγραφή εικόνας, επιστρέφει ένα βελτιωμένο prompt βελτιστοποιημένο για δημιουργία εικόνων</li>\n<li><code>POST /api/describe-image</code> &mdash; Δέχεται ένα URL εικόνας, επιστρέφει μια κειμενική περιγραφή της εικόνας</li>\n<li><code>POST /api/suggest-styles</code> &mdash; Δέχεται μια ιδέα, επιστρέφει προτεινόμενα καλλιτεχνικά στυλ και παραλλαγές</li>\n</ul>\n\n<p><strong>Ρόλος system prompt:</strong> Το LLM λειτουργεί ως μηχανικός prompt εικόνων. Παίρνει απλές περιγραφές όπως &ldquo;μια γάτα σε μια στέγη&rdquo; και τις μετατρέπει σε λεπτομερή prompts βελτιστοποιημένα για δημιουργία εικόνων AI με φωτισμό, στυλ, σύνθεση και τροποποιητές ποιότητας.</p>\n\n<p><strong>Παράδειγμα αιτήματος και απάντησης:</strong></p>\n\n<pre><code># Request:\nPOST /api/enhance-prompt\n{\n  \"prompt\": \"a cat sitting on a roof at sunset\",\n  \"style\": \"photograph\"\n}\n\n# Response:\n{\n  \"enhanced_prompt\": \"A domestic tabby cat sitting gracefully\n    on weathered terracotta roof tiles, golden hour sunset\n    lighting casting long warm shadows, shot from below\n    at a slight angle, shallow depth of field with bokeh\n    city lights in background, professional photography,\n    8K resolution, warm color palette with orange and\n    amber tones, National Geographic style\",\n  \"tokens_used\": 89\n}</code></pre>\n\n<h3>ClipCraft (Δημιουργία Βίντεο)</h3>\n\n<p><strong>Παραγόμενα endpoints:</strong></p>\n<ul>\n<li><code>POST /api/generate-script</code> &mdash; Δημιουργεί ένα δομημένο σενάριο βίντεο με περιγραφές σκηνών</li>\n<li><code>POST /api/describe-scene</code> &mdash; Δημιουργεί λεπτομερείς περιγραφές σκηνών για δημιουργία βίντεο</li>\n</ul>\n\n<p><strong>Ρόλος system prompt:</strong> Το LLM λειτουργεί ως σεναριογράφος βίντεο και σκηνοθέτης. Αναλύει ιδέες βίντεο σε μεμονωμένες σκηνές με κινήσεις κάμερας, χρονισμό και ηχητικές ενδείξεις.</p>\n\n<h3>ProseAI (Επαναγραφή Κειμένου)</h3>\n\n<p><strong>Παραγόμενα endpoints:</strong></p>\n<ul>\n<li><code>POST /api/rewrite</code> &mdash; Επαναγράφει κείμενο στον καθορισμένο τρόπο (formal, casual, concise, expand, simplify)</li>\n<li><code>POST /api/analyze-style</code> &mdash; Αναλύει το στυλ γραφής του κειμένου εισόδου</li>\n<li><code>POST /api/suggest-improvements</code> &mdash; Προτείνει συγκεκριμένες βελτιώσεις στο κείμενο</li>\n</ul>\n\n<p><strong>Ρόλος system prompt:</strong> Το LLM λειτουργεί ως επαγγελματίας επεξεργαστής κειμένου. Κάθε τρόπος επαναγραφής μεταμορφώνει το κείμενο διαφορετικά διατηρώντας παράλληλα όλο το πραγματικό περιεχόμενο.</p>\n\n<p><strong>Παράδειγμα αιτήματος και απάντησης:</strong></p>\n\n<pre><code># Request:\nPOST /api/rewrite\n{\n  \"text\": \"The quarterly financial results have demonstrated\n    a significant improvement in our revenue streams,\n    which can be attributed to the strategic initiatives\n    implemented during the previous fiscal period.\",\n  \"mode\": \"casual\"\n}\n\n# Response:\n{\n  \"rewritten\": \"Great news — we made a lot more money this\n    quarter! Those changes we made last quarter really\n    paid off.\",\n  \"original_word_count\": 28,\n  \"rewritten_word_count\": 20,\n  \"mode\": \"casual\"\n}</code></pre>\n\n<h3>TruthLens (Ανίχνευση Λογοκλοπής/AI)</h3>\n\n<p><strong>Παραγόμενα endpoints:</strong></p>\n<ul>\n<li><code>POST /api/analyze</code> &mdash; Εκτελεί ολοκληρωμένη ανάλυση κειμένου με ανίχνευση μοτίβων</li>\n<li><code>POST /api/compare</code> &mdash; Συγκρίνει δύο κείμενα για ομοιότητα</li>\n</ul>\n\n<p><strong>Ρόλος system prompt:</strong> Το LLM λειτουργεί ως ειδικός ανάλυσης κειμένου. Αξιολογεί τη συνέπεια γραφής, στατιστικά μοτίβα, δομική οργάνωση και πολυπλοκότητα λεξιλογίου, επιστρέφοντας δομημένα αποτελέσματα JSON.</p>\n\n<h3>DocMind (Νοημοσύνη Εγγράφων)</h3>\n\n<p><strong>Παραγόμενα endpoints:</strong></p>\n<ul>\n<li><code>POST /api/ask</code> &mdash; Απαντάει ερωτήσεις για ένα παρεχόμενο έγγραφο</li>\n<li><code>POST /api/summarize</code> &mdash; Συνοψίζει ένα έγγραφο σε διάφορα επίπεδα λεπτομέρειας</li>\n<li><code>POST /api/extract</code> &mdash; Εξάγει συγκεκριμένα σημεία δεδομένων από ένα έγγραφο</li>\n</ul>\n\n<p><strong>Ρόλος system prompt:</strong> Το LLM λειτουργεί ως βοηθός ανάλυσης εγγράφων. Απαντάει αυστηρά βάσει του περιεχομένου του παρεχόμενου εγγράφου, αναφέρει σχετικά αποσπάσματα και δεν κατασκευάζει ποτέ πληροφορίες.</p>\n\n<p><strong>Παράδειγμα αιτήματος και απάντησης:</strong></p>\n\n<pre><code># Request:\nPOST /api/ask\n{\n  \"document\": \"Annual Report 2024: Total revenue was $4.2M,\n    up 35% from $3.1M in 2023. Operating expenses\n    decreased by 12% to $2.8M. Net profit margin\n    improved from 8% to 15%.\",\n  \"question\": \"What was the revenue growth?\"\n}\n\n# Response:\n{\n  \"answer\": \"Revenue grew by 35%, from $3.1M in 2023 to\n    $4.2M in 2024.\",\n  \"confidence\": \"high\",\n  \"source_quote\": \"Total revenue was $4.2M, up 35% from\n    $3.1M in 2023.\"\n}</code></pre>\n\n<h2>5. Ανάπτυξη του API Ενσωμάτωσης</h2>\n\n<p>Ο παραγόμενος κώδικας API τρέχει ως ξεχωριστή υπηρεσία δίπλα στο LLM σας. Ορίστε πώς να τον αναπτύξετε:</p>\n\n<h3>Τοπική Ανάπτυξη</h3>\n\n<pre><code># 1. Save the generated code as main.py\n# 2. Install dependencies:\npip install fastapi uvicorn httpx openai\n\n# 3. Set environment variables:\nexport LLM_URL=http://localhost:8000\nexport OPENAI_API_KEY=sk-...  # Optional fallback\n\n# 4. Run the integration API:\nuvicorn main:app --port 8001\n\n# Your integration API is now at http://localhost:8001\n# Your LLM is at http://localhost:8000\n# Your SaaS frontend calls http://localhost:8001</code></pre>\n\n<h3>Ανάπτυξη Παραγωγής</h3>\n\n<p>Για παραγωγή, αναπτύξτε το API ενσωμάτωσης στο Vercel μαζί με το frontend SaaS σας, ή τρέξτε το ως ξεχωριστό έργο Vercel. Ορίστε τη μεταβλητή περιβάλλοντος <code>LLM_URL</code> στο αναπτυγμένο URL του NeuralForge.</p>\n\n<h2>6. Fallback σε Εμπορικά APIs</h2>\n\n<p>Το μοντέλο Mini GPT-2 σας είναι εντυπωσιακό για αυτό που είναι &mdash; ένα γλωσσικό μοντέλο χτισμένο από το μηδέν. Αλλά είναι ένα μικρό μοντέλο (περίπου 85M παράμετροι) και μπορεί να μην παράγει κείμενο ποιότητας παραγωγής για κάθε περίπτωση χρήσης. Εδώ το <strong>fallback σε εμπορικά APIs</strong> γίνεται ουσιαστικό.</p>\n\n<h3>Πότε να Χρησιμοποιήσετε Fallback</h3>\n\n<ul>\n<li>Ο server του LLM σας είναι εκτός λειτουργίας ή απρόσιτος</li>\n<li>Η ποιότητα απάντησης από το μοντέλο σας είναι κάτω από ένα κατώφλι</li>\n<li>Η εργασία απαιτεί δυνατότητες για τις οποίες το μοντέλο σας δεν εκπαιδεύτηκε (π.χ., σύνθετη συλλογιστική)</li>\n<li>Χρειάζεστε την ποιότητα ενός εμπορικού μοντέλου για χρήστες παραγωγής</li>\n</ul>\n\n<h3>Πώς Λειτουργεί το Fallback</h3>\n\n<p>Ο παραγόμενος κώδικας διαχειρίζεται αυτόματα το fallback. Ορίστε η λογική:</p>\n\n<ol>\n<li>Προσπαθήστε πρώτα να καλέσετε το προσαρμοσμένο LLM σας</li>\n<li>Αν η κλήση αποτύχει (timeout, σφάλμα ή server απρόσιτος), μεταβείτε σε εμπορικό API</li>\n<li>Το εμπορικό API χρησιμοποιεί ακριβώς το ίδιο system prompt, οπότε η συμπεριφορά είναι συνεπής</li>\n<li>Η απάντηση υποδεικνύει ποιο μοντέλο χρησιμοποιήθηκε πραγματικά</li>\n</ol>\n\n<pre><code># The fallback logic in the generated code:\ntry:\n    # Try your custom LLM first\n    response = await call_custom_llm(prompt)\n    return {\"response\": response, \"model\": \"custom-llm\"}\nexcept Exception:\n    # Fall back to commercial API\n    response = await call_openai(prompt)\n    return {\"response\": response, \"model\": \"gpt-4o-mini\"}</code></pre>\n\n<h3>Ρύθμιση Fallback</h3>\n\n<p>Για να ενεργοποιήσετε το fallback σε εμπορικά APIs, ορίστε το κλειδί API ως μεταβλητή περιβάλλοντος:</p>\n\n<pre><code># In your .env file:\nLLM_URL=http://localhost:8000\nOPENAI_API_KEY=sk-...           # OpenAI fallback\nANTHROPIC_API_KEY=sk-ant-...    # Or Anthropic fallback\nGOOGLE_API_KEY=AI...            # Or Google fallback</code></pre>\n\n<p>Ο παραγόμενος κώδικας ελέγχει αυτά τα κλειδιά με τη σειρά και χρησιμοποιεί το πρώτο διαθέσιμο ως πάροχο fallback.</p>\n\n<div class=\"info-box\">\n<strong>Συμβουλή Εξοικονόμησης Κόστους:</strong> Χρησιμοποιήστε το Google Gemini Flash ως πάροχο fallback. Είναι η φθηνότερη εμπορική επιλογή στα $0.10/$0.40 ανά 1M tokens, και το δωρεάν επίπεδο (15 αιτήματα/λεπτό) είναι συχνά επαρκές για σενάρια fallback όπου το προσαρμοσμένο LLM σας χειρίζεται το μεγαλύτερο μέρος της κίνησης.\n</div>\n\n<h2>7. Δοκιμή των Ενσωματώσεών σας</h2>\n\n<p>Πριν την ανάπτυξη στην παραγωγή, δοκιμάστε διεξοδικά κάθε ενσωμάτωση. Ορίστε μια συστηματική προσέγγιση:</p>\n\n<h3>Βήμα 1: Δοκιμή με το NeuralForge Playground</h3>\n\n<p>Η πλατφόρμα NeuralForge περιλαμβάνει ένα Playground όπου μπορείτε να δοκιμάσετε prompts διαδραστικά:</p>\n\n<ol>\n<li>Ανοίξτε το Playground στο NeuralForge</li>\n<li>Επικολλήστε το system prompt για το προϊόν που θέλετε να δοκιμάσετε</li>\n<li>Πληκτρολογήστε δείγματα εισόδων χρήστη</li>\n<li>Ελέγξτε ότι οι απαντήσεις είναι κατάλληλες για το προϊόν</li>\n<li>Ρυθμίστε το system prompt αν χρειάζεται</li>\n</ol>\n\n<h3>Βήμα 2: Δοκιμή του API Ενσωμάτωσης Απευθείας</h3>\n\n<p>Χρησιμοποιήστε το σενάριο δοκιμής (μπλοκ κώδικα 2 παρακάτω) για να καλέσετε κάθε endpoint και να επαληθεύσετε τις απαντήσεις:</p>\n\n<pre><code># Quick test with curl:\ncurl -X POST http://localhost:8001/api/rewrite \\\n  -H \"Content-Type: application/json\" \\\n  -d '{\"text\": \"Hello world\", \"mode\": \"formal\"}'</code></pre>\n\n<h3>Βήμα 3: Σύγκριση Προσαρμοσμένου LLM vs. Εμπορικού API</h3>\n\n<p>Στείλτε το ίδιο prompt τόσο στο προσαρμοσμένο LLM σας όσο και σε ένα εμπορικό API, και μετά συγκρίνετε τα αποτελέσματα δίπλα-δίπλα. Αυτό σας βοηθάει να κατανοήσετε πού υπερέχει το μοντέλο σας και πού μπορεί να θέλετε να χρησιμοποιήσετε fallback.</p>\n\n<h3>Βήμα 4: Δοκιμή Ακραίων Περιπτώσεων</h3>\n\n<ul>\n<li>Κενές εισόδοι &mdash; πρέπει να επιστρέφουν ένα χρήσιμο μήνυμα σφάλματος</li>\n<li>Πολύ μεγάλες εισόδοι &mdash; πρέπει να τις χειρίζονται ομαλά ή να τις περικόπτουν</li>\n<li>Εισόδοι σε άλλες γλώσσες &mdash; πρέπει να απαντούν κατάλληλα</li>\n<li>Κακοσχηματισμένα αιτήματα &mdash; πρέπει να επιστρέφουν κατάλληλα σφάλματα επικύρωσης</li>\n<li>Server LLM εκτός λειτουργίας &mdash; πρέπει να μεταβαίνει στο εμπορικό API</li>\n</ul>\n\n<div class=\"warning-box\">\n<strong>Πριν Ξεκινήσετε:</strong> Δοκιμάστε κάθε endpoint με τουλάχιστον 10 διαφορετικές εισόδους. Δώστε ιδιαίτερη προσοχή στη συμπεριφορά fallback &mdash; σταματήστε σκόπιμα τον server LLM σας και επαληθεύστε ότι το εμπορικό API αναλαμβάνει απρόσκοπτα.\n</div>\n\n<h2>8. Προσαρμογή της Συμπεριφοράς</h2>\n\n<p>Κάθε ενσωμάτωση μπορεί να προσαρμοστεί με πολλούς τρόπους:</p>\n\n<h3>Τροποποίηση του System Prompt</h3>\n\n<p>Το system prompt είναι ο κύριος μηχανισμός ελέγχου. Επεξεργαστείτε το για να αλλάξετε πώς συμπεριφέρεται το LLM για αυτό το προϊόν. Ανατρέξτε στο Μάθημα 49 για τεχνικές μηχανικής προτροπών.</p>\n\n<h3>Ρύθμιση Θερμοκρασίας και Top-K</h3>\n\n<ul>\n<li><strong>PixelForge:</strong> Υψηλότερη θερμοκρασία (0.8&ndash;1.0) για δημιουργική βελτίωση prompts</li>\n<li><strong>ProseAI:</strong> Μέση θερμοκρασία (0.5&ndash;0.7) για ισορροπημένη επαναγραφή</li>\n<li><strong>TruthLens:</strong> Χαμηλή θερμοκρασία (0.0&ndash;0.2) για συνεπή, πραγματική ανάλυση</li>\n<li><strong>DocMind:</strong> Χαμηλή θερμοκρασία (0.0&ndash;0.3) για ακριβείς ερωτήσεις-απαντήσεις εγγράφων</li>\n<li><strong>ClipCraft:</strong> Μέση-υψηλή θερμοκρασία (0.7&ndash;0.9) για δημιουργικά σενάρια</li>\n</ul>\n\n<h3>Προσθήκη Προσαρμοσμένων Endpoints</h3>\n\n<p>Ο παραγόμενος κώδικας είναι ένα σημείο εκκίνησης. Μπορείτε να προσθέσετε νέα endpoints για να χειριστείτε πρόσθετες περιπτώσεις χρήσης ειδικές για το προϊόν SaaS σας. Για παράδειγμα, προσθέστε ένα endpoint <code>/api/batch-rewrite</code> στο ProseAI που επεξεργάζεται πολλαπλά κείμενα ταυτόχρονα.</p>\n\n<h3>Αλλαγή Παρόχου Fallback</h3>\n\n<p>Το προεπιλεγμένο fallback είναι το OpenAI, αλλά μπορείτε να αλλάξετε σε οποιονδήποτε πάροχο από το Μάθημα 48 τροποποιώντας τη συνάρτηση fallback στον παραγόμενο κώδικα.</p>\n\n<h2>Σύνοψη</h2>\n\n<p>Τώρα κατανοείτε πώς να συνδέσετε το NeuralForge LLM σας με όλα τα πέντε προϊόντα GenAI SaaS. Οι βασικές έννοιες είναι:</p>\n\n<ul>\n<li>Κάθε προϊόν επικοινωνεί με το LLM σας μέσω ενός απλού HTTP API</li>\n<li>Το system prompt καθορίζει πώς συμπεριφέρεται το LLM σας για κάθε προϊόν</li>\n<li>Η Γεννήτρια API του NeuralForge δημιουργεί κώδικα ενσωμάτωσης έτοιμο προς χρήση</li>\n<li>Το fallback σε εμπορικά APIs εξασφαλίζει αξιοπιστία όταν το προσαρμοσμένο LLM σας δεν είναι διαθέσιμο</li>\n<li>Η δοκιμή είναι ουσιαστική &mdash; δοκιμάστε κάθε endpoint με πολλαπλές εισόδους πριν την ανάπτυξη</li>\n</ul>\n\n<p>Στο επόμενο και τελευταίο μάθημα, θα αναπτύξετε το πλήρες σύστημα LLM σας στο διαδίκτυο, κάνοντάς το προσβάσιμο από οπουδήποτε στον κόσμο.</p>\n\n</div>"
  },
  "codeBlocks": [
    {
      "id": "block-0",
      "title": "Python",
      "code": "\"\"\"\nProseAI Integration API — Generated by NeuralForge\n\nThis FastAPI application connects ProseAI to your custom LLM.\nIt provides text rewriting, style analysis, and improvement\nsuggestion endpoints with automatic fallback to commercial APIs.\n\"\"\"\n\nimport os\nimport httpx\nfrom enum import Enum\nfrom fastapi import FastAPI, HTTPException\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\n\n\n# ── Configuration ────────────────────────────────────────────────\nLLM_URL = os.getenv(\"LLM_URL\", \"http://localhost:8000\")\nOPENAI_API_KEY = os.getenv(\"OPENAI_API_KEY\", \"\")\nTIMEOUT = 30.0  # seconds\n\n\n# ── System Prompt ────────────────────────────────────────────────\nSYSTEM_PROMPT = \"\"\"You are a professional text editor and rewriter\nwith expertise in multiple writing styles.\n\nThe user will provide text and a rewriting mode. Apply the\nrequested transformation precisely.\n\nAvailable modes:\n- \"formal\": Professional, business-appropriate language.\n  Remove contractions, use precise vocabulary, maintain\n  objective tone.\n- \"casual\": Friendly, conversational. Use contractions,\n  simpler words, and a warm tone.\n- \"concise\": Cut word count by 40% while keeping ALL key\n  information. Remove redundancy and filler.\n- \"expand\": Double the content by adding detail, examples,\n  and explanations.\n- \"simplify\": Rewrite for a 12-year-old reading level.\n  Short sentences, common words, concrete examples.\n\nRules:\n- Preserve ALL factual content\n- Maintain the original meaning\n- Do NOT add information not in the original\n- Return ONLY the rewritten text, no commentary\n\"\"\"\n\n\n# ── Models ───────────────────────────────────────────────────────\nclass RewriteMode(str, Enum):\n    formal = \"formal\"\n    casual = \"casual\"\n    concise = \"concise\"\n    expand = \"expand\"\n    simplify = \"simplify\"\n\n\nclass RewriteRequest(BaseModel):\n    text: str = Field(..., min_length=1, max_length=50000,\n                      description=\"The text to rewrite\")\n    mode: RewriteMode = Field(...,\n                              description=\"Rewriting mode\")\n    temperature: float = Field(default=0.6, ge=0.0, le=2.0)\n\n\nclass RewriteResponse(BaseModel):\n    rewritten: str\n    original_word_count: int\n    rewritten_word_count: int\n    mode: str\n    model_used: str\n\n\nclass StyleAnalysisRequest(BaseModel):\n    text: str = Field(..., min_length=1, max_length=50000)\n\n\nclass HealthResponse(BaseModel):\n    status: str\n    llm_url: str\n    llm_reachable: bool\n\n\n# ── FastAPI App ──────────────────────────────────────────────────\napp = FastAPI(\n    title=\"ProseAI Integration API\",\n    description=\"Connects ProseAI to your NeuralForge LLM\",\n    version=\"1.0.0\",\n)\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],  # Restrict in production\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n\n# ── LLM Communication ───────────────────────────────────────────\nasync def call_custom_llm(user_prompt: str,\n                          temperature: float = 0.6) -> str:\n    \"\"\"Call your custom NeuralForge LLM.\"\"\"\n    async with httpx.AsyncClient(timeout=TIMEOUT) as client:\n        response = await client.post(\n            f\"{LLM_URL}/api/chat\",\n            json={\n                \"messages\": [\n                    {\"role\": \"system\",\n                     \"content\": SYSTEM_PROMPT},\n                    {\"role\": \"user\",\n                     \"content\": user_prompt},\n                ],\n                \"temperature\": temperature,\n                \"max_tokens\": 2048,\n            },\n        )\n        response.raise_for_status()\n        data = response.json()\n        return data.get(\"response\", data.get(\"content\", \"\"))\n\n\nasync def call_openai_fallback(user_prompt: str,\n                               temperature: float = 0.6) -> str:\n    \"\"\"Fallback to OpenAI when custom LLM is unavailable.\"\"\"\n    if not OPENAI_API_KEY:\n        raise HTTPException(\n            status_code=503,\n            detail=\"Custom LLM unavailable and no fallback \"\n                   \"API key configured.\")\n\n    from openai import AsyncOpenAI\n    client = AsyncOpenAI(api_key=OPENAI_API_KEY)\n    response = await client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[\n            {\"role\": \"system\", \"content\": SYSTEM_PROMPT},\n            {\"role\": \"user\", \"content\": user_prompt},\n        ],\n        temperature=temperature,\n    )\n    return response.choices[0].message.content\n\n\nasync def generate_text(user_prompt: str,\n                        temperature: float = 0.6\n                        ) -> tuple[str, str]:\n    \"\"\"Try custom LLM first, fall back to OpenAI.\n\n    Returns (text, model_used) tuple.\n    \"\"\"\n    try:\n        text = await call_custom_llm(user_prompt, temperature)\n        return text, \"custom-neuralforge-llm\"\n    except Exception as e:\n        print(f\"Custom LLM failed: {e}. Falling back...\")\n        text = await call_openai_fallback(\n            user_prompt, temperature)\n        return text, \"gpt-4o-mini (fallback)\"\n\n\n# ── Endpoints ────────────────────────────────────────────────────\n@app.post(\"/api/rewrite\", response_model=RewriteResponse)\nasync def rewrite_text(request: RewriteRequest):\n    \"\"\"Rewrite text in the specified mode.\"\"\"\n    user_prompt = (\n        f\"Rewrite the following text in {request.mode.value} \"\n        f\"mode:\\n\\n{request.text}\")\n\n    rewritten, model = await generate_text(\n        user_prompt, request.temperature)\n\n    return RewriteResponse(\n        rewritten=rewritten,\n        original_word_count=len(request.text.split()),\n        rewritten_word_count=len(rewritten.split()),\n        mode=request.mode.value,\n        model_used=model,\n    )\n\n\n@app.post(\"/api/analyze-style\")\nasync def analyze_style(request: StyleAnalysisRequest):\n    \"\"\"Analyze the writing style of the input text.\"\"\"\n    user_prompt = (\n        \"Analyze the writing style of this text. \"\n        \"Identify: tone, formality level, vocabulary \"\n        \"complexity, sentence structure patterns, and \"\n        \"target audience. Respond in JSON format.\\n\\n\"\n        f\"{request.text}\")\n\n    result, model = await generate_text(user_prompt, 0.3)\n    return {\"analysis\": result, \"model_used\": model}\n\n\n@app.post(\"/api/suggest-improvements\")\nasync def suggest_improvements(request: StyleAnalysisRequest):\n    \"\"\"Suggest specific improvements to the text.\"\"\"\n    user_prompt = (\n        \"Suggest 5 specific improvements for this text. \"\n        \"For each, explain what to change and why. \"\n        \"Format as a numbered list.\\n\\n\"\n        f\"{request.text}\")\n\n    result, model = await generate_text(user_prompt, 0.5)\n    return {\"suggestions\": result, \"model_used\": model}\n\n\n@app.get(\"/health\", response_model=HealthResponse)\nasync def health_check():\n    \"\"\"Check if the service and LLM are healthy.\"\"\"\n    llm_ok = False\n    try:\n        async with httpx.AsyncClient(timeout=5.0) as client:\n            r = await client.get(f\"{LLM_URL}/health\")\n            llm_ok = r.status_code == 200\n    except Exception:\n        pass\n\n    return HealthResponse(\n        status=\"healthy\",\n        llm_url=LLM_URL,\n        llm_reachable=llm_ok,\n    )\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8001)\n",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-1",
      "title": "Python",
      "code": "\"\"\"\nDocMind Integration API — Generated by NeuralForge\n\nThis FastAPI application connects DocMind to your custom LLM.\nIt provides document Q&A, summarization, and data extraction\nendpoints with automatic fallback to commercial APIs.\n\"\"\"\n\nimport os\nimport httpx\nfrom fastapi import FastAPI, HTTPException\nfrom fastapi.middleware.cors import CORSMiddleware\nfrom pydantic import BaseModel, Field\nfrom typing import Optional, List\n\n\n# ── Configuration ────────────────────────────────────────────────\nLLM_URL = os.getenv(\"LLM_URL\", \"http://localhost:8000\")\nOPENAI_API_KEY = os.getenv(\"OPENAI_API_KEY\", \"\")\nTIMEOUT = 60.0  # Longer timeout for document processing\n\n\n# ── System Prompt ────────────────────────────────────────────────\nSYSTEM_PROMPT = \"\"\"You are a document analysis and\nquestion-answering assistant.\n\nStrict Rules:\n1. ONLY answer based on the provided document content\n2. If the answer is NOT in the document, respond:\n   \"This information is not available in the provided document.\"\n3. NEVER fabricate or hallucinate information\n4. Always quote relevant passages\n5. If a question is ambiguous, ask for clarification\n6. For numerical data, double-check calculations\n7. Distinguish facts from your interpretations\n\nResponse Format:\n- Direct answer first\n- Supporting evidence (quotes from the document)\n- Confidence level: High / Medium / Low\n\"\"\"\n\n\n# ── Models ───────────────────────────────────────────────────────\nclass AskRequest(BaseModel):\n    document: str = Field(\n        ..., min_length=1, max_length=200000,\n        description=\"The document text to analyze\")\n    question: str = Field(\n        ..., min_length=1, max_length=2000,\n        description=\"Question about the document\")\n    temperature: float = Field(default=0.1, ge=0.0, le=1.0)\n\n\nclass AskResponse(BaseModel):\n    answer: str\n    model_used: str\n\n\nclass SummarizeRequest(BaseModel):\n    document: str = Field(\n        ..., min_length=1, max_length=200000)\n    detail_level: str = Field(\n        default=\"medium\",\n        description=\"brief, medium, or detailed\")\n\n\nclass ExtractRequest(BaseModel):\n    document: str = Field(\n        ..., min_length=1, max_length=200000)\n    fields: List[str] = Field(\n        ..., min_length=1,\n        description=\"Data fields to extract\")\n\n\n# ── FastAPI App ──────────────────────────────────────────────────\napp = FastAPI(\n    title=\"DocMind Integration API\",\n    description=\"Connects DocMind to your NeuralForge LLM\",\n    version=\"1.0.0\",\n)\n\napp.add_middleware(\n    CORSMiddleware,\n    allow_origins=[\"*\"],\n    allow_credentials=True,\n    allow_methods=[\"*\"],\n    allow_headers=[\"*\"],\n)\n\n\n# ── LLM Communication ───────────────────────────────────────────\nasync def generate_text(user_prompt: str,\n                        temperature: float = 0.1\n                        ) -> tuple[str, str]:\n    \"\"\"Try custom LLM first, fall back to OpenAI.\"\"\"\n    # Try custom LLM\n    try:\n        async with httpx.AsyncClient(timeout=TIMEOUT) as client:\n            response = await client.post(\n                f\"{LLM_URL}/api/chat\",\n                json={\n                    \"messages\": [\n                        {\"role\": \"system\",\n                         \"content\": SYSTEM_PROMPT},\n                        {\"role\": \"user\",\n                         \"content\": user_prompt},\n                    ],\n                    \"temperature\": temperature,\n                    \"max_tokens\": 4096,\n                },\n            )\n            response.raise_for_status()\n            data = response.json()\n            text = data.get(\"response\",\n                           data.get(\"content\", \"\"))\n            return text, \"custom-neuralforge-llm\"\n    except Exception as e:\n        print(f\"Custom LLM failed: {e}. Using fallback...\")\n\n    # Fallback to OpenAI\n    if not OPENAI_API_KEY:\n        raise HTTPException(\n            status_code=503,\n            detail=\"LLM unavailable, no fallback configured.\")\n\n    from openai import AsyncOpenAI\n    client = AsyncOpenAI(api_key=OPENAI_API_KEY)\n    response = await client.chat.completions.create(\n        model=\"gpt-4o-mini\",\n        messages=[\n            {\"role\": \"system\", \"content\": SYSTEM_PROMPT},\n            {\"role\": \"user\", \"content\": user_prompt},\n        ],\n        temperature=temperature,\n    )\n    text = response.choices[0].message.content\n    return text, \"gpt-4o-mini (fallback)\"\n\n\n# ── Endpoints ────────────────────────────────────────────────────\n@app.post(\"/api/ask\", response_model=AskResponse)\nasync def ask_question(request: AskRequest):\n    \"\"\"Answer a question about the provided document.\"\"\"\n    user_prompt = (\n        f\"Document:\\n\\n{request.document}\\n\\n\"\n        f\"Question: {request.question}\\n\\n\"\n        f\"Answer based ONLY on the document above.\")\n\n    answer, model = await generate_text(\n        user_prompt, request.temperature)\n\n    return AskResponse(answer=answer, model_used=model)\n\n\n@app.post(\"/api/summarize\")\nasync def summarize_document(request: SummarizeRequest):\n    \"\"\"Summarize the document at the specified detail level.\"\"\"\n    detail_map = {\n        \"brief\": \"Summarize in 2-3 sentences.\",\n        \"medium\": \"Summarize in 1-2 paragraphs, covering \"\n                  \"all key points.\",\n        \"detailed\": \"Create a comprehensive summary covering \"\n                    \"every major point, with section headers.\",\n    }\n    instruction = detail_map.get(\n        request.detail_level,\n        detail_map[\"medium\"])\n\n    user_prompt = (\n        f\"Document:\\n\\n{request.document}\\n\\n\"\n        f\"{instruction}\")\n\n    summary, model = await generate_text(user_prompt, 0.3)\n    return {\n        \"summary\": summary,\n        \"detail_level\": request.detail_level,\n        \"model_used\": model,\n    }\n\n\n@app.post(\"/api/extract\")\nasync def extract_data(request: ExtractRequest):\n    \"\"\"Extract specific data points from the document.\"\"\"\n    fields_str = \", \".join(request.fields)\n    user_prompt = (\n        f\"Document:\\n\\n{request.document}\\n\\n\"\n        f\"Extract these fields: {fields_str}\\n\\n\"\n        f\"Respond in JSON format with the field names \"\n        f\"as keys. If a field is not found in the \"\n        f\"document, set its value to null.\")\n\n    result, model = await generate_text(user_prompt, 0.0)\n    return {\n        \"extracted\": result,\n        \"requested_fields\": request.fields,\n        \"model_used\": model,\n    }\n\n\n@app.get(\"/health\")\nasync def health_check():\n    \"\"\"Health check endpoint.\"\"\"\n    llm_ok = False\n    try:\n        async with httpx.AsyncClient(timeout=5.0) as client:\n            r = await client.get(f\"{LLM_URL}/health\")\n            llm_ok = r.status_code == 200\n    except Exception:\n        pass\n\n    return {\n        \"status\": \"healthy\",\n        \"llm_url\": LLM_URL,\n        \"llm_reachable\": llm_ok,\n    }\n\n\nif __name__ == \"__main__\":\n    import uvicorn\n    uvicorn.run(app, host=\"0.0.0.0\", port=8002)\n",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-2",
      "title": "Python",
      "code": "\"\"\"\nIntegration Testing Script\n\nTests all 5 SaaS product endpoints to verify your LLM\nintegrations are working correctly.\n\nUsage:\n    python test_integrations.py\n\nMake sure your integration APIs are running:\n    - ProseAI:    http://localhost:8001\n    - DocMind:    http://localhost:8002\n    - PixelForge: http://localhost:8003\n    - TruthLens:  http://localhost:8004\n    - ClipCraft:  http://localhost:8005\n\"\"\"\n\nimport httpx\nimport json\nimport sys\nfrom typing import Dict, Any\n\n\n# ── Configuration ────────────────────────────────────────────────\nENDPOINTS = {\n    \"ProseAI\": {\n        \"base_url\": \"http://localhost:8001\",\n        \"tests\": [\n            {\n                \"name\": \"Rewrite (formal)\",\n                \"method\": \"POST\",\n                \"path\": \"/api/rewrite\",\n                \"body\": {\n                    \"text\": \"Hey, the new update is super cool \"\n                            \"and works great!\",\n                    \"mode\": \"formal\",\n                },\n                \"expect_keys\": [\"rewritten\", \"mode\",\n                                \"model_used\"],\n            },\n            {\n                \"name\": \"Rewrite (concise)\",\n                \"method\": \"POST\",\n                \"path\": \"/api/rewrite\",\n                \"body\": {\n                    \"text\": \"In order to facilitate the \"\n                            \"implementation of the new system, \"\n                            \"it would be beneficial to conduct \"\n                            \"a comprehensive review.\",\n                    \"mode\": \"concise\",\n                },\n                \"expect_keys\": [\"rewritten\"],\n            },\n            {\n                \"name\": \"Analyze style\",\n                \"method\": \"POST\",\n                \"path\": \"/api/analyze-style\",\n                \"body\": {\n                    \"text\": \"Machine learning enables computers \"\n                            \"to learn from data without being \"\n                            \"explicitly programmed.\",\n                },\n                \"expect_keys\": [\"analysis\"],\n            },\n        ],\n    },\n    \"DocMind\": {\n        \"base_url\": \"http://localhost:8002\",\n        \"tests\": [\n            {\n                \"name\": \"Ask question\",\n                \"method\": \"POST\",\n                \"path\": \"/api/ask\",\n                \"body\": {\n                    \"document\": \"Annual Report 2024: Revenue \"\n                                \"was $4.2M, up 35% from 2023. \"\n                                \"Operating expenses decreased \"\n                                \"by 12% to $2.8M.\",\n                    \"question\": \"What was the revenue?\",\n                },\n                \"expect_keys\": [\"answer\", \"model_used\"],\n            },\n            {\n                \"name\": \"Summarize\",\n                \"method\": \"POST\",\n                \"path\": \"/api/summarize\",\n                \"body\": {\n                    \"document\": \"The company launched three new \"\n                                \"products in Q3. Product A \"\n                                \"targets enterprise customers. \"\n                                \"Product B serves SMBs. \"\n                                \"Product C is for consumers.\",\n                    \"detail_level\": \"brief\",\n                },\n                \"expect_keys\": [\"summary\"],\n            },\n            {\n                \"name\": \"Extract data\",\n                \"method\": \"POST\",\n                \"path\": \"/api/extract\",\n                \"body\": {\n                    \"document\": \"Invoice #1234. Date: 2024-01-15. \"\n                                \"Amount: $500. Client: Acme Corp.\",\n                    \"fields\": [\"invoice_number\", \"date\",\n                               \"amount\", \"client\"],\n                },\n                \"expect_keys\": [\"extracted\"],\n            },\n        ],\n    },\n    \"PixelForge\": {\n        \"base_url\": \"http://localhost:8003\",\n        \"tests\": [\n            {\n                \"name\": \"Enhance prompt\",\n                \"method\": \"POST\",\n                \"path\": \"/api/enhance-prompt\",\n                \"body\": {\n                    \"prompt\": \"a cat on a roof\",\n                    \"style\": \"photograph\",\n                },\n                \"expect_keys\": [\"enhanced_prompt\"],\n            },\n        ],\n    },\n    \"TruthLens\": {\n        \"base_url\": \"http://localhost:8004\",\n        \"tests\": [\n            {\n                \"name\": \"Analyze text\",\n                \"method\": \"POST\",\n                \"path\": \"/api/analyze\",\n                \"body\": {\n                    \"text\": \"Machine learning is a subset of AI. \"\n                            \"It enables computers to learn from \"\n                            \"data. This is very important.\",\n                },\n                \"expect_keys\": [\"analysis\"],\n            },\n        ],\n    },\n    \"ClipCraft\": {\n        \"base_url\": \"http://localhost:8005\",\n        \"tests\": [\n            {\n                \"name\": \"Generate script\",\n                \"method\": \"POST\",\n                \"path\": \"/api/generate-script\",\n                \"body\": {\n                    \"concept\": \"A 30-second product intro for \"\n                               \"a mobile app\",\n                    \"duration\": 30,\n                },\n                \"expect_keys\": [\"script\"],\n            },\n        ],\n    },\n}\n\n\n# ── Test Runner ──────────────────────────────────────────────────\ndef run_test(product: str, base_url: str,\n             test: Dict[str, Any]) -> bool:\n    \"\"\"Run a single test and return True if passed.\"\"\"\n    url = f\"{base_url}{test['path']}\"\n    name = test[\"name\"]\n\n    try:\n        with httpx.Client(timeout=30.0) as client:\n            if test[\"method\"] == \"POST\":\n                response = client.post(url, json=test[\"body\"])\n            else:\n                response = client.get(url)\n\n        if response.status_code != 200:\n            print(f\"  FAIL {name}: HTTP {response.status_code}\")\n            print(f\"        {response.text[:200]}\")\n            return False\n\n        data = response.json()\n\n        # Check expected keys\n        missing = [k for k in test.get(\"expect_keys\", [])\n                   if k not in data]\n        if missing:\n            print(f\"  FAIL {name}: Missing keys: {missing}\")\n            return False\n\n        print(f\"  PASS {name}\")\n        # Show a preview of the response\n        preview = json.dumps(data, indent=2)[:200]\n        print(f\"       Response: {preview}...\")\n        return True\n\n    except httpx.ConnectError:\n        print(f\"  SKIP {name}: Cannot connect to {base_url}\")\n        return False\n    except Exception as e:\n        print(f\"  FAIL {name}: {e}\")\n        return False\n\n\ndef main():\n    \"\"\"Run all integration tests.\"\"\"\n    print(\"=\" * 60)\n    print(\"GenAI SaaS Integration Tests\")\n    print(\"=\" * 60)\n\n    total = 0\n    passed = 0\n    skipped = 0\n\n    for product, config in ENDPOINTS.items():\n        print(f\"\\n--- {product} ({config['base_url']}) ---\")\n\n        # Health check first\n        try:\n            with httpx.Client(timeout=5.0) as client:\n                r = client.get(f\"{config['base_url']}/health\")\n                if r.status_code == 200:\n                    health = r.json()\n                    llm_status = health.get(\n                        \"llm_reachable\", \"unknown\")\n                    print(f\"  Health: OK \"\n                          f\"(LLM reachable: {llm_status})\")\n                else:\n                    print(f\"  Health: HTTP {r.status_code}\")\n        except httpx.ConnectError:\n            print(f\"  Health: Cannot connect — \"\n                  f\"is the server running?\")\n            skipped += len(config[\"tests\"])\n            total += len(config[\"tests\"])\n            continue\n\n        # Run tests\n        for test in config[\"tests\"]:\n            total += 1\n            if run_test(product, config[\"base_url\"], test):\n                passed += 1\n\n    # Summary\n    failed = total - passed - skipped\n    print(f\"\\n{'=' * 60}\")\n    print(f\"Results: {passed}/{total} passed, \"\n          f\"{failed} failed, {skipped} skipped\")\n    print(f\"{'=' * 60}\")\n\n    if failed > 0:\n        sys.exit(1)\n\n\nif __name__ == \"__main__\":\n    main()\n",
      "language": "python",
      "runnable": true
    }
  ]
}