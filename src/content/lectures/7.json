{"id": "7", "title": {"en": "Code a Tokenizer from Scratch", "ro": "Codifică un tokenizer de la zero", "el": "Γράψε έναν tokenizer από την αρχή"}, "content": {"en": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">LECTURE 7 <span class=\"stage-badge\">Stage 2: Tokenization &amp; Data</span></div>\n  <h1 class=\"lecture-title\">Code a Tokenizer from Scratch</h1>\n  <div class=\"lecture-subtitle\">Teaching the computer to read &mdash; one character at a time</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 45 min read</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Hands-on coding</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Intermediate</span>\n  </div>\n</div>\n<div class=\"content\">\n\n  <h2>Why Tokenization Matters</h2>\n  <p>\n    Neural networks are mathematical machines. They multiply matrices, compute dot products,\n    and apply activation functions &mdash; all operations that require <strong>numbers</strong>.\n    But language is made of characters, words, and sentences. Tokenization is the bridge\n    between the world of text and the world of numbers.\n  </p>\n  <p>\n    Every single Large Language Model &mdash; GPT-4, Claude, LLaMA, Gemini &mdash; begins its work\n    by converting your text prompt into a sequence of integer IDs called <strong>tokens</strong>.\n    The model never sees raw text. It sees something like <code>[15496, 11, 616, 1438, 318, 13]</code>.\n    Building a tokenizer is therefore the very first hands-on step in building an LLM.\n  </p>\n\n  <div class=\"callout analogy\">\n    <div class=\"callout-title\">&#x1F4A1; Analogy &mdash; Learning to Read</div>\n    <p>\n      Think about how children learn to read. First they learn the <strong>alphabet</strong> &mdash;\n      individual letters. Then they learn to combine letters into <strong>syllables</strong> and\n      <strong>words</strong>. Tokenization follows a similar path: we start with character-level\n      tokenization (the alphabet), then graduate to subword and word-level approaches.\n    </p>\n  </div>\n\n  <h2>The Tokenization Pipeline</h2>\n  <p>\n    At its core, tokenization involves three steps:\n  </p>\n  <div class=\"feature-grid\">\n    <div class=\"feature-card\">\n      <h3>1. Build a Vocabulary</h3>\n      <p>Scan a text corpus and create a mapping from every unique element (character, subword, or word) to a unique integer ID.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>2. Encode</h3>\n      <p>Convert a string of text into a list of integer IDs using the vocabulary.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>3. Decode</h3>\n      <p>Convert a list of integer IDs back into human-readable text.</p>\n    </div>\n  </div>\n\n  <h2>Character-Level Tokenization</h2>\n  <p>\n    The simplest tokenization approach treats every single character as a token. The letter\n    <code>a</code> gets one ID, the letter <code>b</code> gets another, spaces and punctuation\n    each get their own IDs. Let us build this from scratch.\n  </p>\n\n  <h3>Step 1: Building a Character Vocabulary</h3>\n  <p>\n    We start with a corpus of text and extract every unique character. Then we sort them\n    (for reproducibility) and assign each one an integer ID.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Building a character vocabulary from a text corpus\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\n\n# Step 1: Find all unique characters\nunique_chars = sorted(set(corpus))\nprint(f\"Unique characters ({len(unique_chars)}):\")\nprint(unique_chars)\n\n# Step 2: Create char-to-id and id-to-char mappings\nchar_to_id = {ch: i for i, ch in enumerate(unique_chars)}\nid_to_char = {i: ch for ch, i in char_to_id.items()}\n\nprint(f\"\\\nCharacter &rarr; ID mapping:\")\nfor ch, idx in char_to_id.items():\n    display = repr(ch)\n    print(f\"  {display:6s} &rarr; {idx}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <p>\n    Notice that even spaces and punctuation get their own IDs. The vocabulary size for\n    English character-level tokenization is typically small &mdash; around 50&ndash;100 unique\n    characters if you include upper/lowercase, digits, and punctuation.\n  </p>\n\n  <h3>Step 2: The Full CharTokenizer Class</h3>\n  <p>\n    Now let us wrap everything into a proper Python class with <code>encode()</code> and\n    <code>decode()</code> methods, plus support for special tokens.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    \"\"\"A simple character-level tokenizer.\"\"\"\n\n    def __init__(self, corpus=None):\n        # Special tokens\n        self.special_tokens = {\n            \"&lt;|unk|&gt;\": 0,      # unknown character\n            \"&lt;|endoftext|&gt;\": 1, # end-of-text marker\n        }\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        \"\"\"Add all unique characters from text to the vocabulary.\"\"\"\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        \"\"\"Convert text to a list of token IDs.\"\"\"\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        \"\"\"Convert a list of token IDs back to text.\"\"\"\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\n# Build the tokenizer on a corpus\ncorpus = \"\"\"Natural language processing (NLP) is a subfield of\ncomputer science and artificial intelligence. It focuses on\nthe interaction between computers and human language.\"\"\"\n\ntokenizer = CharTokenizer(corpus)\nprint(f\"Vocabulary size: {tokenizer.vocab_size}\")\nprint(f\"Full vocabulary:\")\nfor token, idx in sorted(tokenizer.char_to_id.items(), key=lambda x: x[1]):\n    display = repr(token) if len(token) == 1 else token\n    print(f\"  {idx:3d}: {display}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout tip\">\n    <div class=\"callout-title\">&#x1F4CC; Tip &mdash; Special Tokens</div>\n    <p>\n      Special tokens like <code>&lt;|unk|&gt;</code> and <code>&lt;|endoftext|&gt;</code> play\n      critical roles. The <strong>unknown token</strong> handles characters never seen during\n      vocabulary building. The <strong>end-of-text token</strong> tells the model where one\n      document ends and another begins &mdash; essential for training on multiple documents.\n    </p>\n  </div>\n\n  <h3>Step 3: Encoding and Decoding in Action</h3>\n  <p>\n    Let us encode a sentence, inspect the token IDs, and decode them back to verify\n    our tokenizer works correctly.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    def __init__(self, corpus=None):\n        self.special_tokens = {\"&lt;|unk|&gt;\": 0, \"&lt;|endoftext|&gt;\": 1}\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\ntokenizer = CharTokenizer(corpus)\n\n# Encode a sentence\nsentence = \"The fox is quick.\"\ntoken_ids = tokenizer.encode(sentence)\nprint(f\"Original:  '{sentence}'\")\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Sequence length: {len(token_ids)} tokens\")\n\n# Decode back\ndecoded = tokenizer.decode(token_ids)\nprint(f\"Decoded:   '{decoded}'\")\nprint(f\"Round-trip matches: {sentence == decoded}\")\n\n# Show what happens with unknown characters\nsentence2 = \"Hello! 123\"\ntoken_ids2 = tokenizer.encode(sentence2)\ndecoded2 = tokenizer.decode(token_ids2)\nprint(f\"\\\nWith unknowns: '{sentence2}'\")\nprint(f\"Token IDs: {token_ids2}\")\nprint(f\"Decoded:   '{decoded2}'  (0 = unknown)\")\n\n# End-of-text usage\ndoc1 = tokenizer.encode(\"Hello\")\neot = [tokenizer.special_tokens[\"&lt;|endoftext|&gt;\"]]\ndoc2 = tokenizer.encode(\"World\")\ncombined = doc1 + eot + doc2\nprint(f\"\\\nTwo documents combined: {combined}\")\nprint(f\"Decoded: '{tokenizer.decode(combined)}'\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <h2>Sequence Length: The Problem with Characters</h2>\n  <p>\n    Character-level tokenization is simple and elegant, but it has a critical drawback:\n    sequences become <strong>very long</strong>. A single word like \"tokenization\" becomes\n    12 tokens. This matters because Transformers have a fixed <em>context window</em>,\n    and attention computation scales quadratically with sequence length.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Compare tokenization approaches on the same text\ntext = \"The Transformer architecture revolutionized natural language processing.\"\n\n# Character-level: each char is a token\nchar_tokens = list(text)\n\n# Word-level: split on whitespace\nword_tokens = text.split()\n\n# Simulated subword-level (rough approximation)\n# Real BPE would do this properly - we will build that in Lecture 8\nsubword_tokens = [\"The\", \" Trans\", \"former\", \" architecture\",\n                  \" revolution\", \"ized\", \" natural\",\n                  \" language\", \" processing\", \".\"]\n\nprint(\"=== Sequence Length Comparison ===\")\nprint(f\"Text: '{text}'\")\nprint(f\"Text length: {len(text)} characters\")\nprint()\nprint(f\"Character tokens: {len(char_tokens):3d} tokens\")\nprint(f\"  First 20: {char_tokens[:20]}\")\nprint()\nprint(f\"Word tokens:      {len(word_tokens):3d} tokens\")\nprint(f\"  {word_tokens}\")\nprint()\nprint(f\"Subword tokens:   {len(subword_tokens):3d} tokens\")\nprint(f\"  {subword_tokens}\")\n\nprint()\nprint(\"=== Why This Matters ===\")\ncontext_window = 1024\nchar_words = context_window // (len(text) / len(word_tokens))\nsub_words = context_window // (len(text) / len(subword_tokens))\nprint(f\"With a {context_window}-token context window:\")\nprint(f\"  Character-level can fit ~{context_window} chars\")\nprint(f\"    (~{context_window // 5} words, since avg word ~5 chars)\")\nprint(f\"  Word-level can fit ~{context_window} words\")\nprint(f\"  Subword-level can fit ~{int(sub_words)} words\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout warning\">\n    <div class=\"callout-title\">&#x26A0;&#xFE0F; Limitations of Character Tokenization</div>\n    <p>\n      While character-level tokenization is great for learning, production LLMs almost never\n      use it. Here is why:\n    </p>\n    <ul>\n      <li><strong>Long sequences:</strong> A 1000-word document becomes ~5000 character tokens vs ~1300 subword tokens.</li>\n      <li><strong>No semantic meaning:</strong> The letter \"a\" carries no meaning on its own. The model must learn to compose characters into meaningful units.</li>\n      <li><strong>Computational cost:</strong> Attention is O(n&sup2;) in sequence length, so 4x longer sequences means 16x more computation.</li>\n      <li><strong>Harder to learn:</strong> The model needs more layers and training to learn character compositions.</li>\n    </ul>\n  </div>\n\n  <h2>Comparison: Tokenization Strategies</h2>\n  <table class=\"comparison-table\">\n    <thead>\n      <tr>\n        <th>Strategy</th>\n        <th>Vocab Size</th>\n        <th>Sequence Length</th>\n        <th>Unknown Words?</th>\n        <th>Used By</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td>Character-level</td>\n        <td>~50&ndash;100</td>\n        <td>Very long</td>\n        <td>Almost none</td>\n        <td>Some research models</td>\n      </tr>\n      <tr>\n        <td>Word-level</td>\n        <td>50,000&ndash;200,000+</td>\n        <td>Short</td>\n        <td>Many (OOV problem)</td>\n        <td>Older NLP models</td>\n      </tr>\n      <tr>\n        <td>Subword (BPE)</td>\n        <td>30,000&ndash;100,000</td>\n        <td>Moderate</td>\n        <td>Very rare</td>\n        <td>GPT-2/3/4, LLaMA</td>\n      </tr>\n      <tr>\n        <td>SentencePiece</td>\n        <td>32,000&ndash;128,000</td>\n        <td>Moderate</td>\n        <td>Very rare</td>\n        <td>T5, LLaMA, Gemma</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <h2>The Path Forward: Subword Tokenization</h2>\n  <p>\n    The sweet spot in modern NLP is <strong>subword tokenization</strong>, which balances\n    vocabulary size, sequence length, and coverage. The most popular approach is\n    <strong>Byte Pair Encoding (BPE)</strong>, which we will build from scratch in the next lecture.\n  </p>\n  <p>\n    BPE starts with individual characters (like our CharTokenizer) and iteratively merges\n    the most frequent pairs into new tokens. Common words like \"the\" become single tokens,\n    while rare words like \"tokenization\" might be split into \"token\" + \"ization\". This gives\n    us the best of both worlds.\n  </p>\n\n  <div class=\"callout info\">\n    <div class=\"callout-title\">&#x1F4D6; Key Takeaways</div>\n    <ul>\n      <li>Tokenization converts text to integers so neural networks can process it.</li>\n      <li>A tokenizer needs <code>encode()</code> (text &rarr; IDs) and <code>decode()</code> (IDs &rarr; text) methods.</li>\n      <li>Character-level tokenization is simple but produces long sequences.</li>\n      <li>Special tokens (<code>&lt;|unk|&gt;</code>, <code>&lt;|endoftext|&gt;</code>) handle edge cases.</li>\n      <li>Modern LLMs use subword tokenization (BPE) for the best tradeoff.</li>\n    </ul>\n  </div>\n\n  <div class=\"exercise-box\">\n    <h3>&#x1F3CB;&#xFE0F; Exercise: Extend the CharTokenizer</h3>\n    <ol>\n      <li>Add a method <code>encode_batch(texts)</code> that takes a list of strings and returns a list of token-ID lists, each separated by <code>&lt;|endoftext|&gt;</code>.</li>\n      <li>Add a <code>save_vocab(filepath)</code> and <code>load_vocab(filepath)</code> method pair.</li>\n      <li>Try building a vocabulary from a larger text (e.g., a paragraph from Wikipedia) and see how the vocabulary size changes.</li>\n    </ol>\n  </div>", "ro": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">LECȚIA 7 <span class=\"stage-badge\">Etapa 2: Tokenizare &amp; Date</span></div>\n  <h1 class=\"lecture-title\">Codifică un tokenizer de la zero</h1>\n  <div class=\"lecture-subtitle\">Învățăm calculatorul să citească &mdash; un caracter pe rând</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 45 min de citit</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Programare practică</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Intermediar</span>\n  </div>\n</div>\n<div class=\"content\">\n\n  <h2>De ce contează tokenizarea</h2>\n  <p>\n    Rețelele neuronale sunt mașini matematice. Ele înmulțesc matrice, calculează produse scalare\n    și aplică funcții de activare &mdash; toate operații care necesită <strong>numere</strong>.\n    Dar limbajul este format din caractere, cuvinte și propoziții. Tokenizarea este puntea\n    dintre lumea textului și lumea numerelor.\n  </p>\n  <p>\n    Fiecare model de limbaj mare &mdash; GPT-4, Claude, LLaMA, Gemini &mdash; își începe munca\n    prin convertirea prompt-ului tău de text într-o secvență de ID-uri întregi numite <strong>tokeni</strong>.\n    Modelul nu vede niciodată text brut. Vede ceva de genul <code>[15496, 11, 616, 1438, 318, 13]</code>.\n    Construirea unui tokenizer este, prin urmare, primul pas practic în construirea unui LLM.\n  </p>\n\n  <div class=\"callout analogy\">\n    <div class=\"callout-title\">&#x1F4A1; Analogie &mdash; A învăța să citești</div>\n    <p>\n      Gândește-te la cum învață copiii să citească. Mai întâi învață <strong>alfabetul</strong> &mdash;\n      litere individuale. Apoi învață să combine literele în <strong>silabe</strong> și\n      <strong>cuvinte</strong>. Tokenizarea urmează un parcurs similar: începem cu tokenizarea\n      la nivel de caracter (alfabetul), apoi trecem la abordări la nivel de subcuvânt și cuvânt.\n    </p>\n  </div>\n\n  <h2>Pipeline-ul de tokenizare</h2>\n  <p>\n    La bază, tokenizarea implică trei pași:\n  </p>\n  <div class=\"feature-grid\">\n    <div class=\"feature-card\">\n      <h3>1. Construiește un vocabular</h3>\n      <p>Scanează un corpus de text și creează o mapare de la fiecare element unic (caracter, subcuvânt sau cuvânt) la un ID întreg unic.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>2. Codifică</h3>\n      <p>Convertește un șir de text într-o listă de ID-uri întregi folosind vocabularul.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>3. Decodifică</h3>\n      <p>Convertește o listă de ID-uri întregi înapoi în text lizibil de om.</p>\n    </div>\n  </div>\n\n  <h2>Tokenizarea la nivel de caracter</h2>\n  <p>\n    Cea mai simplă abordare a tokenizării tratează fiecare caracter individual ca un token. Litera\n    <code>a</code> primește un ID, litera <code>b</code> primește altul, spațiile și semnele de\n    punctuație primesc fiecare propriile ID-uri. Să construim asta de la zero.\n  </p>\n\n  <h3>Pasul 1: Construirea unui vocabular de caractere</h3>\n  <p>\n    Începem cu un corpus de text și extragem fiecare caracter unic. Apoi le sortăm\n    (pentru reproductibilitate) și atribuim fiecăruia un ID întreg.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Building a character vocabulary from a text corpus\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\n\n# Step 1: Find all unique characters\nunique_chars = sorted(set(corpus))\nprint(f\"Unique characters ({len(unique_chars)}):\")\nprint(unique_chars)\n\n# Step 2: Create char-to-id and id-to-char mappings\nchar_to_id = {ch: i for i, ch in enumerate(unique_chars)}\nid_to_char = {i: ch for ch, i in char_to_id.items()}\n\nprint(f\"\\\nCharacter &rarr; ID mapping:\")\nfor ch, idx in char_to_id.items():\n    display = repr(ch)\n    print(f\"  {display:6s} &rarr; {idx}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <p>\n    Observă că și spațiile și semnele de punctuație primesc propriile ID-uri. Dimensiunea vocabularului\n    pentru tokenizarea la nivel de caracter în engleză este de obicei mică &mdash; aproximativ\n    50&ndash;100 de caractere unice dacă incluzi majuscule/minuscule, cifre și punctuație.\n  </p>\n\n  <h3>Pasul 2: Clasa completă CharTokenizer</h3>\n  <p>\n    Acum să împachetăm totul într-o clasă Python corectă cu metode <code>encode()</code> și\n    <code>decode()</code>, plus suport pentru tokeni speciali.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    \"\"\"A simple character-level tokenizer.\"\"\"\n\n    def __init__(self, corpus=None):\n        # Special tokens\n        self.special_tokens = {\n            \"&lt;|unk|&gt;\": 0,      # unknown character\n            \"&lt;|endoftext|&gt;\": 1, # end-of-text marker\n        }\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        \"\"\"Add all unique characters from text to the vocabulary.\"\"\"\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        \"\"\"Convert text to a list of token IDs.\"\"\"\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        \"\"\"Convert a list of token IDs back to text.\"\"\"\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\n# Build the tokenizer on a corpus\ncorpus = \"\"\"Natural language processing (NLP) is a subfield of\ncomputer science and artificial intelligence. It focuses on\nthe interaction between computers and human language.\"\"\"\n\ntokenizer = CharTokenizer(corpus)\nprint(f\"Vocabulary size: {tokenizer.vocab_size}\")\nprint(f\"Full vocabulary:\")\nfor token, idx in sorted(tokenizer.char_to_id.items(), key=lambda x: x[1]):\n    display = repr(token) if len(token) == 1 else token\n    print(f\"  {idx:3d}: {display}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout tip\">\n    <div class=\"callout-title\">&#x1F4CC; Sfat &mdash; Tokeni speciali</div>\n    <p>\n      Tokenii speciali precum <code>&lt;|unk|&gt;</code> și <code>&lt;|endoftext|&gt;</code> joacă\n      roluri esențiale. <strong>Tokenul necunoscut</strong> gestionează caracterele nevăzute\n      niciodată în timpul construirii vocabularului. <strong>Tokenul de sfârșit de text</strong> îi\n      spune modelului unde se termină un document și unde începe altul &mdash; esențial pentru\n      antrenamentul pe mai multe documente.\n    </p>\n  </div>\n\n  <h3>Pasul 3: Codificarea și decodificarea în acțiune</h3>\n  <p>\n    Să codificăm o propoziție, să inspectăm ID-urile tokenilor și să le decodificăm înapoi\n    pentru a verifica dacă tokenizer-ul nostru funcționează corect.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    def __init__(self, corpus=None):\n        self.special_tokens = {\"&lt;|unk|&gt;\": 0, \"&lt;|endoftext|&gt;\": 1}\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\ntokenizer = CharTokenizer(corpus)\n\n# Encode a sentence\nsentence = \"The fox is quick.\"\ntoken_ids = tokenizer.encode(sentence)\nprint(f\"Original:  '{sentence}'\")\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Sequence length: {len(token_ids)} tokens\")\n\n# Decode back\ndecoded = tokenizer.decode(token_ids)\nprint(f\"Decoded:   '{decoded}'\")\nprint(f\"Round-trip matches: {sentence == decoded}\")\n\n# Show what happens with unknown characters\nsentence2 = \"Hello! 123\"\ntoken_ids2 = tokenizer.encode(sentence2)\ndecoded2 = tokenizer.decode(token_ids2)\nprint(f\"\\\nWith unknowns: '{sentence2}'\")\nprint(f\"Token IDs: {token_ids2}\")\nprint(f\"Decoded:   '{decoded2}'  (0 = unknown)\")\n\n# End-of-text usage\ndoc1 = tokenizer.encode(\"Hello\")\neot = [tokenizer.special_tokens[\"&lt;|endoftext|&gt;\"]]\ndoc2 = tokenizer.encode(\"World\")\ncombined = doc1 + eot + doc2\nprint(f\"\\\nTwo documents combined: {combined}\")\nprint(f\"Decoded: '{tokenizer.decode(combined)}'\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <h2>Lungimea secvenței: Problema cu caracterele</h2>\n  <p>\n    Tokenizarea la nivel de caracter este simplă și elegantă, dar are un dezavantaj critic:\n    secvențele devin <strong>foarte lungi</strong>. Un singur cuvânt precum \"tokenization\" devine\n    12 tokeni. Acest lucru contează deoarece Transformer-ele au o <em>fereastră de context</em> fixă,\n    iar calculul de atenție crește pătratic cu lungimea secvenței.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Compare tokenization approaches on the same text\ntext = \"The Transformer architecture revolutionized natural language processing.\"\n\n# Character-level: each char is a token\nchar_tokens = list(text)\n\n# Word-level: split on whitespace\nword_tokens = text.split()\n\n# Simulated subword-level (rough approximation)\n# Real BPE would do this properly - we will build that in Lecture 8\nsubword_tokens = [\"The\", \" Trans\", \"former\", \" architecture\",\n                  \" revolution\", \"ized\", \" natural\",\n                  \" language\", \" processing\", \".\"]\n\nprint(\"=== Sequence Length Comparison ===\")\nprint(f\"Text: '{text}'\")\nprint(f\"Text length: {len(text)} characters\")\nprint()\nprint(f\"Character tokens: {len(char_tokens):3d} tokens\")\nprint(f\"  First 20: {char_tokens[:20]}\")\nprint()\nprint(f\"Word tokens:      {len(word_tokens):3d} tokens\")\nprint(f\"  {word_tokens}\")\nprint()\nprint(f\"Subword tokens:   {len(subword_tokens):3d} tokens\")\nprint(f\"  {subword_tokens}\")\n\nprint()\nprint(\"=== Why This Matters ===\")\ncontext_window = 1024\nchar_words = context_window // (len(text) / len(word_tokens))\nsub_words = context_window // (len(text) / len(subword_tokens))\nprint(f\"With a {context_window}-token context window:\")\nprint(f\"  Character-level can fit ~{context_window} chars\")\nprint(f\"    (~{context_window // 5} words, since avg word ~5 chars)\")\nprint(f\"  Word-level can fit ~{context_window} words\")\nprint(f\"  Subword-level can fit ~{int(sub_words)} words\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout warning\">\n    <div class=\"callout-title\">&#x26A0;&#xFE0F; Limitările tokenizării la nivel de caracter</div>\n    <p>\n      Deși tokenizarea la nivel de caracter este excelentă pentru învățare, LLM-urile de producție\n      aproape niciodată nu o folosesc. Iată de ce:\n    </p>\n    <ul>\n      <li><strong>Secvențe lungi:</strong> Un document de 1000 de cuvinte devine ~5000 de tokeni caracter vs ~1300 de tokeni subcuvânt.</li>\n      <li><strong>Niciun sens semantic:</strong> Litera \"a\" nu poartă niciun sens singură. Modelul trebuie să învețe să compună caractere în unități semnificative.</li>\n      <li><strong>Cost computațional:</strong> Atenția este O(n&sup2;) în lungimea secvenței, deci secvențe de 4x mai lungi înseamnă de 16x mai multe calcule.</li>\n      <li><strong>Mai greu de învățat:</strong> Modelul are nevoie de mai multe straturi și antrenament pentru a învăța compunerea caracterelor.</li>\n    </ul>\n  </div>\n\n  <h2>Comparație: Strategii de tokenizare</h2>\n  <table class=\"comparison-table\">\n    <thead>\n      <tr>\n        <th>Strategie</th>\n        <th>Dimensiune vocabular</th>\n        <th>Lungime secvență</th>\n        <th>Cuvinte necunoscute?</th>\n        <th>Folosit de</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td>Nivel de caracter</td>\n        <td>~50&ndash;100</td>\n        <td>Foarte lungă</td>\n        <td>Aproape niciuna</td>\n        <td>Unele modele de cercetare</td>\n      </tr>\n      <tr>\n        <td>Nivel de cuvânt</td>\n        <td>50,000&ndash;200,000+</td>\n        <td>Scurtă</td>\n        <td>Multe (problema OOV)</td>\n        <td>Modele NLP mai vechi</td>\n      </tr>\n      <tr>\n        <td>Subcuvânt (BPE)</td>\n        <td>30,000&ndash;100,000</td>\n        <td>Moderată</td>\n        <td>Foarte rare</td>\n        <td>GPT-2/3/4, LLaMA</td>\n      </tr>\n      <tr>\n        <td>SentencePiece</td>\n        <td>32,000&ndash;128,000</td>\n        <td>Moderată</td>\n        <td>Foarte rare</td>\n        <td>T5, LLaMA, Gemma</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <h2>Calea de urmat: Tokenizarea la nivel de subcuvânt</h2>\n  <p>\n    Punctul optim în NLP modern este <strong>tokenizarea la nivel de subcuvânt</strong>, care echilibrează\n    dimensiunea vocabularului, lungimea secvenței și acoperirea. Cea mai populară abordare este\n    <strong>Byte Pair Encoding (BPE)</strong>, pe care o vom construi de la zero în lecția următoare.\n  </p>\n  <p>\n    BPE începe cu caractere individuale (precum CharTokenizer-ul nostru) și fuzionează iterativ\n    cele mai frecvente perechi în tokeni noi. Cuvintele comune precum \"the\" devin tokeni unici,\n    în timp ce cuvintele rare precum \"tokenization\" pot fi împărțite în \"token\" + \"ization\". Acest lucru\n    ne oferă ce este mai bun din ambele lumi.\n  </p>\n\n  <div class=\"callout info\">\n    <div class=\"callout-title\">&#x1F4D6; Concluzii cheie</div>\n    <ul>\n      <li>Tokenizarea convertește textul în numere întregi pentru ca rețelele neuronale să îl poată procesa.</li>\n      <li>Un tokenizer are nevoie de metode <code>encode()</code> (text &rarr; ID-uri) și <code>decode()</code> (ID-uri &rarr; text).</li>\n      <li>Tokenizarea la nivel de caracter este simplă dar produce secvențe lungi.</li>\n      <li>Tokenii speciali (<code>&lt;|unk|&gt;</code>, <code>&lt;|endoftext|&gt;</code>) gestionează cazurile limită.</li>\n      <li>LLM-urile moderne folosesc tokenizarea la nivel de subcuvânt (BPE) pentru cel mai bun compromis.</li>\n    </ul>\n  </div>\n\n  <div class=\"exercise-box\">\n    <h3>&#x1F3CB;&#xFE0F; Exercițiu: Extinde CharTokenizer</h3>\n    <ol>\n      <li>Adaugă o metodă <code>encode_batch(texts)</code> care primește o listă de șiruri și returnează o listă de liste de ID-uri de tokeni, fiecare separată de <code>&lt;|endoftext|&gt;</code>.</li>\n      <li>Adaugă o pereche de metode <code>save_vocab(filepath)</code> și <code>load_vocab(filepath)</code>.</li>\n      <li>Încearcă să construiești un vocabular dintr-un text mai mare (de ex., un paragraf din Wikipedia) și observă cum se schimbă dimensiunea vocabularului.</li>\n    </ol>\n  </div>", "el": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n  <div class=\"lecture-label\">ΔΙΑΛΕΞΗ 7 <span class=\"stage-badge\">Στάδιο 2: Tokenization &amp; Δεδομένα</span></div>\n  <h1 class=\"lecture-title\">Γράψε έναν tokenizer από την αρχή</h1>\n  <div class=\"lecture-subtitle\">Διδάσκοντας τον υπολογιστή να διαβάζει &mdash; ένα χαρακτήρα τη φορά</div>\n  <div class=\"lecture-meta\">\n    <span class=\"lecture-meta-item\">&#x23F1; 45 λεπτά ανάγνωση</span>\n    <span class=\"lecture-meta-item\">&#x1F4BB; Πρακτική κωδικοποίηση</span>\n    <span class=\"lecture-meta-item\">&#x1F3AF; Ενδιάμεσο</span>\n  </div>\n</div>\n<div class=\"content\">\n\n  <h2>Γιατί η tokenization έχει σημασία</h2>\n  <p>\n    Τα νευρωνικά δίκτυα είναι μαθηματικές μηχανές. Πολλαπλασιάζουν πίνακες, υπολογίζουν\n    εσωτερικά γινόμενα και εφαρμόζουν συναρτήσεις ενεργοποίησης &mdash; όλες λειτουργίες\n    που απαιτούν <strong>αριθμούς</strong>. Αλλά η γλώσσα αποτελείται από χαρακτήρες, λέξεις\n    και προτάσεις. Η tokenization είναι η γέφυρα μεταξύ του κόσμου του κειμένου και του\n    κόσμου των αριθμών.\n  </p>\n  <p>\n    Κάθε μεγάλο γλωσσικό μοντέλο &mdash; GPT-4, Claude, LLaMA, Gemini &mdash; ξεκινά τη\n    δουλειά του μετατρέποντας το κείμενο prompt σου σε μια ακολουθία ακέραιων αναγνωριστικών\n    που ονομάζονται <strong>tokens</strong>. Το μοντέλο δεν βλέπει ποτέ ακατέργαστο κείμενο.\n    Βλέπει κάτι σαν <code>[15496, 11, 616, 1438, 318, 13]</code>. Η κατασκευή ενός tokenizer\n    είναι επομένως το πρώτο πρακτικό βήμα στην κατασκευή ενός LLM.\n  </p>\n\n  <div class=\"callout analogy\">\n    <div class=\"callout-title\">&#x1F4A1; Αναλογία &mdash; Μαθαίνοντας να διαβάζεις</div>\n    <p>\n      Σκέψου πώς μαθαίνουν τα παιδιά να διαβάζουν. Πρώτα μαθαίνουν το <strong>αλφάβητο</strong>\n      &mdash; μεμονωμένα γράμματα. Μετά μαθαίνουν να συνδυάζουν γράμματα σε <strong>συλλαβές</strong>\n      και <strong>λέξεις</strong>. Η tokenization ακολουθεί μια παρόμοια πορεία: ξεκινάμε με\n      tokenization σε επίπεδο χαρακτήρα (το αλφάβητο), μετά προχωράμε σε προσεγγίσεις\n      σε επίπεδο υπολέξης και λέξης.\n    </p>\n  </div>\n\n  <h2>Η αγωγός tokenization</h2>\n  <p>\n    Στον πυρήνα της, η tokenization περιλαμβάνει τρία βήματα:\n  </p>\n  <div class=\"feature-grid\">\n    <div class=\"feature-card\">\n      <h3>1. Δημιούργησε ένα λεξιλόγιο</h3>\n      <p>Σάρωσε ένα corpus κειμένου και δημιούργησε μια αντιστοίχιση από κάθε μοναδικό στοιχείο (χαρακτήρα, υπολέξη ή λέξη) σε ένα μοναδικό ακέραιο ID.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>2. Κωδικοποίησε</h3>\n      <p>Μετέτρεψε ένα κείμενο σε μια λίστα ακέραιων ID χρησιμοποιώντας το λεξιλόγιο.</p>\n    </div>\n    <div class=\"feature-card\">\n      <h3>3. Αποκωδικοποίησε</h3>\n      <p>Μετέτρεψε μια λίστα ακέραιων ID πίσω σε αναγνώσιμο κείμενο.</p>\n    </div>\n  </div>\n\n  <h2>Tokenization σε επίπεδο χαρακτήρα</h2>\n  <p>\n    Η πιο απλή προσέγγιση tokenization αντιμετωπίζει κάθε μεμονωμένο χαρακτήρα ως token.\n    Το γράμμα <code>a</code> παίρνει ένα ID, το γράμμα <code>b</code> παίρνει ένα άλλο,\n    τα κενά και τα σημεία στίξης παίρνουν τα δικά τους ID. Ας το χτίσουμε από την αρχή.\n  </p>\n\n  <h3>Βήμα 1: Κατασκευή λεξιλογίου χαρακτήρων</h3>\n  <p>\n    Ξεκινάμε με ένα corpus κειμένου και εξάγουμε κάθε μοναδικό χαρακτήρα. Στη συνέχεια\n    τους ταξινομούμε (για αναπαραγωγιμότητα) και αναθέτουμε σε καθέναν ένα ακέραιο ID.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Building a character vocabulary from a text corpus\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\n\n# Step 1: Find all unique characters\nunique_chars = sorted(set(corpus))\nprint(f\"Unique characters ({len(unique_chars)}):\")\nprint(unique_chars)\n\n# Step 2: Create char-to-id and id-to-char mappings\nchar_to_id = {ch: i for i, ch in enumerate(unique_chars)}\nid_to_char = {i: ch for ch, i in char_to_id.items()}\n\nprint(f\"\\\nCharacter &rarr; ID mapping:\")\nfor ch, idx in char_to_id.items():\n    display = repr(ch)\n    print(f\"  {display:6s} &rarr; {idx}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <p>\n    Παρατήρησε ότι ακόμα και τα κενά και τα σημεία στίξης παίρνουν τα δικά τους ID. Το μέγεθος\n    λεξιλογίου για tokenization σε επίπεδο χαρακτήρα στα αγγλικά είναι συνήθως μικρό &mdash;\n    περίπου 50&ndash;100 μοναδικοί χαρακτήρες αν συμπεριλάβεις κεφαλαία/πεζά, ψηφία και στίξη.\n  </p>\n\n  <h3>Βήμα 2: Η πλήρης κλάση CharTokenizer</h3>\n  <p>\n    Τώρα ας τυλίξουμε τα πάντα σε μια σωστή κλάση Python με μεθόδους <code>encode()</code>\n    και <code>decode()</code>, συν υποστήριξη για ειδικά tokens.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    \"\"\"A simple character-level tokenizer.\"\"\"\n\n    def __init__(self, corpus=None):\n        # Special tokens\n        self.special_tokens = {\n            \"&lt;|unk|&gt;\": 0,      # unknown character\n            \"&lt;|endoftext|&gt;\": 1, # end-of-text marker\n        }\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        \"\"\"Add all unique characters from text to the vocabulary.\"\"\"\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        \"\"\"Convert text to a list of token IDs.\"\"\"\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        \"\"\"Convert a list of token IDs back to text.\"\"\"\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\n# Build the tokenizer on a corpus\ncorpus = \"\"\"Natural language processing (NLP) is a subfield of\ncomputer science and artificial intelligence. It focuses on\nthe interaction between computers and human language.\"\"\"\n\ntokenizer = CharTokenizer(corpus)\nprint(f\"Vocabulary size: {tokenizer.vocab_size}\")\nprint(f\"Full vocabulary:\")\nfor token, idx in sorted(tokenizer.char_to_id.items(), key=lambda x: x[1]):\n    display = repr(token) if len(token) == 1 else token\n    print(f\"  {idx:3d}: {display}\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout tip\">\n    <div class=\"callout-title\">&#x1F4CC; Συμβουλή &mdash; Ειδικά tokens</div>\n    <p>\n      Τα ειδικά tokens όπως <code>&lt;|unk|&gt;</code> και <code>&lt;|endoftext|&gt;</code> παίζουν\n      κρίσιμους ρόλους. Το <strong>token αγνώστου</strong> χειρίζεται χαρακτήρες που δεν\n      εμφανίστηκαν ποτέ κατά τη δημιουργία του λεξιλογίου. Το <strong>token τέλους κειμένου</strong>\n      λέει στο μοντέλο πού τελειώνει ένα έγγραφο και πού αρχίζει ένα άλλο &mdash; απαραίτητο\n      για εκπαίδευση σε πολλαπλά έγγραφα.\n    </p>\n  </div>\n\n  <h3>Βήμα 3: Κωδικοποίηση και αποκωδικοποίηση σε δράση</h3>\n  <p>\n    Ας κωδικοποιήσουμε μια πρόταση, ας επιθεωρήσουμε τα ID των tokens και ας τα\n    αποκωδικοποιήσουμε πίσω για να επαληθεύσουμε ότι ο tokenizer μας λειτουργεί σωστά.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\nclass CharTokenizer:\n    def __init__(self, corpus=None):\n        self.special_tokens = {\"&lt;|unk|&gt;\": 0, \"&lt;|endoftext|&gt;\": 1}\n        self.char_to_id = dict(self.special_tokens)\n        self.id_to_char = {v: k for k, v in self.special_tokens.items()}\n        self.next_id = len(self.special_tokens)\n        if corpus:\n            self.build_vocab(corpus)\n\n    def build_vocab(self, text):\n        for ch in sorted(set(text)):\n            if ch not in self.char_to_id:\n                self.char_to_id[ch] = self.next_id\n                self.id_to_char[self.next_id] = ch\n                self.next_id += 1\n\n    @property\n    def vocab_size(self):\n        return len(self.char_to_id)\n\n    def encode(self, text):\n        unk_id = self.special_tokens[\"&lt;|unk|&gt;\"]\n        return [self.char_to_id.get(ch, unk_id) for ch in text]\n\n    def decode(self, ids):\n        return \"\".join(self.id_to_char.get(i, \"?\") for i in ids)\n\n\ncorpus = \"The quick brown fox jumps over the lazy dog.\"\ntokenizer = CharTokenizer(corpus)\n\n# Encode a sentence\nsentence = \"The fox is quick.\"\ntoken_ids = tokenizer.encode(sentence)\nprint(f\"Original:  '{sentence}'\")\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Sequence length: {len(token_ids)} tokens\")\n\n# Decode back\ndecoded = tokenizer.decode(token_ids)\nprint(f\"Decoded:   '{decoded}'\")\nprint(f\"Round-trip matches: {sentence == decoded}\")\n\n# Show what happens with unknown characters\nsentence2 = \"Hello! 123\"\ntoken_ids2 = tokenizer.encode(sentence2)\ndecoded2 = tokenizer.decode(token_ids2)\nprint(f\"\\\nWith unknowns: '{sentence2}'\")\nprint(f\"Token IDs: {token_ids2}\")\nprint(f\"Decoded:   '{decoded2}'  (0 = unknown)\")\n\n# End-of-text usage\ndoc1 = tokenizer.encode(\"Hello\")\neot = [tokenizer.special_tokens[\"&lt;|endoftext|&gt;\"]]\ndoc2 = tokenizer.encode(\"World\")\ncombined = doc1 + eot + doc2\nprint(f\"\\\nTwo documents combined: {combined}\")\nprint(f\"Decoded: '{tokenizer.decode(combined)}'\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <h2>Μήκος ακολουθίας: Το πρόβλημα με τους χαρακτήρες</h2>\n  <p>\n    Η tokenization σε επίπεδο χαρακτήρα είναι απλή και κομψή, αλλά έχει ένα κρίσιμο\n    μειονέκτημα: οι ακολουθίες γίνονται <strong>πολύ μακριές</strong>. Μια μόνο λέξη όπως\n    \"tokenization\" γίνεται 12 tokens. Αυτό έχει σημασία γιατί οι Transformers έχουν σταθερό\n    <em>παράθυρο context</em> και ο υπολογισμός προσοχής κλιμακώνεται τετραγωνικά με το\n    μήκος ακολουθίας.\n  </p>\n\n  <div class=\"code-block\">\n    <div class=\"code-header\">\n      <span class=\"code-lang\">Python</span>\n      <div class=\"code-actions\">\n        <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n      </div>\n    </div>\n    <pre><code class=\"language-python\">\n# Compare tokenization approaches on the same text\ntext = \"The Transformer architecture revolutionized natural language processing.\"\n\n# Character-level: each char is a token\nchar_tokens = list(text)\n\n# Word-level: split on whitespace\nword_tokens = text.split()\n\n# Simulated subword-level (rough approximation)\n# Real BPE would do this properly - we will build that in Lecture 8\nsubword_tokens = [\"The\", \" Trans\", \"former\", \" architecture\",\n                  \" revolution\", \"ized\", \" natural\",\n                  \" language\", \" processing\", \".\"]\n\nprint(\"=== Sequence Length Comparison ===\")\nprint(f\"Text: '{text}'\")\nprint(f\"Text length: {len(text)} characters\")\nprint()\nprint(f\"Character tokens: {len(char_tokens):3d} tokens\")\nprint(f\"  First 20: {char_tokens[:20]}\")\nprint()\nprint(f\"Word tokens:      {len(word_tokens):3d} tokens\")\nprint(f\"  {word_tokens}\")\nprint()\nprint(f\"Subword tokens:   {len(subword_tokens):3d} tokens\")\nprint(f\"  {subword_tokens}\")\n\nprint()\nprint(\"=== Why This Matters ===\")\ncontext_window = 1024\nchar_words = context_window // (len(text) / len(word_tokens))\nsub_words = context_window // (len(text) / len(subword_tokens))\nprint(f\"With a {context_window}-token context window:\")\nprint(f\"  Character-level can fit ~{context_window} chars\")\nprint(f\"    (~{context_window // 5} words, since avg word ~5 chars)\")\nprint(f\"  Word-level can fit ~{context_window} words\")\nprint(f\"  Subword-level can fit ~{int(sub_words)} words\")\n    </code></pre>\n    <div class=\"sandbox-output\"></div>\n  </div>\n\n  <div class=\"callout warning\">\n    <div class=\"callout-title\">&#x26A0;&#xFE0F; Περιορισμοί tokenization σε επίπεδο χαρακτήρα</div>\n    <p>\n      Ενώ η tokenization σε επίπεδο χαρακτήρα είναι εξαιρετική για μάθηση, τα LLM παραγωγής\n      σχεδόν ποτέ δεν τη χρησιμοποιούν. Δες γιατί:\n    </p>\n    <ul>\n      <li><strong>Μακριές ακολουθίες:</strong> Ένα έγγραφο 1000 λέξεων γίνεται ~5000 tokens χαρακτήρων vs ~1300 tokens υπολέξεων.</li>\n      <li><strong>Χωρίς σημασιολογική σημασία:</strong> Το γράμμα \"a\" δεν φέρει νόημα μόνο του. Το μοντέλο πρέπει να μάθει να συνθέτει χαρακτήρες σε νοηματικές μονάδες.</li>\n      <li><strong>Υπολογιστικό κόστος:</strong> Η προσοχή είναι O(n&sup2;) στο μήκος ακολουθίας, οπότε 4x μακρύτερες ακολουθίες σημαίνουν 16x περισσότερους υπολογισμούς.</li>\n      <li><strong>Δυσκολότερη μάθηση:</strong> Το μοντέλο χρειάζεται περισσότερα στρώματα και εκπαίδευση για να μάθει τη σύνθεση χαρακτήρων.</li>\n    </ul>\n  </div>\n\n  <h2>Σύγκριση: Στρατηγικές tokenization</h2>\n  <table class=\"comparison-table\">\n    <thead>\n      <tr>\n        <th>Στρατηγική</th>\n        <th>Μέγεθος λεξιλογίου</th>\n        <th>Μήκος ακολουθίας</th>\n        <th>Άγνωστες λέξεις;</th>\n        <th>Χρησιμοποιείται από</th>\n      </tr>\n    </thead>\n    <tbody>\n      <tr>\n        <td>Επίπεδο χαρακτήρα</td>\n        <td>~50&ndash;100</td>\n        <td>Πολύ μακρύ</td>\n        <td>Σχεδόν καμία</td>\n        <td>Ορισμένα ερευνητικά μοντέλα</td>\n      </tr>\n      <tr>\n        <td>Επίπεδο λέξης</td>\n        <td>50,000&ndash;200,000+</td>\n        <td>Κοντό</td>\n        <td>Πολλές (πρόβλημα OOV)</td>\n        <td>Παλαιότερα μοντέλα NLP</td>\n      </tr>\n      <tr>\n        <td>Υπολέξη (BPE)</td>\n        <td>30,000&ndash;100,000</td>\n        <td>Μέτριο</td>\n        <td>Πολύ σπάνιες</td>\n        <td>GPT-2/3/4, LLaMA</td>\n      </tr>\n      <tr>\n        <td>SentencePiece</td>\n        <td>32,000&ndash;128,000</td>\n        <td>Μέτριο</td>\n        <td>Πολύ σπάνιες</td>\n        <td>T5, LLaMA, Gemma</td>\n      </tr>\n    </tbody>\n  </table>\n\n  <h2>Η πορεία μπροστά: Tokenization υπολέξεων</h2>\n  <p>\n    Η ιδανική ισορροπία στη σύγχρονη NLP είναι η <strong>tokenization υπολέξεων</strong>, που\n    εξισορροπεί μέγεθος λεξιλογίου, μήκος ακολουθίας και κάλυψη. Η πιο δημοφιλής προσέγγιση\n    είναι η <strong>Byte Pair Encoding (BPE)</strong>, την οποία θα χτίσουμε από την αρχή\n    στην επόμενη διάλεξη.\n  </p>\n  <p>\n    Η BPE ξεκινά με μεμονωμένους χαρακτήρες (όπως ο CharTokenizer μας) και επαναληπτικά\n    συγχωνεύει τα πιο συχνά ζεύγη σε νέα tokens. Κοινές λέξεις όπως \"the\" γίνονται μεμονωμένα\n    tokens, ενώ σπάνιες λέξεις όπως \"tokenization\" μπορεί να χωριστούν σε \"token\" + \"ization\".\n    Αυτό μας δίνει τα καλύτερα και των δύο κόσμων.\n  </p>\n\n  <div class=\"callout info\">\n    <div class=\"callout-title\">&#x1F4D6; Βασικά συμπεράσματα</div>\n    <ul>\n      <li>Η tokenization μετατρέπει κείμενο σε ακέραιους ώστε τα νευρωνικά δίκτυα να μπορούν να το επεξεργαστούν.</li>\n      <li>Ένας tokenizer χρειάζεται μεθόδους <code>encode()</code> (κείμενο &rarr; ID) και <code>decode()</code> (ID &rarr; κείμενο).</li>\n      <li>Η tokenization σε επίπεδο χαρακτήρα είναι απλή αλλά παράγει μακριές ακολουθίες.</li>\n      <li>Τα ειδικά tokens (<code>&lt;|unk|&gt;</code>, <code>&lt;|endoftext|&gt;</code>) χειρίζονται ακραίες περιπτώσεις.</li>\n      <li>Τα σύγχρονα LLM χρησιμοποιούν tokenization υπολέξεων (BPE) για τον καλύτερο συμβιβασμό.</li>\n    </ul>\n  </div>\n\n  <div class=\"exercise-box\">\n    <h3>&#x1F3CB;&#xFE0F; Άσκηση: Επέκτεινε τον CharTokenizer</h3>\n    <ol>\n      <li>Πρόσθεσε μια μέθοδο <code>encode_batch(texts)</code> που δέχεται μια λίστα κειμένων και επιστρέφει μια λίστα από λίστες ID tokens, χωρισμένες με <code>&lt;|endoftext|&gt;</code>.</li>\n      <li>Πρόσθεσε ένα ζεύγος μεθόδων <code>save_vocab(filepath)</code> και <code>load_vocab(filepath)</code>.</li>\n      <li>Δοκίμασε να δημιουργήσεις ένα λεξιλόγιο από ένα μεγαλύτερο κείμενο (π.χ. μια παράγραφο από τη Wikipedia) και δες πώς αλλάζει το μέγεθος λεξιλογίου.</li>\n    </ol>\n  </div>"}, "codeBlocks": []}