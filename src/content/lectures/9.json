{"id": "9", "title": {"en": "Input-Target Pairs & DataLoader", "ro": "Perechi intrare-țintă & DataLoader", "el": "Ζεύγη εισόδου-στόχου & DataLoader", "de": "Eingabe-Ziel-Paare & DataLoader", "it": "Coppie Input-Target & DataLoader", "fr": "Paires entrée-cible & DataLoader", "ar": "أزواج المُدخل-الهدف و DataLoader"}, "content": {"en": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">LECTURE 9 <span class=\"stage-badge\">Stage 2: Tokenization &amp; Data</span></div>\n <h1 class=\"lecture-title\">Input-Target Pairs &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">From raw text to training batches &mdash; building the data pipeline</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>45 min read</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Hands-on coding</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"m12 14 4-4\"/><path d=\"M3.34 19a10 10 0 1 1 17.32 0\"/></svg>Intermediate</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>How LLMs Learn: Next-Token Prediction</h2>\n <p>\n At its core, training a language model is remarkably simple in concept: given a sequence\n of tokens, <strong>predict the next one</strong>. The model sees \"The cat sat on the\"\n and learns to predict \"mat\" (or \"floor\" or \"rug\"). It does this for billions of\n sequences, gradually learning the patterns of language.\n </p>\n <p>\n But how exactly do we create training data from raw text? We cannot just feed the model\n an entire book and say \"learn this.\" We need to break the text into manageable\n <strong>input-target pairs</strong> where every input is a sequence of tokens and the\n target is the very next token the model should predict.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Analogy &mdash; The Reading Comprehension Exercise</div>\n <p>\n Imagine a reading exercise where you cover the next word with your finger and try to\n guess it. You read \"The weather today is\" and predict \"sunny\" or \"cold\". Then you\n slide your finger forward: \"weather today is sunny\" and predict \"and\" or \"but\".\n Each position of your finger creates one training example. That is exactly what\n our data pipeline does &mdash; it slides a window across the text, creating thousands\n of prediction exercises for the model.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Creating Input-Target Pairs</h2>\n <p>\n Given a sequence of token IDs, we create input-target pairs by shifting the sequence\n by one position. The input is tokens <code>[0, 1, ..., n-1]</code> and the target is\n tokens <code>[1, 2, ..., n]</code>. Each position in the input maps to the\n <strong>next</strong> token as its target.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>The Context Window (Block Size)</h2>\n <p>\n In the example above, the context grows with each position &mdash; from 1 token up to\n <code>n-1</code> tokens. But Transformer models have a <strong>fixed context window</strong>\n (also called <code>max_length</code> or <code>block_size</code>). GPT-2 uses 1024 tokens,\n GPT-3 uses 2048, and GPT-4 uses up to 128K. We need to limit our input sequences\n to this fixed size.\n </p>\n <p>\n This is where the <strong>sliding window</strong> approach comes in. We slide a\n fixed-size window across the tokenized text, creating input-target pairs of uniform\n length.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Context Window Sizes in Practice</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Model</th><th>Context Window</th><th>Approx. Pages of Text</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1,024 tokens</td><td>~1.5 pages</td></tr>\n <tr><td>GPT-3</td><td>2,048 tokens</td><td>~3 pages</td></tr>\n <tr><td>LLaMA 2</td><td>4,096 tokens</td><td>~6 pages</td></tr>\n <tr><td>GPT-4</td><td>8,192&ndash;128K tokens</td><td>~12&ndash;200 pages</td></tr>\n <tr><td>Claude 3</td><td>200K tokens</td><td>~300 pages</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Sliding Window with Different Strides</h3>\n <p>\n The <strong>stride</strong> controls how far the window moves between consecutive examples.\n A stride of 1 means maximum overlap (most data-efficient), while a stride equal to the\n block size means no overlap (fastest training).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#059669\"><path d=\"M22 11.08V12a10 10 0 1 1-5.93-9.14\"/><path d=\"m9 11 3 3L22 4\"/></svg>Tip &mdash; Choosing Stride</div>\n <p>\n In practice, most LLM training uses <strong>stride = block_size</strong> (no overlap)\n for efficiency. With billions of tokens in the training corpus, there is already enough\n data diversity. Overlap is more useful when your corpus is small and you want to\n squeeze out every possible training example. Sebastian Raschka&rsquo;s book uses\n stride = 1 for teaching purposes and stride = block_size for the final model.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z\"/><path d=\"m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z\"/><path d=\"M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0\"/><path d=\"M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5\"/></svg>Building a Dataset Class</h2>\n <p>\n In PyTorch, data is organized using <code>Dataset</code> objects that implement\n <code>__len__</code> (how many examples) and <code>__getitem__</code> (get one example\n by index). Let us build one for our LLM training data. We will keep it pure Python\n here (no PyTorch dependency needed to understand the concept).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num &gt;= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>The Complete Data Pipeline</h2>\n <p>\n Let us put it all together: from raw text all the way to training batches.\n This is the full pipeline that an LLM sees before any neural network computation begins.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Data Pipeline Overview</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num &gt;= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Why Shuffling Matters</div>\n <p>\n Without shuffling, the DataLoader feeds examples in sequential order. This means\n consecutive batches contain highly correlated data (overlapping text windows), which\n can hurt training stability. Shuffling ensures each batch contains diverse examples\n from different parts of the corpus, helping the model learn more effectively. However,\n note that we shuffle <em>examples</em> (windows), not the tokens within each window &mdash;\n the internal order of each sequence must be preserved.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>From Pure Python to PyTorch</h2>\n <p>\n The classes we built above mirror PyTorch&rsquo;s API exactly. When you move to real\n training with GPUs, you simply:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Inherit from torch.utils.data.Dataset</h3>\n <p>Replace our plain class with <code>class LLMDataset(torch.utils.data.Dataset)</code> and convert lists to <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. Use torch.utils.data.DataLoader</h3>\n <p>PyTorch&rsquo;s DataLoader adds multi-worker data loading, GPU pinned memory, and automatic batching with <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Add Token Embeddings</h3>\n <p>Each token ID gets looked up in an embedding table (<code>nn.Embedding</code>) to produce a dense vector &mdash; the first layer of the Transformer.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Add Positional Encodings</h3>\n <p>Since Transformers have no built-in notion of order, we add position information to each token embedding (covered in a later lecture).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>Key Takeaways</div>\n <ul>\n <li>LLMs learn by predicting the next token. Each position in a sequence is a training example.</li>\n <li>Input-target pairs are created by shifting a token sequence by one position.</li>\n <li>The <strong>context window</strong> (block_size) limits how many tokens the model can see at once.</li>\n <li><strong>Stride</strong> controls the overlap between consecutive training examples.</li>\n <li>The Dataset class organizes data; the DataLoader handles batching and shuffling.</li>\n <li>This same pipeline (tokenize &rarr; sliding window &rarr; batch) is used by GPT-2, LLaMA, and other production LLMs.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Exercise: Build Your Own Data Pipeline</h3>\n <ol>\n <li>Take a paragraph of text, tokenize it with the <code>CharTokenizer</code> from Lecture 7, and create a Dataset with block_size=16 and stride=8.</li>\n <li>Implement a <code>drop_last</code> parameter in SimpleDataLoader that discards the final batch if it is smaller than <code>batch_size</code>. This is important for training because all batches should have the same shape.</li>\n <li>Calculate how many training examples you get from a 1-million-token corpus with block_size=1024 and stride=1024. How does this change with stride=512?</li>\n <li>Bonus: Add a method to DataLoader that reports statistics &mdash; total tokens processed, unique tokens seen, and estimated epochs needed for a target number of training steps.</li>\n </ol>\n </div>", "ro": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">LECȚIA 9 <span class=\"stage-badge\">Etapa 2: Tokenizare &amp; Date</span></div>\n <h1 class=\"lecture-title\">Perechi intrare-țintă &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">De la text brut la loturi de antrenament &mdash; construirea pipeline-ului de date</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>45 min de citit</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Programare practică</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Intermediar</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>Cum învață LLM-urile: Predicția următorului token</h2>\n <p>\n La bază, antrenamentul unui model de limbaj este remarcabil de simplu ca și concept: dată\n fiind o secvență de tokeni, <strong>prezice următorul</strong>. Modelul vede \"The cat sat on the\"\n și învață să prezică \"mat\" (sau \"floor\" sau \"rug\"). Face acest lucru pentru miliarde de\n secvențe, învățând treptat pattern-urile limbajului.\n </p>\n <p>\n Dar cum creăm exact date de antrenament din text brut? Nu putem pur și simplu să dăm modelului\n o carte întreagă și să spunem \"învață asta.\" Trebuie să împărțim textul în\n <strong>perechi intrare-țintă</strong> gestionabile, unde fiecare intrare este o secvență de\n tokeni și ținta este chiar următorul token pe care modelul trebuie să îl prezică.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Analogie &mdash; Exercițiul de înțelegere a lecturii</div>\n <p>\n Imaginează-ți un exercițiu de lectură în care acoperi următorul cuvânt cu degetul și\n încerci să îl ghicești. Citești \"The weather today is\" și prezici \"sunny\" sau \"cold\".\n Apoi îți muți degetul înainte: \"weather today is sunny\" și prezici \"and\" sau \"but\".\n Fiecare poziție a degetului tău creează un exemplu de antrenament. Exact asta face\n pipeline-ul nostru de date &mdash; glisează o fereastră peste text, creând mii de\n exerciții de predicție pentru model.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Crearea perechilor intrare-țintă</h2>\n <p>\n Dată fiind o secvență de ID-uri de tokeni, creăm perechi intrare-țintă prin deplasarea\n secvenței cu o poziție. Intrarea este tokenii <code>[0, 1, ..., n-1]</code> și ținta este\n tokenii <code>[1, 2, ..., n]</code>. Fiecare poziție din intrare se mapează la\n <strong>următorul</strong> token ca țintă a sa.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>Fereastra de context (dimensiunea blocului)</h2>\n <p>\n În exemplul de mai sus, contextul crește cu fiecare poziție &mdash; de la 1 token până la\n <code>n-1</code> tokeni. Dar modelele Transformer au o <strong>fereastră de context fixă</strong>\n (numită și <code>max_length</code> sau <code>block_size</code>). GPT-2 folosește 1024 de tokeni,\n GPT-3 folosește 2048, iar GPT-4 folosește până la 128K. Trebuie să limităm secvențele noastre\n de intrare la această dimensiune fixă.\n </p>\n <p>\n Aici intervine abordarea cu <strong>fereastră glisantă</strong>. Glisăm o fereastră de\n dimensiune fixă peste textul tokenizat, creând perechi intrare-țintă de lungime uniformă.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Dimensiuni ale ferestrei de context în practică</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Model</th><th>Fereastră de context</th><th>Aprox. pagini de text</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1.024 tokeni</td><td>~1,5 pagini</td></tr>\n <tr><td>GPT-3</td><td>2.048 tokeni</td><td>~3 pagini</td></tr>\n <tr><td>LLaMA 2</td><td>4.096 tokeni</td><td>~6 pagini</td></tr>\n <tr><td>GPT-4</td><td>8.192&ndash;128K tokeni</td><td>~12&ndash;200 pagini</td></tr>\n <tr><td>Claude 3</td><td>200K tokeni</td><td>~300 pagini</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Fereastră glisantă cu diferite pasuri</h3>\n <p>\n <strong>Pasul</strong> controlează cât de departe se mișcă fereastra între exemple consecutive.\n Un pas de 1 înseamnă suprapunere maximă (cea mai eficientă din punct de vedere al datelor),\n în timp ce un pas egal cu dimensiunea blocului înseamnă fără suprapunere (antrenament cel\n mai rapid).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Sfat &mdash; Alegerea pasului</div>\n <p>\n În practică, majoritatea antrenamentelor LLM folosesc <strong>pas = block_size</strong>\n (fără suprapunere) pentru eficiență. Cu miliarde de tokeni în corpusul de antrenament,\n există deja suficientă diversitate de date. Suprapunerea este mai utilă când corpusul tău\n este mic și dorești să extragi fiecare exemplu de antrenament posibil. Cartea lui\n Sebastian Raschka folosește pas = 1 în scopuri didactice și pas = block_size pentru\n modelul final.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Construirea unei clase Dataset</h2>\n <p>\n În PyTorch, datele sunt organizate folosind obiecte <code>Dataset</code> care implementează\n <code>__len__</code> (câte exemple) și <code>__getitem__</code> (obține un exemplu\n după index). Să construim unul pentru datele noastre de antrenament LLM. Vom păstra totul\n în Python pur aici (nu este necesară dependența PyTorch pentru a înțelege conceptul).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num >= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Pipeline-ul complet de date</h2>\n <p>\n Să punem totul cap la cap: de la text brut până la loturi de antrenament.\n Acesta este pipeline-ul complet pe care îl vede un LLM înainte de a începe vreo\n computație cu rețeaua neuronală.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Prezentare generală a pipeline-ului de date</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num >= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>De ce contează amestecarea</div>\n <p>\n Fără amestecare, DataLoader-ul furnizează exemplele în ordine secvențială. Aceasta înseamnă\n că loturile consecutive conțin date puternic corelate (ferestre de text suprapuse), ceea ce\n poate afecta stabilitatea antrenamentului. Amestecarea asigură că fiecare lot conține exemple\n diverse din diferite părți ale corpusului, ajutând modelul să învețe mai eficient. Totuși,\n rețineți că amestecăm <em>exemplele</em> (ferestrele), nu tokenii din interiorul fiecărei\n ferestre &mdash; ordinea internă a fiecărei secvențe trebuie păstrată.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>De la Python pur la PyTorch</h2>\n <p>\n Clasele pe care le-am construit mai sus reflectă exact API-ul PyTorch. Când treci la\n antrenamentul real cu GPU-uri, pur și simplu:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Moștenește din torch.utils.data.Dataset</h3>\n <p>Înlocuiește clasa noastră simplă cu <code>class LLMDataset(torch.utils.data.Dataset)</code> și convertește listele în <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. Folosește torch.utils.data.DataLoader</h3>\n <p>DataLoader-ul PyTorch adaugă încărcare de date cu mai mulți workers, memorie pinned pentru GPU și grupare automată cu <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Adaugă embedding-uri de tokeni</h3>\n <p>Fiecare ID de token este căutat într-un tabel de embedding (<code>nn.Embedding</code>) pentru a produce un vector dens &mdash; primul strat al Transformer-ului.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Adaugă codificări poziționale</h3>\n <p>Deoarece Transformer-ele nu au o noțiune încorporată de ordine, adăugăm informații de poziție la fiecare embedding de token (acoperit într-o lecție ulterioară).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Concluzii cheie</div>\n <ul>\n <li>LLM-urile învață prin predicția următorului token. Fiecare poziție într-o secvență este un exemplu de antrenament.</li>\n <li>Perechile intrare-țintă sunt create prin deplasarea unei secvențe de tokeni cu o poziție.</li>\n <li><strong>Fereastra de context</strong> (block_size) limitează câți tokeni poate vedea modelul simultan.</li>\n <li><strong>Pasul</strong> controlează suprapunerea între exemplele consecutive de antrenament.</li>\n <li>Clasa Dataset organizează datele; DataLoader-ul gestionează gruparea și amestecarea.</li>\n <li>Același pipeline (tokenizare &rarr; fereastră glisantă &rarr; lot) este folosit de GPT-2, LLaMA și alte LLM-uri de producție.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Exercițiu: Construiește propriul tău pipeline de date</h3>\n <ol>\n <li>Ia un paragraf de text, tokenizează-l cu <code>CharTokenizer</code> din Lecția 7 și creează un Dataset cu block_size=16 și stride=8.</li>\n <li>Implementează un parametru <code>drop_last</code> în SimpleDataLoader care renunță la ultimul lot dacă este mai mic decât <code>batch_size</code>. Acest lucru este important pentru antrenament deoarece toate loturile trebuie să aibă aceeași formă.</li>\n <li>Calculează câte exemple de antrenament obții dintr-un corpus de 1 milion de tokeni cu block_size=1024 și stride=1024. Cum se schimbă acest lucru cu stride=512?</li>\n <li>Bonus: Adaugă o metodă DataLoader-ului care raportează statistici &mdash; total tokeni procesați, tokeni unici văzuți și epoci estimate necesare pentru un număr țintă de pași de antrenament.</li>\n </ol>\n </div>", "el": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">ΔΙΑΛΕΞΗ 9 <span class=\"stage-badge\">Στάδιο 2: Tokenization &amp; Δεδομένα</span></div>\n <h1 class=\"lecture-title\">Ζεύγη εισόδου-στόχου &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">Από ακατέργαστο κείμενο σε παρτίδες εκπαίδευσης &mdash; χτίζοντας τον αγωγό δεδομένων</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>45 λεπτά ανάγνωση</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Πρακτική κωδικοποίηση</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Ενδιάμεσο</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>Πώς μαθαίνουν τα LLM: Πρόβλεψη επόμενου token</h2>\n <p>\n Στον πυρήνα της, η εκπαίδευση ενός γλωσσικού μοντέλου είναι εκπληκτικά απλή ως ιδέα:\n δεδομένης μιας ακολουθίας tokens, <strong>πρόβλεψε το επόμενο</strong>. Το μοντέλο βλέπει\n \"The cat sat on the\" και μαθαίνει να προβλέπει \"mat\" (ή \"floor\" ή \"rug\"). Το κάνει αυτό\n για δισεκατομμύρια ακολουθίες, μαθαίνοντας σταδιακά τα μοτίβα της γλώσσας.\n </p>\n <p>\n Αλλά πώς ακριβώς δημιουργούμε δεδομένα εκπαίδευσης από ακατέργαστο κείμενο; Δεν μπορούμε\n απλά να δώσουμε στο μοντέλο ένα ολόκληρο βιβλίο και να πούμε \"μάθε αυτό.\" Πρέπει να\n σπάσουμε το κείμενο σε διαχειρίσιμα <strong>ζεύγη εισόδου-στόχου</strong> όπου κάθε\n είσοδος είναι μια ακολουθία tokens και ο στόχος είναι ακριβώς το επόμενο token που πρέπει\n να προβλέψει το μοντέλο.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Αναλογία &mdash; Η άσκηση κατανόησης ανάγνωσης</div>\n <p>\n Φαντάσου μια άσκηση ανάγνωσης όπου καλύπτεις την επόμενη λέξη με το δάχτυλό σου και\n προσπαθείς να τη μαντέψεις. Διαβάζεις \"The weather today is\" και προβλέπεις \"sunny\"\n ή \"cold\". Μετά σύρεις το δάχτυλό σου μπροστά: \"weather today is sunny\" και προβλέπεις\n \"and\" ή \"but\". Κάθε θέση του δαχτύλου σου δημιουργεί ένα παράδειγμα εκπαίδευσης. Αυτό\n ακριβώς κάνει ο αγωγός δεδομένων μας &mdash; σύρει ένα παράθυρο κατά μήκος του κειμένου,\n δημιουργώντας χιλιάδες ασκήσεις πρόβλεψης για το μοντέλο.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Δημιουργία ζευγών εισόδου-στόχου</h2>\n <p>\n Δεδομένης μιας ακολουθίας ID tokens, δημιουργούμε ζεύγη εισόδου-στόχου μετατοπίζοντας\n την ακολουθία κατά μία θέση. Η είσοδος είναι τα tokens <code>[0, 1, ..., n-1]</code> και\n ο στόχος είναι τα tokens <code>[1, 2, ..., n]</code>. Κάθε θέση στην είσοδο αντιστοιχίζεται\n στο <strong>επόμενο</strong> token ως στόχο.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>Το παράθυρο context (μέγεθος μπλοκ)</h2>\n <p>\n Στο παραπάνω παράδειγμα, το context αυξάνεται με κάθε θέση &mdash; από 1 token μέχρι\n <code>n-1</code> tokens. Αλλά τα μοντέλα Transformer έχουν <strong>σταθερό παράθυρο\n context</strong> (που ονομάζεται επίσης <code>max_length</code> ή <code>block_size</code>).\n Το GPT-2 χρησιμοποιεί 1024 tokens, το GPT-3 χρησιμοποιεί 2048 και το GPT-4 χρησιμοποιεί\n μέχρι 128K. Πρέπει να περιορίσουμε τις ακολουθίες εισόδου μας σε αυτό το σταθερό μέγεθος.\n </p>\n <p>\n Εδώ μπαίνει η προσέγγιση <strong>συρόμενου παραθύρου</strong>. Σύρουμε ένα παράθυρο\n σταθερού μεγέθους κατά μήκος του tokenized κειμένου, δημιουργώντας ζεύγη εισόδου-στόχου\n ομοιόμορφου μήκους.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Μεγέθη παραθύρου context στην πράξη</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Μοντέλο</th><th>Παράθυρο context</th><th>Περίπου σελίδες κειμένου</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1.024 tokens</td><td>~1,5 σελίδες</td></tr>\n <tr><td>GPT-3</td><td>2.048 tokens</td><td>~3 σελίδες</td></tr>\n <tr><td>LLaMA 2</td><td>4.096 tokens</td><td>~6 σελίδες</td></tr>\n <tr><td>GPT-4</td><td>8.192&ndash;128K tokens</td><td>~12&ndash;200 σελίδες</td></tr>\n <tr><td>Claude 3</td><td>200K tokens</td><td>~300 σελίδες</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Συρόμενο παράθυρο με διαφορετικά βήματα</h3>\n <p>\n Το <strong>βήμα (stride)</strong> ελέγχει πόσο μακριά μετακινείται το παράθυρο μεταξύ\n διαδοχικών παραδειγμάτων. Βήμα 1 σημαίνει μέγιστη επικάλυψη (πιο αποδοτική ως προς τα\n δεδομένα), ενώ βήμα ίσο με το μέγεθος μπλοκ σημαίνει χωρίς επικάλυψη (ταχύτερη εκπαίδευση).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Συμβουλή &mdash; Επιλογή βήματος</div>\n <p>\n Στην πράξη, οι περισσότερες εκπαιδεύσεις LLM χρησιμοποιούν <strong>βήμα = block_size</strong>\n (χωρίς επικάλυψη) για αποδοτικότητα. Με δισεκατομμύρια tokens στο corpus εκπαίδευσης,\n υπάρχει ήδη αρκετή ποικιλία δεδομένων. Η επικάλυψη είναι πιο χρήσιμη όταν το corpus\n σου είναι μικρό και θέλεις να αξιοποιήσεις κάθε δυνατό παράδειγμα εκπαίδευσης. Το βιβλίο\n του Sebastian Raschka χρησιμοποιεί βήμα = 1 για εκπαιδευτικούς σκοπούς και βήμα =\n block_size για το τελικό μοντέλο.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Κατασκευή κλάσης Dataset</h2>\n <p>\n Στο PyTorch, τα δεδομένα οργανώνονται χρησιμοποιώντας αντικείμενα <code>Dataset</code>\n που υλοποιούν <code>__len__</code> (πόσα παραδείγματα) και <code>__getitem__</code>\n (λάβε ένα παράδειγμα κατά δείκτη). Ας χτίσουμε ένα για τα δεδομένα εκπαίδευσης LLM.\n Θα το κρατήσουμε σε καθαρή Python εδώ (δεν χρειάζεται εξάρτηση PyTorch για να\n κατανοήσουμε την ιδέα).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num >= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Ο πλήρης αγωγός δεδομένων</h2>\n <p>\n Ας τα βάλουμε όλα μαζί: από ακατέργαστο κείμενο μέχρι παρτίδες εκπαίδευσης.\n Αυτός είναι ο πλήρης αγωγός που βλέπει ένα LLM πριν ξεκινήσει οποιοσδήποτε\n υπολογισμός νευρωνικού δικτύου.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Επισκόπηση αγωγού δεδομένων</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num >= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Γιατί η ανακάτεμα έχει σημασία</div>\n <p>\n Χωρίς ανακάτεμα, ο DataLoader τροφοδοτεί τα παραδείγματα σε σειριακή σειρά. Αυτό σημαίνει\n ότι διαδοχικές παρτίδες περιέχουν υψηλά συσχετισμένα δεδομένα (επικαλυπτόμενα παράθυρα\n κειμένου), κάτι που μπορεί να βλάψει τη σταθερότητα εκπαίδευσης. Η ανακάτεμα εξασφαλίζει\n ότι κάθε παρτίδα περιέχει ποικίλα παραδείγματα από διαφορετικά μέρη του corpus,\n βοηθώντας το μοντέλο να μάθει πιο αποτελεσματικά. Ωστόσο, σημείωσε ότι ανακατεύουμε\n <em>παραδείγματα</em> (παράθυρα), όχι τα tokens μέσα σε κάθε παράθυρο &mdash; η εσωτερική\n σειρά κάθε ακολουθίας πρέπει να διατηρηθεί.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Από καθαρή Python σε PyTorch</h2>\n <p>\n Οι κλάσεις που χτίσαμε παραπάνω αντικατοπτρίζουν ακριβώς το API του PyTorch. Όταν\n μεταβείς σε πραγματική εκπαίδευση με GPU, απλά:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Κληρονόμησε από torch.utils.data.Dataset</h3>\n <p>Αντικατέστησε την απλή κλάση μας με <code>class LLMDataset(torch.utils.data.Dataset)</code> και μετέτρεψε τις λίστες σε <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. Χρησιμοποίησε torch.utils.data.DataLoader</h3>\n <p>Ο DataLoader του PyTorch προσθέτει φόρτωση δεδομένων με πολλούς workers, pinned μνήμη GPU και αυτόματη δημιουργία παρτίδων με <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Πρόσθεσε embeddings tokens</h3>\n <p>Κάθε ID token αναζητείται σε έναν πίνακα embedding (<code>nn.Embedding</code>) για να παράγει ένα πυκνό διάνυσμα &mdash; το πρώτο στρώμα του Transformer.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Πρόσθεσε κωδικοποιήσεις θέσης</h3>\n <p>Καθώς οι Transformers δεν έχουν ενσωματωμένη αίσθηση σειράς, προσθέτουμε πληροφορία θέσης σε κάθε embedding token (καλύπτεται σε μεταγενέστερη διάλεξη).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Βασικά συμπεράσματα</div>\n <ul>\n <li>Τα LLM μαθαίνουν μέσω πρόβλεψης του επόμενου token. Κάθε θέση σε μια ακολουθία είναι ένα παράδειγμα εκπαίδευσης.</li>\n <li>Τα ζεύγη εισόδου-στόχου δημιουργούνται μετατοπίζοντας μια ακολουθία tokens κατά μία θέση.</li>\n <li>Το <strong>παράθυρο context</strong> (block_size) περιορίζει πόσα tokens μπορεί να δει το μοντέλο ταυτόχρονα.</li>\n <li>Το <strong>βήμα (stride)</strong> ελέγχει την επικάλυψη μεταξύ διαδοχικών παραδειγμάτων εκπαίδευσης.</li>\n <li>Η κλάση Dataset οργανώνει τα δεδομένα. Ο DataLoader χειρίζεται τη δημιουργία παρτίδων και την ανακάτεμα.</li>\n <li>Ο ίδιος αγωγός (tokenization &rarr; συρόμενο παράθυρο &rarr; παρτίδα) χρησιμοποιείται από τα GPT-2, LLaMA και άλλα LLM παραγωγής.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Άσκηση: Χτίσε τον δικό σου αγωγό δεδομένων</h3>\n <ol>\n <li>Πάρε μια παράγραφο κειμένου, κάνε tokenize με τον <code>CharTokenizer</code> από τη Διάλεξη 7 και δημιούργησε ένα Dataset με block_size=16 και stride=8.</li>\n <li>Υλοποίησε μια παράμετρο <code>drop_last</code> στον SimpleDataLoader που απορρίπτει την τελευταία παρτίδα αν είναι μικρότερη από <code>batch_size</code>. Αυτό είναι σημαντικό για την εκπαίδευση γιατί όλες οι παρτίδες πρέπει να έχουν το ίδιο σχήμα.</li>\n <li>Υπολόγισε πόσα παραδείγματα εκπαίδευσης παίρνεις από ένα corpus 1 εκατομμυρίου tokens με block_size=1024 και stride=1024. Πώς αλλάζει αυτό με stride=512;</li>\n <li>Bonus: Πρόσθεσε μια μέθοδο στον DataLoader που αναφέρει στατιστικά &mdash; συνολικά tokens που επεξεργάστηκαν, μοναδικά tokens που είδε και εκτιμώμενες εποχές που χρειάζονται για ένα στόχο αριθμό βημάτων εκπαίδευσης.</li>\n </ol>\n </div>", "de": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">LEKTION 9 <span class=\"stage-badge\">Stufe 2: Tokenisierung &amp; Daten</span></div>\n <h1 class=\"lecture-title\">Eingabe-Ziel-Paare &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">Von rohem Text zu Trainings-Batches &mdash; Aufbau der Datenpipeline</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>45 Min. Lesezeit</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Praktisches Programmieren</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"m12 14 4-4\"/><path d=\"M3.34 19a10 10 0 1 1 17.32 0\"/></svg>Mittelstufe</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>Wie LLMs lernen: Vorhersage des nächsten Tokens</h2>\n <p>\n Im Kern ist das Training eines Sprachmodells konzeptionell erstaunlich einfach: Gegeben eine Sequenz\n von Tokens, <strong>sage das nächste vorher</strong>. Das Modell sieht \"The cat sat on the\"\n und lernt, \"mat\" (oder \"floor\" oder \"rug\") vorherzusagen. Es tut dies für Milliarden von\n Sequenzen und lernt dabei nach und nach die Muster der Sprache.\n </p>\n <p>\n Aber wie genau erzeugen wir Trainingsdaten aus rohem Text? Wir können dem Modell nicht einfach\n ein ganzes Buch geben und sagen \"lerne das\". Wir müssen den Text in überschaubare\n <strong>Eingabe-Ziel-Paare</strong> zerlegen, bei denen jede Eingabe eine Sequenz von Tokens ist und das\n Ziel das jeweils nächste Token ist, das das Modell vorhersagen soll.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Analogie &mdash; Die Leseverständnisübung</div>\n <p>\n Stell dir eine Leseübung vor, bei der du das nächste Wort mit deinem Finger verdeckst und versuchst,\n es zu erraten. Du liest \"The weather today is\" und sagst \"sunny\" oder \"cold\" voraus. Dann\n schiebst du deinen Finger weiter: \"weather today is sunny\" und sagst \"and\" oder \"but\" voraus.\n Jede Position deines Fingers erzeugt ein Trainingsbeispiel. Genau das tut\n unsere Datenpipeline &mdash; sie schiebt ein Fenster über den Text und erzeugt Tausende\n von Vorhersageübungen für das Modell.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Erzeugen von Eingabe-Ziel-Paaren</h2>\n <p>\n Gegeben eine Sequenz von Token-IDs, erzeugen wir Eingabe-Ziel-Paare, indem wir die Sequenz\n um eine Position verschieben. Die Eingabe sind die Tokens <code>[0, 1, ..., n-1]</code> und das Ziel sind\n die Tokens <code>[1, 2, ..., n]</code>. Jede Position in der Eingabe wird auf das\n <strong>nächste</strong> Token als ihr Ziel abgebildet.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>Das Kontextfenster (Block-Größe)</h2>\n <p>\n Im obigen Beispiel wächst der Kontext mit jeder Position &mdash; von 1 Token bis zu\n <code>n-1</code> Tokens. Transformer-Modelle haben jedoch ein <strong>festes Kontextfenster</strong>\n (auch <code>max_length</code> oder <code>block_size</code> genannt). GPT-2 verwendet 1024 Tokens,\n GPT-3 verwendet 2048, und GPT-4 verwendet bis zu 128K. Wir müssen unsere Eingabesequenzen\n auf diese feste Größe begrenzen.\n </p>\n <p>\n Hier kommt der Ansatz des <strong>gleitenden Fensters</strong> ins Spiel. Wir schieben ein\n Fenster fester Größe über den tokenisierten Text und erzeugen dabei Eingabe-Ziel-Paare einheitlicher\n Länge.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Kontextfenstergrößen in der Praxis</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Modell</th><th>Kontextfenster</th><th>Ca. Seiten Text</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1.024 Tokens</td><td>~1,5 Seiten</td></tr>\n <tr><td>GPT-3</td><td>2.048 Tokens</td><td>~3 Seiten</td></tr>\n <tr><td>LLaMA 2</td><td>4.096 Tokens</td><td>~6 Seiten</td></tr>\n <tr><td>GPT-4</td><td>8.192&ndash;128K Tokens</td><td>~12&ndash;200 Seiten</td></tr>\n <tr><td>Claude 3</td><td>200K Tokens</td><td>~300 Seiten</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Gleitendes Fenster mit unterschiedlichen Strides</h3>\n <p>\n Der <strong>Stride</strong> steuert, wie weit sich das Fenster zwischen aufeinanderfolgenden Beispielen bewegt.\n Ein Stride von 1 bedeutet maximale Überlappung (am dateneffizientesten), während ein Stride, der der\n Block-Größe entspricht, keine Überlappung bedeutet (schnellstes Training).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#059669\"><path d=\"M22 11.08V12a10 10 0 1 1-5.93-9.14\"/><path d=\"m9 11 3 3L22 4\"/></svg>Tipp &mdash; Stride wählen</div>\n <p>\n In der Praxis verwendet das meiste LLM-Training <strong>stride = block_size</strong> (keine Überlappung)\n aus Effizienzgründen. Bei Milliarden von Tokens im Trainingskorpus gibt es bereits genug\n Datenvielfalt. Überlappung ist nützlicher, wenn dein Korpus klein ist und du jedes\n mögliche Trainingsbeispiel herausholen möchtest. Sebastian Raschkas Buch verwendet\n stride = 1 für Lehrzwecke und stride = block_size für das endgültige Modell.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z\"/><path d=\"m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z\"/><path d=\"M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0\"/><path d=\"M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5\"/></svg>Eine Dataset-Klasse erstellen</h2>\n <p>\n In PyTorch werden Daten mithilfe von <code>Dataset</code>-Objekten organisiert, die\n <code>__len__</code> (Anzahl der Beispiele) und <code>__getitem__</code> (ein Beispiel\n anhand seines Index abrufen) implementieren. Lass uns eine für unsere LLM-Trainingsdaten bauen. Wir bleiben dabei\n bei purem Python (keine PyTorch-Abhängigkeit nötig, um das Konzept zu verstehen).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num &gt;= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Die vollständige Datenpipeline</h2>\n <p>\n Lass uns alles zusammensetzen: vom rohen Text bis hin zu den Trainings-Batches.\n Das ist die vollständige Pipeline, die ein LLM durchläuft, bevor überhaupt eine Berechnung im neuronalen Netz beginnt.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Überblick über die Datenpipeline</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num &gt;= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Warum Shuffling wichtig ist</div>\n <p>\n Ohne Shuffling liefert der DataLoader die Beispiele in sequenzieller Reihenfolge. Das bedeutet,\n dass aufeinanderfolgende Batches stark korrelierte Daten enthalten (überlappende Textfenster), was\n der Trainingsstabilität schaden kann. Shuffling stellt sicher, dass jeder Batch vielfältige Beispiele\n aus unterschiedlichen Teilen des Korpus enthält, was dem Modell hilft, effektiver zu lernen. Beachte\n jedoch, dass wir <em>Beispiele</em> (Fenster) mischen, nicht die Tokens innerhalb jedes Fensters &mdash;\n die interne Reihenfolge jeder Sequenz muss erhalten bleiben.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Von purem Python zu PyTorch</h2>\n <p>\n Die Klassen, die wir oben gebaut haben, spiegeln die API von PyTorch exakt wider. Wenn du zu echtem\n Training mit GPUs übergehst, musst du einfach:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Von torch.utils.data.Dataset erben</h3>\n <p>Ersetze unsere einfache Klasse durch <code>class LLMDataset(torch.utils.data.Dataset)</code> und wandle Listen in <code>torch.tensor</code> um.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. torch.utils.data.DataLoader verwenden</h3>\n <p>Der DataLoader von PyTorch fügt Multi-Worker-Datenladen, GPU-Pinned-Memory und automatisches Batching mit <code>collate_fn</code> hinzu.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Token-Embeddings hinzufügen</h3>\n <p>Jede Token-ID wird in einer Embedding-Tabelle (<code>nn.Embedding</code>) nachgeschlagen, um einen dichten Vektor zu erzeugen &mdash; die erste Schicht des Transformers.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Positionscodierungen hinzufügen</h3>\n <p>Da Transformer keinen eingebauten Sinn für Reihenfolge haben, fügen wir jeder Token-Embedding Positionsinformationen hinzu (behandelt in einer späteren Lektion).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>Wichtige Erkenntnisse</div>\n <ul>\n <li>LLMs lernen, indem sie das nächste Token vorhersagen. Jede Position in einer Sequenz ist ein Trainingsbeispiel.</li>\n <li>Eingabe-Ziel-Paare werden erzeugt, indem eine Token-Sequenz um eine Position verschoben wird.</li>\n <li>Das <strong>Kontextfenster</strong> (block_size) begrenzt, wie viele Tokens das Modell auf einmal sehen kann.</li>\n <li><strong>Stride</strong> steuert die Überlappung zwischen aufeinanderfolgenden Trainingsbeispielen.</li>\n <li>Die Dataset-Klasse organisiert die Daten; der DataLoader übernimmt Batching und Shuffling.</li>\n <li>Dieselbe Pipeline (Tokenisieren &rarr; gleitendes Fenster &rarr; Batch) wird von GPT-2, LLaMA und anderen produktiven LLMs verwendet.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Übung: Baue deine eigene Datenpipeline</h3>\n <ol>\n <li>Nimm einen Textabsatz, tokenisiere ihn mit dem <code>CharTokenizer</code> aus Lektion 7 und erstelle ein Dataset mit block_size=16 und stride=8.</li>\n <li>Implementiere einen Parameter <code>drop_last</code> in SimpleDataLoader, der den letzten Batch verwirft, wenn er kleiner als <code>batch_size</code> ist. Das ist wichtig für das Training, da alle Batches dieselbe Form haben sollten.</li>\n <li>Berechne, wie viele Trainingsbeispiele du aus einem Korpus mit 1 Million Tokens bei block_size=1024 und stride=1024 erhältst. Wie ändert sich das bei stride=512?</li>\n <li>Bonus: Füge dem DataLoader eine Methode hinzu, die Statistiken meldet &mdash; verarbeitete Tokens insgesamt, gesehene eindeutige Tokens und geschätzte benötigte Epochen für eine Zielanzahl von Trainingsschritten.</li>\n </ol>\n </div>", "it": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">LEZIONE 9 <span class=\"stage-badge\">Fase 2: Tokenizzazione &amp; Dati</span></div>\n <h1 class=\"lecture-title\">Coppie Input-Target &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">Dal testo grezzo ai batch di addestramento &mdash; costruire la pipeline dei dati</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>45 min di lettura</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Programmazione pratica</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"m12 14 4-4\"/><path d=\"M3.34 19a10 10 0 1 1 17.32 0\"/></svg>Intermedio</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>Come imparano gli LLM: predizione del token successivo</h2>\n <p>\n In sostanza, addestrare un modello linguistico è concettualmente sorprendentemente semplice: data\n una sequenza di token, <strong>prevedere il successivo</strong>. Il modello vede \"The cat sat on the\"\n e impara a prevedere \"mat\" (o \"floor\" o \"rug\"). Fa questo per miliardi di\n sequenze, imparando gradualmente gli schemi del linguaggio.\n </p>\n <p>\n Ma come creiamo esattamente i dati di addestramento a partire dal testo grezzo? Non possiamo semplicemente dare al modello\n un intero libro e dire \"impara questo.\" Dobbiamo suddividere il testo in\n <strong>coppie input-target</strong> gestibili, dove ogni input è una sequenza di token e il\n target è il token immediatamente successivo che il modello deve prevedere.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Analogia &mdash; L'esercizio di comprensione del testo</div>\n <p>\n Immagina un esercizio di lettura in cui copri la parola successiva con il dito e provi a\n indovinarla. Leggi \"The weather today is\" e prevedi \"sunny\" o \"cold\". Poi\n sposti il dito in avanti: \"weather today is sunny\" e prevedi \"and\" o \"but\".\n Ogni posizione del tuo dito crea un esempio di addestramento. Questo è esattamente ciò che\n fa la nostra pipeline dei dati &mdash; fa scorrere una finestra lungo il testo, creando migliaia\n di esercizi di predizione per il modello.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Creare le coppie input-target</h2>\n <p>\n Data una sequenza di ID di token, creiamo le coppie input-target spostando la sequenza\n di una posizione. L'input è i token <code>[0, 1, ..., n-1]</code> e il target è\n i token <code>[1, 2, ..., n]</code>. Ogni posizione nell'input corrisponde al token\n <strong>successivo</strong> come proprio target.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>La finestra di contesto (block size)</h2>\n <p>\n Nell'esempio precedente, il contesto cresce a ogni posizione &mdash; da 1 token fino a\n <code>n-1</code> token. Ma i modelli Transformer hanno una <strong>finestra di contesto fissa</strong>\n (chiamata anche <code>max_length</code> o <code>block_size</code>). GPT-2 usa 1024 token,\n GPT-3 usa 2048, e GPT-4 arriva fino a 128K. Dobbiamo limitare le nostre sequenze di input\n a questa dimensione fissa.\n </p>\n <p>\n È qui che entra in gioco l'approccio a <strong>finestra scorrevole</strong>. Facciamo scorrere una\n finestra di dimensione fissa lungo il testo tokenizzato, creando coppie input-target di lunghezza\n uniforme.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Dimensioni della finestra di contesto nella pratica</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Modello</th><th>Finestra di contesto</th><th>Pagine di testo approssimative</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1.024 token</td><td>~1,5 pagine</td></tr>\n <tr><td>GPT-3</td><td>2.048 token</td><td>~3 pagine</td></tr>\n <tr><td>LLaMA 2</td><td>4.096 token</td><td>~6 pagine</td></tr>\n <tr><td>GPT-4</td><td>8.192&ndash;128K token</td><td>~12&ndash;200 pagine</td></tr>\n <tr><td>Claude 3</td><td>200K token</td><td>~300 pagine</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Finestra scorrevole con stride diversi</h3>\n <p>\n Lo <strong>stride</strong> controlla di quanto si sposta la finestra tra esempi consecutivi.\n Uno stride di 1 significa sovrapposizione massima (il più efficiente in termini di dati), mentre uno stride pari alla\n block size significa nessuna sovrapposizione (addestramento più rapido).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#059669\"><path d=\"M22 11.08V12a10 10 0 1 1-5.93-9.14\"/><path d=\"m9 11 3 3L22 4\"/></svg>Consiglio &mdash; Scegliere lo stride</div>\n <p>\n In pratica, la maggior parte degli addestramenti di LLM usa <strong>stride = block_size</strong> (nessuna sovrapposizione)\n per efficienza. Con miliardi di token nel corpus di addestramento, c'è già abbastanza\n diversità nei dati. La sovrapposizione è più utile quando il corpus è piccolo e si vuole\n spremere ogni possibile esempio di addestramento. Il libro di Sebastian Raschka usa\n stride = 1 a scopo didattico e stride = block_size per il modello finale.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z\"/><path d=\"m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z\"/><path d=\"M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0\"/><path d=\"M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5\"/></svg>Costruire una classe Dataset</h2>\n <p>\n In PyTorch, i dati sono organizzati usando oggetti <code>Dataset</code> che implementano\n <code>__len__</code> (quanti esempi) e <code>__getitem__</code> (ottenere un esempio\n tramite indice). Costruiamone uno per i nostri dati di addestramento LLM. Lo manterremo\n in Python puro qui (non è necessaria alcuna dipendenza da PyTorch per capire il concetto).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num &gt;= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>La pipeline dei dati completa</h2>\n <p>\n Mettiamo tutto insieme: dal testo grezzo fino ai batch di addestramento.\n Questa è la pipeline completa che un LLM vede prima che inizi qualsiasi calcolo della rete neurale.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Panoramica della pipeline dei dati</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num &gt;= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Perché il rimescolamento (shuffling) è importante</div>\n <p>\n Senza rimescolamento, il DataLoader fornisce gli esempi in ordine sequenziale. Questo significa\n che i batch consecutivi contengono dati altamente correlati (finestre di testo sovrapposte), il che\n può danneggiare la stabilità dell'addestramento. Il rimescolamento assicura che ogni batch contenga esempi diversi\n da parti differenti del corpus, aiutando il modello a imparare in modo più efficace. Tuttavia,\n nota che rimescoliamo gli <em>esempi</em> (finestre), non i token all'interno di ciascuna finestra &mdash;\n l'ordine interno di ogni sequenza deve essere preservato.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Da Python puro a PyTorch</h2>\n <p>\n Le classi che abbiamo costruito sopra rispecchiano esattamente l'API di PyTorch. Quando passi\n all'addestramento reale con le GPU, devi semplicemente:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Ereditare da torch.utils.data.Dataset</h3>\n <p>Sostituisci la nostra classe semplice con <code>class LLMDataset(torch.utils.data.Dataset)</code> e converti le liste in <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. Usare torch.utils.data.DataLoader</h3>\n <p>Il DataLoader di PyTorch aggiunge il caricamento dei dati multi-worker, la memoria pinned della GPU e il batching automatico con <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Aggiungere gli embedding dei token</h3>\n <p>Ogni ID di token viene cercato in una tabella di embedding (<code>nn.Embedding</code>) per produrre un vettore denso &mdash; il primo layer del Transformer.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Aggiungere le codifiche posizionali</h3>\n <p>Poiché i Transformer non hanno una nozione intrinseca di ordine, aggiungiamo l'informazione di posizione a ogni embedding di token (trattato in una lezione successiva).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>Punti chiave</div>\n <ul>\n <li>Gli LLM imparano prevedendo il token successivo. Ogni posizione in una sequenza è un esempio di addestramento.</li>\n <li>Le coppie input-target sono create spostando una sequenza di token di una posizione.</li>\n <li>La <strong>finestra di contesto</strong> (block_size) limita quanti token il modello può vedere alla volta.</li>\n <li>Lo <strong>stride</strong> controlla la sovrapposizione tra esempi di addestramento consecutivi.</li>\n <li>La classe Dataset organizza i dati; il DataLoader gestisce il batching e il rimescolamento.</li>\n <li>Questa stessa pipeline (tokenizzazione &rarr; finestra scorrevole &rarr; batch) è usata da GPT-2, LLaMA e altri LLM in produzione.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Esercizio: costruisci la tua pipeline dei dati</h3>\n <ol>\n <li>Prendi un paragrafo di testo, tokenizzalo con il <code>CharTokenizer</code> della Lezione 7, e crea un Dataset con block_size=16 e stride=8.</li>\n <li>Implementa un parametro <code>drop_last</code> in SimpleDataLoader che scarti l'ultimo batch se è più piccolo di <code>batch_size</code>. Questo è importante per l'addestramento perché tutti i batch devono avere la stessa forma.</li>\n <li>Calcola quanti esempi di addestramento ottieni da un corpus di 1 milione di token con block_size=1024 e stride=1024. Come cambia questo con stride=512?</li>\n <li>Bonus: aggiungi un metodo a DataLoader che riporti delle statistiche &mdash; token totali processati, token unici visti, ed epoche stimate necessarie per un numero target di passi di addestramento.</li>\n </ol>\n </div>", "fr": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">LEÇON 9 <span class=\"stage-badge\">Étape 2 : Tokenisation &amp; Données</span></div>\n <h1 class=\"lecture-title\">Paires entrée-cible &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">Du texte brut aux lots d'entraînement &mdash; construction du pipeline de données</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>45 min de lecture</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>Programmation pratique</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"m12 14 4-4\"/><path d=\"M3.34 19a10 10 0 1 1 17.32 0\"/></svg>Intermédiaire</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>Comment les LLM apprennent : la prédiction du token suivant</h2>\n <p>\n Dans son essence, l'entraînement d'un modèle de langage est remarquablement simple sur le plan conceptuel : étant donné une séquence\n de tokens, <strong>prédire le suivant</strong>. Le modèle voit \"The cat sat on the\"\n et apprend à prédire \"mat\" (ou \"floor\" ou \"rug\"). Il fait cela pour des milliards de\n séquences, apprenant progressivement les motifs du langage.\n </p>\n <p>\n Mais comment créons-nous exactement des données d'entraînement à partir de texte brut ? Nous ne pouvons pas simplement donner au modèle\n un livre entier et dire \"apprends ceci.\" Nous devons découper le texte en\n <strong>paires entrée-cible</strong> gérables, où chaque entrée est une séquence de tokens et la\n cible est le tout prochain token que le modèle doit prédire.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Analogie &mdash; L'exercice de compréhension de lecture</div>\n <p>\n Imaginez un exercice de lecture où vous cachez le mot suivant avec votre doigt et essayez de\n le deviner. Vous lisez \"The weather today is\" et prédisez \"sunny\" ou \"cold\". Puis vous\n glissez votre doigt vers l'avant : \"weather today is sunny\" et prédisez \"and\" ou \"but\".\n Chaque position de votre doigt crée un exemple d'entraînement. C'est exactement ce que\n fait notre pipeline de données &mdash; il fait glisser une fenêtre sur le texte, créant des milliers\n d'exercices de prédiction pour le modèle.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Créer des paires entrée-cible</h2>\n <p>\n Étant donné une séquence d'IDs de tokens, nous créons des paires entrée-cible en décalant la séquence\n d'une position. L'entrée est constituée des tokens <code>[0, 1, ..., n-1]</code> et la cible est\n constituée des tokens <code>[1, 2, ..., n]</code>. Chaque position dans l'entrée correspond au token\n <strong>suivant</strong> comme cible.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>La fenêtre de contexte (Block Size)</h2>\n <p>\n Dans l'exemple ci-dessus, le contexte grandit à chaque position &mdash; de 1 token jusqu'à\n <code>n-1</code> tokens. Mais les modèles Transformer ont une <strong>fenêtre de contexte fixe</strong>\n (aussi appelée <code>max_length</code> ou <code>block_size</code>). GPT-2 utilise 1024 tokens,\n GPT-3 utilise 2048, et GPT-4 utilise jusqu'à 128K. Nous devons limiter nos séquences\n d'entrée à cette taille fixe.\n </p>\n <p>\n C'est ici qu'intervient l'approche de la <strong>fenêtre glissante</strong>. Nous faisons glisser une\n fenêtre de taille fixe sur le texte tokenisé, créant des paires entrée-cible de longueur\n uniforme.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>Tailles de fenêtre de contexte en pratique</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>Modèle</th><th>Fenêtre de contexte</th><th>Pages de texte approx.</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1 024 tokens</td><td>~1,5 page</td></tr>\n <tr><td>GPT-3</td><td>2 048 tokens</td><td>~3 pages</td></tr>\n <tr><td>LLaMA 2</td><td>4 096 tokens</td><td>~6 pages</td></tr>\n <tr><td>GPT-4</td><td>8 192&ndash;128K tokens</td><td>~12&ndash;200 pages</td></tr>\n <tr><td>Claude 3</td><td>200K tokens</td><td>~300 pages</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>Fenêtre glissante avec différents strides</h3>\n <p>\n Le <strong>stride</strong> contrôle la distance de déplacement de la fenêtre entre exemples consécutifs.\n Un stride de 1 signifie un chevauchement maximal (le plus efficace en données), tandis qu'un stride égal à la\n taille du bloc signifie aucun chevauchement (l'entraînement le plus rapide).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#059669\"><path d=\"M22 11.08V12a10 10 0 1 1-5.93-9.14\"/><path d=\"m9 11 3 3L22 4\"/></svg>Astuce &mdash; Choisir le stride</div>\n <p>\n En pratique, la plupart des entraînements de LLM utilisent <strong>stride = block_size</strong> (aucun chevauchement)\n pour l'efficacité. Avec des milliards de tokens dans le corpus d'entraînement, il y a déjà suffisamment\n de diversité de données. Le chevauchement est plus utile lorsque votre corpus est petit et que vous souhaitez\n extraire chaque exemple d'entraînement possible. Le livre de Sebastian Raschka utilise\n stride = 1 à des fins pédagogiques et stride = block_size pour le modèle final.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z\"/><path d=\"m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z\"/><path d=\"M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0\"/><path d=\"M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5\"/></svg>Construire une classe Dataset</h2>\n <p>\n En PyTorch, les données sont organisées à l'aide d'objets <code>Dataset</code> qui implémentent\n <code>__len__</code> (combien d'exemples) et <code>__getitem__</code> (obtenir un exemple\n par son index). Construisons-en un pour nos données d'entraînement de LLM. Nous le garderons en Python pur\n ici (aucune dépendance à PyTorch n'est nécessaire pour comprendre le concept).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num &gt;= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>Le pipeline de données complet</h2>\n <p>\n Assemblons maintenant le tout : du texte brut jusqu'aux lots d'entraînement.\n Voici le pipeline complet qu'un LLM traverse avant même que le calcul du réseau neuronal ne commence.\n </p>\n\n <div class=\"diagram-container\">\n <h3>Vue d'ensemble du pipeline de données</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num &gt;= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>Pourquoi le mélange (shuffling) est important</div>\n <p>\n Sans mélange, le DataLoader fournit les exemples dans l'ordre séquentiel. Cela signifie que des\n lots consécutifs contiennent des données fortement corrélées (fenêtres de texte qui se chevauchent), ce qui\n peut nuire à la stabilité de l'entraînement. Le mélange garantit que chaque lot contient des exemples diversifiés\n provenant de différentes parties du corpus, aidant le modèle à apprendre plus efficacement. Notez toutefois\n que nous mélangeons les <em>exemples</em> (fenêtres), pas les tokens à l'intérieur de chaque fenêtre &mdash;\n l'ordre interne de chaque séquence doit être préservé.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>Du Python pur à PyTorch</h2>\n <p>\n Les classes que nous avons construites ci-dessus reflètent exactement l'API de PyTorch. Lorsque vous passez à un\n entraînement réel avec des GPU, vous devez simplement :\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. Hériter de torch.utils.data.Dataset</h3>\n <p>Remplacez notre classe simple par <code>class LLMDataset(torch.utils.data.Dataset)</code> et convertissez les listes en <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. Utiliser torch.utils.data.DataLoader</h3>\n <p>Le DataLoader de PyTorch ajoute le chargement de données multi-worker, la mémoire épinglée (pinned memory) pour le GPU, et le batching automatique avec <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. Ajouter des Token Embeddings</h3>\n <p>Chaque ID de token est recherché dans une table d'embeddings (<code>nn.Embedding</code>) pour produire un vecteur dense &mdash; la première couche du Transformer.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. Ajouter des encodages positionnels</h3>\n <p>Comme les Transformers n'ont pas de notion intégrée de l'ordre, nous ajoutons une information de position à chaque embedding de token (abordé dans une leçon ultérieure).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>Points clés à retenir</div>\n <ul>\n <li>Les LLM apprennent en prédisant le token suivant. Chaque position dans une séquence est un exemple d'entraînement.</li>\n <li>Les paires entrée-cible sont créées en décalant une séquence de tokens d'une position.</li>\n <li>La <strong>fenêtre de contexte</strong> (block_size) limite le nombre de tokens que le modèle peut voir à la fois.</li>\n <li>Le <strong>stride</strong> contrôle le chevauchement entre exemples d'entraînement consécutifs.</li>\n <li>La classe Dataset organise les données ; le DataLoader gère le batching et le mélange.</li>\n <li>Ce même pipeline (tokeniser &rarr; fenêtre glissante &rarr; lot) est utilisé par GPT-2, LLaMA et d'autres LLM en production.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> Exercice : construisez votre propre pipeline de données</h3>\n <ol>\n <li>Prenez un paragraphe de texte, tokenisez-le avec le <code>CharTokenizer</code> de la Leçon 7, et créez un Dataset avec block_size=16 et stride=8.</li>\n <li>Implémentez un paramètre <code>drop_last</code> dans SimpleDataLoader qui écarte le dernier lot s'il est plus petit que <code>batch_size</code>. C'est important pour l'entraînement car tous les lots doivent avoir la même forme.</li>\n <li>Calculez combien d'exemples d'entraînement vous obtenez à partir d'un corpus d'un million de tokens avec block_size=1024 et stride=1024. Comment cela change-t-il avec stride=512 ?</li>\n <li>Bonus : ajoutez une méthode à DataLoader qui rapporte des statistiques &mdash; total des tokens traités, tokens uniques vus, et nombre d'epochs estimé nécessaire pour un nombre cible d'étapes d'entraînement.</li>\n </ol>\n </div>", "ar": "<div class=\"lecture-container\">\n<div class=\"lecture-header\">\n <div class=\"lecture-label\">المحاضرة 9 <span class=\"stage-badge\">المرحلة 2: التوكنة &amp; البيانات</span></div>\n <h1 class=\"lecture-title\">أزواج المُدخل-الهدف &amp; DataLoader</h1>\n <div class=\"lecture-subtitle\">من النص الخام إلى دفعات التدريب &mdash; بناء خط أنابيب البيانات</div>\n <div class=\"lecture-meta\">\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><polyline points=\"12 6 12 12 16 14\"/></svg>45 دقيقة قراءة</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"M12.586 2.586A2 2 0 0 0 11.172 2H4a2 2 0 0 0-2 2v7.172a2 2 0 0 0 .586 1.414l8.704 8.704a2.426 2.426 0 0 0 3.42 0l6.58-6.58a2.426 2.426 0 0 0 0-3.42z\"/><circle cx=\"7.5\" cy=\"7.5\" r=\".5\" fill=\"currentColor\"/></svg>برمجة عملية</span>\n <span class=\"lecture-meta-item\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"16\" height=\"16\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:4px\"><path d=\"m12 14 4-4\"/><path d=\"M3.34 19a10 10 0 1 1 17.32 0\"/></svg>متوسط</span>\n </div>\n</div>\n<div class=\"content\">\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><polyline points=\"16 18 22 12 16 6\"/><polyline points=\"8 6 2 12 8 18\"/></svg>كيف تتعلم نماذج اللغة الكبيرة: التنبؤ بالرمز التالي</h2>\n <p>\n في جوهرها، عملية تدريب نموذج لغوي بسيطة للغاية من الناحية المفاهيمية: بالنظر إلى سلسلة\n من الرموز (tokens)، <strong>التنبؤ بالرمز التالي</strong>. يرى النموذج \"The cat sat on the\"\n ويتعلم التنبؤ بـ \"mat\" (أو \"floor\" أو \"rug\"). يقوم بذلك لمليارات\n من التسلسلات، متعلمًا تدريجيًا أنماط اللغة.\n </p>\n <p>\n لكن كيف ننشئ بالضبط بيانات التدريب من النص الخام؟ لا يمكننا ببساطة أن نعطي النموذج\n كتابًا كاملاً ونقول \"تعلّم هذا.\" نحتاج إلى تقسيم النص إلى\n <strong>أزواج مُدخل-هدف</strong> يمكن التعامل معها، حيث يكون كل مُدخل سلسلة من الرموز والهدف\n هو الرمز التالي مباشرة الذي يجب على النموذج التنبؤ به.\n </p>\n\n <div class=\"callout analogy\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>تشبيه &mdash; تمرين استيعاب القراءة</div>\n <p>\n تخيل تمرين قراءة تُغطي فيه الكلمة التالية بإصبعك وتحاول\n تخمينها. تقرأ \"The weather today is\" وتتنبأ بـ \"sunny\" أو \"cold\". ثم\n تُحرّك إصبعك للأمام: \"weather today is sunny\" وتتنبأ بـ \"and\" أو \"but\".\n كل موضع لإصبعك ينشئ مثالاً تدريبيًا واحدًا. هذا بالضبط ما\n يفعله خط أنابيب البيانات لدينا &mdash; فهو يُحرّك نافذة عبر النص، مُنشئًا آلاف\n من تمارين التنبؤ للنموذج.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>إنشاء أزواج المُدخل-الهدف</h2>\n <p>\n بالنظر إلى سلسلة من معرّفات الرموز (token IDs)، ننشئ أزواج المُدخل-الهدف عن طريق إزاحة السلسلة\n بموضع واحد. المُدخل هو الرموز <code>[0, 1, ..., n-1]</code> والهدف هو\n الرموز <code>[1, 2, ..., n]</code>. كل موضع في المُدخل يُقابل الرمز\n <strong>التالي</strong> كهدفه.\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Creating input-target pairs from a sentence\n\n# Simulate tokenized text (using word-level tokens for readability)\ntext = \"The cat sat on the mat and the dog lay on the rug\"\nwords = text.split()\n# Assign simple IDs\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"Vocabulary:\", vocab)\nprint(f\"Token IDs: {token_ids}\")\nprint(f\"Words: {words}\")\nprint()\n\n# The simplest approach: each position predicts the next token\nprint(\"=== Input-Target Pairs (full sequence) ===\")\nprint(f\"{'Position':<10} {'Input (context)':<40} {'Target (next word)'}\")\nprint(\"-\" * 70)\nfor i in range(len(token_ids) - 1):\n context_ids = token_ids[:i+1]\n target_id = token_ids[i+1]\n context_words = words[:i+1]\n target_word = words[i+1]\n print(f\"{i:<10} {str(context_words):<40} {target_word} (ID={target_id})\")\n\nprint(f\"\\\nTotal training examples from this sentence: {len(token_ids) - 1}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M17 3a2.85 2.83 0 1 1 4 4L7.5 20.5 2 22l1.5-5.5Z\"/></svg>نافذة السياق (حجم الكتلة)</h2>\n <p>\n في المثال أعلاه، يتزايد السياق مع كل موضع &mdash; من رمز واحد حتى\n <code>n-1</code> رمزًا. لكن نماذج Transformer تمتلك <strong>نافذة سياق ثابتة</strong>\n (تُسمى أيضًا <code>max_length</code> أو <code>block_size</code>). يستخدم GPT-2 عدد 1024 رمزًا،\n ويستخدم GPT-3 عدد 2048، ويستخدم GPT-4 حتى 128K. نحتاج إلى تحديد تسلسلات المُدخل لدينا\n بهذا الحجم الثابت.\n </p>\n <p>\n وهنا يأتي دور نهج <strong>النافذة المنزلقة</strong>. نُحرّك نافذة\n ذات حجم ثابت عبر النص المُرمّز (المُقسّم إلى رموز)، مُنشئين أزواج مُدخل-هدف بطول\n موحّد.\n </p>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#2563eb\"><circle cx=\"12\" cy=\"12\" r=\"10\"/><path d=\"M12 16v-4\"/><path d=\"M12 8h.01\"/></svg>أحجام نافذة السياق في الممارسة العملية</div>\n <table class=\"comparison-table\">\n <thead>\n <tr><th>النموذج</th><th>نافذة السياق</th><th>عدد الصفحات التقريبي</th></tr>\n </thead>\n <tbody>\n <tr><td>GPT-2</td><td>1,024 رمزًا</td><td>~1.5 صفحة</td></tr>\n <tr><td>GPT-3</td><td>2,048 رمزًا</td><td>~3 صفحات</td></tr>\n <tr><td>LLaMA 2</td><td>4,096 رمزًا</td><td>~6 صفحات</td></tr>\n <tr><td>GPT-4</td><td>8,192&ndash;128K رمزًا</td><td>~12&ndash;200 صفحة</td></tr>\n <tr><td>Claude 3</td><td>200K رمزًا</td><td>~300 صفحة</td></tr>\n </tbody>\n </table>\n </div>\n\n <h3>النافذة المنزلقة بخطوات (strides) مختلفة</h3>\n <p>\n تتحكم <strong>الخطوة (stride)</strong> في المسافة التي تتحركها النافذة بين الأمثلة المتتالية.\n خطوة بقيمة 1 تعني أقصى تداخل (الأكثر كفاءة من حيث البيانات)، بينما خطوة تساوي\n حجم الكتلة تعني عدم وجود تداخل (الأسرع في التدريب).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\n# Sliding window approach for creating input-target pairs\n\ndef create_input_target_pairs(token_ids, block_size, stride=1):\n \"\"\"\n Create input-target pairs using a sliding window.\n\n Args:\n token_ids: List of token IDs from the corpus\n block_size: Context window size (max_length)\n stride: How many tokens to move the window each step\n Returns:\n List of (input, target) tuples\n \"\"\"\n pairs = []\n for i in range(0, len(token_ids) - block_size, stride):\n input_seq = token_ids[i : i + block_size]\n target_seq = token_ids[i + 1 : i + block_size + 1]\n pairs.append((input_seq, target_seq))\n return pairs\n\n# A longer sequence of token IDs (simulated)\ntext = \"The quick brown fox jumps over the lazy dog and the cat sleeps\"\nwords = text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\ntoken_ids = [vocab[w] for w in words]\nid_to_word = {i: w for w, i in vocab.items()}\n\nprint(f\"Full sequence ({len(token_ids)} tokens): {token_ids}\")\nprint(f\"Words: {words}\\\n\")\n\nblock_size = 4\n\n# Stride = 1 (maximum overlap)\npairs_s1 = create_input_target_pairs(token_ids, block_size, stride=1)\nprint(f\"=== Block size = {block_size}, Stride = 1 ===\")\nprint(f\"Number of examples: {len(pairs_s1)}\")\nfor i, (inp, tgt) in enumerate(pairs_s1[:6]):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = block_size (no overlap)\npairs_sb = create_input_target_pairs(token_ids, block_size, stride=block_size)\nprint(f\"=== Block size = {block_size}, Stride = {block_size} (no overlap) ===\")\nprint(f\"Number of examples: {len(pairs_sb)}\")\nfor i, (inp, tgt) in enumerate(pairs_sb):\n inp_words = [id_to_word[t] for t in inp]\n tgt_words = [id_to_word[t] for t in tgt]\n print(f\" [{i}] Input: {inp} = {inp_words}\")\n print(f\" Target: {tgt} = {tgt_words}\")\n print()\n\n# Stride = 2 (partial overlap)\npairs_s2 = create_input_target_pairs(token_ids, block_size, stride=2)\nprint(f\"=== Block size = {block_size}, Stride = 2 ===\")\nprint(f\"Number of examples: {len(pairs_s2)}\")\n\nprint(f\"\\\n=== Summary ===\")\nprint(f\"Stride 1: {len(pairs_s1)} examples (max data, slowest)\")\nprint(f\"Stride 2: {len(pairs_s2)} examples (balanced)\")\nprint(f\"Stride {block_size} (no overlap): {len(pairs_sb)} examples (min data, fastest)\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout tip\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#059669\"><path d=\"M22 11.08V12a10 10 0 1 1-5.93-9.14\"/><path d=\"m9 11 3 3L22 4\"/></svg>نصيحة &mdash; اختيار الخطوة (Stride)</div>\n <p>\n من الناحية العملية، يستخدم معظم تدريب نماذج اللغة الكبيرة <strong>الخطوة = حجم الكتلة</strong> (بدون تداخل)\n لتحقيق الكفاءة. مع وجود مليارات الرموز في مجموعة بيانات التدريب، يوجد بالفعل تنوع كافٍ\n في البيانات. يكون التداخل أكثر فائدة عندما تكون مجموعة البيانات لديك صغيرة وتريد\n استخلاص كل مثال تدريبي ممكن. يستخدم كتاب سيباستيان راشكا\n الخطوة = 1 لأغراض تعليمية والخطوة = حجم الكتلة للنموذج النهائي.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4.5 16.5c-1.5 1.26-2 5-2 5s3.74-.5 5-2c.71-.84.7-2.13-.09-2.91a2.18 2.18 0 0 0-2.91-.09z\"/><path d=\"m12 15-3-3a22 22 0 0 1 2-3.95A12.88 12.88 0 0 1 22 2c0 2.72-.78 7.5-6 11a22.35 22.35 0 0 1-4 2z\"/><path d=\"M9 12H4s.55-3.03 2-4c1.62-1.08 5 0 5 0\"/><path d=\"M12 15v5s3.03-.55 4-2c1.08-1.62 0-5 0-5\"/></svg>بناء صنف (Class) لمجموعة البيانات</h2>\n <p>\n في PyTorch، تُنظَّم البيانات باستخدام كائنات <code>Dataset</code> التي تُنفّذ\n <code>__len__</code> (كم عدد الأمثلة) و<code>__getitem__</code> (الحصول على مثال واحد\n حسب الفهرس). لنبنِ واحدًا لبيانات تدريب نموذج اللغة الكبيرة لدينا. سنبقيه بلغة Python خالصة\n هنا (لا حاجة لاعتمادية PyTorch لفهم المفهوم).\n </p>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\nclass LLMDataset:\n \"\"\"\n A PyTorch-style Dataset for language model training.\n Creates input-target pairs using a sliding window.\n \"\"\"\n\n def __init__(self, token_ids, block_size, stride=1):\n \"\"\"\n Args:\n token_ids: List of integer token IDs (the full corpus)\n block_size: Context window size\n stride: Step size for the sliding window\n \"\"\"\n self.block_size = block_size\n self.input_ids = []\n self.target_ids = []\n\n # Create all input-target pairs\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n\n def __len__(self):\n return len(self.input_ids)\n\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\n\nclass SimpleDataLoader:\n \"\"\"\n A simple DataLoader that yields batches from a dataset.\n \"\"\"\n\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n\n def __len__(self):\n \"\"\"Number of batches per epoch.\"\"\"\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n\n def __iter__(self):\n \"\"\"Yield batches of (input, target) pairs.\"\"\"\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n\n for start in range(0, len(indices), self.batch_size):\n batch_indices = indices[start : start + self.batch_size]\n batch_inputs = []\n batch_targets = []\n for idx in batch_indices:\n inp, tgt = self.dataset[idx]\n batch_inputs.append(inp)\n batch_targets.append(tgt)\n yield batch_inputs, batch_targets\n\n\n# === Demo ===\n# Simulated corpus: 50 token IDs\nrandom.seed(42)\ncorpus_ids = [random.randint(0, 99) for _ in range(50)]\nprint(f\"Corpus: {len(corpus_ids)} tokens\")\nprint(f\"First 20: {corpus_ids[:20]}\\\n\")\n\n# Create dataset\nblock_size = 8\nstride = 4\ndataset = LLMDataset(corpus_ids, block_size=block_size, stride=stride)\nprint(f\"Dataset: {len(dataset)} examples (block_size={block_size}, stride={stride})\")\nprint(f\"\\\nFirst 3 examples:\")\nfor i in range(min(3, len(dataset))):\n inp, tgt = dataset[i]\n print(f\" Example {i}:\")\n print(f\" Input: {inp}\")\n print(f\" Target: {tgt}\")\n\n# Create DataLoader\nbatch_size = 3\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=42)\nprint(f\"\\\nDataLoader: {len(loader)} batches of size {batch_size}\")\n\nprint(f\"\\\n=== First Epoch (shuffled) ===\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\"\\\nBatch {batch_num + 1}:\")\n print(f\" Batch shape: {len(inputs)} examples x {len(inputs[0])} tokens\")\n for i in range(len(inputs)):\n print(f\" inputs[{i}]: {inputs[i]}\")\n print(f\" targets[{i}]: {targets[i]}\")\n if batch_num &gt;= 1:\n print(\" ... (remaining batches omitted)\")\n break\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><ellipse cx=\"12\" cy=\"5\" rx=\"9\" ry=\"3\"/><path d=\"M3 5V19A9 3 0 0 0 21 19V5\"/><path d=\"M3 12A9 3 0 0 0 21 12\"/></svg>خط أنابيب البيانات الكامل</h2>\n <p>\n لنجمع كل شيء معًا: من النص الخام إلى دفعات التدريب.\n هذا هو خط الأنابيب الكامل الذي يراه نموذج اللغة الكبيرة قبل أن تبدأ أي حسابات للشبكة العصبية.\n </p>\n\n <div class=\"diagram-container\">\n <h3>نظرة عامة على خط أنابيب البيانات</h3>\n <pre>\nRaw Text (e.g., \"The cat sat on the mat.\")\n |\n v\n Tokenizer.encode()\n |\n v\nToken IDs (e.g., [464, 3797, 3332, 319, 262, 2603, 13])\n |\n v\n LLMDataset (sliding window)\n |\n v\nInput-Target Pairs:\n Input: [464, 3797, 3332, 319] Target: [3797, 3332, 319, 262]\n Input: [3797, 3332, 319, 262] Target: [3332, 319, 262, 2603]\n ...\n |\n v\n DataLoader (batching + shuffling)\n |\n v\nBatches of shape [batch_size, block_size]:\n Batch 1: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n Batch 2: inputs=[[...], [...], [...]] targets=[[...], [...], [...]]\n ...\n |\n v\n Neural Network (Transformer) &larr; Training begins here!\n </pre>\n </div>\n\n <div class=\"code-block\">\n <div class=\"code-header\">\n <span class=\"code-lang\">Python</span>\n <div class=\"code-actions\">\n <button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\"> Run</button>\n </div>\n </div>\n <pre><code class=\"language-python\">\nimport random\n\n# === STEP 1: Raw Text ===\nraw_text = \"\"\"\nLanguage models are trained on large amounts of text data.\nThey learn to predict the next word in a sequence.\nThis simple objective leads to powerful general capabilities.\nThe model learns grammar, facts, reasoning, and even some\ncommon sense from this next-token prediction task.\n\"\"\"\nprint(\"STEP 1 - Raw Text:\")\nprint(f\" Length: {len(raw_text)} characters\")\nprint(f\" Preview: '{raw_text[:60].strip()}...'\\\n\")\n\n# === STEP 2: Tokenize ===\n# Simple word-level tokenizer for demonstration\nwords = raw_text.split()\nvocab = {w: i for i, w in enumerate(sorted(set(words)))}\nid_to_word = {i: w for w, i in vocab.items()}\ntoken_ids = [vocab[w] for w in words]\n\nprint(\"STEP 2 - Tokenize:\")\nprint(f\" Vocabulary size: {len(vocab)}\")\nprint(f\" Total tokens: {len(token_ids)}\")\nprint(f\" First 10 tokens: {token_ids[:10]}\")\nprint(f\" = {[id_to_word[i] for i in token_ids[:10]]}\\\n\")\n\n# === STEP 3: Create Dataset ===\nclass LLMDataset:\n def __init__(self, token_ids, block_size, stride=1):\n self.input_ids = []\n self.target_ids = []\n for i in range(0, len(token_ids) - block_size, stride):\n self.input_ids.append(token_ids[i : i + block_size])\n self.target_ids.append(token_ids[i + 1 : i + block_size + 1])\n def __len__(self):\n return len(self.input_ids)\n def __getitem__(self, idx):\n return self.input_ids[idx], self.target_ids[idx]\n\nblock_size = 6\nstride = 3\ndataset = LLMDataset(token_ids, block_size=block_size, stride=stride)\n\nprint(\"STEP 3 - Create Dataset:\")\nprint(f\" Block size: {block_size}\")\nprint(f\" Stride: {stride}\")\nprint(f\" Number of examples: {len(dataset)}\")\n\n# Show one example decoded\ninp, tgt = dataset[0]\nprint(f\" Example 0:\")\nprint(f\" Input: {[id_to_word[i] for i in inp]}\")\nprint(f\" Target: {[id_to_word[i] for i in tgt]}\")\nprint(f\" (Each input word predicts the corresponding target word)\\\n\")\n\n# === STEP 4: Create DataLoader ===\nclass SimpleDataLoader:\n def __init__(self, dataset, batch_size, shuffle=True, seed=None):\n self.dataset = dataset\n self.batch_size = batch_size\n self.shuffle = shuffle\n if seed is not None:\n random.seed(seed)\n def __len__(self):\n return (len(self.dataset) + self.batch_size - 1) // self.batch_size\n def __iter__(self):\n indices = list(range(len(self.dataset)))\n if self.shuffle:\n random.shuffle(indices)\n for start in range(0, len(indices), self.batch_size):\n batch_idx = indices[start : start + self.batch_size]\n b_inp = [self.dataset[i][0] for i in batch_idx]\n b_tgt = [self.dataset[i][1] for i in batch_idx]\n yield b_inp, b_tgt\n\nbatch_size = 4\nloader = SimpleDataLoader(dataset, batch_size=batch_size, shuffle=True, seed=123)\n\nprint(\"STEP 4 - DataLoader:\")\nprint(f\" Batch size: {batch_size}\")\nprint(f\" Batches per epoch: {len(loader)}\")\nprint()\n\n# === STEP 5: Iterate ===\nprint(\"STEP 5 - Training Loop (first 2 batches):\")\nfor batch_num, (inputs, targets) in enumerate(loader):\n print(f\" Batch {batch_num + 1}: {len(inputs)} examples x {len(inputs[0])} tokens\")\n if batch_num == 0:\n for j in range(min(2, len(inputs))):\n inp_words = [id_to_word[i] for i in inputs[j]]\n tgt_words = [id_to_word[i] for i in targets[j]]\n print(f\" inputs[{j}]: {inp_words}\")\n print(f\" targets[{j}]: {tgt_words}\")\n if batch_num &gt;= 1:\n break\n\nprint()\nprint(\"=== Pipeline Summary ===\")\nprint(f\" Raw text: {len(raw_text)} chars\")\nprint(f\" Tokens: {len(token_ids)}\")\nprint(f\" Training pairs: {len(dataset)}\")\nprint(f\" Batches/epoch: {len(loader)}\")\nprint(f\" Tokens/batch: {batch_size} x {block_size} = {batch_size * block_size}\")\n </code></pre>\n <div class=\"sandbox-output\"></div>\n </div>\n\n <div class=\"callout warning\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><path d=\"M15 14c.2-1 .7-1.7 1.5-2.5 1-.9 1.5-2.2 1.5-3.5A6 6 0 0 0 6 8c0 1 .2 2.2 1.5 3.5.7.7 1.3 1.5 1.5 2.5\"/><path d=\"M9 18h6\"/><path d=\"M10 22h4\"/></svg>لماذا يهم الخلط (Shuffling)</div>\n <p>\n بدون الخلط، يُقدّم DataLoader الأمثلة بترتيب متسلسل. هذا يعني أن\n الدفعات المتتالية تحتوي على بيانات مترابطة بشدة (نوافذ نصية متداخلة)، مما\n قد يضر باستقرار التدريب. يضمن الخلط أن تحتوي كل دفعة على أمثلة متنوعة\n من أجزاء مختلفة من مجموعة البيانات، مما يساعد النموذج على التعلم بشكل أكثر فعالية. مع ذلك،\n لاحظ أننا نخلط <em>الأمثلة</em> (النوافذ)، وليس الرموز داخل كل نافذة &mdash;\n يجب الحفاظ على الترتيب الداخلي لكل تسلسل.\n </p>\n </div>\n\n <h2><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"20\" height=\"20\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:8px;color:#E8A731\"><path d=\"M4 19.5v-15A2.5 2.5 0 0 1 6.5 2H20v20H6.5a2.5 2.5 0 0 1 0-5H20\"/></svg>من Python الخالصة إلى PyTorch</h2>\n <p>\n الأصناف (classes) التي بنيناها أعلاه تعكس واجهة برمجة تطبيقات PyTorch بدقة. عندما تنتقل إلى\n التدريب الحقيقي باستخدام وحدات معالجة الرسومات (GPUs)، كل ما عليك فعله هو:\n </p>\n <div class=\"feature-grid\">\n <div class=\"feature-card\">\n <h3>1. الوراثة من torch.utils.data.Dataset</h3>\n <p>استبدل صنفنا العادي بـ <code>class LLMDataset(torch.utils.data.Dataset)</code> وحوّل القوائم إلى <code>torch.tensor</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>2. استخدام torch.utils.data.DataLoader</h3>\n <p>يضيف DataLoader الخاص بـ PyTorch تحميل بيانات متعدد العمّال (multi-worker)، وذاكرة مثبّتة لوحدة معالجة الرسومات (GPU pinned memory)، وتجميعًا تلقائيًا للدفعات باستخدام <code>collate_fn</code>.</p>\n </div>\n <div class=\"feature-card\">\n <h3>3. إضافة تضمينات الرموز (Token Embeddings)</h3>\n <p>يُبحث عن كل معرّف رمز في جدول تضمين (<code>nn.Embedding</code>) لإنتاج متجه كثيف &mdash; الطبقة الأولى من Transformer.</p>\n </div>\n <div class=\"feature-card\">\n <h3>4. إضافة الترميز الموضعي (Positional Encodings)</h3>\n <p>بما أن نماذج Transformer ليس لديها مفهوم مدمج للترتيب، فإننا نضيف معلومات الموضع إلى كل تضمين رمز (سيتم تناولها في محاضرة لاحقة).</p>\n </div>\n </div>\n\n <div class=\"callout info\">\n <div class=\"callout-title\"><svg xmlns=\"http://www.w3.org/2000/svg\" width=\"18\" height=\"18\" viewBox=\"0 0 24 24\" fill=\"none\" stroke=\"currentColor\" stroke-width=\"2\" stroke-linecap=\"round\" stroke-linejoin=\"round\" style=\"display:inline-block;vertical-align:middle;margin-right:6px;color:#E8A731\"><polygon points=\"12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2\"/></svg>النقاط الرئيسية</div>\n <ul>\n <li>تتعلم نماذج اللغة الكبيرة عن طريق التنبؤ بالرمز التالي. كل موضع في التسلسل هو مثال تدريبي.</li>\n <li>تُنشأ أزواج المُدخل-الهدف عن طريق إزاحة تسلسل الرموز بموضع واحد.</li>\n <li><strong>نافذة السياق</strong> (block_size) تحدّ من عدد الرموز التي يمكن للنموذج رؤيتها دفعة واحدة.</li>\n <li><strong>الخطوة (Stride)</strong> تتحكم في التداخل بين الأمثلة التدريبية المتتالية.</li>\n <li>صنف Dataset ينظّم البيانات؛ ويتولى DataLoader عملية التجميع في دفعات والخلط.</li>\n <li>خط الأنابيب هذا نفسه (التجزئة إلى رموز &rarr; النافذة المنزلقة &rarr; التجميع في دفعات) يُستخدم من قِبل GPT-2 وLLaMA ونماذج لغوية كبيرة إنتاجية أخرى.</li>\n </ul>\n </div>\n\n <div class=\"exercise-box\">\n <h3> تمرين: ابنِ خط أنابيب البيانات الخاص بك</h3>\n <ol>\n <li>خذ فقرة من النص، جزّئها إلى رموز باستخدام <code>CharTokenizer</code> من المحاضرة 7، وأنشئ Dataset بحجم كتلة block_size=16 وخطوة stride=8.</li>\n <li>نفّذ معامل <code>drop_last</code> في SimpleDataLoader يُهمل الدفعة الأخيرة إذا كانت أصغر من <code>batch_size</code>. هذا مهم للتدريب لأن جميع الدفعات يجب أن يكون لها نفس الشكل.</li>\n <li>احسب عدد الأمثلة التدريبية التي تحصل عليها من مجموعة بيانات (corpus) مكوّنة من مليون رمز مع block_size=1024 وstride=1024. كيف يتغير هذا مع stride=512؟</li>\n <li>إضافي: أضف طريقة (method) إلى DataLoader تُبلّغ عن إحصائيات &mdash; إجمالي الرموز المُعالَجة، والرموز الفريدة التي شوهدت، وعدد العصور (epochs) المُقدَّر المطلوبة للوصول إلى عدد مستهدف من خطوات التدريب.</li>\n </ol>\n </div>"}, "codeBlocks": []}