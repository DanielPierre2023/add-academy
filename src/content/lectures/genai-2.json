{
  "id": "genai-2",
  "title": {
    "en": "GenAI-2: Build a Video Generation SaaS",
    "ro": "Lecția GenAI 2: Construiește un SaaS de Generare Video",
    "el": "Διάλεξη GenAI 2: Κατασκευή ενός SaaS Δημιουργίας Βίντεο"
  },
  "content": {
    "en": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\" style=\"background: linear-gradient(135deg, #7c3aed, #a855f7);\">BUILD: AI SaaS Product</div>\n<h1>GenAI-2: Build a Video Generation SaaS</h1>\n<p class=\"lecture-subtitle\">Build ClipCraft &mdash; a production-ready video generation service with async job queues, progress tracking, and a FastAPI backend that orchestrates text-to-video models.</p>\n<div class=\"lecture-meta\">\n<span>~75 min</span> | <span>Advanced / Hands-On</span> | <span>You will build and deploy a real product</span>\n</div>\n</div>\n\n<h2>What You Will Build</h2>\n\n<p>In this lesson you build <strong>ClipCraft</strong> &mdash; a video generation SaaS. Users submit text prompts, your server dispatches the request to a text-to-video model (Runway, Pika, or Stability AI Video), manages the long-running generation process via an async job queue, streams progress updates, and delivers the final video. You will implement the full stack: API server, video processing pipeline, job management, progress tracking, and deployment.</p>\n\n<p>Video generation is fundamentally different from image generation. A single video can take <strong>30 seconds to 5 minutes</strong> to produce, meaning you cannot use a simple request-response pattern. Instead, you need an <strong>asynchronous architecture</strong> with job queues, status polling, and webhook notifications.</p>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #7c3aed;\">\n<h3>ClipCraft &mdash; Complete Project Overview</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Component</th>\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Technology</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">API Server</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Python + FastAPI</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Video Generation</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway Gen-3, Pika Labs, or Stability AI Video (via API)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Job Queue</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">In-process async queue (dev) / Redis + Celery (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Progress Tracking</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Polling endpoint + webhook callbacks</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Video Processing</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">NumPy (frame math) + FFmpeg (encoding)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Database</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">SQLite (dev) / PostgreSQL (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Frontend</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">HTML + vanilla JS</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Deployment</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Docker + any cloud provider</td></tr>\n</table>\n</div>\n\n<h2>Project Structure</h2>\n\n<p>Create this folder structure on your machine. Every file is explained in this lesson.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Project Layout</span></div>\n<pre><code>clipcraft/\n&boxvr;&boxh;&boxh; app/\n&boxv;   &boxvr;&boxh;&boxh; __init__.py\n&boxv;   &boxvr;&boxh;&boxh; main.py            # FastAPI application\n&boxv;   &boxvr;&boxh;&boxh; config.py          # Settings and API keys\n&boxv;   &boxvr;&boxh;&boxh; models.py          # Pydantic request/response models\n&boxv;   &boxvr;&boxh;&boxh; video_service.py   # Video generation API wrapper\n&boxv;   &boxvr;&boxh;&boxh; job_queue.py       # Async job queue system\n&boxv;   &boxvr;&boxh;&boxh; progress.py        # Progress tracking and webhooks\n&boxv;   &boxvr;&boxh;&boxh; processing.py      # Video frame processing\n&boxv;   &boxvr;&boxh;&boxh; billing.py         # Usage tracking and billing\n&boxv;   &boxur;&boxh;&boxh; database.py        # Database connection\n&boxvr;&boxh;&boxh; static/\n&boxv;   &boxvr;&boxh;&boxh; index.html         # Frontend UI\n&boxv;   &boxur;&boxh;&boxh; style.css\n&boxvr;&boxh;&boxh; tests/\n&boxv;   &boxur;&boxh;&boxh; test_api.py\n&boxvr;&boxh;&boxh; requirements.txt\n&boxvr;&boxh;&boxh; Dockerfile\n&boxvr;&boxh;&boxh; docker-compose.yml\n&boxur;&boxh;&boxh; .env.example</code></pre>\n</div>\n\n<h2>Part 1: How Video Generation APIs Work</h2>\n\n<p>Text-to-video models extend diffusion techniques from images to temporal sequences. Instead of denoising a single frame, they denoise a <strong>spatio-temporal volume</strong> &mdash; producing multiple coherent frames that flow naturally. This is orders of magnitude more expensive than image generation and takes significantly longer.</p>\n\n<div class=\"concept-card\">\n<h3>Video Generation API Comparison (2024&ndash;2025)</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Provider</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Model</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Cost per video</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Duration / Resolution</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Gen-3 Alpha</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.25&ndash;$1.00</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5&ndash;10s / up to 1280&times;768</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika Labs</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika 1.0</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.20&ndash;$0.80</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">3&ndash;4s / up to 1024&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stability AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stable Video Diffusion</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.10&ndash;$0.50</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">4s / 576&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Luma AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Dream Machine</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.30&ndash;$0.90</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5s / up to 1360&times;752</td></tr>\n</table>\n</div>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #f59e0b;\">\n<h3>Key Difference from Image Generation</h3>\n<p>Image generation returns a result in 2&ndash;10 seconds. Video generation takes <strong>30 seconds to 5 minutes</strong>. This means you cannot hold an HTTP connection open and wait. You need:</p>\n<ul>\n<li><strong>Job Queue:</strong> Accept the request, return a job ID immediately, process in the background</li>\n<li><strong>Progress Tracking:</strong> Let clients poll for status or receive webhook callbacks</li>\n<li><strong>Retry Logic:</strong> Long-running tasks fail more often &mdash; you must handle retries gracefully</li>\n<li><strong>Resource Management:</strong> Limit concurrent jobs to control costs and prevent overload</li>\n</ul>\n</div>\n\n<p>The architectural pattern is: <strong>accept &rarr; enqueue &rarr; process &rarr; notify</strong>. Let us build each piece.</p>\n\n<h2>Part 2: Video Processing Pipeline</h2>\n\n<p>Before integrating external APIs, let us understand how video frames work. A video is a sequence of image frames played in rapid succession. Each frame is a NumPy array of pixel data. The code below demonstrates how to generate and process video frames programmatically.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# processing.py - Video frame generation and processing\n\nimport numpy as np\nimport time\n\nclass VideoFrameProcessor:\n    \"\"\"Generate and process video frames using NumPy.\"\"\"\n\n    @staticmethod\n    def generate_gradient_frames(width, height, num_frames, fps=24):\n        \"\"\"Generate a sequence of gradient frames that animate over time.\"\"\"\n        frames = []\n        for i in range(num_frames):\n            frame = np.zeros((height, width, 3), dtype=np.uint8)\n            t = i / num_frames  # normalized time 0..1\n\n            for y in range(height):\n                for x in range(width):\n                    # Red channel: horizontal sweep that shifts over time\n                    r = int(255 * ((x / width + t) % 1.0))\n                    # Green channel: vertical pulse\n                    g = int(255 * ((y / height + t * 0.5) % 1.0))\n                    # Blue channel: radial wave from center\n                    cx, cy = width / 2, height / 2\n                    dist = ((x - cx)**2 + (y - cy)**2) ** 0.5\n                    max_dist = (cx**2 + cy**2) ** 0.5\n                    b = int(127 + 128 * np.sin(dist / max_dist * 6.28 - t * 6.28))\n                    frame[y, x] = [r, g, b]\n\n            frames.append(frame)\n        return frames\n\n    @staticmethod\n    def apply_fade_transition(frames, fade_length=5):\n        \"\"\"Apply a fade-in at the start and fade-out at the end.\"\"\"\n        result = []\n        total = len(frames)\n        for i, frame in enumerate(frames):\n            factor = 1.0\n            if i < fade_length:\n                factor = i / fade_length\n            elif i >= total - fade_length:\n                factor = (total - 1 - i) / fade_length\n            faded = (frame.astype(np.float64) * factor).astype(np.uint8)\n            result.append(faded)\n        return result\n\n    @staticmethod\n    def compute_frame_stats(frames):\n        \"\"\"Compute statistics across all frames.\"\"\"\n        brightnesses = []\n        for frame in frames:\n            avg = frame.mean()\n            brightnesses.append(round(float(avg), 2))\n\n        return {\n            \"num_frames\": len(frames),\n            \"resolution\": f\"{frames[0].shape[1]}x{frames[0].shape[0]}\",\n            \"avg_brightness\": round(np.mean(brightnesses), 2),\n            \"min_brightness\": min(brightnesses),\n            \"max_brightness\": max(brightnesses),\n            \"total_pixels\": frames[0].shape[0] * frames[0].shape[1] * len(frames),\n            \"memory_mb\": round(sum(f.nbytes for f in frames) / (1024*1024), 2)\n        }\n\n    @staticmethod\n    def add_text_overlay_region(frame, x, y, box_w, box_h, color=(255, 255, 255)):\n        \"\"\"Add a colored rectangle region (simulating text overlay area).\"\"\"\n        result = frame.copy()\n        y_end = min(y + box_h, frame.shape[0])\n        x_end = min(x + box_w, frame.shape[1])\n        # Semi-transparent overlay\n        overlay = result[y:y_end, x:x_end].astype(np.float64)\n        overlay = overlay * 0.5 + np.array(color, dtype=np.float64) * 0.5\n        result[y:y_end, x:x_end] = overlay.astype(np.uint8)\n        return result\n\n\n# --- Demo: Generate and process video frames ---\nproc = VideoFrameProcessor()\n\nprint(\"=== ClipCraft Video Processing Demo ===\\\n\")\n\n# 1. Generate frames (small for demo)\nprint(\"1. Generating 12 frames at 32x24 resolution...\")\nstart = time.time()\nframes = proc.generate_gradient_frames(32, 24, 12, fps=24)\nelapsed = round(time.time() - start, 3)\nprint(f\"   Generated in {elapsed}s\")\n\nstats = proc.compute_frame_stats(frames)\nfor key, val in stats.items():\n    print(f\"   {key}: {val}\")\n\n# 2. Apply fade transition\nprint(\"\\\n2. Applying fade-in/fade-out transition...\")\nfaded = proc.apply_fade_transition(frames, fade_length=3)\nfaded_stats = proc.compute_frame_stats(faded)\nprint(f\"   Avg brightness before fade: {stats['avg_brightness']}\")\nprint(f\"   Avg brightness after fade:  {faded_stats['avg_brightness']}\")\nprint(f\"   (Lower because start/end frames are darkened)\")\n\n# 3. Add overlay region to a frame\nprint(\"\\\n3. Adding text overlay region to frame 6...\")\noverlay_frame = proc.add_text_overlay_region(frames[5], 2, 2, 20, 8)\nbefore_mean = round(float(frames[5][2:10, 2:22].mean()), 1)\nafter_mean = round(float(overlay_frame[2:10, 2:22].mean()), 1)\nprint(f\"   Region avg before: {before_mean}\")\nprint(f\"   Region avg after:  {after_mean} (blended with white)\")\n\n# 4. Estimate production video sizes\nprint(\"\\\n4. Production video size estimates:\")\nresolutions = [\n    (\"720p\", 1280, 720, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 10),\n]\nfor label, w, h, fps, duration in resolutions:\n    raw_mb = w * h * 3 * fps * duration / (1024**2)\n    compressed_mb = raw_mb * 0.02  # typical H.264 ratio\n    print(f\"   {label} {duration}s @ {fps}fps: \"\n          f\"{raw_mb:.0f} MB raw, ~{compressed_mb:.1f} MB compressed (H.264)\")\n\nprint(\"\\\nDone! Frame processing pipeline is ready.\")\n</code></pre>\n</div>\n\n<h2>Part 3: Async Job Queue</h2>\n\n<p>Video generation takes too long for synchronous HTTP. We need a job queue that accepts requests, processes them in the background, and lets clients check status. This is the core architectural pattern for any long-running task in a SaaS product.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# job_queue.py - Async job queue for video generation\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass JobStatus:\n    PENDING = \"pending\"\n    PROCESSING = \"processing\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    CANCELLED = \"cancelled\"\n\nclass VideoJob:\n    \"\"\"Represents a single video generation job.\"\"\"\n\n    def __init__(self, prompt, style, duration_sec, resolution, user_id):\n        self.id = str(uuid.uuid4())[:8]\n        self.prompt = prompt\n        self.style = style\n        self.duration_sec = duration_sec\n        self.resolution = resolution\n        self.user_id = user_id\n        self.status = JobStatus.PENDING\n        self.progress = 0\n        self.created_at = datetime.now().isoformat()\n        self.started_at = None\n        self.completed_at = None\n        self.result_url = None\n        self.error = None\n        self.retry_count = 0\n        self.max_retries = 3\n\n    def to_dict(self):\n        return {\n            \"id\": self.id,\n            \"prompt\": self.prompt[:40],\n            \"status\": self.status,\n            \"progress\": self.progress,\n            \"created_at\": self.created_at,\n            \"started_at\": self.started_at,\n            \"completed_at\": self.completed_at,\n            \"result_url\": self.result_url,\n            \"error\": self.error\n        }\n\n\nclass JobQueue:\n    \"\"\"In-process job queue for video generation tasks.\"\"\"\n\n    def __init__(self, max_concurrent=3):\n        self.jobs = {}\n        self.queue = []        # pending job IDs\n        self.active = []       # currently processing job IDs\n        self.max_concurrent = max_concurrent\n\n    def submit(self, prompt, style, duration_sec, resolution, user_id):\n        \"\"\"Submit a new video generation job. Returns job ID.\"\"\"\n        job = VideoJob(prompt, style, duration_sec, resolution, user_id)\n        self.jobs[job.id] = job\n        self.queue.append(job.id)\n        return job.id\n\n    def get_status(self, job_id):\n        \"\"\"Get current status of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job.to_dict()\n\n    def process_next(self):\n        \"\"\"Try to start the next pending job. Returns job or None.\"\"\"\n        if len(self.active) >= self.max_concurrent:\n            return None\n        if not self.queue:\n            return None\n\n        job_id = self.queue.pop(0)\n        job = self.jobs[job_id]\n        job.status = JobStatus.PROCESSING\n        job.started_at = datetime.now().isoformat()\n        self.active.append(job_id)\n        return job\n\n    def update_progress(self, job_id, progress):\n        \"\"\"Update job progress (0-100).\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.progress = min(progress, 100)\n\n    def complete_job(self, job_id, result_url):\n        \"\"\"Mark job as completed with result URL.\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.status = JobStatus.COMPLETED\n            job.progress = 100\n            job.completed_at = datetime.now().isoformat()\n            job.result_url = result_url\n            if job_id in self.active:\n                self.active.remove(job_id)\n\n    def fail_job(self, job_id, error):\n        \"\"\"Mark job as failed. Re-queue if retries remain.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n        if job_id in self.active:\n            self.active.remove(job_id)\n\n        job.retry_count += 1\n        if job.retry_count < job.max_retries:\n            job.status = JobStatus.PENDING\n            job.progress = 0\n            self.queue.append(job_id)\n            return f\"Retrying ({job.retry_count}/{job.max_retries})\"\n        else:\n            job.status = JobStatus.FAILED\n            job.error = error\n            job.completed_at = datetime.now().isoformat()\n            return \"Max retries exceeded\"\n\n    def cancel_job(self, job_id):\n        \"\"\"Cancel a pending or active job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return False\n        job.status = JobStatus.CANCELLED\n        job.completed_at = datetime.now().isoformat()\n        if job_id in self.queue:\n            self.queue.remove(job_id)\n        if job_id in self.active:\n            self.active.remove(job_id)\n        return True\n\n    def get_queue_stats(self):\n        \"\"\"Get overall queue statistics.\"\"\"\n        statuses = {}\n        for job in self.jobs.values():\n            statuses[job.status] = statuses.get(job.status, 0) + 1\n        return {\n            \"total_jobs\": len(self.jobs),\n            \"queue_length\": len(self.queue),\n            \"active_jobs\": len(self.active),\n            \"max_concurrent\": self.max_concurrent,\n            \"by_status\": statuses\n        }\n\n\n# --- Demo: Simulate a video generation workload ---\nqueue = JobQueue(max_concurrent=2)\n\nprint(\"=== ClipCraft Job Queue Demo ===\\\n\")\n\n# Submit several jobs\nprompts = [\n    (\"A serene lake at sunset with mountains\", \"cinematic\", 5),\n    (\"Robot dancing in a neon city\", \"anime\", 4),\n    (\"Aerial flyover of a tropical island\", \"drone\", 8),\n    (\"A flower blooming in timelapse\", \"macro\", 3),\n    (\"Astronaut walking on Mars\", \"cinematic\", 6),\n]\n\nprint(\"Submitting 5 video generation jobs:\")\njob_ids = []\nfor prompt, style, dur in prompts:\n    jid = queue.submit(prompt, style, dur, \"1280x720\", \"user_demo\")\n    job_ids.append(jid)\n    print(f\"  Job {jid}: '{prompt[:35]}...' ({dur}s, {style})\")\n\nprint(f\"\\\nQueue stats: {queue.get_queue_stats()}\")\n\n# Process jobs step by step\nprint(\"\\\n--- Processing Loop ---\\\n\")\n\nstep = 0\nwhile queue.queue or queue.active:\n    step += 1\n\n    # Start next jobs if capacity available\n    while len(queue.active) < queue.max_concurrent and queue.queue:\n        job = queue.process_next()\n        if job:\n            print(f\"Step {step}: STARTED job {job.id} \"\n                  f\"({job.prompt[:25]}...)\")\n\n    # Simulate progress on active jobs\n    for jid in list(queue.active):\n        job = queue.jobs[jid]\n        job.progress += 35  # simulate chunk of progress\n\n        if job.progress >= 100:\n            # Simulate: 1 in 5 chance of failure on first attempt\n            if job.retry_count == 0 and job.id == job_ids[2]:\n                msg = queue.fail_job(jid, \"GPU timeout\")\n                print(f\"Step {step}: FAILED job {jid} -> {msg}\")\n            else:\n                url = f\"https://cdn.clipcraft.dev/v/{jid}.mp4\"\n                queue.complete_job(jid, url)\n                print(f\"Step {step}: COMPLETED job {jid} \"\n                      f\"-> {url}\")\n        else:\n            queue.update_progress(jid, job.progress)\n            print(f\"Step {step}: job {jid} progress: \"\n                  f\"{job.progress}%\")\n\nprint(f\"\\\n--- Final Results ---\\\n\")\nfor jid in job_ids:\n    status = queue.get_status(jid)\n    print(f\"Job {jid}: {status['status']}\"\n          f\" | progress: {status['progress']}%\"\n          f\" | result: {status.get('result_url', 'N/A')}\"\n          f\" | error: {status.get('error', 'none')}\")\n\nprint(f\"\\\nFinal queue stats: {queue.get_queue_stats()}\")\n</code></pre>\n</div>\n\n<h2>Part 4: Progress Tracking</h2>\n\n<p>Clients need to know how their video is progressing. We support two patterns: <strong>polling</strong> (client checks periodically) and <strong>webhooks</strong> (server pushes updates). Both are essential for a production SaaS. The code below demonstrates the progress tracker with simulated webhook delivery.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# progress.py - Progress tracking with polling and webhooks\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass ProgressTracker:\n    \"\"\"Track video generation progress with polling and webhook support.\"\"\"\n\n    def __init__(self):\n        self.jobs = {}\n        self.webhook_log = []  # simulated webhook deliveries\n\n    def register_job(self, job_id, webhook_url=None):\n        \"\"\"Register a job for progress tracking.\"\"\"\n        self.jobs[job_id] = {\n            \"job_id\": job_id,\n            \"progress\": 0,\n            \"stage\": \"queued\",\n            \"webhook_url\": webhook_url,\n            \"events\": [],\n            \"started\": datetime.now().isoformat()\n        }\n\n    def update(self, job_id, progress, stage, message=\"\"):\n        \"\"\"Update progress and optionally fire webhook.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n\n        job[\"progress\"] = progress\n        job[\"stage\"] = stage\n\n        event = {\n            \"timestamp\": datetime.now().isoformat(),\n            \"progress\": progress,\n            \"stage\": stage,\n            \"message\": message\n        }\n        job[\"events\"].append(event)\n\n        # Fire webhook if configured\n        if job[\"webhook_url\"]:\n            self._fire_webhook(job_id, event)\n\n    def _fire_webhook(self, job_id, event):\n        \"\"\"Simulate sending a webhook notification.\"\"\"\n        payload = {\n            \"event\": \"video.progress\",\n            \"job_id\": job_id,\n            \"data\": event,\n            \"delivery_id\": str(uuid.uuid4())[:8]\n        }\n        self.webhook_log.append(payload)\n\n    def poll(self, job_id):\n        \"\"\"Client polls for current status (the polling endpoint).\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return {\"error\": \"Job not found\"}\n        return {\n            \"job_id\": job[\"job_id\"],\n            \"progress\": job[\"progress\"],\n            \"stage\": job[\"stage\"],\n            \"latest_event\": job[\"events\"][-1] if job[\"events\"] else None\n        }\n\n    def get_full_history(self, job_id):\n        \"\"\"Get the full event history of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job[\"events\"]\n\n\n# --- Demo: Simulate video generation with progress ---\ntracker = ProgressTracker()\n\nprint(\"=== ClipCraft Progress Tracking Demo ===\\\n\")\n\n# Job 1: With webhook\njob1 = \"vid_abc123\"\ntracker.register_job(job1, webhook_url=\"https://myapp.com/hooks/clipcraft\")\n\n# Job 2: Polling only\njob2 = \"vid_def456\"\ntracker.register_job(job2, webhook_url=None)\n\n# Simulate the video generation stages\nstages = [\n    (0,   \"queued\",       \"Job accepted, waiting for GPU\"),\n    (5,   \"initializing\", \"Loading model weights\"),\n    (15,  \"generating\",   \"Generating frames 1-30\"),\n    (40,  \"generating\",   \"Generating frames 31-60\"),\n    (65,  \"generating\",   \"Generating frames 61-90\"),\n    (80,  \"generating\",   \"Generating frames 91-120\"),\n    (90,  \"encoding\",     \"Encoding H.264 video\"),\n    (95,  \"uploading\",    \"Uploading to CDN\"),\n    (100, \"completed\",    \"Video ready for download\"),\n]\n\nprint(\"--- Job 1 (with webhook) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job1, progress, stage, message)\n    status = tracker.poll(job1)\n    print(f\"  [{status['progress']:3d}%] {status['stage']:14s} | {message}\")\n\nprint(f\"\\\n  Webhooks fired: {len(tracker.webhook_log)}\")\nfor wh in tracker.webhook_log[:3]:\n    print(f\"    -> {wh['event']}: {wh['data']['stage']} \"\n          f\"({wh['data']['progress']}%) [delivery: {wh['delivery_id']}]\")\nprint(f\"    ... and {len(tracker.webhook_log) - 3} more\")\n\nprint(\"\\\n--- Job 2 (polling only) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job2, progress, stage, message)\n\n# Simulate a client polling at intervals\npoll_points = [0, 20, 50, 80, 100]\nprint(\"  Client polling at intervals:\")\nhistory = tracker.get_full_history(job2)\nfor i, event in enumerate(history):\n    if event[\"progress\"] in poll_points:\n        print(f\"    Poll response: {event['progress']}% - {event['stage']}\")\n\nprint(f\"\\\n  Total events recorded: {len(history)}\")\n\n# Show webhook payload structure\nprint(\"\\\n--- Webhook Payload Structure ---\")\nsample = tracker.webhook_log[4]\nprint(\"  {\")\nfor key, val in sample.items():\n    if key == \"data\":\n        print(f'    \"data\": {{')\n        for dk, dv in val.items():\n            print(f'      \"{dk}\": \"{dv}\",')\n        print(f'    }},')\n    else:\n        print(f'    \"{key}\": \"{val}\",')\nprint(\"  }\")\n\nprint(\"\\\nBoth patterns ready for production use.\")\n</code></pre>\n</div>\n\n<h2>Part 5: The FastAPI Server</h2>\n\n<p>This is the heart of ClipCraft &mdash; the API server that ties together the job queue, progress tracking, video processing, and billing. The key difference from an image generation server is the <strong>async pattern</strong>: the <code>/api/generate</code> endpoint returns immediately with a job ID, and clients poll <code>/api/jobs/{id}</code> for progress.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/main.py</span></div>\n<pre><code class=\"language-python\">\"\"\"ClipCraft API Server - Video Generation SaaS.\"\"\"\nfrom fastapi import FastAPI, HTTPException, Depends, Header, BackgroundTasks\nfrom fastapi.staticfiles import StaticFiles\nfrom fastapi.responses import JSONResponse\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\nimport uuid\nimport asyncio\nimport time\n\nfrom .config import settings\nfrom .video_service import VideoService\nfrom .job_queue import JobQueue, JobStatus\nfrom .progress import ProgressTracker\nfrom .billing import BillingSystem\n\napp = FastAPI(title=\"ClipCraft API\", version=\"1.0.0\")\napp.mount(\"/static\", StaticFiles(directory=\"static\"), name=\"static\")\n\n# Initialize services\nvideo_service = VideoService(\n    provider=settings.VIDEO_PROVIDER,\n    api_key=settings.VIDEO_API_KEY\n)\njob_queue = JobQueue(max_concurrent=settings.MAX_CONCURRENT_JOBS)\nprogress_tracker = ProgressTracker()\nbilling = BillingSystem()\n\n\n# --- Request/Response Models ---\nclass GenerateRequest(BaseModel):\n    prompt: str = Field(..., min_length=3, max_length=500)\n    style: str = Field(default=\"cinematic\")\n    duration: int = Field(default=5, ge=2, le=15)\n    resolution: str = Field(default=\"1280x720\")\n    webhook_url: Optional[str] = None\n\nclass JobResponse(BaseModel):\n    job_id: str\n    status: str\n    message: str\n    poll_url: str\n\nclass JobStatusResponse(BaseModel):\n    job_id: str\n    status: str\n    progress: int\n    stage: str\n    result_url: Optional[str] = None\n    error: Optional[str] = None\n    estimated_remaining_sec: Optional[int] = None\n\n\n# --- API key authentication ---\nasync def get_user(x_api_key: str = Header(...)):\n    user = billing.get_user_by_key(x_api_key)\n    if not user:\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n    return user\n\n\n# --- Background video generation task ---\nasync def process_video_job(job_id: str):\n    \"\"\"Background task that processes a video generation job.\"\"\"\n    job = job_queue.jobs.get(job_id)\n    if not job:\n        return\n\n    try:\n        progress_tracker.update(job_id, 5, \"initializing\",\n                                \"Loading model and preparing generation\")\n\n        # Call the video generation API\n        result = await video_service.generate(\n            prompt=job.prompt,\n            style=job.style,\n            duration_sec=job.duration_sec,\n            resolution=job.resolution,\n            progress_callback=lambda p, s, m:\n                progress_tracker.update(job_id, p, s, m)\n        )\n\n        progress_tracker.update(job_id, 95, \"uploading\",\n                                \"Uploading to CDN\")\n\n        video_url = result[\"video_url\"]\n        job_queue.complete_job(job_id, video_url)\n\n        progress_tracker.update(job_id, 100, \"completed\",\n                                \"Video ready for download\")\n\n        billing.record_generation(\n            job.user_id, job.prompt,\n            result[\"cost\"], job.resolution\n        )\n\n    except Exception as e:\n        msg = job_queue.fail_job(job_id, str(e))\n        progress_tracker.update(job_id, 0, \"failed\", str(e))\n\n\n# --- Endpoints ---\n@app.post(\"/api/generate\", response_model=JobResponse)\nasync def generate_video(req: GenerateRequest,\n                         background_tasks: BackgroundTasks,\n                         user=Depends(get_user)):\n    \"\"\"Submit a video generation job. Returns immediately with a job ID.\"\"\"\n    # Check billing limits\n    can, reason = billing.can_generate(user[\"id\"])\n    if not can:\n        raise HTTPException(status_code=429, detail=reason)\n\n    # Submit to job queue\n    job_id = job_queue.submit(\n        prompt=req.prompt,\n        style=req.style,\n        duration_sec=req.duration,\n        resolution=req.resolution,\n        user_id=user[\"id\"]\n    )\n\n    # Register progress tracking\n    progress_tracker.register_job(job_id, webhook_url=req.webhook_url)\n\n    # Start processing in the background\n    job = job_queue.process_next()\n    if job:\n        background_tasks.add_task(process_video_job, job.id)\n\n    return JobResponse(\n        job_id=job_id,\n        status=\"pending\",\n        message=\"Video generation job submitted\",\n        poll_url=f\"/api/jobs/{job_id}\"\n    )\n\n\n@app.get(\"/api/jobs/{job_id}\", response_model=JobStatusResponse)\nasync def get_job_status(job_id: str, user=Depends(get_user)):\n    \"\"\"Poll for job status and progress.\"\"\"\n    status = job_queue.get_status(job_id)\n    if not status:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n\n    poll_data = progress_tracker.poll(job_id)\n    estimated = None\n    if poll_data and poll_data.get(\"progress\", 0) > 0:\n        pct = poll_data[\"progress\"]\n        if pct < 100:\n            estimated = int((100 - pct) * 1.5)\n\n    return JobStatusResponse(\n        job_id=job_id,\n        status=status[\"status\"],\n        progress=status.get(\"progress\", 0),\n        stage=poll_data.get(\"stage\", \"unknown\") if poll_data else \"unknown\",\n        result_url=status.get(\"result_url\"),\n        error=status.get(\"error\"),\n        estimated_remaining_sec=estimated\n    )\n\n\n@app.delete(\"/api/jobs/{job_id}\")\nasync def cancel_job(job_id: str, user=Depends(get_user)):\n    \"\"\"Cancel a pending or active job.\"\"\"\n    success = job_queue.cancel_job(job_id)\n    if not success:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n    return {\"message\": \"Job cancelled\", \"job_id\": job_id}\n\n\n@app.get(\"/api/jobs\")\nasync def list_jobs(user=Depends(get_user)):\n    \"\"\"List all jobs for the authenticated user.\"\"\"\n    user_jobs = [\n        job.to_dict() for job in job_queue.jobs.values()\n        if job.user_id == user[\"id\"]\n    ]\n    return {\"jobs\": user_jobs, \"total\": len(user_jobs)}\n\n\n@app.get(\"/api/queue/stats\")\nasync def queue_stats():\n    \"\"\"Get queue statistics (admin endpoint).\"\"\"\n    return job_queue.get_queue_stats()\n\n\n@app.get(\"/api/health\")\nasync def health():\n    return {\n        \"status\": \"ok\",\n        \"provider\": settings.VIDEO_PROVIDER,\n        \"queue\": job_queue.get_queue_stats()\n    }</code></pre>\n</div>\n\n<h2>Part 6: Configuration and Deployment</h2>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/config.py</span></div>\n<pre><code class=\"language-python\">\"\"\"Configuration loaded from environment variables.\"\"\"\nfrom pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    VIDEO_PROVIDER: str = \"runway\"  # \"runway\", \"pika\", or \"stability\"\n    VIDEO_API_KEY: str = \"\"\n    DATABASE_URL: str = \"sqlite:///./clipcraft.db\"\n    MAX_CONCURRENT_JOBS: int = 3\n    MAX_VIDEO_DURATION: int = 15  # seconds\n    STORAGE_BUCKET: str = \"clipcraft-videos\"\n    WEBHOOK_SECRET: str = \"\"\n\n    class Config:\n        env_file = \".env\"\n\nsettings = Settings()</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>.env.example</span></div>\n<pre><code>VIDEO_PROVIDER=runway\nVIDEO_API_KEY=your_api_key_here\nDATABASE_URL=sqlite:///./clipcraft.db\nMAX_CONCURRENT_JOBS=3\nMAX_VIDEO_DURATION=15\nWEBHOOK_SECRET=your_webhook_secret</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>requirements.txt</span></div>\n<pre><code>fastapi==0.109.0\nuvicorn==0.27.0\nhttpx==0.26.0\npydantic==2.5.3\npydantic-settings==2.1.0\npython-multipart==0.0.6\naiofiles==23.2.1</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Dockerfile</span></div>\n<pre><code>FROM python:3.11-slim\n\n# Install FFmpeg for video processing\nRUN apt-get update &amp;&amp; \\\\\n    apt-get install -y --no-install-recommends ffmpeg &amp;&amp; \\\\\n    rm -rf /var/lib/apt/lists/*\n\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n\nEXPOSE 8000\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>docker-compose.yml</span></div>\n<pre><code>version: \"3.8\"\nservices:\n  clipcraft:\n    build: .\n    ports:\n      - \"8000:8000\"\n    env_file:\n      - .env\n    volumes:\n      - ./videos:/app/videos\n      - ./static:/app/static\n    deploy:\n      resources:\n        limits:\n          memory: 2G</code></pre>\n</div>\n\n<p>To run locally:</p>\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Terminal</span></div>\n<pre><code># Development\npip install -r requirements.txt\nuvicorn app.main:app --reload\n\n# Or with Docker\ndocker-compose up --build\n\n# Submit a video generation job\ncurl -X POST http://localhost:8000/api/generate \\\\\n  -H \"X-API-Key: your_key\" \\\\\n  -H \"Content-Type: application/json\" \\\\\n  -d '{\"prompt\": \"sunset over ocean waves\", \"style\": \"cinematic\", \"duration\": 5}'\n\n# Poll for progress\ncurl http://localhost:8000/api/jobs/JOB_ID \\\\\n  -H \"X-API-Key: your_key\"</code></pre>\n</div>\n\n<h2>Part 7: Frontend UI</h2>\n\n<p>The ClipCraft frontend handles the async workflow: submit a prompt, then poll for progress and display the result when ready. Save this as <code>static/index.html</code>.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>static/index.html (key parts)</span></div>\n<pre><code class=\"language-html\">&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n  &lt;title&gt;ClipCraft - AI Video Generation&lt;/title&gt;\n  &lt;style&gt;\n    body { font-family: system-ui; max-width: 800px; margin: 0 auto; padding: 20px;\n           background: #0f0f0f; color: #e5e5e5; }\n    h1 { background: linear-gradient(135deg, #7c3aed, #a855f7);\n         -webkit-background-clip: text; -webkit-text-fill-color: transparent; }\n    .prompt-input { width: 100%; padding: 14px; font-size: 16px;\n                    border: 2px solid #333; border-radius: 10px;\n                    background: #1a1a1a; color: #e5e5e5; margin: 10px 0; }\n    .controls { display: flex; gap: 12px; margin: 12px 0; align-items: center; }\n    .controls select { padding: 10px; border-radius: 8px; background: #1a1a1a;\n                        color: #e5e5e5; border: 2px solid #333; }\n    .generate-btn { padding: 14px 32px; background: #7c3aed; color: white;\n                    border: none; border-radius: 10px; font-size: 16px;\n                    cursor: pointer; }\n    .generate-btn:disabled { opacity: 0.5; cursor: not-allowed; }\n    .progress-bar { width: 100%; height: 8px; background: #333;\n                    border-radius: 4px; margin: 16px 0; display: none; }\n    .progress-fill { height: 100%; background: linear-gradient(90deg, #7c3aed, #a855f7);\n                     border-radius: 4px; transition: width 0.3s; width: 0%; }\n    .status-text { color: #a78bfa; font-size: 14px; margin: 8px 0; }\n    .result video { width: 100%; border-radius: 12px; margin: 20px 0; }\n  &lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;ClipCraft&lt;/h1&gt;\n  &lt;p&gt;Generate videos from text descriptions&lt;/p&gt;\n  &lt;input class=\"prompt-input\" placeholder=\"Describe the video you want...\" id=\"prompt\"&gt;\n  &lt;div class=\"controls\"&gt;\n    &lt;select id=\"style\"&gt;\n      &lt;option value=\"cinematic\"&gt;Cinematic&lt;/option&gt;\n      &lt;option value=\"anime\"&gt;Anime&lt;/option&gt;\n      &lt;option value=\"drone\"&gt;Drone Shot&lt;/option&gt;\n      &lt;option value=\"macro\"&gt;Macro&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;select id=\"duration\"&gt;\n      &lt;option value=\"3\"&gt;3 seconds&lt;/option&gt;\n      &lt;option value=\"5\" selected&gt;5 seconds&lt;/option&gt;\n      &lt;option value=\"10\"&gt;10 seconds&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;button class=\"generate-btn\" id=\"genBtn\" onclick=\"generate()\"&gt;Generate Video&lt;/button&gt;\n  &lt;/div&gt;\n  &lt;div class=\"progress-bar\" id=\"progressBar\"&gt;\n    &lt;div class=\"progress-fill\" id=\"progressFill\"&gt;&lt;/div&gt;\n  &lt;/div&gt;\n  &lt;p class=\"status-text\" id=\"statusText\"&gt;&lt;/p&gt;\n  &lt;div class=\"result\" id=\"result\"&gt;&lt;/div&gt;\n\n  &lt;script&gt;\n  let pollInterval = null;\n\n  async function generate() {\n    const prompt = document.getElementById('prompt').value;\n    const style = document.getElementById('style').value;\n    const duration = parseInt(document.getElementById('duration').value);\n    const btn = document.getElementById('genBtn');\n    btn.disabled = true;\n    document.getElementById('progressBar').style.display = 'block';\n\n    const res = await fetch('/api/generate', {\n      method: 'POST',\n      headers: {'Content-Type': 'application/json', 'X-API-Key': 'demo'},\n      body: JSON.stringify({prompt, style, duration})\n    });\n    const data = await res.json();\n    pollForResult(data.job_id);\n  }\n\n  function pollForResult(jobId) {\n    pollInterval = setInterval(async () =&gt; {\n      const res = await fetch('/api/jobs/' + jobId,\n        {headers: {'X-API-Key': 'demo'}});\n      const data = await res.json();\n      document.getElementById('progressFill').style.width = data.progress + '%';\n      document.getElementById('statusText').textContent =\n        data.stage + ' (' + data.progress + '%)';\n      if (data.status === 'completed') {\n        clearInterval(pollInterval);\n        document.getElementById('result').innerHTML =\n          '&lt;video controls autoplay src=\"' + data.result_url + '\"&gt;&lt;/video&gt;';\n        document.getElementById('genBtn').disabled = false;\n      } else if (data.status === 'failed') {\n        clearInterval(pollInterval);\n        document.getElementById('statusText').textContent = 'Error: ' + data.error;\n        document.getElementById('genBtn').disabled = false;\n      }\n    }, 2000);\n  }\n  &lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;</code></pre>\n</div>\n\n\n<h2>Part 11: Connect to Your Own LLM</h2>\n\n<p>So far we have used third-party APIs. But the real power &mdash; and the real margins &mdash; come when you run <strong>your own models</strong>. This eliminates per-request API costs, gives you full control over latency and privacy, and lets you fine-tune models for your specific use case.</p>\n\n<p>Three main approaches to self-host:</p>\n<ul>\n<li><strong>vLLM</strong> &mdash; highest throughput, OpenAI-compatible API, best for production</li>\n<li><strong>Ollama</strong> &mdash; easiest setup, great for development and small-scale deployment</li>\n<li><strong>llama.cpp</strong> &mdash; lightest weight, runs on CPU, ideal for edge deployment</li>\n</ul>\n\n<p>For video generation, you can self-host models like <strong>CogVideoX</strong>, <strong>Mochi</strong>, or <strong>AnimateDiff</strong>:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>serve_own_model.py &mdash; Self-hosted Video Generation</span></div>\n<pre><code class=\"language-python\">\"\"\"\nSelf-hosting video generation models for ClipCraft.\nOptions: CogVideoX, Mochi-1, AnimateDiff, SVD (Stable Video Diffusion).\nRequires: GPU with >= 24GB VRAM (A100/A10G or RTX 4090).\n\"\"\"\n\nclass LocalVideoGenerator:\n    \"\"\"Generate videos using locally hosted models.\"\"\"\n\n    def __init__(self, model=\"THUDM/CogVideoX-5b\"):\n        self.model = model\n        self.device = \"cuda\"\n\n    def load_model(self):\n        \"\"\"Load video generation model. Takes 30-60s, needs 24GB+ VRAM.\"\"\"\n        # In production:\n        # import torch\n        # from diffusers import CogVideoXPipeline\n        # self.pipe = CogVideoXPipeline.from_pretrained(\n        #     self.model, torch_dtype=torch.float16\n        # ).to(self.device)\n        # self.pipe.enable_model_cpu_offload()\n        print(f\"Loaded {self.model}\")\n        print(f\"VRAM: ~20GB (fp16), Generation: ~2min per 6s clip\")\n\n    def generate(self, prompt, num_frames=49, fps=8, width=720, height=480):\n        \"\"\"Generate a video clip from text.\"\"\"\n        # In production:\n        # video = self.pipe(\n        #     prompt=prompt,\n        #     num_frames=num_frames,\n        #     guidance_scale=6,\n        #     num_inference_steps=50,\n        # ).frames[0]\n        # from diffusers.utils import export_to_video\n        # export_to_video(video, \"output.mp4\", fps=fps)\n\n        duration = num_frames / fps\n        print(f\"Generating {duration:.1f}s video: \\\\\"{prompt}\\\\\"\")\n        print(f\"  {num_frames} frames at {fps}fps, {width}x{height}\")\n        print(f\"  Estimated time: ~{num_frames * 2.5:.0f}s on A100\")\n        return {\"status\": \"generated\", \"duration\": duration, \"cost\": 0.0}\n\nclass RunwayMLClient:\n    \"\"\"Fallback: Use Runway ML API for production quality.\"\"\"\n\n    def __init__(self, api_key=\"your-runway-key\"):\n        self.api_key = api_key\n        self.base_url = \"https://api.runwayml.com/v1\"\n\n    def generate(self, prompt, duration=4):\n        \"\"\"Generate video via Runway Gen-3 Alpha API.\"\"\"\n        # import requests\n        # r = requests.post(f\"{self.base_url}/generate\",\n        #     headers={\"Authorization\": f\"Bearer {self.api_key}\"},\n        #     json={\"prompt\": prompt, \"duration\": duration})\n        cost = duration * 0.05  # ~$0.05 per second\n        print(f\"Runway API: \\\\\"{prompt}\\\\\" ({duration}s)\")\n        print(f\"  Cost: \\${cost:.2f}\")\n        return {\"status\": \"queued\", \"cost\": cost}\n\n\n# --- Demo ---\nprint(\"=== Self-Hosted Video Generation ===\\\n\")\ngen = LocalVideoGenerator()\ngen.load_model()\nresult = gen.generate(\"A cat walking through a garden, cinematic\", num_frames=49)\nprint(f\"\\\n  Result: {result}\\\n\")\n\n# Cost comparison\nprint(\"=== Cost Comparison ===\")\nprint(\"| Method            | Per 4s clip | 1K clips/mo | GPU Required  |\")\nprint(\"|-------------------|------------|-------------|---------------|\")\nprint(\"| Runway Gen-3      | $0.20      | $200        | None (API)    |\")\nprint(\"| Replicate SVD     | $0.10      | $100        | None (API)    |\")\nprint(\"| Self-host CogVideo| ~$0.02     | ~$20+$300/mo| A100 40GB     |\")\nprint(\"| Self-host AnimDiff| ~$0.01     | ~$10+$150/mo| RTX 4090 24GB |\")\nprint(\"\\\nBreakeven: ~500 clips/month for self-hosting\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Part 12: Deployment Alternatives</h2>\n\n<p>You have multiple deployment paths depending on your scale, budget, and technical requirements. Here is a complete comparison with actual commands:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>deployment_options.py &mdash; ClipCraft Deployment Guide</span></div>\n<pre><code class=\"language-python\">\"\"\"\nDeployment options for ClipCraft - from hobby to enterprise scale.\nEach option includes: setup commands, estimated costs, and pros/cons.\n\"\"\"\n\nclass DeploymentGuide:\n    \"\"\"Compare deployment platforms for your SaaS.\"\"\"\n\n    def __init__(self, app_name=\"{product_name.lower()}\"):\n        self.app_name = app_name\n\n    def railway(self):\n        \"\"\"Railway.app - Easiest PaaS deployment.\"\"\"\n        print(\"=== Railway.app ===\")\n        print(\"Best for: MVPs, startups, small-to-medium scale\")\n        print(\"Cost: $5/mo hobby, ~$20-50/mo production\")\n        print()\n        print(\"Setup commands:\")\n        print(f\"  npm install -g @railway/cli\")\n        print(f\"  railway login\")\n        print(f\"  railway init\")\n        print(f\"  railway up  # deploys from Dockerfile\")\n        print()\n        print(\"railway.toml:\")\n        print(f'  [build]')\n        print(f'  builder = \"dockerfile\"')\n        print(f'  dockerfilePath = \"Dockerfile\"')\n        print()\n        print(f'  [deploy]')\n        print(f'  healthcheckPath = \"/health\"')\n        print(f'  healthcheckTimeout = 30')\n        print(f'  restartPolicyType = \"on_failure\"')\n        print()\n        print(\"Pros: Auto-scaling, free PostgreSQL, easy env vars\")\n        print(\"Cons: No GPU support, cold starts possible\")\n\n    def render(self):\n        \"\"\"Render.com - Good middle ground.\"\"\"\n        print(\"\\\n=== Render.com ===\")\n        print(\"Best for: Production apps, auto-scaling needed\")\n        print(\"Cost: Free tier available, $7/mo starter, ~$25/mo production\")\n        print()\n        print(\"render.yaml (Infrastructure as Code):\")\n        print(f\"  services:\")\n        print(f\"    - type: web\")\n        print(f\"      name: {self.app_name}\")\n        print(f\"      runtime: docker\")\n        print(f\"      plan: starter\")\n        print(f\"      healthCheckPath: /health\")\n        print(f\"      envVars:\")\n        print(f\"        - key: DATABASE_URL\")\n        print(f\"          fromDatabase:\")\n        print(f\"            name: {self.app_name}-db\")\n        print(f\"            property: connectionString\")\n        print(f\"    - type: worker\")\n        print(f\"      name: {self.app_name}-worker\")\n        print(f\"      runtime: docker\")\n        print(f\"      dockerfilePath: ./Dockerfile.worker\")\n        print()\n        print(\"  databases:\")\n        print(f\"    - name: {self.app_name}-db\")\n        print(f\"      plan: starter\")\n        print()\n        print(\"Pros: Free SSL, managed PostgreSQL, auto-deploy from Git\")\n        print(\"Cons: No GPU, slower builds than Railway\")\n\n    def aws(self):\n        \"\"\"AWS - Full enterprise deployment.\"\"\"\n        print(\"\\\n=== AWS (ECS + Fargate) ===\")\n        print(\"Best for: Enterprise, GPU workloads, full control\")\n        print(\"Cost: ~$50-500/mo depending on scale\")\n        print()\n        print(\"Architecture:\")\n        print(\"  ALB -> ECS Fargate -> RDS PostgreSQL\")\n        print(\"         |-> ElastiCache (Redis)\")\n        print(\"         |-> S3 (file storage)\")\n        print(\"         |-> SQS (job queue)\")\n        print()\n        print(\"Setup (using AWS CDK):\")\n        print(f\"  npm install -g aws-cdk\")\n        print(f\"  cdk init app --language python\")\n        print(f\"  # Define ECS service, RDS, Redis in stack\")\n        print(f\"  cdk deploy\")\n        print()\n        print(\"For GPU (e.g., self-hosted models):\")\n        print(f\"  # Use EC2 with g5.xlarge (A10G GPU, ~$1.00/hr)\")\n        print(f\"  # Or SageMaker endpoints for managed inference\")\n        print()\n        print(\"Pros: GPU support, infinite scale, enterprise features\")\n        print(\"Cons: Complex setup, higher cost, AWS expertise needed\")\n\n    def digitalocean(self):\n        \"\"\"DigitalOcean App Platform.\"\"\"\n        print(\"\\\n=== DigitalOcean App Platform ===\")\n        print(\"Best for: Simple deployments, predictable pricing\")\n        print(\"Cost: $5/mo basic, $12/mo pro, GPU droplets ~$2.50/hr\")\n        print()\n        print(\".do/app.yaml:\")\n        print(f\"  name: {self.app_name}\")\n        print(f\"  services:\")\n        print(f\"    - name: api\")\n        print(f\"      github:\")\n        print(f\"        repo: yourusername/{self.app_name}\")\n        print(f\"        branch: main\")\n        print(f\"        deploy_on_push: true\")\n        print(f\"      dockerfile_path: Dockerfile\")\n        print(f\"      instance_count: 2\")\n        print(f\"      instance_size_slug: professional-xs\")\n        print(f\"      health_check:\")\n        print(f\"        http_path: /health\")\n        print()\n        print(\"Pros: Simple pricing, good docs, GPU droplets available\")\n        print(\"Cons: Smaller ecosystem than AWS, limited auto-scaling\")\n\n    def self_hosted(self):\n        \"\"\"Self-hosted on bare metal or VPS.\"\"\"\n        print(\"\\\n=== Self-Hosted (VPS/Bare Metal) ===\")\n        print(\"Best for: Maximum control, GPU inference, cost optimization\")\n        print(\"Cost: $20-100/mo VPS, $100-300/mo GPU server\")\n        print()\n        print(\"Stack: Nginx + Gunicorn + FastAPI + PostgreSQL + Redis\")\n        print()\n        print(\"docker-compose.yml:\")\n        print(f\"  services:\")\n        print(f\"    app:\")\n        print(f\"      build: .\")\n        print(f\"      ports: [\\\\\"8000:8000\\\\\"]\")\n        print(f\"      environment:\")\n        print(f\"        - DATABASE_URL=postgresql://user:pass@db:5432/{self.app_name}\")\n        print(f\"        - REDIS_URL=redis://redis:6379\")\n        print(f\"      depends_on: [db, redis]\")\n        print(f\"      deploy:\")\n        print(f\"        replicas: 3\")\n        print(f\"    db:\")\n        print(f\"      image: postgres:16-alpine\")\n        print(f\"      volumes: [postgres_data:/var/lib/postgresql/data]\")\n        print(f\"    redis:\")\n        print(f\"      image: redis:7-alpine\")\n        print(f\"    nginx:\")\n        print(f\"      image: nginx:alpine\")\n        print(f\"      ports: [\\\\\"80:80\\\\\", \\\\\"443:443\\\\\"]\")\n        print(f\"      volumes: [./nginx.conf:/etc/nginx/nginx.conf]\")\n        print()\n        print(\"GPU server providers:\")\n        print(\"  - Lambda Labs: A100 ~$1.10/hr, H100 ~$2.49/hr\")\n        print(\"  - RunPod: A100 ~$1.64/hr, community GPUs from $0.20/hr\")\n        print(\"  - Vast.ai: Cheapest GPUs, ~$0.15-0.50/hr\")\n        print(\"  - Hetzner: Dedicated servers from EUR39/mo (no GPU)\")\n\n# --- Demo ---\nguide = DeploymentGuide()\nguide.railway()\nguide.render()\nguide.aws()\nguide.digitalocean()\nguide.self_hosted()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(\"RECOMMENDATION by scale:\")\nprint(\"  Hobby/MVP     -> Railway or Render (free tier)\")\nprint(\"  Startup       -> Railway or DigitalOcean ($20-50/mo)\")\nprint(\"  Growth        -> AWS ECS or DigitalOcean ($100-500/mo)\")\nprint(\"  Enterprise    -> AWS/GCP with GPU instances\")\nprint(\"  Self-hosted AI-> VPS + GPU from Lambda/RunPod/Vast.ai\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Part 13: Rate Limiting &amp; API Protection</h2>\n\n<p>Every production SaaS needs rate limiting to prevent abuse, control costs, and ensure fair usage. Here is a production-grade implementation:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>rate_limiter.py &mdash; Production Rate Limiting for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nProduction rate limiting for ClipCraft.\nThree tiers: in-memory (development), Redis (production), token bucket (advanced).\n\"\"\"\nimport time\nfrom collections import defaultdict\n\n# === TIER 1: In-Memory Rate Limiter (Development) ===\nclass InMemoryRateLimiter:\n    \"\"\"Simple sliding window rate limiter. Works for single-server setup.\"\"\"\n\n    def __init__(self):\n        self.requests = defaultdict(list)\n\n    def is_allowed(self, key, max_requests, window_seconds):\n        \"\"\"Check if request is allowed under the rate limit.\"\"\"\n        now = time.time()\n        window_start = now - window_seconds\n\n        # Clean old entries\n        self.requests[key] = [t for t in self.requests[key] if t > window_start]\n\n        if len(self.requests[key]) >= max_requests:\n            retry_after = self.requests[key][0] - window_start\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"limit\": max_requests,\n                \"window\": window_seconds,\n                \"retry_after\": round(retry_after, 1),\n                \"current\": len(self.requests[key])\n            }}\n\n        self.requests[key].append(now)\n        return True, {{\n            \"remaining\": max_requests - len(self.requests[key]),\n            \"limit\": max_requests,\n            \"reset\": round(window_start + window_seconds - now, 1)\n        }}\n\n# === TIER 2: Redis Rate Limiter (Production) ===\nclass RedisRateLimiter:\n    \"\"\"Redis-based rate limiter for distributed deployments.\n\n    In production, install: pip install redis\n    And use a real Redis connection.\n    \"\"\"\n\n    def __init__(self):\n        # In production: self.redis = redis.Redis(host='redis', port=6379, db=0)\n        self.memory = defaultdict(list)  # Simulating Redis\n\n    def check_rate_limit(self, user_id, tier=\"free\"):\n        \"\"\"Check rate limit based on user tier.\"\"\"\n        limits = {{\n            \"free\":       {{\"requests_per_minute\": 5,  \"requests_per_day\": 50}},\n            \"starter\":    {{\"requests_per_minute\": 20, \"requests_per_day\": 500}},\n            \"pro\":        {{\"requests_per_minute\": 60, \"requests_per_day\": 5000}},\n            \"enterprise\": {{\"requests_per_minute\": 200, \"requests_per_day\": 50000}},\n        }}\n\n        tier_limits = limits.get(tier, limits[\"free\"])\n        now = time.time()\n\n        # Check per-minute limit\n        minute_key = f\"rate:{{user_id}}:minute\"\n        self.memory[minute_key] = [t for t in self.memory[minute_key] if t > now - 60]\n\n        if len(self.memory[minute_key]) >= tier_limits[\"requests_per_minute\"]:\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"type\": \"per_minute\",\n                \"limit\": tier_limits[\"requests_per_minute\"],\n                \"retry_after_seconds\": 60 - (now - self.memory[minute_key][0])\n            }}\n\n        # Check per-day limit\n        day_key = f\"rate:{{user_id}}:day\"\n        self.memory[day_key] = [t for t in self.memory[day_key] if t > now - 86400]\n\n        if len(self.memory[day_key]) >= tier_limits[\"requests_per_day\"]:\n            return False, {{\n                \"error\": \"daily_limit_exceeded\",\n                \"type\": \"per_day\",\n                \"limit\": tier_limits[\"requests_per_day\"],\n            }}\n\n        # Allow request\n        self.memory[minute_key].append(now)\n        self.memory[day_key].append(now)\n\n        return True, {{\n            \"remaining_minute\": tier_limits[\"requests_per_minute\"] - len(self.memory[minute_key]),\n            \"remaining_day\": tier_limits[\"requests_per_day\"] - len(self.memory[day_key]),\n        }}\n\n# === FastAPI Middleware ===\nclass RateLimitMiddleware:\n    \"\"\"\n    FastAPI middleware for rate limiting.\n\n    Usage in your app:\n        from rate_limiter import RateLimitMiddleware\n        app.add_middleware(RateLimitMiddleware)\n\n    Or as a dependency:\n        @app.get(\"/generate\")\n        async def generate(rate_limit: dict = Depends(check_rate_limit)):\n            ...\n    \"\"\"\n\n    def __init__(self):\n        self.limiter = RedisRateLimiter()\n\n    def get_middleware_code(self):\n        \"\"\"Print the actual FastAPI middleware code.\"\"\"\n        code = \"# Add to your FastAPI app:\\\n\\\n\"\n        code += \"from fastapi import Request, Response\\\n\"\n        code += \"from starlette.middleware.base import BaseHTTPMiddleware\\\n\\\n\"\n        code += \"class RateLimitMiddleware(BaseHTTPMiddleware):\\\n\"\n        code += \"    async def dispatch(self, request, call_next):\\\n\"\n        code += \"        api_key = request.headers.get('X-API-Key', 'anonymous')\\\n\"\n        code += \"        user_id = self.get_user_from_key(api_key)\\\n\"\n        code += \"        tier = self.get_user_tier(user_id)\\\n\"\n        code += \"        allowed, info = self.limiter.check_rate_limit(user_id, tier)\\\n\"\n        code += \"        if not allowed:\\\n\"\n        code += \"            return JSONResponse(status_code=429, content=info)\\\n\"\n        code += \"        response = await call_next(request)\\\n\"\n        code += \"        response.headers['X-RateLimit-Remaining'] = str(info.get('remaining_minute', 0))\\\n\"\n        code += \"        return response\\\n\"\n        print(code)\n\n\n# --- Demo ---\nprint(\"=== Rate Limiting Demo for ClipCraft ===\\\n\")\n\nlimiter = RedisRateLimiter()\n\n# Simulate requests from different users\nusers = [\n    (\"user_free_1\", \"free\"),\n    (\"user_pro_1\", \"pro\"),\n    (\"user_enterprise_1\", \"enterprise\"),\n]\n\nfor user_id, tier in users:\n    print(f\"User: {{user_id}} ({{tier}} tier)\")\n    for i in range(8):\n        allowed, info = limiter.check_rate_limit(user_id, tier)\n        status = \"ALLOWED\" if allowed else \"BLOCKED\"\n        print(f\"  Request {{i+1}}: {{status}} | {{info}}\")\n        if not allowed:\n            break\n    print()\n\n# Show middleware code\nmw = RateLimitMiddleware()\nmw.get_middleware_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Part 14: Stripe Billing Integration</h2>\n\n<p>Real SaaS needs real billing. Here is a complete Stripe integration with subscription tiers, usage-based billing, and webhook handling:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>billing.py &mdash; Stripe Integration for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nComplete Stripe billing for ClipCraft.\nSupports: subscription tiers, usage-based billing, webhooks.\n\nSetup:\n  pip install stripe\n  # Get keys from https://dashboard.stripe.com/apikeys\n  # Set STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET in .env\n\"\"\"\n\nclass StripeBilling:\n    \"\"\"Full Stripe integration for SaaS billing.\"\"\"\n\n    def __init__(self, secret_key=\"sk_test_...\"):\n        # In production: import stripe; stripe.api_key = secret_key\n        self.plans = {{\n            \"free\":       {{\"price\": 0,    \"name\": \"Free\",       \"stripe_price_id\": None}},\n            \"starter\":    {{\"price\": 19,   \"name\": \"Starter\",    \"stripe_price_id\": \"price_starter_monthly\"}},\n            \"pro\":        {{\"price\": 49,   \"name\": \"Professional\",\"stripe_price_id\": \"price_pro_monthly\"}},\n            \"enterprise\": {{\"price\": 199,  \"name\": \"Enterprise\", \"stripe_price_id\": \"price_enterprise_monthly\"}},\n        }}\n\n    def create_checkout_session(self, user_email, plan_key):\n        \"\"\"Create a Stripe Checkout session for subscription.\"\"\"\n        plan = self.plans[plan_key]\n        if plan[\"price\"] == 0:\n            return {{\"status\": \"free_tier\", \"message\": \"No payment needed\"}}\n\n        # In production:\n        # session = stripe.checkout.Session.create(\n        #     customer_email=user_email,\n        #     payment_method_types=[\"card\"],\n        #     line_items=[{{\n        #         \"price\": plan[\"stripe_price_id\"],\n        #         \"quantity\": 1,\n        #     }}],\n        #     mode=\"subscription\",\n        #     success_url=\"https://{product_name.lower()}.com/billing/success?session_id={{CHECKOUT_SESSION_ID}}\",\n        #     cancel_url=\"https://{product_name.lower()}.com/billing/cancel\",\n        #     metadata={{\"user_email\": user_email, \"plan\": plan_key}},\n        # )\n        # return {{\"checkout_url\": session.url, \"session_id\": session.id}}\n\n        print(f\"Creating checkout for {{user_email}} -> {{plan['name']}} (\\${{plan['price']}}/mo)\")\n        return {{\n            \"checkout_url\": f\"https://checkout.stripe.com/pay/cs_test_...\",\n            \"plan\": plan[\"name\"],\n            \"price\": f\"\\${{plan['price']}}/month\"\n        }}\n\n    def create_usage_record(self, subscription_item_id, quantity, action=\"increment\"):\n        \"\"\"Record usage for metered billing (pay-per-use on top of subscription).\"\"\"\n        # In production:\n        # stripe.SubscriptionItem.create_usage_record(\n        #     subscription_item_id,\n        #     quantity=quantity,\n        #     action=action,\n        #     timestamp=int(time.time())\n        # )\n        print(f\"Usage recorded: {{quantity}} units on {{subscription_item_id}}\")\n        return {{\"status\": \"recorded\", \"quantity\": quantity}}\n\n    def handle_webhook(self, event_type, data):\n        \"\"\"Handle Stripe webhook events.\"\"\"\n        handlers = {{\n            \"checkout.session.completed\": self._on_checkout_complete,\n            \"customer.subscription.updated\": self._on_subscription_update,\n            \"customer.subscription.deleted\": self._on_subscription_cancel,\n            \"invoice.payment_failed\": self._on_payment_failed,\n            \"invoice.paid\": self._on_invoice_paid,\n        }}\n\n        handler = handlers.get(event_type)\n        if handler:\n            return handler(data)\n        return {{\"status\": \"unhandled\", \"event\": event_type}}\n\n    def _on_checkout_complete(self, data):\n        print(f\"  Checkout completed! Activate subscription for user.\")\n        return {{\"action\": \"activate_subscription\"}}\n\n    def _on_subscription_update(self, data):\n        print(f\"  Subscription updated. Sync plan tier in database.\")\n        return {{\"action\": \"sync_tier\"}}\n\n    def _on_subscription_cancel(self, data):\n        print(f\"  Subscription canceled. Downgrade to free tier.\")\n        return {{\"action\": \"downgrade_to_free\"}}\n\n    def _on_payment_failed(self, data):\n        print(f\"  Payment failed! Send dunning email, grace period 7 days.\")\n        return {{\"action\": \"send_dunning_email\", \"grace_days\": 7}}\n\n    def _on_invoice_paid(self, data):\n        print(f\"  Invoice paid. Reset monthly usage counters.\")\n        return {{\"action\": \"reset_usage\"}}\n\n    def get_fastapi_webhook_code(self):\n        \"\"\"Print the FastAPI webhook endpoint code.\"\"\"\n        code = \"# FastAPI webhook endpoint:\\\n\\\n\"\n        code += \"@app.post('/webhooks/stripe')\\\n\"\n        code += \"async def stripe_webhook(request: Request):\\\n\"\n        code += \"    payload = await request.body()\\\n\"\n        code += \"    sig_header = request.headers.get('stripe-signature')\\\n\"\n        code += \"    try:\\\n\"\n        code += \"        event = stripe.Webhook.construct_event(\\\n\"\n        code += \"            payload, sig_header, STRIPE_WEBHOOK_SECRET)\\\n\"\n        code += \"    except stripe.error.SignatureVerificationError:\\\n\"\n        code += \"        raise HTTPException(400, 'Invalid signature')\\\n\"\n        code += \"    billing = StripeBilling()\\\n\"\n        code += \"    result = billing.handle_webhook(event['type'], event['data']['object'])\\\n\"\n        code += \"    return {'status': 'ok', 'result': result}\\\n\"\n        print(code)\n\n    def get_pricing_page_data(self):\n        \"\"\"Generate pricing page data.\"\"\"\n        features = {{\n            \"free\": [\n                \"50 requests/day\",\n                \"Basic quality\",\n                \"Community support\",\n                \"Watermarked output\",\n            ],\n            \"starter\": [\n                \"500 requests/day\",\n                \"Standard quality\",\n                \"Email support\",\n                \"No watermark\",\n                \"API access\",\n            ],\n            \"pro\": [\n                \"5,000 requests/day\",\n                \"Highest quality\",\n                \"Priority support\",\n                \"4K resolution, custom transitions\",\n                \"Batch processing\",\n                \"Webhooks\",\n            ],\n            \"enterprise\": [\n                \"50,000 requests/day\",\n                \"Custom model fine-tuning\",\n                \"Dedicated support\",\n                \"SLA guarantee\",\n                \"SSO/SAML\",\n                \"On-premise option\",\n            ],\n        }}\n        return features\n\n\n# --- Demo ---\nprint(\"=== Stripe Billing for ClipCraft ===\\\n\")\n\nbilling = StripeBilling()\n\n# 1. Show pricing tiers\nprint(\"Pricing Tiers:\")\nfeatures = billing.get_pricing_page_data()\nfor tier, plan in billing.plans.items():\n    print(f\"\\\n  {{plan['name']}} - \\${{plan['price']}}/month\")\n    for f in features[tier]:\n        print(f\"    - {{f}}\")\n\n# 2. Create checkout session\nprint(\"\\\n\\\n--- Creating Checkout ---\")\nresult = billing.create_checkout_session(\"user@example.com\", \"pro\")\nprint(f\"Result: {{result}}\")\n\n# 3. Simulate webhook events\nprint(\"\\\n--- Webhook Events ---\")\nevents = [\n    (\"checkout.session.completed\", {{}}),\n    (\"invoice.paid\", {{}}),\n    (\"customer.subscription.updated\", {{}}),\n    (\"invoice.payment_failed\", {{}}),\n    (\"customer.subscription.deleted\", {{}}),\n]\nfor event_type, data in events:\n    print(f\"\\\nEvent: {{event_type}}\")\n    billing.handle_webhook(event_type, data)\n\n# 4. Usage-based billing\nprint(\"\\\n\\\n--- Usage-Based Billing ---\")\nbilling.create_usage_record(\"si_test_123\", 25, \"increment\")\n\n# 5. Show webhook code\nprint(\"\\\n--- Webhook Endpoint Code ---\")\nbilling.get_fastapi_webhook_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Part 15: Scaling to a Mass Product</h2>\n\n<p>Scaling from MVP to mass product requires architectural changes at every layer. Here is the complete scaling playbook:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>scaling.py &mdash; ClipCraft Scaling Architecture</span></div>\n<pre><code class=\"language-python\">\"\"\"\nScaling ClipCraft from 100 to 1,000,000 users.\nCovers: horizontal scaling, queue-based processing, caching, monitoring.\n\"\"\"\n\nclass ScalingPlaybook:\n    \"\"\"Complete scaling guide from MVP to enterprise.\"\"\"\n\n    def __init__(self, product=\"ClipCraft\"):\n        self.product = product\n\n    def stage_1_mvp(self):\n        \"\"\"Stage 1: 0-100 users, single server.\"\"\"\n        print(\"=== Stage 1: MVP (0-100 users) ===\")\n        print(\"Architecture: Single server, SQLite/PostgreSQL\")\n        print(\"Cost: $5-20/month\")\n        print()\n        print(\"Stack:\")\n        print(\"  - 1x server (Railway/Render free tier)\")\n        print(\"  - SQLite or managed PostgreSQL\")\n        print(\"  - In-memory caching (Python dict)\")\n        print(\"  - Direct API calls (no queue)\")\n        print()\n        print(\"Focus on:\")\n        print(\"  1. Validate product-market fit\")\n        print(\"  2. Get 10 paying customers\")\n        print(\"  3. Measure: response time, error rate, user retention\")\n\n    def stage_2_growth(self):\n        \"\"\"Stage 2: 100-1K users, add resilience.\"\"\"\n        print(\"\\\n=== Stage 2: Growth (100-1,000 users) ===\")\n        print(\"Architecture: App server + Background workers + Redis\")\n        print(\"Cost: $50-200/month\")\n        print()\n        print(\"Changes from Stage 1:\")\n        print(\"  1. Add Redis for caching and rate limiting\")\n        print(\"  2. Move heavy work to background queues (Celery/ARQ)\")\n        print(\"  3. Add health checks and basic monitoring\")\n        print(\"  4. Set up CI/CD pipeline\")\n        print()\n        print(\"docker-compose additions:\")\n        print(\"  redis:\")\n        print(\"    image: redis:7-alpine\")\n        print(\"  worker:\")\n        print(\"    build: .\")\n        print(\"    command: arq app.worker.WorkerSettings\")\n        print(\"    depends_on: [redis]\")\n        print()\n        print(\"Queue pattern for heavy tasks:\")\n        print(\"  @app.post('/generate')\")\n        print(\"  async def generate(request: GenerateRequest):\")\n        print(\"      job_id = str(uuid.uuid4())\")\n        print(\"      await redis.enqueue_job('process_generation',\")\n        print(\"          job_id=job_id, prompt=request.prompt)\")\n        print(\"      return {{'job_id': job_id, 'status': 'processing'}}\")\n        print()\n        print(\"  @app.get('/jobs/{{job_id}}')\")\n        print(\"  async def get_job(job_id: str):\")\n        print(\"      result = await redis.get(f'job:{{job_id}}')\")\n        print(\"      return result or {{'status': 'processing'}}\")\n\n    def stage_3_scale(self):\n        \"\"\"Stage 3: 1K-10K users, horizontal scaling.\"\"\"\n        print(\"\\\n=== Stage 3: Scale (1,000-10,000 users) ===\")\n        print(\"Architecture: Load balancer + Multiple app servers + Queue\")\n        print(\"Cost: $200-1,000/month\")\n        print()\n        print(\"Architecture diagram:\")\n        print(\"  Users -> CDN (Cloudflare)\")\n        print(\"       -> Load Balancer (nginx/ALB)\")\n        print(\"       -> App Server 1 \\\\ \")\n        print(\"       -> App Server 2  |-> PostgreSQL (managed)\")\n        print(\"       -> App Server 3 /    |-> Redis Cluster\")\n        print(\"       -> Worker 1 \\\\         |-> S3 (file storage)\")\n        print(\"       -> Worker 2  |-> Queue (Redis/SQS)\")\n        print(\"       -> Worker 3 /\")\n        print()\n        print(\"Key changes:\")\n        print(\"  1. Multiple app servers behind load balancer\")\n        print(\"  2. Managed database (RDS/Cloud SQL)\")\n        print(\"  3. Separate worker pool for heavy processing\")\n        print(\"  4. CDN for static assets and cached responses\")\n        print(\"  5. Structured logging + APM (Datadog/Grafana)\")\n        print()\n        print(\"nginx load balancer config:\")\n        print(\"  upstream api {{\")\n        print(\"      least_conn;\")\n        print(\"      server app1:8000;\")\n        print(\"      server app2:8000;\")\n        print(\"      server app3:8000;\")\n        print(\"  }}\")\n        print(\"  server {{\")\n        print(\"      listen 80;\")\n        print(\"      location / {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          proxy_set_header X-Real-IP $remote_addr;\")\n        print(\"      }}\")\n        print(\"      location /health {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          access_log off;\")\n        print(\"      }}\")\n        print(\"  }}\")\n\n    def stage_4_mass(self):\n        \"\"\"Stage 4: 10K+ users, enterprise features.\"\"\"\n        print(\"\\\n=== Stage 4: Mass Product (10,000+ users) ===\")\n        print(\"Architecture: Microservices or modular monolith\")\n        print(\"Cost: $1,000-10,000/month\")\n        print()\n        print(\"Enterprise features to add:\")\n        print(\"  1. Multi-tenant architecture (org-level isolation)\")\n        print(\"  2. SSO/SAML authentication\")\n        print(\"  3. Audit logging\")\n        print(\"  4. Data residency (EU/US regions)\")\n        print(\"  5. Custom SLAs and dedicated support\")\n        print(\"  6. API versioning (v1, v2)\")\n        print(\"  7. Webhook system for integrations\")\n        print(\"  8. Admin dashboard with analytics\")\n        print()\n        print(\"Monitoring stack:\")\n        print(\"  - Prometheus + Grafana (metrics)\")\n        print(\"  - Sentry (error tracking)\")\n        print(\"  - PostHog or Mixpanel (product analytics)\")\n        print(\"  - PagerDuty (alerting)\")\n        print()\n        print(\"SLA targets:\")\n        print(\"  - Uptime: 99.9% (8.7 hours downtime/year)\")\n        print(\"  - API latency p99: < 2 seconds\")\n        print(\"  - Error rate: < 0.1%\")\n\n    def growth_metrics(self):\n        \"\"\"Key metrics to track at each stage.\"\"\"\n        print(\"\\\n=== Key Metrics Dashboard ===\")\n        print()\n        metrics = [\n            (\"MRR (Monthly Recurring Revenue)\", \"$0\", \"$500\", \"$5K\", \"$50K+\"),\n            (\"Active Users (DAU)\", \"10\", \"100\", \"1,000\", \"10,000+\"),\n            (\"API Requests/day\", \"100\", \"5K\", \"100K\", \"1M+\"),\n            (\"Error Rate\", \"<5%\", \"<1%\", \"<0.1%\", \"<0.01%\"),\n            (\"P99 Latency\", \"<5s\", \"<3s\", \"<2s\", \"<1s\"),\n            (\"Churn Rate (monthly)\", \"N/A\", \"<10%\", \"<5%\", \"<3%\"),\n        ]\n        print(f\"| {{'Metric':<30}} | {{'Stage 1':<8}} | {{'Stage 2':<8}} | {{'Stage 3':<8}} | {{'Stage 4':<10}} |\")\n        print(f\"|{{'-'*30}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*10}}-|\")\n        for name, *stages in metrics:\n            print(f\"| {{name:<30}} | {{stages[0]:<8}} | {{stages[1]:<8}} | {{stages[2]:<8}} | {{stages[3]:<10}} |\")\n\n# --- Demo ---\nplaybook = ScalingPlaybook()\nplaybook.stage_1_mvp()\nplaybook.stage_2_growth()\nplaybook.stage_3_scale()\nplaybook.stage_4_mass()\nplaybook.growth_metrics()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(f\"Summary: Build ClipCraft as MVP first.\")\nprint(\"Get 10 paying customers before optimizing.\")\nprint(\"Scale horizontally only when metrics demand it.\")\nprint(\"Monitor everything from day one.\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Key Takeaways</h2>\n\n<ul>\n<li><strong>Async is mandatory for video:</strong> Video generation takes 30 seconds to 5 minutes. You must use a job queue with background processing &mdash; synchronous HTTP will time out.</li>\n<li><strong>Progress tracking builds trust:</strong> Users need to see that something is happening. Offer both polling endpoints and webhook callbacks for flexibility.</li>\n<li><strong>Retry logic is critical:</strong> Long-running GPU tasks fail more often than fast API calls. Automatic retries with exponential backoff keep your service reliable.</li>\n<li><strong>Control concurrency to control costs:</strong> Limit the number of simultaneous jobs. Each video costs $0.10&ndash;$1.00 in API fees &mdash; runaway concurrency can drain your budget in minutes.</li>\n<li><strong>Video is 10&ndash;50x more expensive than images:</strong> Price your tiers accordingly. A $10/month plan that allows 100 images might only allow 10&ndash;20 videos.</li>\n<li><strong>FFmpeg is your best friend:</strong> Use it for transcoding, resolution changes, watermarking, and format conversion. It runs everywhere and handles every codec.</li>\n</ul>\n\n<p><strong>Next:</strong> In GenAI-3, you build a <strong>Voice and Audio Generation SaaS</strong> &mdash; handling text-to-speech, voice cloning, and audio processing pipelines.</p>\n\n${navButtons('genai-1','genai-3')}",
    "ro": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\" style=\"background: linear-gradient(135deg, #7c3aed, #a855f7);\">BUILD: Produs AI SaaS</div>\n<h1>Lecția GenAI 2: Construiește un SaaS de Generare Video</h1>\n<p class=\"lecture-subtitle\">Construiește ClipCraft &mdash; un serviciu de generare video pregătit pentru producție, cu cozi de job-uri asincrone, urmărirea progresului și un backend FastAPI care orchestrează modele text-to-video.</p>\n<div class=\"lecture-meta\">\n<span>~75 min</span> | <span>Avansat / Practic</span> | <span>Vei construi și lansa un produs real</span>\n</div>\n</div>\n\n<h2>Ce Vei Construi</h2>\n\n<p>În această lecție construiești <strong>ClipCraft</strong> &mdash; un SaaS de generare video. Utilizatorii trimit prompt-uri text, serverul tău dispecerizează cererea către un model text-to-video (Runway, Pika sau Stability AI Video), gestionează procesul îndelungat de generare printr-o coadă de job-uri asincronă, transmite actualizări de progres și livrează videoclipul final. Vei implementa întregul stack: server API, pipeline de procesare video, gestionarea job-urilor, urmărirea progresului și deployment.</p>\n\n<p>Generarea video este fundamental diferită de generarea de imagini. Un singur videoclip poate dura <strong>între 30 de secunde și 5 minute</strong> pentru a fi produs, ceea ce înseamnă că nu poți folosi un simplu model cerere-răspuns. În schimb, ai nevoie de o <strong>arhitectură asincronă</strong> cu cozi de job-uri, interogare periodică a stării și notificări webhook.</p>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #7c3aed;\">\n<h3>ClipCraft &mdash; Prezentare Completă a Proiectului</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Componentă</th>\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Tehnologie</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Server API</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Python + FastAPI</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Generare Video</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway Gen-3, Pika Labs sau Stability AI Video (prin API)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Coadă de Job-uri</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Coadă asincronă în proces (dev) / Redis + Celery (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Urmărirea Progresului</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Endpoint de interogare + callback-uri webhook</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Procesare Video</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">NumPy (calcule pe cadre) + FFmpeg (codificare)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Bază de Date</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">SQLite (dev) / PostgreSQL (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Frontend</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">HTML + vanilla JS</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Deployment</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Docker + orice furnizor cloud</td></tr>\n</table>\n</div>\n\n<h2>Structura Proiectului</h2>\n\n<p>Creează această structură de foldere pe mașina ta. Fiecare fișier este explicat în această lecție.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Project Layout</span></div>\n<pre><code>clipcraft/\n&boxvr;&boxh;&boxh; app/\n&boxv;   &boxvr;&boxh;&boxh; __init__.py\n&boxv;   &boxvr;&boxh;&boxh; main.py            # FastAPI application\n&boxv;   &boxvr;&boxh;&boxh; config.py          # Settings and API keys\n&boxv;   &boxvr;&boxh;&boxh; models.py          # Pydantic request/response models\n&boxv;   &boxvr;&boxh;&boxh; video_service.py   # Video generation API wrapper\n&boxv;   &boxvr;&boxh;&boxh; job_queue.py       # Async job queue system\n&boxv;   &boxvr;&boxh;&boxh; progress.py        # Progress tracking and webhooks\n&boxv;   &boxvr;&boxh;&boxh; processing.py      # Video frame processing\n&boxv;   &boxvr;&boxh;&boxh; billing.py         # Usage tracking and billing\n&boxv;   &boxur;&boxh;&boxh; database.py        # Database connection\n&boxvr;&boxh;&boxh; static/\n&boxv;   &boxvr;&boxh;&boxh; index.html         # Frontend UI\n&boxv;   &boxur;&boxh;&boxh; style.css\n&boxvr;&boxh;&boxh; tests/\n&boxv;   &boxur;&boxh;&boxh; test_api.py\n&boxvr;&boxh;&boxh; requirements.txt\n&boxvr;&boxh;&boxh; Dockerfile\n&boxvr;&boxh;&boxh; docker-compose.yml\n&boxur;&boxh;&boxh; .env.example</code></pre>\n</div>\n\n<h2>Partea 1: Cum Funcționează API-urile de Generare Video</h2>\n\n<p>Modelele text-to-video extind tehnicile de difuzie de la imagini la secvențe temporale. În loc să elimine zgomotul dintr-un singur cadru, elimină zgomotul dintr-un <strong>volum spațio-temporal</strong> &mdash; producând multiple cadre coerente care curg natural. Aceasta este cu ordine de mărime mai scumpă decât generarea de imagini și durează semnificativ mai mult.</p>\n\n<div class=\"concept-card\">\n<h3>Comparație API-uri de Generare Video (2024&ndash;2025)</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Furnizor</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Model</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Cost per videoclip</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Durată / Rezoluție</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Gen-3 Alpha</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.25&ndash;$1.00</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5&ndash;10s / până la 1280&times;768</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika Labs</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika 1.0</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.20&ndash;$0.80</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">3&ndash;4s / până la 1024&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stability AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stable Video Diffusion</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.10&ndash;$0.50</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">4s / 576&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Luma AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Dream Machine</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.30&ndash;$0.90</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5s / până la 1360&times;752</td></tr>\n</table>\n</div>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #f59e0b;\">\n<h3>Diferența Cheie față de Generarea de Imagini</h3>\n<p>Generarea de imagini returnează un rezultat în 2&ndash;10 secunde. Generarea video durează <strong>între 30 de secunde și 5 minute</strong>. Aceasta înseamnă că nu poți ține o conexiune HTTP deschisă și aștepta. Ai nevoie de:</p>\n<ul>\n<li><strong>Coadă de Job-uri:</strong> Acceptă cererea, returnează un ID de job imediat, procesează în fundal</li>\n<li><strong>Urmărirea Progresului:</strong> Permite clienților să interogheze starea sau să primească callback-uri webhook</li>\n<li><strong>Logică de Reîncercare:</strong> Sarcinile de lungă durată eșuează mai des &mdash; trebuie să gestionezi reîncercările cu grație</li>\n<li><strong>Gestionarea Resurselor:</strong> Limitează job-urile concurente pentru a controla costurile și a preveni supraîncărcarea</li>\n</ul>\n</div>\n\n<p>Modelul arhitectural este: <strong>acceptă &rarr; pune în coadă &rarr; procesează &rarr; notifică</strong>. Să construim fiecare parte.</p>\n\n<h2>Partea 2: Pipeline-ul de Procesare Video</h2>\n\n<p>Înainte de a integra API-uri externe, să înțelegem cum funcționează cadrele video. Un videoclip este o secvență de cadre de imagine redate în succesiune rapidă. Fiecare cadru este un array NumPy de date pixel. Codul de mai jos demonstrează cum să generezi și să procesezi cadre video programatic.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# processing.py - Video frame generation and processing\n\nimport numpy as np\nimport time\n\nclass VideoFrameProcessor:\n    \"\"\"Generate and process video frames using NumPy.\"\"\"\n\n    @staticmethod\n    def generate_gradient_frames(width, height, num_frames, fps=24):\n        \"\"\"Generate a sequence of gradient frames that animate over time.\"\"\"\n        frames = []\n        for i in range(num_frames):\n            frame = np.zeros((height, width, 3), dtype=np.uint8)\n            t = i / num_frames  # normalized time 0..1\n\n            for y in range(height):\n                for x in range(width):\n                    # Red channel: horizontal sweep that shifts over time\n                    r = int(255 * ((x / width + t) % 1.0))\n                    # Green channel: vertical pulse\n                    g = int(255 * ((y / height + t * 0.5) % 1.0))\n                    # Blue channel: radial wave from center\n                    cx, cy = width / 2, height / 2\n                    dist = ((x - cx)**2 + (y - cy)**2) ** 0.5\n                    max_dist = (cx**2 + cy**2) ** 0.5\n                    b = int(127 + 128 * np.sin(dist / max_dist * 6.28 - t * 6.28))\n                    frame[y, x] = [r, g, b]\n\n            frames.append(frame)\n        return frames\n\n    @staticmethod\n    def apply_fade_transition(frames, fade_length=5):\n        \"\"\"Apply a fade-in at the start and fade-out at the end.\"\"\"\n        result = []\n        total = len(frames)\n        for i, frame in enumerate(frames):\n            factor = 1.0\n            if i < fade_length:\n                factor = i / fade_length\n            elif i >= total - fade_length:\n                factor = (total - 1 - i) / fade_length\n            faded = (frame.astype(np.float64) * factor).astype(np.uint8)\n            result.append(faded)\n        return result\n\n    @staticmethod\n    def compute_frame_stats(frames):\n        \"\"\"Compute statistics across all frames.\"\"\"\n        brightnesses = []\n        for frame in frames:\n            avg = frame.mean()\n            brightnesses.append(round(float(avg), 2))\n\n        return {\n            \"num_frames\": len(frames),\n            \"resolution\": f\"{frames[0].shape[1]}x{frames[0].shape[0]}\",\n            \"avg_brightness\": round(np.mean(brightnesses), 2),\n            \"min_brightness\": min(brightnesses),\n            \"max_brightness\": max(brightnesses),\n            \"total_pixels\": frames[0].shape[0] * frames[0].shape[1] * len(frames),\n            \"memory_mb\": round(sum(f.nbytes for f in frames) / (1024*1024), 2)\n        }\n\n    @staticmethod\n    def add_text_overlay_region(frame, x, y, box_w, box_h, color=(255, 255, 255)):\n        \"\"\"Add a colored rectangle region (simulating text overlay area).\"\"\"\n        result = frame.copy()\n        y_end = min(y + box_h, frame.shape[0])\n        x_end = min(x + box_w, frame.shape[1])\n        # Semi-transparent overlay\n        overlay = result[y:y_end, x:x_end].astype(np.float64)\n        overlay = overlay * 0.5 + np.array(color, dtype=np.float64) * 0.5\n        result[y:y_end, x:x_end] = overlay.astype(np.uint8)\n        return result\n\n\n# --- Demo: Generate and process video frames ---\nproc = VideoFrameProcessor()\n\nprint(\"=== ClipCraft Video Processing Demo ===\\\n\")\n\n# 1. Generate frames (small for demo)\nprint(\"1. Generating 12 frames at 32x24 resolution...\")\nstart = time.time()\nframes = proc.generate_gradient_frames(32, 24, 12, fps=24)\nelapsed = round(time.time() - start, 3)\nprint(f\"   Generated in {elapsed}s\")\n\nstats = proc.compute_frame_stats(frames)\nfor key, val in stats.items():\n    print(f\"   {key}: {val}\")\n\n# 2. Apply fade transition\nprint(\"\\\n2. Applying fade-in/fade-out transition...\")\nfaded = proc.apply_fade_transition(frames, fade_length=3)\nfaded_stats = proc.compute_frame_stats(faded)\nprint(f\"   Avg brightness before fade: {stats['avg_brightness']}\")\nprint(f\"   Avg brightness after fade:  {faded_stats['avg_brightness']}\")\nprint(f\"   (Lower because start/end frames are darkened)\")\n\n# 3. Add overlay region to a frame\nprint(\"\\\n3. Adding text overlay region to frame 6...\")\noverlay_frame = proc.add_text_overlay_region(frames[5], 2, 2, 20, 8)\nbefore_mean = round(float(frames[5][2:10, 2:22].mean()), 1)\nafter_mean = round(float(overlay_frame[2:10, 2:22].mean()), 1)\nprint(f\"   Region avg before: {before_mean}\")\nprint(f\"   Region avg after:  {after_mean} (blended with white)\")\n\n# 4. Estimate production video sizes\nprint(\"\\\n4. Production video size estimates:\")\nresolutions = [\n    (\"720p\", 1280, 720, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 10),\n]\nfor label, w, h, fps, duration in resolutions:\n    raw_mb = w * h * 3 * fps * duration / (1024**2)\n    compressed_mb = raw_mb * 0.02  # typical H.264 ratio\n    print(f\"   {label} {duration}s @ {fps}fps: \"\n          f\"{raw_mb:.0f} MB raw, ~{compressed_mb:.1f} MB compressed (H.264)\")\n\nprint(\"\\\nDone! Frame processing pipeline is ready.\")\n</code></pre>\n</div>\n\n<h2>Partea 3: Coada de Job-uri Asincronă</h2>\n\n<p>Generarea video durează prea mult pentru HTTP sincron. Avem nevoie de o coadă de job-uri care acceptă cereri, le procesează în fundal și permite clienților să verifice starea. Acesta este modelul arhitectural fundamental pentru orice sarcină de lungă durată într-un produs SaaS.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# job_queue.py - Async job queue for video generation\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass JobStatus:\n    PENDING = \"pending\"\n    PROCESSING = \"processing\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    CANCELLED = \"cancelled\"\n\nclass VideoJob:\n    \"\"\"Represents a single video generation job.\"\"\"\n\n    def __init__(self, prompt, style, duration_sec, resolution, user_id):\n        self.id = str(uuid.uuid4())[:8]\n        self.prompt = prompt\n        self.style = style\n        self.duration_sec = duration_sec\n        self.resolution = resolution\n        self.user_id = user_id\n        self.status = JobStatus.PENDING\n        self.progress = 0\n        self.created_at = datetime.now().isoformat()\n        self.started_at = None\n        self.completed_at = None\n        self.result_url = None\n        self.error = None\n        self.retry_count = 0\n        self.max_retries = 3\n\n    def to_dict(self):\n        return {\n            \"id\": self.id,\n            \"prompt\": self.prompt[:40],\n            \"status\": self.status,\n            \"progress\": self.progress,\n            \"created_at\": self.created_at,\n            \"started_at\": self.started_at,\n            \"completed_at\": self.completed_at,\n            \"result_url\": self.result_url,\n            \"error\": self.error\n        }\n\n\nclass JobQueue:\n    \"\"\"In-process job queue for video generation tasks.\"\"\"\n\n    def __init__(self, max_concurrent=3):\n        self.jobs = {}\n        self.queue = []        # pending job IDs\n        self.active = []       # currently processing job IDs\n        self.max_concurrent = max_concurrent\n\n    def submit(self, prompt, style, duration_sec, resolution, user_id):\n        \"\"\"Submit a new video generation job. Returns job ID.\"\"\"\n        job = VideoJob(prompt, style, duration_sec, resolution, user_id)\n        self.jobs[job.id] = job\n        self.queue.append(job.id)\n        return job.id\n\n    def get_status(self, job_id):\n        \"\"\"Get current status of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job.to_dict()\n\n    def process_next(self):\n        \"\"\"Try to start the next pending job. Returns job or None.\"\"\"\n        if len(self.active) >= self.max_concurrent:\n            return None\n        if not self.queue:\n            return None\n\n        job_id = self.queue.pop(0)\n        job = self.jobs[job_id]\n        job.status = JobStatus.PROCESSING\n        job.started_at = datetime.now().isoformat()\n        self.active.append(job_id)\n        return job\n\n    def update_progress(self, job_id, progress):\n        \"\"\"Update job progress (0-100).\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.progress = min(progress, 100)\n\n    def complete_job(self, job_id, result_url):\n        \"\"\"Mark job as completed with result URL.\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.status = JobStatus.COMPLETED\n            job.progress = 100\n            job.completed_at = datetime.now().isoformat()\n            job.result_url = result_url\n            if job_id in self.active:\n                self.active.remove(job_id)\n\n    def fail_job(self, job_id, error):\n        \"\"\"Mark job as failed. Re-queue if retries remain.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n        if job_id in self.active:\n            self.active.remove(job_id)\n\n        job.retry_count += 1\n        if job.retry_count < job.max_retries:\n            job.status = JobStatus.PENDING\n            job.progress = 0\n            self.queue.append(job_id)\n            return f\"Retrying ({job.retry_count}/{job.max_retries})\"\n        else:\n            job.status = JobStatus.FAILED\n            job.error = error\n            job.completed_at = datetime.now().isoformat()\n            return \"Max retries exceeded\"\n\n    def cancel_job(self, job_id):\n        \"\"\"Cancel a pending or active job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return False\n        job.status = JobStatus.CANCELLED\n        job.completed_at = datetime.now().isoformat()\n        if job_id in self.queue:\n            self.queue.remove(job_id)\n        if job_id in self.active:\n            self.active.remove(job_id)\n        return True\n\n    def get_queue_stats(self):\n        \"\"\"Get overall queue statistics.\"\"\"\n        statuses = {}\n        for job in self.jobs.values():\n            statuses[job.status] = statuses.get(job.status, 0) + 1\n        return {\n            \"total_jobs\": len(self.jobs),\n            \"queue_length\": len(self.queue),\n            \"active_jobs\": len(self.active),\n            \"max_concurrent\": self.max_concurrent,\n            \"by_status\": statuses\n        }\n\n\n# --- Demo: Simulate a video generation workload ---\nqueue = JobQueue(max_concurrent=2)\n\nprint(\"=== ClipCraft Job Queue Demo ===\\\n\")\n\n# Submit several jobs\nprompts = [\n    (\"A serene lake at sunset with mountains\", \"cinematic\", 5),\n    (\"Robot dancing in a neon city\", \"anime\", 4),\n    (\"Aerial flyover of a tropical island\", \"drone\", 8),\n    (\"A flower blooming in timelapse\", \"macro\", 3),\n    (\"Astronaut walking on Mars\", \"cinematic\", 6),\n]\n\nprint(\"Submitting 5 video generation jobs:\")\njob_ids = []\nfor prompt, style, dur in prompts:\n    jid = queue.submit(prompt, style, dur, \"1280x720\", \"user_demo\")\n    job_ids.append(jid)\n    print(f\"  Job {jid}: '{prompt[:35]}...' ({dur}s, {style})\")\n\nprint(f\"\\\nQueue stats: {queue.get_queue_stats()}\")\n\n# Process jobs step by step\nprint(\"\\\n--- Processing Loop ---\\\n\")\n\nstep = 0\nwhile queue.queue or queue.active:\n    step += 1\n\n    # Start next jobs if capacity available\n    while len(queue.active) < queue.max_concurrent and queue.queue:\n        job = queue.process_next()\n        if job:\n            print(f\"Step {step}: STARTED job {job.id} \"\n                  f\"({job.prompt[:25]}...)\")\n\n    # Simulate progress on active jobs\n    for jid in list(queue.active):\n        job = queue.jobs[jid]\n        job.progress += 35  # simulate chunk of progress\n\n        if job.progress >= 100:\n            # Simulate: 1 in 5 chance of failure on first attempt\n            if job.retry_count == 0 and job.id == job_ids[2]:\n                msg = queue.fail_job(jid, \"GPU timeout\")\n                print(f\"Step {step}: FAILED job {jid} -> {msg}\")\n            else:\n                url = f\"https://cdn.clipcraft.dev/v/{jid}.mp4\"\n                queue.complete_job(jid, url)\n                print(f\"Step {step}: COMPLETED job {jid} \"\n                      f\"-> {url}\")\n        else:\n            queue.update_progress(jid, job.progress)\n            print(f\"Step {step}: job {jid} progress: \"\n                  f\"{job.progress}%\")\n\nprint(f\"\\\n--- Final Results ---\\\n\")\nfor jid in job_ids:\n    status = queue.get_status(jid)\n    print(f\"Job {jid}: {status['status']}\"\n          f\" | progress: {status['progress']}%\"\n          f\" | result: {status.get('result_url', 'N/A')}\"\n          f\" | error: {status.get('error', 'none')}\")\n\nprint(f\"\\\nFinal queue stats: {queue.get_queue_stats()}\")\n</code></pre>\n</div>\n\n<h2>Partea 4: Urmărirea Progresului</h2>\n\n<p>Clienții trebuie să știe cum progresează videoclipul lor. Suportăm două modele: <strong>interogare periodică</strong> (clientul verifică periodic) și <strong>webhook-uri</strong> (serverul trimite actualizări). Ambele sunt esențiale pentru un SaaS de producție. Codul de mai jos demonstrează tracker-ul de progres cu livrare simulată de webhook-uri.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# progress.py - Progress tracking with polling and webhooks\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass ProgressTracker:\n    \"\"\"Track video generation progress with polling and webhook support.\"\"\"\n\n    def __init__(self):\n        self.jobs = {}\n        self.webhook_log = []  # simulated webhook deliveries\n\n    def register_job(self, job_id, webhook_url=None):\n        \"\"\"Register a job for progress tracking.\"\"\"\n        self.jobs[job_id] = {\n            \"job_id\": job_id,\n            \"progress\": 0,\n            \"stage\": \"queued\",\n            \"webhook_url\": webhook_url,\n            \"events\": [],\n            \"started\": datetime.now().isoformat()\n        }\n\n    def update(self, job_id, progress, stage, message=\"\"):\n        \"\"\"Update progress and optionally fire webhook.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n\n        job[\"progress\"] = progress\n        job[\"stage\"] = stage\n\n        event = {\n            \"timestamp\": datetime.now().isoformat(),\n            \"progress\": progress,\n            \"stage\": stage,\n            \"message\": message\n        }\n        job[\"events\"].append(event)\n\n        # Fire webhook if configured\n        if job[\"webhook_url\"]:\n            self._fire_webhook(job_id, event)\n\n    def _fire_webhook(self, job_id, event):\n        \"\"\"Simulate sending a webhook notification.\"\"\"\n        payload = {\n            \"event\": \"video.progress\",\n            \"job_id\": job_id,\n            \"data\": event,\n            \"delivery_id\": str(uuid.uuid4())[:8]\n        }\n        self.webhook_log.append(payload)\n\n    def poll(self, job_id):\n        \"\"\"Client polls for current status (the polling endpoint).\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return {\"error\": \"Job not found\"}\n        return {\n            \"job_id\": job[\"job_id\"],\n            \"progress\": job[\"progress\"],\n            \"stage\": job[\"stage\"],\n            \"latest_event\": job[\"events\"][-1] if job[\"events\"] else None\n        }\n\n    def get_full_history(self, job_id):\n        \"\"\"Get the full event history of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job[\"events\"]\n\n\n# --- Demo: Simulate video generation with progress ---\ntracker = ProgressTracker()\n\nprint(\"=== ClipCraft Progress Tracking Demo ===\\\n\")\n\n# Job 1: With webhook\njob1 = \"vid_abc123\"\ntracker.register_job(job1, webhook_url=\"https://myapp.com/hooks/clipcraft\")\n\n# Job 2: Polling only\njob2 = \"vid_def456\"\ntracker.register_job(job2, webhook_url=None)\n\n# Simulate the video generation stages\nstages = [\n    (0,   \"queued\",       \"Job accepted, waiting for GPU\"),\n    (5,   \"initializing\", \"Loading model weights\"),\n    (15,  \"generating\",   \"Generating frames 1-30\"),\n    (40,  \"generating\",   \"Generating frames 31-60\"),\n    (65,  \"generating\",   \"Generating frames 61-90\"),\n    (80,  \"generating\",   \"Generating frames 91-120\"),\n    (90,  \"encoding\",     \"Encoding H.264 video\"),\n    (95,  \"uploading\",    \"Uploading to CDN\"),\n    (100, \"completed\",    \"Video ready for download\"),\n]\n\nprint(\"--- Job 1 (with webhook) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job1, progress, stage, message)\n    status = tracker.poll(job1)\n    print(f\"  [{status['progress']:3d}%] {status['stage']:14s} | {message}\")\n\nprint(f\"\\\n  Webhooks fired: {len(tracker.webhook_log)}\")\nfor wh in tracker.webhook_log[:3]:\n    print(f\"    -> {wh['event']}: {wh['data']['stage']} \"\n          f\"({wh['data']['progress']}%) [delivery: {wh['delivery_id']}]\")\nprint(f\"    ... and {len(tracker.webhook_log) - 3} more\")\n\nprint(\"\\\n--- Job 2 (polling only) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job2, progress, stage, message)\n\n# Simulate a client polling at intervals\npoll_points = [0, 20, 50, 80, 100]\nprint(\"  Client polling at intervals:\")\nhistory = tracker.get_full_history(job2)\nfor i, event in enumerate(history):\n    if event[\"progress\"] in poll_points:\n        print(f\"    Poll response: {event['progress']}% - {event['stage']}\")\n\nprint(f\"\\\n  Total events recorded: {len(history)}\")\n\n# Show webhook payload structure\nprint(\"\\\n--- Webhook Payload Structure ---\")\nsample = tracker.webhook_log[4]\nprint(\"  {\")\nfor key, val in sample.items():\n    if key == \"data\":\n        print(f'    \"data\": {{')\n        for dk, dv in val.items():\n            print(f'      \"{dk}\": \"{dv}\",')\n        print(f'    }},')\n    else:\n        print(f'    \"{key}\": \"{val}\",')\nprint(\"  }\")\n\nprint(\"\\\nBoth patterns ready for production use.\")\n</code></pre>\n</div>\n\n<h2>Partea 5: Serverul FastAPI</h2>\n\n<p>Aceasta este inima ClipCraft &mdash; serverul API care leagă coada de job-uri, urmărirea progresului, procesarea video și facturarea. Diferența cheie față de un server de generare de imagini este <strong>modelul asincron</strong>: endpoint-ul <code>/api/generate</code> returnează imediat cu un ID de job, iar clienții interoghează <code>/api/jobs/{id}</code> pentru progres.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/main.py</span></div>\n<pre><code class=\"language-python\">\"\"\"ClipCraft API Server - Video Generation SaaS.\"\"\"\nfrom fastapi import FastAPI, HTTPException, Depends, Header, BackgroundTasks\nfrom fastapi.staticfiles import StaticFiles\nfrom fastapi.responses import JSONResponse\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\nimport uuid\nimport asyncio\nimport time\n\nfrom .config import settings\nfrom .video_service import VideoService\nfrom .job_queue import JobQueue, JobStatus\nfrom .progress import ProgressTracker\nfrom .billing import BillingSystem\n\napp = FastAPI(title=\"ClipCraft API\", version=\"1.0.0\")\napp.mount(\"/static\", StaticFiles(directory=\"static\"), name=\"static\")\n\n# Initialize services\nvideo_service = VideoService(\n    provider=settings.VIDEO_PROVIDER,\n    api_key=settings.VIDEO_API_KEY\n)\njob_queue = JobQueue(max_concurrent=settings.MAX_CONCURRENT_JOBS)\nprogress_tracker = ProgressTracker()\nbilling = BillingSystem()\n\n\n# --- Request/Response Models ---\nclass GenerateRequest(BaseModel):\n    prompt: str = Field(..., min_length=3, max_length=500)\n    style: str = Field(default=\"cinematic\")\n    duration: int = Field(default=5, ge=2, le=15)\n    resolution: str = Field(default=\"1280x720\")\n    webhook_url: Optional[str] = None\n\nclass JobResponse(BaseModel):\n    job_id: str\n    status: str\n    message: str\n    poll_url: str\n\nclass JobStatusResponse(BaseModel):\n    job_id: str\n    status: str\n    progress: int\n    stage: str\n    result_url: Optional[str] = None\n    error: Optional[str] = None\n    estimated_remaining_sec: Optional[int] = None\n\n\n# --- API key authentication ---\nasync def get_user(x_api_key: str = Header(...)):\n    user = billing.get_user_by_key(x_api_key)\n    if not user:\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n    return user\n\n\n# --- Background video generation task ---\nasync def process_video_job(job_id: str):\n    \"\"\"Background task that processes a video generation job.\"\"\"\n    job = job_queue.jobs.get(job_id)\n    if not job:\n        return\n\n    try:\n        progress_tracker.update(job_id, 5, \"initializing\",\n                                \"Loading model and preparing generation\")\n\n        # Call the video generation API\n        result = await video_service.generate(\n            prompt=job.prompt,\n            style=job.style,\n            duration_sec=job.duration_sec,\n            resolution=job.resolution,\n            progress_callback=lambda p, s, m:\n                progress_tracker.update(job_id, p, s, m)\n        )\n\n        progress_tracker.update(job_id, 95, \"uploading\",\n                                \"Uploading to CDN\")\n\n        video_url = result[\"video_url\"]\n        job_queue.complete_job(job_id, video_url)\n\n        progress_tracker.update(job_id, 100, \"completed\",\n                                \"Video ready for download\")\n\n        billing.record_generation(\n            job.user_id, job.prompt,\n            result[\"cost\"], job.resolution\n        )\n\n    except Exception as e:\n        msg = job_queue.fail_job(job_id, str(e))\n        progress_tracker.update(job_id, 0, \"failed\", str(e))\n\n\n# --- Endpoints ---\n@app.post(\"/api/generate\", response_model=JobResponse)\nasync def generate_video(req: GenerateRequest,\n                         background_tasks: BackgroundTasks,\n                         user=Depends(get_user)):\n    \"\"\"Submit a video generation job. Returns immediately with a job ID.\"\"\"\n    # Check billing limits\n    can, reason = billing.can_generate(user[\"id\"])\n    if not can:\n        raise HTTPException(status_code=429, detail=reason)\n\n    # Submit to job queue\n    job_id = job_queue.submit(\n        prompt=req.prompt,\n        style=req.style,\n        duration_sec=req.duration,\n        resolution=req.resolution,\n        user_id=user[\"id\"]\n    )\n\n    # Register progress tracking\n    progress_tracker.register_job(job_id, webhook_url=req.webhook_url)\n\n    # Start processing in the background\n    job = job_queue.process_next()\n    if job:\n        background_tasks.add_task(process_video_job, job.id)\n\n    return JobResponse(\n        job_id=job_id,\n        status=\"pending\",\n        message=\"Video generation job submitted\",\n        poll_url=f\"/api/jobs/{job_id}\"\n    )\n\n\n@app.get(\"/api/jobs/{job_id}\", response_model=JobStatusResponse)\nasync def get_job_status(job_id: str, user=Depends(get_user)):\n    \"\"\"Poll for job status and progress.\"\"\"\n    status = job_queue.get_status(job_id)\n    if not status:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n\n    poll_data = progress_tracker.poll(job_id)\n    estimated = None\n    if poll_data and poll_data.get(\"progress\", 0) > 0:\n        pct = poll_data[\"progress\"]\n        if pct < 100:\n            estimated = int((100 - pct) * 1.5)\n\n    return JobStatusResponse(\n        job_id=job_id,\n        status=status[\"status\"],\n        progress=status.get(\"progress\", 0),\n        stage=poll_data.get(\"stage\", \"unknown\") if poll_data else \"unknown\",\n        result_url=status.get(\"result_url\"),\n        error=status.get(\"error\"),\n        estimated_remaining_sec=estimated\n    )\n\n\n@app.delete(\"/api/jobs/{job_id}\")\nasync def cancel_job(job_id: str, user=Depends(get_user)):\n    \"\"\"Cancel a pending or active job.\"\"\"\n    success = job_queue.cancel_job(job_id)\n    if not success:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n    return {\"message\": \"Job cancelled\", \"job_id\": job_id}\n\n\n@app.get(\"/api/jobs\")\nasync def list_jobs(user=Depends(get_user)):\n    \"\"\"List all jobs for the authenticated user.\"\"\"\n    user_jobs = [\n        job.to_dict() for job in job_queue.jobs.values()\n        if job.user_id == user[\"id\"]\n    ]\n    return {\"jobs\": user_jobs, \"total\": len(user_jobs)}\n\n\n@app.get(\"/api/queue/stats\")\nasync def queue_stats():\n    \"\"\"Get queue statistics (admin endpoint).\"\"\"\n    return job_queue.get_queue_stats()\n\n\n@app.get(\"/api/health\")\nasync def health():\n    return {\n        \"status\": \"ok\",\n        \"provider\": settings.VIDEO_PROVIDER,\n        \"queue\": job_queue.get_queue_stats()\n    }</code></pre>\n</div>\n\n<h2>Partea 6: Configurare și Deployment</h2>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/config.py</span></div>\n<pre><code class=\"language-python\">\"\"\"Configuration loaded from environment variables.\"\"\"\nfrom pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    VIDEO_PROVIDER: str = \"runway\"  # \"runway\", \"pika\", or \"stability\"\n    VIDEO_API_KEY: str = \"\"\n    DATABASE_URL: str = \"sqlite:///./clipcraft.db\"\n    MAX_CONCURRENT_JOBS: int = 3\n    MAX_VIDEO_DURATION: int = 15  # seconds\n    STORAGE_BUCKET: str = \"clipcraft-videos\"\n    WEBHOOK_SECRET: str = \"\"\n\n    class Config:\n        env_file = \".env\"\n\nsettings = Settings()</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>.env.example</span></div>\n<pre><code>VIDEO_PROVIDER=runway\nVIDEO_API_KEY=your_api_key_here\nDATABASE_URL=sqlite:///./clipcraft.db\nMAX_CONCURRENT_JOBS=3\nMAX_VIDEO_DURATION=15\nWEBHOOK_SECRET=your_webhook_secret</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>requirements.txt</span></div>\n<pre><code>fastapi==0.109.0\nuvicorn==0.27.0\nhttpx==0.26.0\npydantic==2.5.3\npydantic-settings==2.1.0\npython-multipart==0.0.6\naiofiles==23.2.1</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Dockerfile</span></div>\n<pre><code>FROM python:3.11-slim\n\n# Install FFmpeg for video processing\nRUN apt-get update &amp;&amp; \\\\\n    apt-get install -y --no-install-recommends ffmpeg &amp;&amp; \\\\\n    rm -rf /var/lib/apt/lists/*\n\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n\nEXPOSE 8000\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>docker-compose.yml</span></div>\n<pre><code>version: \"3.8\"\nservices:\n  clipcraft:\n    build: .\n    ports:\n      - \"8000:8000\"\n    env_file:\n      - .env\n    volumes:\n      - ./videos:/app/videos\n      - ./static:/app/static\n    deploy:\n      resources:\n        limits:\n          memory: 2G</code></pre>\n</div>\n\n<p>Pentru a rula local:</p>\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Terminal</span></div>\n<pre><code># Development\npip install -r requirements.txt\nuvicorn app.main:app --reload\n\n# Or with Docker\ndocker-compose up --build\n\n# Submit a video generation job\ncurl -X POST http://localhost:8000/api/generate \\\\\n  -H \"X-API-Key: your_key\" \\\\\n  -H \"Content-Type: application/json\" \\\\\n  -d '{\"prompt\": \"sunset over ocean waves\", \"style\": \"cinematic\", \"duration\": 5}'\n\n# Poll for progress\ncurl http://localhost:8000/api/jobs/JOB_ID \\\\\n  -H \"X-API-Key: your_key\"</code></pre>\n</div>\n\n<h2>Partea 7: Interfața Frontend</h2>\n\n<p>Frontend-ul ClipCraft gestionează fluxul asincron: trimite un prompt, apoi interoghează progresul și afișează rezultatul când este gata. Salvează aceasta ca <code>static/index.html</code>.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>static/index.html (key parts)</span></div>\n<pre><code class=\"language-html\">&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n  &lt;title&gt;ClipCraft - AI Video Generation&lt;/title&gt;\n  &lt;style&gt;\n    body { font-family: system-ui; max-width: 800px; margin: 0 auto; padding: 20px;\n           background: #0f0f0f; color: #e5e5e5; }\n    h1 { background: linear-gradient(135deg, #7c3aed, #a855f7);\n         -webkit-background-clip: text; -webkit-text-fill-color: transparent; }\n    .prompt-input { width: 100%; padding: 14px; font-size: 16px;\n                    border: 2px solid #333; border-radius: 10px;\n                    background: #1a1a1a; color: #e5e5e5; margin: 10px 0; }\n    .controls { display: flex; gap: 12px; margin: 12px 0; align-items: center; }\n    .controls select { padding: 10px; border-radius: 8px; background: #1a1a1a;\n                        color: #e5e5e5; border: 2px solid #333; }\n    .generate-btn { padding: 14px 32px; background: #7c3aed; color: white;\n                    border: none; border-radius: 10px; font-size: 16px;\n                    cursor: pointer; }\n    .generate-btn:disabled { opacity: 0.5; cursor: not-allowed; }\n    .progress-bar { width: 100%; height: 8px; background: #333;\n                    border-radius: 4px; margin: 16px 0; display: none; }\n    .progress-fill { height: 100%; background: linear-gradient(90deg, #7c3aed, #a855f7);\n                     border-radius: 4px; transition: width 0.3s; width: 0%; }\n    .status-text { color: #a78bfa; font-size: 14px; margin: 8px 0; }\n    .result video { width: 100%; border-radius: 12px; margin: 20px 0; }\n  &lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;ClipCraft&lt;/h1&gt;\n  &lt;p&gt;Generate videos from text descriptions&lt;/p&gt;\n  &lt;input class=\"prompt-input\" placeholder=\"Describe the video you want...\" id=\"prompt\"&gt;\n  &lt;div class=\"controls\"&gt;\n    &lt;select id=\"style\"&gt;\n      &lt;option value=\"cinematic\"&gt;Cinematic&lt;/option&gt;\n      &lt;option value=\"anime\"&gt;Anime&lt;/option&gt;\n      &lt;option value=\"drone\"&gt;Drone Shot&lt;/option&gt;\n      &lt;option value=\"macro\"&gt;Macro&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;select id=\"duration\"&gt;\n      &lt;option value=\"3\"&gt;3 seconds&lt;/option&gt;\n      &lt;option value=\"5\" selected&gt;5 seconds&lt;/option&gt;\n      &lt;option value=\"10\"&gt;10 seconds&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;button class=\"generate-btn\" id=\"genBtn\" onclick=\"generate()\"&gt;Generate Video&lt;/button&gt;\n  &lt;/div&gt;\n  &lt;div class=\"progress-bar\" id=\"progressBar\"&gt;\n    &lt;div class=\"progress-fill\" id=\"progressFill\"&gt;&lt;/div&gt;\n  &lt;/div&gt;\n  &lt;p class=\"status-text\" id=\"statusText\"&gt;&lt;/p&gt;\n  &lt;div class=\"result\" id=\"result\"&gt;&lt;/div&gt;\n\n  &lt;script&gt;\n  let pollInterval = null;\n\n  async function generate() {\n    const prompt = document.getElementById('prompt').value;\n    const style = document.getElementById('style').value;\n    const duration = parseInt(document.getElementById('duration').value);\n    const btn = document.getElementById('genBtn');\n    btn.disabled = true;\n    document.getElementById('progressBar').style.display = 'block';\n\n    const res = await fetch('/api/generate', {\n      method: 'POST',\n      headers: {'Content-Type': 'application/json', 'X-API-Key': 'demo'},\n      body: JSON.stringify({prompt, style, duration})\n    });\n    const data = await res.json();\n    pollForResult(data.job_id);\n  }\n\n  function pollForResult(jobId) {\n    pollInterval = setInterval(async () =&gt; {\n      const res = await fetch('/api/jobs/' + jobId,\n        {headers: {'X-API-Key': 'demo'}});\n      const data = await res.json();\n      document.getElementById('progressFill').style.width = data.progress + '%';\n      document.getElementById('statusText').textContent =\n        data.stage + ' (' + data.progress + '%)';\n      if (data.status === 'completed') {\n        clearInterval(pollInterval);\n        document.getElementById('result').innerHTML =\n          '&lt;video controls autoplay src=\"' + data.result_url + '\"&gt;&lt;/video&gt;';\n        document.getElementById('genBtn').disabled = false;\n      } else if (data.status === 'failed') {\n        clearInterval(pollInterval);\n        document.getElementById('statusText').textContent = 'Error: ' + data.error;\n        document.getElementById('genBtn').disabled = false;\n      }\n    }, 2000);\n  }\n  &lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;</code></pre>\n</div>\n\n\n<h2>Partea 11: Conectare la Propriul LLM</h2>\n\n<p>Până acum am folosit API-uri terțe. Dar puterea reală &mdash; și marjele reale &mdash; vin când rulezi <strong>propriile modele</strong>. Aceasta elimină costurile API per cerere, îți oferă control total asupra latenței și confidențialității, și îți permite să ajustezi modelele pentru cazul tău specific de utilizare.</p>\n\n<p>Trei abordări principale pentru auto-hosting:</p>\n<ul>\n<li><strong>vLLM</strong> &mdash; cel mai mare debit, API compatibil OpenAI, ideal pentru producție</li>\n<li><strong>Ollama</strong> &mdash; cea mai ușoară configurare, ideal pentru dezvoltare și deployment la scară mică</li>\n<li><strong>llama.cpp</strong> &mdash; cel mai ușor, rulează pe CPU, ideal pentru deployment la margine</li>\n</ul>\n\n<p>Pentru generarea de video, poți auto-găzdui modele precum <strong>CogVideoX</strong>, <strong>Mochi</strong> sau <strong>AnimateDiff</strong>:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>serve_own_model.py &mdash; Self-hosted Video Generation</span></div>\n<pre><code class=\"language-python\">\"\"\"\nSelf-hosting video generation models for ClipCraft.\nOptions: CogVideoX, Mochi-1, AnimateDiff, SVD (Stable Video Diffusion).\nRequires: GPU with >= 24GB VRAM (A100/A10G or RTX 4090).\n\"\"\"\n\nclass LocalVideoGenerator:\n    \"\"\"Generate videos using locally hosted models.\"\"\"\n\n    def __init__(self, model=\"THUDM/CogVideoX-5b\"):\n        self.model = model\n        self.device = \"cuda\"\n\n    def load_model(self):\n        \"\"\"Load video generation model. Takes 30-60s, needs 24GB+ VRAM.\"\"\"\n        # In production:\n        # import torch\n        # from diffusers import CogVideoXPipeline\n        # self.pipe = CogVideoXPipeline.from_pretrained(\n        #     self.model, torch_dtype=torch.float16\n        # ).to(self.device)\n        # self.pipe.enable_model_cpu_offload()\n        print(f\"Loaded {self.model}\")\n        print(f\"VRAM: ~20GB (fp16), Generation: ~2min per 6s clip\")\n\n    def generate(self, prompt, num_frames=49, fps=8, width=720, height=480):\n        \"\"\"Generate a video clip from text.\"\"\"\n        # In production:\n        # video = self.pipe(\n        #     prompt=prompt,\n        #     num_frames=num_frames,\n        #     guidance_scale=6,\n        #     num_inference_steps=50,\n        # ).frames[0]\n        # from diffusers.utils import export_to_video\n        # export_to_video(video, \"output.mp4\", fps=fps)\n\n        duration = num_frames / fps\n        print(f\"Generating {duration:.1f}s video: \\\\\"{prompt}\\\\\"\")\n        print(f\"  {num_frames} frames at {fps}fps, {width}x{height}\")\n        print(f\"  Estimated time: ~{num_frames * 2.5:.0f}s on A100\")\n        return {\"status\": \"generated\", \"duration\": duration, \"cost\": 0.0}\n\nclass RunwayMLClient:\n    \"\"\"Fallback: Use Runway ML API for production quality.\"\"\"\n\n    def __init__(self, api_key=\"your-runway-key\"):\n        self.api_key = api_key\n        self.base_url = \"https://api.runwayml.com/v1\"\n\n    def generate(self, prompt, duration=4):\n        \"\"\"Generate video via Runway Gen-3 Alpha API.\"\"\"\n        # import requests\n        # r = requests.post(f\"{self.base_url}/generate\",\n        #     headers={\"Authorization\": f\"Bearer {self.api_key}\"},\n        #     json={\"prompt\": prompt, \"duration\": duration})\n        cost = duration * 0.05  # ~$0.05 per second\n        print(f\"Runway API: \\\\\"{prompt}\\\\\" ({duration}s)\")\n        print(f\"  Cost: \\${cost:.2f}\")\n        return {\"status\": \"queued\", \"cost\": cost}\n\n\n# --- Demo ---\nprint(\"=== Self-Hosted Video Generation ===\\\n\")\ngen = LocalVideoGenerator()\ngen.load_model()\nresult = gen.generate(\"A cat walking through a garden, cinematic\", num_frames=49)\nprint(f\"\\\n  Result: {result}\\\n\")\n\n# Cost comparison\nprint(\"=== Cost Comparison ===\")\nprint(\"| Method            | Per 4s clip | 1K clips/mo | GPU Required  |\")\nprint(\"|-------------------|------------|-------------|---------------|\")\nprint(\"| Runway Gen-3      | $0.20      | $200        | None (API)    |\")\nprint(\"| Replicate SVD     | $0.10      | $100        | None (API)    |\")\nprint(\"| Self-host CogVideo| ~$0.02     | ~$20+$300/mo| A100 40GB     |\")\nprint(\"| Self-host AnimDiff| ~$0.01     | ~$10+$150/mo| RTX 4090 24GB |\")\nprint(\"\\\nBreakeven: ~500 clips/month for self-hosting\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Partea 12: Alternative de Deployment</h2>\n\n<p>Ai multiple căi de deployment în funcție de scala, bugetul și cerințele tehnice. Iată o comparație completă cu comenzi reale:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>deployment_options.py &mdash; ClipCraft Deployment Guide</span></div>\n<pre><code class=\"language-python\">\"\"\"\nDeployment options for ClipCraft - from hobby to enterprise scale.\nEach option includes: setup commands, estimated costs, and pros/cons.\n\"\"\"\n\nclass DeploymentGuide:\n    \"\"\"Compare deployment platforms for your SaaS.\"\"\"\n\n    def __init__(self, app_name=\"{product_name.lower()}\"):\n        self.app_name = app_name\n\n    def railway(self):\n        \"\"\"Railway.app - Easiest PaaS deployment.\"\"\"\n        print(\"=== Railway.app ===\")\n        print(\"Best for: MVPs, startups, small-to-medium scale\")\n        print(\"Cost: $5/mo hobby, ~$20-50/mo production\")\n        print()\n        print(\"Setup commands:\")\n        print(f\"  npm install -g @railway/cli\")\n        print(f\"  railway login\")\n        print(f\"  railway init\")\n        print(f\"  railway up  # deploys from Dockerfile\")\n        print()\n        print(\"railway.toml:\")\n        print(f'  [build]')\n        print(f'  builder = \"dockerfile\"')\n        print(f'  dockerfilePath = \"Dockerfile\"')\n        print()\n        print(f'  [deploy]')\n        print(f'  healthcheckPath = \"/health\"')\n        print(f'  healthcheckTimeout = 30')\n        print(f'  restartPolicyType = \"on_failure\"')\n        print()\n        print(\"Pros: Auto-scaling, free PostgreSQL, easy env vars\")\n        print(\"Cons: No GPU support, cold starts possible\")\n\n    def render(self):\n        \"\"\"Render.com - Good middle ground.\"\"\"\n        print(\"\\\n=== Render.com ===\")\n        print(\"Best for: Production apps, auto-scaling needed\")\n        print(\"Cost: Free tier available, $7/mo starter, ~$25/mo production\")\n        print()\n        print(\"render.yaml (Infrastructure as Code):\")\n        print(f\"  services:\")\n        print(f\"    - type: web\")\n        print(f\"      name: {self.app_name}\")\n        print(f\"      runtime: docker\")\n        print(f\"      plan: starter\")\n        print(f\"      healthCheckPath: /health\")\n        print(f\"      envVars:\")\n        print(f\"        - key: DATABASE_URL\")\n        print(f\"          fromDatabase:\")\n        print(f\"            name: {self.app_name}-db\")\n        print(f\"            property: connectionString\")\n        print(f\"    - type: worker\")\n        print(f\"      name: {self.app_name}-worker\")\n        print(f\"      runtime: docker\")\n        print(f\"      dockerfilePath: ./Dockerfile.worker\")\n        print()\n        print(\"  databases:\")\n        print(f\"    - name: {self.app_name}-db\")\n        print(f\"      plan: starter\")\n        print()\n        print(\"Pros: Free SSL, managed PostgreSQL, auto-deploy from Git\")\n        print(\"Cons: No GPU, slower builds than Railway\")\n\n    def aws(self):\n        \"\"\"AWS - Full enterprise deployment.\"\"\"\n        print(\"\\\n=== AWS (ECS + Fargate) ===\")\n        print(\"Best for: Enterprise, GPU workloads, full control\")\n        print(\"Cost: ~$50-500/mo depending on scale\")\n        print()\n        print(\"Architecture:\")\n        print(\"  ALB -> ECS Fargate -> RDS PostgreSQL\")\n        print(\"         |-> ElastiCache (Redis)\")\n        print(\"         |-> S3 (file storage)\")\n        print(\"         |-> SQS (job queue)\")\n        print()\n        print(\"Setup (using AWS CDK):\")\n        print(f\"  npm install -g aws-cdk\")\n        print(f\"  cdk init app --language python\")\n        print(f\"  # Define ECS service, RDS, Redis in stack\")\n        print(f\"  cdk deploy\")\n        print()\n        print(\"For GPU (e.g., self-hosted models):\")\n        print(f\"  # Use EC2 with g5.xlarge (A10G GPU, ~$1.00/hr)\")\n        print(f\"  # Or SageMaker endpoints for managed inference\")\n        print()\n        print(\"Pros: GPU support, infinite scale, enterprise features\")\n        print(\"Cons: Complex setup, higher cost, AWS expertise needed\")\n\n    def digitalocean(self):\n        \"\"\"DigitalOcean App Platform.\"\"\"\n        print(\"\\\n=== DigitalOcean App Platform ===\")\n        print(\"Best for: Simple deployments, predictable pricing\")\n        print(\"Cost: $5/mo basic, $12/mo pro, GPU droplets ~$2.50/hr\")\n        print()\n        print(\".do/app.yaml:\")\n        print(f\"  name: {self.app_name}\")\n        print(f\"  services:\")\n        print(f\"    - name: api\")\n        print(f\"      github:\")\n        print(f\"        repo: yourusername/{self.app_name}\")\n        print(f\"        branch: main\")\n        print(f\"        deploy_on_push: true\")\n        print(f\"      dockerfile_path: Dockerfile\")\n        print(f\"      instance_count: 2\")\n        print(f\"      instance_size_slug: professional-xs\")\n        print(f\"      health_check:\")\n        print(f\"        http_path: /health\")\n        print()\n        print(\"Pros: Simple pricing, good docs, GPU droplets available\")\n        print(\"Cons: Smaller ecosystem than AWS, limited auto-scaling\")\n\n    def self_hosted(self):\n        \"\"\"Self-hosted on bare metal or VPS.\"\"\"\n        print(\"\\\n=== Self-Hosted (VPS/Bare Metal) ===\")\n        print(\"Best for: Maximum control, GPU inference, cost optimization\")\n        print(\"Cost: $20-100/mo VPS, $100-300/mo GPU server\")\n        print()\n        print(\"Stack: Nginx + Gunicorn + FastAPI + PostgreSQL + Redis\")\n        print()\n        print(\"docker-compose.yml:\")\n        print(f\"  services:\")\n        print(f\"    app:\")\n        print(f\"      build: .\")\n        print(f\"      ports: [\\\\\"8000:8000\\\\\"]\")\n        print(f\"      environment:\")\n        print(f\"        - DATABASE_URL=postgresql://user:pass@db:5432/{self.app_name}\")\n        print(f\"        - REDIS_URL=redis://redis:6379\")\n        print(f\"      depends_on: [db, redis]\")\n        print(f\"      deploy:\")\n        print(f\"        replicas: 3\")\n        print(f\"    db:\")\n        print(f\"      image: postgres:16-alpine\")\n        print(f\"      volumes: [postgres_data:/var/lib/postgresql/data]\")\n        print(f\"    redis:\")\n        print(f\"      image: redis:7-alpine\")\n        print(f\"    nginx:\")\n        print(f\"      image: nginx:alpine\")\n        print(f\"      ports: [\\\\\"80:80\\\\\", \\\\\"443:443\\\\\"]\")\n        print(f\"      volumes: [./nginx.conf:/etc/nginx/nginx.conf]\")\n        print()\n        print(\"GPU server providers:\")\n        print(\"  - Lambda Labs: A100 ~$1.10/hr, H100 ~$2.49/hr\")\n        print(\"  - RunPod: A100 ~$1.64/hr, community GPUs from $0.20/hr\")\n        print(\"  - Vast.ai: Cheapest GPUs, ~$0.15-0.50/hr\")\n        print(\"  - Hetzner: Dedicated servers from EUR39/mo (no GPU)\")\n\n# --- Demo ---\nguide = DeploymentGuide()\nguide.railway()\nguide.render()\nguide.aws()\nguide.digitalocean()\nguide.self_hosted()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(\"RECOMMENDATION by scale:\")\nprint(\"  Hobby/MVP     -> Railway or Render (free tier)\")\nprint(\"  Startup       -> Railway or DigitalOcean ($20-50/mo)\")\nprint(\"  Growth        -> AWS ECS or DigitalOcean ($100-500/mo)\")\nprint(\"  Enterprise    -> AWS/GCP with GPU instances\")\nprint(\"  Self-hosted AI-> VPS + GPU from Lambda/RunPod/Vast.ai\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Partea 13: Limitarea Ratei &amp; Protecția API</h2>\n\n<p>Fiecare SaaS de producție necesită limitarea ratei pentru a preveni abuzul, controla costurile și asigura utilizarea echitabilă. Iată o implementare de nivel producție:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>rate_limiter.py &mdash; Production Rate Limiting for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nProduction rate limiting for ClipCraft.\nThree tiers: in-memory (development), Redis (production), token bucket (advanced).\n\"\"\"\nimport time\nfrom collections import defaultdict\n\n# === TIER 1: In-Memory Rate Limiter (Development) ===\nclass InMemoryRateLimiter:\n    \"\"\"Simple sliding window rate limiter. Works for single-server setup.\"\"\"\n\n    def __init__(self):\n        self.requests = defaultdict(list)\n\n    def is_allowed(self, key, max_requests, window_seconds):\n        \"\"\"Check if request is allowed under the rate limit.\"\"\"\n        now = time.time()\n        window_start = now - window_seconds\n\n        # Clean old entries\n        self.requests[key] = [t for t in self.requests[key] if t > window_start]\n\n        if len(self.requests[key]) >= max_requests:\n            retry_after = self.requests[key][0] - window_start\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"limit\": max_requests,\n                \"window\": window_seconds,\n                \"retry_after\": round(retry_after, 1),\n                \"current\": len(self.requests[key])\n            }}\n\n        self.requests[key].append(now)\n        return True, {{\n            \"remaining\": max_requests - len(self.requests[key]),\n            \"limit\": max_requests,\n            \"reset\": round(window_start + window_seconds - now, 1)\n        }}\n\n# === TIER 2: Redis Rate Limiter (Production) ===\nclass RedisRateLimiter:\n    \"\"\"Redis-based rate limiter for distributed deployments.\n\n    In production, install: pip install redis\n    And use a real Redis connection.\n    \"\"\"\n\n    def __init__(self):\n        # In production: self.redis = redis.Redis(host='redis', port=6379, db=0)\n        self.memory = defaultdict(list)  # Simulating Redis\n\n    def check_rate_limit(self, user_id, tier=\"free\"):\n        \"\"\"Check rate limit based on user tier.\"\"\"\n        limits = {{\n            \"free\":       {{\"requests_per_minute\": 5,  \"requests_per_day\": 50}},\n            \"starter\":    {{\"requests_per_minute\": 20, \"requests_per_day\": 500}},\n            \"pro\":        {{\"requests_per_minute\": 60, \"requests_per_day\": 5000}},\n            \"enterprise\": {{\"requests_per_minute\": 200, \"requests_per_day\": 50000}},\n        }}\n\n        tier_limits = limits.get(tier, limits[\"free\"])\n        now = time.time()\n\n        # Check per-minute limit\n        minute_key = f\"rate:{{user_id}}:minute\"\n        self.memory[minute_key] = [t for t in self.memory[minute_key] if t > now - 60]\n\n        if len(self.memory[minute_key]) >= tier_limits[\"requests_per_minute\"]:\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"type\": \"per_minute\",\n                \"limit\": tier_limits[\"requests_per_minute\"],\n                \"retry_after_seconds\": 60 - (now - self.memory[minute_key][0])\n            }}\n\n        # Check per-day limit\n        day_key = f\"rate:{{user_id}}:day\"\n        self.memory[day_key] = [t for t in self.memory[day_key] if t > now - 86400]\n\n        if len(self.memory[day_key]) >= tier_limits[\"requests_per_day\"]:\n            return False, {{\n                \"error\": \"daily_limit_exceeded\",\n                \"type\": \"per_day\",\n                \"limit\": tier_limits[\"requests_per_day\"],\n            }}\n\n        # Allow request\n        self.memory[minute_key].append(now)\n        self.memory[day_key].append(now)\n\n        return True, {{\n            \"remaining_minute\": tier_limits[\"requests_per_minute\"] - len(self.memory[minute_key]),\n            \"remaining_day\": tier_limits[\"requests_per_day\"] - len(self.memory[day_key]),\n        }}\n\n# === FastAPI Middleware ===\nclass RateLimitMiddleware:\n    \"\"\"\n    FastAPI middleware for rate limiting.\n\n    Usage in your app:\n        from rate_limiter import RateLimitMiddleware\n        app.add_middleware(RateLimitMiddleware)\n\n    Or as a dependency:\n        @app.get(\"/generate\")\n        async def generate(rate_limit: dict = Depends(check_rate_limit)):\n            ...\n    \"\"\"\n\n    def __init__(self):\n        self.limiter = RedisRateLimiter()\n\n    def get_middleware_code(self):\n        \"\"\"Print the actual FastAPI middleware code.\"\"\"\n        code = \"# Add to your FastAPI app:\\\n\\\n\"\n        code += \"from fastapi import Request, Response\\\n\"\n        code += \"from starlette.middleware.base import BaseHTTPMiddleware\\\n\\\n\"\n        code += \"class RateLimitMiddleware(BaseHTTPMiddleware):\\\n\"\n        code += \"    async def dispatch(self, request, call_next):\\\n\"\n        code += \"        api_key = request.headers.get('X-API-Key', 'anonymous')\\\n\"\n        code += \"        user_id = self.get_user_from_key(api_key)\\\n\"\n        code += \"        tier = self.get_user_tier(user_id)\\\n\"\n        code += \"        allowed, info = self.limiter.check_rate_limit(user_id, tier)\\\n\"\n        code += \"        if not allowed:\\\n\"\n        code += \"            return JSONResponse(status_code=429, content=info)\\\n\"\n        code += \"        response = await call_next(request)\\\n\"\n        code += \"        response.headers['X-RateLimit-Remaining'] = str(info.get('remaining_minute', 0))\\\n\"\n        code += \"        return response\\\n\"\n        print(code)\n\n\n# --- Demo ---\nprint(\"=== Rate Limiting Demo for ClipCraft ===\\\n\")\n\nlimiter = RedisRateLimiter()\n\n# Simulate requests from different users\nusers = [\n    (\"user_free_1\", \"free\"),\n    (\"user_pro_1\", \"pro\"),\n    (\"user_enterprise_1\", \"enterprise\"),\n]\n\nfor user_id, tier in users:\n    print(f\"User: {{user_id}} ({{tier}} tier)\")\n    for i in range(8):\n        allowed, info = limiter.check_rate_limit(user_id, tier)\n        status = \"ALLOWED\" if allowed else \"BLOCKED\"\n        print(f\"  Request {{i+1}}: {{status}} | {{info}}\")\n        if not allowed:\n            break\n    print()\n\n# Show middleware code\nmw = RateLimitMiddleware()\nmw.get_middleware_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Partea 14: Integrare Facturare Stripe</h2>\n\n<p>Un SaaS real necesită facturare reală. Iată o integrare completă Stripe cu niveluri de abonament, facturare bazată pe utilizare și gestionarea webhook-urilor:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>billing.py &mdash; Stripe Integration for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nComplete Stripe billing for ClipCraft.\nSupports: subscription tiers, usage-based billing, webhooks.\n\nSetup:\n  pip install stripe\n  # Get keys from https://dashboard.stripe.com/apikeys\n  # Set STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET in .env\n\"\"\"\n\nclass StripeBilling:\n    \"\"\"Full Stripe integration for SaaS billing.\"\"\"\n\n    def __init__(self, secret_key=\"sk_test_...\"):\n        # In production: import stripe; stripe.api_key = secret_key\n        self.plans = {{\n            \"free\":       {{\"price\": 0,    \"name\": \"Free\",       \"stripe_price_id\": None}},\n            \"starter\":    {{\"price\": 19,   \"name\": \"Starter\",    \"stripe_price_id\": \"price_starter_monthly\"}},\n            \"pro\":        {{\"price\": 49,   \"name\": \"Professional\",\"stripe_price_id\": \"price_pro_monthly\"}},\n            \"enterprise\": {{\"price\": 199,  \"name\": \"Enterprise\", \"stripe_price_id\": \"price_enterprise_monthly\"}},\n        }}\n\n    def create_checkout_session(self, user_email, plan_key):\n        \"\"\"Create a Stripe Checkout session for subscription.\"\"\"\n        plan = self.plans[plan_key]\n        if plan[\"price\"] == 0:\n            return {{\"status\": \"free_tier\", \"message\": \"No payment needed\"}}\n\n        # In production:\n        # session = stripe.checkout.Session.create(\n        #     customer_email=user_email,\n        #     payment_method_types=[\"card\"],\n        #     line_items=[{{\n        #         \"price\": plan[\"stripe_price_id\"],\n        #         \"quantity\": 1,\n        #     }}],\n        #     mode=\"subscription\",\n        #     success_url=\"https://{product_name.lower()}.com/billing/success?session_id={{CHECKOUT_SESSION_ID}}\",\n        #     cancel_url=\"https://{product_name.lower()}.com/billing/cancel\",\n        #     metadata={{\"user_email\": user_email, \"plan\": plan_key}},\n        # )\n        # return {{\"checkout_url\": session.url, \"session_id\": session.id}}\n\n        print(f\"Creating checkout for {{user_email}} -> {{plan['name']}} (\\${{plan['price']}}/mo)\")\n        return {{\n            \"checkout_url\": f\"https://checkout.stripe.com/pay/cs_test_...\",\n            \"plan\": plan[\"name\"],\n            \"price\": f\"\\${{plan['price']}}/month\"\n        }}\n\n    def create_usage_record(self, subscription_item_id, quantity, action=\"increment\"):\n        \"\"\"Record usage for metered billing (pay-per-use on top of subscription).\"\"\"\n        # In production:\n        # stripe.SubscriptionItem.create_usage_record(\n        #     subscription_item_id,\n        #     quantity=quantity,\n        #     action=action,\n        #     timestamp=int(time.time())\n        # )\n        print(f\"Usage recorded: {{quantity}} units on {{subscription_item_id}}\")\n        return {{\"status\": \"recorded\", \"quantity\": quantity}}\n\n    def handle_webhook(self, event_type, data):\n        \"\"\"Handle Stripe webhook events.\"\"\"\n        handlers = {{\n            \"checkout.session.completed\": self._on_checkout_complete,\n            \"customer.subscription.updated\": self._on_subscription_update,\n            \"customer.subscription.deleted\": self._on_subscription_cancel,\n            \"invoice.payment_failed\": self._on_payment_failed,\n            \"invoice.paid\": self._on_invoice_paid,\n        }}\n\n        handler = handlers.get(event_type)\n        if handler:\n            return handler(data)\n        return {{\"status\": \"unhandled\", \"event\": event_type}}\n\n    def _on_checkout_complete(self, data):\n        print(f\"  Checkout completed! Activate subscription for user.\")\n        return {{\"action\": \"activate_subscription\"}}\n\n    def _on_subscription_update(self, data):\n        print(f\"  Subscription updated. Sync plan tier in database.\")\n        return {{\"action\": \"sync_tier\"}}\n\n    def _on_subscription_cancel(self, data):\n        print(f\"  Subscription canceled. Downgrade to free tier.\")\n        return {{\"action\": \"downgrade_to_free\"}}\n\n    def _on_payment_failed(self, data):\n        print(f\"  Payment failed! Send dunning email, grace period 7 days.\")\n        return {{\"action\": \"send_dunning_email\", \"grace_days\": 7}}\n\n    def _on_invoice_paid(self, data):\n        print(f\"  Invoice paid. Reset monthly usage counters.\")\n        return {{\"action\": \"reset_usage\"}}\n\n    def get_fastapi_webhook_code(self):\n        \"\"\"Print the FastAPI webhook endpoint code.\"\"\"\n        code = \"# FastAPI webhook endpoint:\\\n\\\n\"\n        code += \"@app.post('/webhooks/stripe')\\\n\"\n        code += \"async def stripe_webhook(request: Request):\\\n\"\n        code += \"    payload = await request.body()\\\n\"\n        code += \"    sig_header = request.headers.get('stripe-signature')\\\n\"\n        code += \"    try:\\\n\"\n        code += \"        event = stripe.Webhook.construct_event(\\\n\"\n        code += \"            payload, sig_header, STRIPE_WEBHOOK_SECRET)\\\n\"\n        code += \"    except stripe.error.SignatureVerificationError:\\\n\"\n        code += \"        raise HTTPException(400, 'Invalid signature')\\\n\"\n        code += \"    billing = StripeBilling()\\\n\"\n        code += \"    result = billing.handle_webhook(event['type'], event['data']['object'])\\\n\"\n        code += \"    return {'status': 'ok', 'result': result}\\\n\"\n        print(code)\n\n    def get_pricing_page_data(self):\n        \"\"\"Generate pricing page data.\"\"\"\n        features = {{\n            \"free\": [\n                \"50 requests/day\",\n                \"Basic quality\",\n                \"Community support\",\n                \"Watermarked output\",\n            ],\n            \"starter\": [\n                \"500 requests/day\",\n                \"Standard quality\",\n                \"Email support\",\n                \"No watermark\",\n                \"API access\",\n            ],\n            \"pro\": [\n                \"5,000 requests/day\",\n                \"Highest quality\",\n                \"Priority support\",\n                \"4K resolution, custom transitions\",\n                \"Batch processing\",\n                \"Webhooks\",\n            ],\n            \"enterprise\": [\n                \"50,000 requests/day\",\n                \"Custom model fine-tuning\",\n                \"Dedicated support\",\n                \"SLA guarantee\",\n                \"SSO/SAML\",\n                \"On-premise option\",\n            ],\n        }}\n        return features\n\n\n# --- Demo ---\nprint(\"=== Stripe Billing for ClipCraft ===\\\n\")\n\nbilling = StripeBilling()\n\n# 1. Show pricing tiers\nprint(\"Pricing Tiers:\")\nfeatures = billing.get_pricing_page_data()\nfor tier, plan in billing.plans.items():\n    print(f\"\\\n  {{plan['name']}} - \\${{plan['price']}}/month\")\n    for f in features[tier]:\n        print(f\"    - {{f}}\")\n\n# 2. Create checkout session\nprint(\"\\\n\\\n--- Creating Checkout ---\")\nresult = billing.create_checkout_session(\"user@example.com\", \"pro\")\nprint(f\"Result: {{result}}\")\n\n# 3. Simulate webhook events\nprint(\"\\\n--- Webhook Events ---\")\nevents = [\n    (\"checkout.session.completed\", {{}}),\n    (\"invoice.paid\", {{}}),\n    (\"customer.subscription.updated\", {{}}),\n    (\"invoice.payment_failed\", {{}}),\n    (\"customer.subscription.deleted\", {{}}),\n]\nfor event_type, data in events:\n    print(f\"\\\nEvent: {{event_type}}\")\n    billing.handle_webhook(event_type, data)\n\n# 4. Usage-based billing\nprint(\"\\\n\\\n--- Usage-Based Billing ---\")\nbilling.create_usage_record(\"si_test_123\", 25, \"increment\")\n\n# 5. Show webhook code\nprint(\"\\\n--- Webhook Endpoint Code ---\")\nbilling.get_fastapi_webhook_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Partea 15: Scalarea la un Produs de Masă</h2>\n\n<p>Scalarea de la MVP la produs de masă necesită modificări arhitecturale la fiecare nivel. Iată ghidul complet de scalare:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>scaling.py &mdash; ClipCraft Scaling Architecture</span></div>\n<pre><code class=\"language-python\">\"\"\"\nScaling ClipCraft from 100 to 1,000,000 users.\nCovers: horizontal scaling, queue-based processing, caching, monitoring.\n\"\"\"\n\nclass ScalingPlaybook:\n    \"\"\"Complete scaling guide from MVP to enterprise.\"\"\"\n\n    def __init__(self, product=\"ClipCraft\"):\n        self.product = product\n\n    def stage_1_mvp(self):\n        \"\"\"Stage 1: 0-100 users, single server.\"\"\"\n        print(\"=== Stage 1: MVP (0-100 users) ===\")\n        print(\"Architecture: Single server, SQLite/PostgreSQL\")\n        print(\"Cost: $5-20/month\")\n        print()\n        print(\"Stack:\")\n        print(\"  - 1x server (Railway/Render free tier)\")\n        print(\"  - SQLite or managed PostgreSQL\")\n        print(\"  - In-memory caching (Python dict)\")\n        print(\"  - Direct API calls (no queue)\")\n        print()\n        print(\"Focus on:\")\n        print(\"  1. Validate product-market fit\")\n        print(\"  2. Get 10 paying customers\")\n        print(\"  3. Measure: response time, error rate, user retention\")\n\n    def stage_2_growth(self):\n        \"\"\"Stage 2: 100-1K users, add resilience.\"\"\"\n        print(\"\\\n=== Stage 2: Growth (100-1,000 users) ===\")\n        print(\"Architecture: App server + Background workers + Redis\")\n        print(\"Cost: $50-200/month\")\n        print()\n        print(\"Changes from Stage 1:\")\n        print(\"  1. Add Redis for caching and rate limiting\")\n        print(\"  2. Move heavy work to background queues (Celery/ARQ)\")\n        print(\"  3. Add health checks and basic monitoring\")\n        print(\"  4. Set up CI/CD pipeline\")\n        print()\n        print(\"docker-compose additions:\")\n        print(\"  redis:\")\n        print(\"    image: redis:7-alpine\")\n        print(\"  worker:\")\n        print(\"    build: .\")\n        print(\"    command: arq app.worker.WorkerSettings\")\n        print(\"    depends_on: [redis]\")\n        print()\n        print(\"Queue pattern for heavy tasks:\")\n        print(\"  @app.post('/generate')\")\n        print(\"  async def generate(request: GenerateRequest):\")\n        print(\"      job_id = str(uuid.uuid4())\")\n        print(\"      await redis.enqueue_job('process_generation',\")\n        print(\"          job_id=job_id, prompt=request.prompt)\")\n        print(\"      return {{'job_id': job_id, 'status': 'processing'}}\")\n        print()\n        print(\"  @app.get('/jobs/{{job_id}}')\")\n        print(\"  async def get_job(job_id: str):\")\n        print(\"      result = await redis.get(f'job:{{job_id}}')\")\n        print(\"      return result or {{'status': 'processing'}}\")\n\n    def stage_3_scale(self):\n        \"\"\"Stage 3: 1K-10K users, horizontal scaling.\"\"\"\n        print(\"\\\n=== Stage 3: Scale (1,000-10,000 users) ===\")\n        print(\"Architecture: Load balancer + Multiple app servers + Queue\")\n        print(\"Cost: $200-1,000/month\")\n        print()\n        print(\"Architecture diagram:\")\n        print(\"  Users -> CDN (Cloudflare)\")\n        print(\"       -> Load Balancer (nginx/ALB)\")\n        print(\"       -> App Server 1 \\\\ \")\n        print(\"       -> App Server 2  |-> PostgreSQL (managed)\")\n        print(\"       -> App Server 3 /    |-> Redis Cluster\")\n        print(\"       -> Worker 1 \\\\         |-> S3 (file storage)\")\n        print(\"       -> Worker 2  |-> Queue (Redis/SQS)\")\n        print(\"       -> Worker 3 /\")\n        print()\n        print(\"Key changes:\")\n        print(\"  1. Multiple app servers behind load balancer\")\n        print(\"  2. Managed database (RDS/Cloud SQL)\")\n        print(\"  3. Separate worker pool for heavy processing\")\n        print(\"  4. CDN for static assets and cached responses\")\n        print(\"  5. Structured logging + APM (Datadog/Grafana)\")\n        print()\n        print(\"nginx load balancer config:\")\n        print(\"  upstream api {{\")\n        print(\"      least_conn;\")\n        print(\"      server app1:8000;\")\n        print(\"      server app2:8000;\")\n        print(\"      server app3:8000;\")\n        print(\"  }}\")\n        print(\"  server {{\")\n        print(\"      listen 80;\")\n        print(\"      location / {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          proxy_set_header X-Real-IP $remote_addr;\")\n        print(\"      }}\")\n        print(\"      location /health {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          access_log off;\")\n        print(\"      }}\")\n        print(\"  }}\")\n\n    def stage_4_mass(self):\n        \"\"\"Stage 4: 10K+ users, enterprise features.\"\"\"\n        print(\"\\\n=== Stage 4: Mass Product (10,000+ users) ===\")\n        print(\"Architecture: Microservices or modular monolith\")\n        print(\"Cost: $1,000-10,000/month\")\n        print()\n        print(\"Enterprise features to add:\")\n        print(\"  1. Multi-tenant architecture (org-level isolation)\")\n        print(\"  2. SSO/SAML authentication\")\n        print(\"  3. Audit logging\")\n        print(\"  4. Data residency (EU/US regions)\")\n        print(\"  5. Custom SLAs and dedicated support\")\n        print(\"  6. API versioning (v1, v2)\")\n        print(\"  7. Webhook system for integrations\")\n        print(\"  8. Admin dashboard with analytics\")\n        print()\n        print(\"Monitoring stack:\")\n        print(\"  - Prometheus + Grafana (metrics)\")\n        print(\"  - Sentry (error tracking)\")\n        print(\"  - PostHog or Mixpanel (product analytics)\")\n        print(\"  - PagerDuty (alerting)\")\n        print()\n        print(\"SLA targets:\")\n        print(\"  - Uptime: 99.9% (8.7 hours downtime/year)\")\n        print(\"  - API latency p99: < 2 seconds\")\n        print(\"  - Error rate: < 0.1%\")\n\n    def growth_metrics(self):\n        \"\"\"Key metrics to track at each stage.\"\"\"\n        print(\"\\\n=== Key Metrics Dashboard ===\")\n        print()\n        metrics = [\n            (\"MRR (Monthly Recurring Revenue)\", \"$0\", \"$500\", \"$5K\", \"$50K+\"),\n            (\"Active Users (DAU)\", \"10\", \"100\", \"1,000\", \"10,000+\"),\n            (\"API Requests/day\", \"100\", \"5K\", \"100K\", \"1M+\"),\n            (\"Error Rate\", \"<5%\", \"<1%\", \"<0.1%\", \"<0.01%\"),\n            (\"P99 Latency\", \"<5s\", \"<3s\", \"<2s\", \"<1s\"),\n            (\"Churn Rate (monthly)\", \"N/A\", \"<10%\", \"<5%\", \"<3%\"),\n        ]\n        print(f\"| {{'Metric':<30}} | {{'Stage 1':<8}} | {{'Stage 2':<8}} | {{'Stage 3':<8}} | {{'Stage 4':<10}} |\")\n        print(f\"|{{'-'*30}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*10}}-|\")\n        for name, *stages in metrics:\n            print(f\"| {{name:<30}} | {{stages[0]:<8}} | {{stages[1]:<8}} | {{stages[2]:<8}} | {{stages[3]:<10}} |\")\n\n# --- Demo ---\nplaybook = ScalingPlaybook()\nplaybook.stage_1_mvp()\nplaybook.stage_2_growth()\nplaybook.stage_3_scale()\nplaybook.stage_4_mass()\nplaybook.growth_metrics()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(f\"Summary: Build ClipCraft as MVP first.\")\nprint(\"Get 10 paying customers before optimizing.\")\nprint(\"Scale horizontally only when metrics demand it.\")\nprint(\"Monitor everything from day one.\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Concluzii Cheie</h2>\n\n<ul>\n<li><strong>Asincronul este obligatoriu pentru video:</strong> Generarea video durează între 30 de secunde și 5 minute. Trebuie să folosești o coadă de job-uri cu procesare în fundal &mdash; HTTP-ul sincron va expira.</li>\n<li><strong>Urmărirea progresului construiește încredere:</strong> Utilizatorii trebuie să vadă că ceva se întâmplă. Oferă atât endpoint-uri de interogare, cât și callback-uri webhook pentru flexibilitate.</li>\n<li><strong>Logica de reîncercare este critică:</strong> Sarcinile GPU de lungă durată eșuează mai des decât apelurile API rapide. Reîncercările automate cu backoff exponențial mențin serviciul tău fiabil.</li>\n<li><strong>Controlează concurența pentru a controla costurile:</strong> Limitează numărul de job-uri simultane. Fiecare videoclip costă $0.10&ndash;$1.00 în taxe API &mdash; concurența necontrolată îți poate goli bugetul în câteva minute.</li>\n<li><strong>Videoclipul este de 10&ndash;50x mai scump decât imaginile:</strong> Stabilește prețurile nivelurilor în consecință. Un plan de $10/lună care permite 100 de imagini ar putea permite doar 10&ndash;20 de videoclipuri.</li>\n<li><strong>FFmpeg este cel mai bun prieten al tău:</strong> Folosește-l pentru transcodare, schimbări de rezoluție, filigranare și conversie de format. Rulează peste tot și suportă orice codec.</li>\n</ul>\n\n<p><strong>Următorul:</strong> În GenAI-3, construiești un <strong>SaaS de Generare de Voce și Audio</strong> &mdash; gestionând text-to-speech, clonare de voce și pipeline-uri de procesare audio.</p>\n\n${navButtons('genai-1','genai-3')}",
    "el": "<div class=\"lecture-container\">\n\n<div class=\"lecture-header\">\n<div class=\"stage-badge\" style=\"background: linear-gradient(135deg, #7c3aed, #a855f7);\">BUILD: Προϊόν AI SaaS</div>\n<h1>Διάλεξη GenAI 2: Κατασκευή ενός SaaS Δημιουργίας Βίντεο</h1>\n<p class=\"lecture-subtitle\">Κατασκεύασε το ClipCraft &mdash; μια υπηρεσία δημιουργίας βίντεο έτοιμη για παραγωγή, με ασύγχρονες ουρές εργασιών, παρακολούθηση προόδου και backend FastAPI που ενορχηστρώνει μοντέλα text-to-video.</p>\n<div class=\"lecture-meta\">\n<span>~75 λεπτά</span> | <span>Προχωρημένο / Πρακτικό</span> | <span>Θα κατασκευάσεις και θα αναπτύξεις ένα πραγματικό προϊόν</span>\n</div>\n</div>\n\n<h2>Τι θα κατασκευάσεις</h2>\n\n<p>Σε αυτό το μάθημα κατασκευάζεις το <strong>ClipCraft</strong> &mdash; ένα SaaS δημιουργίας βίντεο. Οι χρήστες υποβάλλουν κειμενικά prompts, ο server σου δρομολογεί το αίτημα σε ένα μοντέλο text-to-video (Runway, Pika ή Stability AI Video), διαχειρίζεται τη μακρόχρονη διαδικασία δημιουργίας μέσω ασύγχρονης ουράς εργασιών, στέλνει ενημερώσεις προόδου σε πραγματικό χρόνο και παραδίδει το τελικό βίντεο. Θα υλοποιήσεις ολόκληρο το stack: server API, pipeline επεξεργασίας βίντεο, διαχείριση εργασιών, παρακολούθηση προόδου και ανάπτυξη.</p>\n\n<p>Η δημιουργία βίντεο είναι θεμελιωδώς διαφορετική από τη δημιουργία εικόνων. Ένα μεμονωμένο βίντεο μπορεί να χρειαστεί <strong>30 δευτερόλεπτα έως 5 λεπτά</strong> για να παραχθεί, που σημαίνει ότι δεν μπορείς να χρησιμοποιήσεις ένα απλό μοτίβο αίτησης-απάντησης. Αντ' αυτού, χρειάζεσαι μια <strong>ασύγχρονη αρχιτεκτονική</strong> με ουρές εργασιών, polling κατάστασης και ειδοποιήσεις webhook.</p>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #7c3aed;\">\n<h3>ClipCraft &mdash; Πλήρης επισκόπηση του έργου</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Στοιχείο</th>\n<th style=\"padding: 10px; text-align: left; border-bottom: 2px solid #d1d5db;\">Τεχνολογία</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Server API</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Python + FastAPI</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Δημιουργία βίντεο</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway Gen-3, Pika Labs ή Stability AI Video (μέσω API)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Ουρά εργασιών</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">In-process async queue (dev) / Redis + Celery (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Παρακολούθηση προόδου</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Polling endpoint + webhook callbacks</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Επεξεργασία βίντεο</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">NumPy (υπολογισμοί καρέ) + FFmpeg (κωδικοποίηση)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Βάση δεδομένων</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">SQLite (dev) / PostgreSQL (prod)</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Frontend</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">HTML + vanilla JS</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Ανάπτυξη</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Docker + οποιοσδήποτε πάροχος cloud</td></tr>\n</table>\n</div>\n\n<h2>Δομή του έργου</h2>\n\n<p>Δημιούργησε αυτή τη δομή φακέλων στο μηχάνημά σου. Κάθε αρχείο εξηγείται σε αυτό το μάθημα.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Project Layout</span></div>\n<pre><code>clipcraft/\n&boxvr;&boxh;&boxh; app/\n&boxv;   &boxvr;&boxh;&boxh; __init__.py\n&boxv;   &boxvr;&boxh;&boxh; main.py            # FastAPI application\n&boxv;   &boxvr;&boxh;&boxh; config.py          # Settings and API keys\n&boxv;   &boxvr;&boxh;&boxh; models.py          # Pydantic request/response models\n&boxv;   &boxvr;&boxh;&boxh; video_service.py   # Video generation API wrapper\n&boxv;   &boxvr;&boxh;&boxh; job_queue.py       # Async job queue system\n&boxv;   &boxvr;&boxh;&boxh; progress.py        # Progress tracking and webhooks\n&boxv;   &boxvr;&boxh;&boxh; processing.py      # Video frame processing\n&boxv;   &boxvr;&boxh;&boxh; billing.py         # Usage tracking and billing\n&boxv;   &boxur;&boxh;&boxh; database.py        # Database connection\n&boxvr;&boxh;&boxh; static/\n&boxv;   &boxvr;&boxh;&boxh; index.html         # Frontend UI\n&boxv;   &boxur;&boxh;&boxh; style.css\n&boxvr;&boxh;&boxh; tests/\n&boxv;   &boxur;&boxh;&boxh; test_api.py\n&boxvr;&boxh;&boxh; requirements.txt\n&boxvr;&boxh;&boxh; Dockerfile\n&boxvr;&boxh;&boxh; docker-compose.yml\n&boxur;&boxh;&boxh; .env.example</code></pre>\n</div>\n\n<h2>Μέρος 1: Πώς λειτουργούν τα API δημιουργίας βίντεο</h2>\n\n<p>Τα μοντέλα text-to-video επεκτείνουν τις τεχνικές diffusion από εικόνες σε χρονικές ακολουθίες. Αντί να αποθορυβοποιούν ένα μεμονωμένο καρέ, αποθορυβοποιούν έναν <strong>χωροχρονικό όγκο</strong> &mdash; παράγοντας πολλαπλά συνεκτικά καρέ που ρέουν φυσικά. Αυτό είναι τάξεις μεγέθους πιο ακριβό από τη δημιουργία εικόνων και διαρκεί σημαντικά περισσότερο.</p>\n\n<div class=\"concept-card\">\n<h3>Σύγκριση API δημιουργίας βίντεο (2024&ndash;2025)</h3>\n<table style=\"width:100%; border-collapse: collapse; margin: 1em 0;\">\n<tr style=\"background: #f3f4f6;\">\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Πάροχος</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Μοντέλο</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Κόστος ανά βίντεο</th>\n<th style=\"padding: 8px; text-align: left; border-bottom: 2px solid #d1d5db;\">Διάρκεια / Ανάλυση</th>\n</tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Runway</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Gen-3 Alpha</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.25&ndash;$1.00</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5&ndash;10s / έως 1280&times;768</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika Labs</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Pika 1.0</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.20&ndash;$0.80</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">3&ndash;4s / έως 1024&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stability AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Stable Video Diffusion</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.10&ndash;$0.50</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">4s / 576&times;1024</td></tr>\n<tr><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Luma AI</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">Dream Machine</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">$0.30&ndash;$0.90</td><td style=\"padding: 8px; border-bottom: 1px solid #e5e7eb;\">5s / έως 1360&times;752</td></tr>\n</table>\n</div>\n\n<div class=\"concept-card\" style=\"border-left: 4px solid #f59e0b;\">\n<h3>Βασική διαφορά από τη δημιουργία εικόνων</h3>\n<p>Η δημιουργία εικόνων επιστρέφει αποτέλεσμα σε 2&ndash;10 δευτερόλεπτα. Η δημιουργία βίντεο χρειάζεται <strong>30 δευτερόλεπτα έως 5 λεπτά</strong>. Αυτό σημαίνει ότι δεν μπορείς να κρατήσεις μια σύνδεση HTTP ανοιχτή και να περιμένεις. Χρειάζεσαι:</p>\n<ul>\n<li><strong>Ουρά εργασιών:</strong> Αποδοχή του αιτήματος, άμεση επιστροφή ενός ID εργασίας, επεξεργασία στο παρασκήνιο</li>\n<li><strong>Παρακολούθηση προόδου:</strong> Να επιτρέπεις στους πελάτες να κάνουν polling για την κατάσταση ή να λαμβάνουν webhook callbacks</li>\n<li><strong>Λογική επαναπροσπαθειών:</strong> Οι μακρόχρονες εργασίες αποτυγχάνουν πιο συχνά &mdash; πρέπει να χειρίζεσαι τις επαναπροσπάθειες χαριτωμένα</li>\n<li><strong>Διαχείριση πόρων:</strong> Περιορισμός ταυτόχρονων εργασιών για έλεγχο κόστους και αποτροπή υπερφόρτωσης</li>\n</ul>\n</div>\n\n<p>Το αρχιτεκτονικό μοτίβο είναι: <strong>αποδοχή &rarr; ένταξη σε ουρά &rarr; επεξεργασία &rarr; ειδοποίηση</strong>. Ας χτίσουμε κάθε κομμάτι.</p>\n\n<h2>Μέρος 2: Pipeline επεξεργασίας βίντεο</h2>\n\n<p>Πριν ενσωματώσουμε εξωτερικά API, ας κατανοήσουμε πώς λειτουργούν τα καρέ βίντεο. Ένα βίντεο είναι μια ακολουθία καρέ εικόνας που αναπαράγονται σε γρήγορη διαδοχή. Κάθε καρέ είναι ένας πίνακας NumPy δεδομένων pixel. Ο παρακάτω κώδικας δείχνει πώς να δημιουργείς και να επεξεργάζεσαι καρέ βίντεο μέσω προγραμματισμού.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# processing.py - Video frame generation and processing\n\nimport numpy as np\nimport time\n\nclass VideoFrameProcessor:\n    \"\"\"Generate and process video frames using NumPy.\"\"\"\n\n    @staticmethod\n    def generate_gradient_frames(width, height, num_frames, fps=24):\n        \"\"\"Generate a sequence of gradient frames that animate over time.\"\"\"\n        frames = []\n        for i in range(num_frames):\n            frame = np.zeros((height, width, 3), dtype=np.uint8)\n            t = i / num_frames  # normalized time 0..1\n\n            for y in range(height):\n                for x in range(width):\n                    # Red channel: horizontal sweep that shifts over time\n                    r = int(255 * ((x / width + t) % 1.0))\n                    # Green channel: vertical pulse\n                    g = int(255 * ((y / height + t * 0.5) % 1.0))\n                    # Blue channel: radial wave from center\n                    cx, cy = width / 2, height / 2\n                    dist = ((x - cx)**2 + (y - cy)**2) ** 0.5\n                    max_dist = (cx**2 + cy**2) ** 0.5\n                    b = int(127 + 128 * np.sin(dist / max_dist * 6.28 - t * 6.28))\n                    frame[y, x] = [r, g, b]\n\n            frames.append(frame)\n        return frames\n\n    @staticmethod\n    def apply_fade_transition(frames, fade_length=5):\n        \"\"\"Apply a fade-in at the start and fade-out at the end.\"\"\"\n        result = []\n        total = len(frames)\n        for i, frame in enumerate(frames):\n            factor = 1.0\n            if i < fade_length:\n                factor = i / fade_length\n            elif i >= total - fade_length:\n                factor = (total - 1 - i) / fade_length\n            faded = (frame.astype(np.float64) * factor).astype(np.uint8)\n            result.append(faded)\n        return result\n\n    @staticmethod\n    def compute_frame_stats(frames):\n        \"\"\"Compute statistics across all frames.\"\"\"\n        brightnesses = []\n        for frame in frames:\n            avg = frame.mean()\n            brightnesses.append(round(float(avg), 2))\n\n        return {\n            \"num_frames\": len(frames),\n            \"resolution\": f\"{frames[0].shape[1]}x{frames[0].shape[0]}\",\n            \"avg_brightness\": round(np.mean(brightnesses), 2),\n            \"min_brightness\": min(brightnesses),\n            \"max_brightness\": max(brightnesses),\n            \"total_pixels\": frames[0].shape[0] * frames[0].shape[1] * len(frames),\n            \"memory_mb\": round(sum(f.nbytes for f in frames) / (1024*1024), 2)\n        }\n\n    @staticmethod\n    def add_text_overlay_region(frame, x, y, box_w, box_h, color=(255, 255, 255)):\n        \"\"\"Add a colored rectangle region (simulating text overlay area).\"\"\"\n        result = frame.copy()\n        y_end = min(y + box_h, frame.shape[0])\n        x_end = min(x + box_w, frame.shape[1])\n        # Semi-transparent overlay\n        overlay = result[y:y_end, x:x_end].astype(np.float64)\n        overlay = overlay * 0.5 + np.array(color, dtype=np.float64) * 0.5\n        result[y:y_end, x:x_end] = overlay.astype(np.uint8)\n        return result\n\n\n# --- Demo: Generate and process video frames ---\nproc = VideoFrameProcessor()\n\nprint(\"=== ClipCraft Video Processing Demo ===\\\n\")\n\n# 1. Generate frames (small for demo)\nprint(\"1. Generating 12 frames at 32x24 resolution...\")\nstart = time.time()\nframes = proc.generate_gradient_frames(32, 24, 12, fps=24)\nelapsed = round(time.time() - start, 3)\nprint(f\"   Generated in {elapsed}s\")\n\nstats = proc.compute_frame_stats(frames)\nfor key, val in stats.items():\n    print(f\"   {key}: {val}\")\n\n# 2. Apply fade transition\nprint(\"\\\n2. Applying fade-in/fade-out transition...\")\nfaded = proc.apply_fade_transition(frames, fade_length=3)\nfaded_stats = proc.compute_frame_stats(faded)\nprint(f\"   Avg brightness before fade: {stats['avg_brightness']}\")\nprint(f\"   Avg brightness after fade:  {faded_stats['avg_brightness']}\")\nprint(f\"   (Lower because start/end frames are darkened)\")\n\n# 3. Add overlay region to a frame\nprint(\"\\\n3. Adding text overlay region to frame 6...\")\noverlay_frame = proc.add_text_overlay_region(frames[5], 2, 2, 20, 8)\nbefore_mean = round(float(frames[5][2:10, 2:22].mean()), 1)\nafter_mean = round(float(overlay_frame[2:10, 2:22].mean()), 1)\nprint(f\"   Region avg before: {before_mean}\")\nprint(f\"   Region avg after:  {after_mean} (blended with white)\")\n\n# 4. Estimate production video sizes\nprint(\"\\\n4. Production video size estimates:\")\nresolutions = [\n    (\"720p\", 1280, 720, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 5),\n    (\"1080p\", 1920, 1080, 24, 10),\n]\nfor label, w, h, fps, duration in resolutions:\n    raw_mb = w * h * 3 * fps * duration / (1024**2)\n    compressed_mb = raw_mb * 0.02  # typical H.264 ratio\n    print(f\"   {label} {duration}s @ {fps}fps: \"\n          f\"{raw_mb:.0f} MB raw, ~{compressed_mb:.1f} MB compressed (H.264)\")\n\nprint(\"\\\nDone! Frame processing pipeline is ready.\")\n</code></pre>\n</div>\n\n<h2>Μέρος 3: Ασύγχρονη ουρά εργασιών</h2>\n\n<p>Η δημιουργία βίντεο διαρκεί πολύ για σύγχρονο HTTP. Χρειαζόμαστε μια ουρά εργασιών που αποδέχεται αιτήματα, τα επεξεργάζεται στο παρασκήνιο και επιτρέπει στους πελάτες να ελέγχουν την κατάσταση. Αυτό είναι το βασικό αρχιτεκτονικό μοτίβο για οποιαδήποτε μακρόχρονη εργασία σε ένα προϊόν SaaS.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# job_queue.py - Async job queue for video generation\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass JobStatus:\n    PENDING = \"pending\"\n    PROCESSING = \"processing\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    CANCELLED = \"cancelled\"\n\nclass VideoJob:\n    \"\"\"Represents a single video generation job.\"\"\"\n\n    def __init__(self, prompt, style, duration_sec, resolution, user_id):\n        self.id = str(uuid.uuid4())[:8]\n        self.prompt = prompt\n        self.style = style\n        self.duration_sec = duration_sec\n        self.resolution = resolution\n        self.user_id = user_id\n        self.status = JobStatus.PENDING\n        self.progress = 0\n        self.created_at = datetime.now().isoformat()\n        self.started_at = None\n        self.completed_at = None\n        self.result_url = None\n        self.error = None\n        self.retry_count = 0\n        self.max_retries = 3\n\n    def to_dict(self):\n        return {\n            \"id\": self.id,\n            \"prompt\": self.prompt[:40],\n            \"status\": self.status,\n            \"progress\": self.progress,\n            \"created_at\": self.created_at,\n            \"started_at\": self.started_at,\n            \"completed_at\": self.completed_at,\n            \"result_url\": self.result_url,\n            \"error\": self.error\n        }\n\n\nclass JobQueue:\n    \"\"\"In-process job queue for video generation tasks.\"\"\"\n\n    def __init__(self, max_concurrent=3):\n        self.jobs = {}\n        self.queue = []        # pending job IDs\n        self.active = []       # currently processing job IDs\n        self.max_concurrent = max_concurrent\n\n    def submit(self, prompt, style, duration_sec, resolution, user_id):\n        \"\"\"Submit a new video generation job. Returns job ID.\"\"\"\n        job = VideoJob(prompt, style, duration_sec, resolution, user_id)\n        self.jobs[job.id] = job\n        self.queue.append(job.id)\n        return job.id\n\n    def get_status(self, job_id):\n        \"\"\"Get current status of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job.to_dict()\n\n    def process_next(self):\n        \"\"\"Try to start the next pending job. Returns job or None.\"\"\"\n        if len(self.active) >= self.max_concurrent:\n            return None\n        if not self.queue:\n            return None\n\n        job_id = self.queue.pop(0)\n        job = self.jobs[job_id]\n        job.status = JobStatus.PROCESSING\n        job.started_at = datetime.now().isoformat()\n        self.active.append(job_id)\n        return job\n\n    def update_progress(self, job_id, progress):\n        \"\"\"Update job progress (0-100).\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.progress = min(progress, 100)\n\n    def complete_job(self, job_id, result_url):\n        \"\"\"Mark job as completed with result URL.\"\"\"\n        job = self.jobs.get(job_id)\n        if job:\n            job.status = JobStatus.COMPLETED\n            job.progress = 100\n            job.completed_at = datetime.now().isoformat()\n            job.result_url = result_url\n            if job_id in self.active:\n                self.active.remove(job_id)\n\n    def fail_job(self, job_id, error):\n        \"\"\"Mark job as failed. Re-queue if retries remain.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n        if job_id in self.active:\n            self.active.remove(job_id)\n\n        job.retry_count += 1\n        if job.retry_count < job.max_retries:\n            job.status = JobStatus.PENDING\n            job.progress = 0\n            self.queue.append(job_id)\n            return f\"Retrying ({job.retry_count}/{job.max_retries})\"\n        else:\n            job.status = JobStatus.FAILED\n            job.error = error\n            job.completed_at = datetime.now().isoformat()\n            return \"Max retries exceeded\"\n\n    def cancel_job(self, job_id):\n        \"\"\"Cancel a pending or active job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return False\n        job.status = JobStatus.CANCELLED\n        job.completed_at = datetime.now().isoformat()\n        if job_id in self.queue:\n            self.queue.remove(job_id)\n        if job_id in self.active:\n            self.active.remove(job_id)\n        return True\n\n    def get_queue_stats(self):\n        \"\"\"Get overall queue statistics.\"\"\"\n        statuses = {}\n        for job in self.jobs.values():\n            statuses[job.status] = statuses.get(job.status, 0) + 1\n        return {\n            \"total_jobs\": len(self.jobs),\n            \"queue_length\": len(self.queue),\n            \"active_jobs\": len(self.active),\n            \"max_concurrent\": self.max_concurrent,\n            \"by_status\": statuses\n        }\n\n\n# --- Demo: Simulate a video generation workload ---\nqueue = JobQueue(max_concurrent=2)\n\nprint(\"=== ClipCraft Job Queue Demo ===\\\n\")\n\n# Submit several jobs\nprompts = [\n    (\"A serene lake at sunset with mountains\", \"cinematic\", 5),\n    (\"Robot dancing in a neon city\", \"anime\", 4),\n    (\"Aerial flyover of a tropical island\", \"drone\", 8),\n    (\"A flower blooming in timelapse\", \"macro\", 3),\n    (\"Astronaut walking on Mars\", \"cinematic\", 6),\n]\n\nprint(\"Submitting 5 video generation jobs:\")\njob_ids = []\nfor prompt, style, dur in prompts:\n    jid = queue.submit(prompt, style, dur, \"1280x720\", \"user_demo\")\n    job_ids.append(jid)\n    print(f\"  Job {jid}: '{prompt[:35]}...' ({dur}s, {style})\")\n\nprint(f\"\\\nQueue stats: {queue.get_queue_stats()}\")\n\n# Process jobs step by step\nprint(\"\\\n--- Processing Loop ---\\\n\")\n\nstep = 0\nwhile queue.queue or queue.active:\n    step += 1\n\n    # Start next jobs if capacity available\n    while len(queue.active) < queue.max_concurrent and queue.queue:\n        job = queue.process_next()\n        if job:\n            print(f\"Step {step}: STARTED job {job.id} \"\n                  f\"({job.prompt[:25]}...)\")\n\n    # Simulate progress on active jobs\n    for jid in list(queue.active):\n        job = queue.jobs[jid]\n        job.progress += 35  # simulate chunk of progress\n\n        if job.progress >= 100:\n            # Simulate: 1 in 5 chance of failure on first attempt\n            if job.retry_count == 0 and job.id == job_ids[2]:\n                msg = queue.fail_job(jid, \"GPU timeout\")\n                print(f\"Step {step}: FAILED job {jid} -> {msg}\")\n            else:\n                url = f\"https://cdn.clipcraft.dev/v/{jid}.mp4\"\n                queue.complete_job(jid, url)\n                print(f\"Step {step}: COMPLETED job {jid} \"\n                      f\"-> {url}\")\n        else:\n            queue.update_progress(jid, job.progress)\n            print(f\"Step {step}: job {jid} progress: \"\n                  f\"{job.progress}%\")\n\nprint(f\"\\\n--- Final Results ---\\\n\")\nfor jid in job_ids:\n    status = queue.get_status(jid)\n    print(f\"Job {jid}: {status['status']}\"\n          f\" | progress: {status['progress']}%\"\n          f\" | result: {status.get('result_url', 'N/A')}\"\n          f\" | error: {status.get('error', 'none')}\")\n\nprint(f\"\\\nFinal queue stats: {queue.get_queue_stats()}\")\n</code></pre>\n</div>\n\n<h2>Μέρος 4: Παρακολούθηση προόδου</h2>\n\n<p>Οι πελάτες πρέπει να γνωρίζουν πώς εξελίσσεται το βίντεό τους. Υποστηρίζουμε δύο μοτίβα: <strong>polling</strong> (ο πελάτης ελέγχει περιοδικά) και <strong>webhooks</strong> (ο server στέλνει ενημερώσεις). Και τα δύο είναι απαραίτητα για ένα SaaS παραγωγής. Ο παρακάτω κώδικας δείχνει τον tracker προόδου με προσομοιωμένη αποστολή webhook.</p>\n\n<div class=\"code-block\">\n<div class=\"code-header\">\n<span class=\"code-lang\">Python</span>\n<div class=\"code-actions\">\n<button class=\"code-btn run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n</div>\n<pre><code class=\"language-python\">\n# progress.py - Progress tracking with polling and webhooks\n\nimport time\nimport uuid\nfrom datetime import datetime\n\nclass ProgressTracker:\n    \"\"\"Track video generation progress with polling and webhook support.\"\"\"\n\n    def __init__(self):\n        self.jobs = {}\n        self.webhook_log = []  # simulated webhook deliveries\n\n    def register_job(self, job_id, webhook_url=None):\n        \"\"\"Register a job for progress tracking.\"\"\"\n        self.jobs[job_id] = {\n            \"job_id\": job_id,\n            \"progress\": 0,\n            \"stage\": \"queued\",\n            \"webhook_url\": webhook_url,\n            \"events\": [],\n            \"started\": datetime.now().isoformat()\n        }\n\n    def update(self, job_id, progress, stage, message=\"\"):\n        \"\"\"Update progress and optionally fire webhook.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return\n\n        job[\"progress\"] = progress\n        job[\"stage\"] = stage\n\n        event = {\n            \"timestamp\": datetime.now().isoformat(),\n            \"progress\": progress,\n            \"stage\": stage,\n            \"message\": message\n        }\n        job[\"events\"].append(event)\n\n        # Fire webhook if configured\n        if job[\"webhook_url\"]:\n            self._fire_webhook(job_id, event)\n\n    def _fire_webhook(self, job_id, event):\n        \"\"\"Simulate sending a webhook notification.\"\"\"\n        payload = {\n            \"event\": \"video.progress\",\n            \"job_id\": job_id,\n            \"data\": event,\n            \"delivery_id\": str(uuid.uuid4())[:8]\n        }\n        self.webhook_log.append(payload)\n\n    def poll(self, job_id):\n        \"\"\"Client polls for current status (the polling endpoint).\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return {\"error\": \"Job not found\"}\n        return {\n            \"job_id\": job[\"job_id\"],\n            \"progress\": job[\"progress\"],\n            \"stage\": job[\"stage\"],\n            \"latest_event\": job[\"events\"][-1] if job[\"events\"] else None\n        }\n\n    def get_full_history(self, job_id):\n        \"\"\"Get the full event history of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            return None\n        return job[\"events\"]\n\n\n# --- Demo: Simulate video generation with progress ---\ntracker = ProgressTracker()\n\nprint(\"=== ClipCraft Progress Tracking Demo ===\\\n\")\n\n# Job 1: With webhook\njob1 = \"vid_abc123\"\ntracker.register_job(job1, webhook_url=\"https://myapp.com/hooks/clipcraft\")\n\n# Job 2: Polling only\njob2 = \"vid_def456\"\ntracker.register_job(job2, webhook_url=None)\n\n# Simulate the video generation stages\nstages = [\n    (0,   \"queued\",       \"Job accepted, waiting for GPU\"),\n    (5,   \"initializing\", \"Loading model weights\"),\n    (15,  \"generating\",   \"Generating frames 1-30\"),\n    (40,  \"generating\",   \"Generating frames 31-60\"),\n    (65,  \"generating\",   \"Generating frames 61-90\"),\n    (80,  \"generating\",   \"Generating frames 91-120\"),\n    (90,  \"encoding\",     \"Encoding H.264 video\"),\n    (95,  \"uploading\",    \"Uploading to CDN\"),\n    (100, \"completed\",    \"Video ready for download\"),\n]\n\nprint(\"--- Job 1 (with webhook) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job1, progress, stage, message)\n    status = tracker.poll(job1)\n    print(f\"  [{status['progress']:3d}%] {status['stage']:14s} | {message}\")\n\nprint(f\"\\\n  Webhooks fired: {len(tracker.webhook_log)}\")\nfor wh in tracker.webhook_log[:3]:\n    print(f\"    -> {wh['event']}: {wh['data']['stage']} \"\n          f\"({wh['data']['progress']}%) [delivery: {wh['delivery_id']}]\")\nprint(f\"    ... and {len(tracker.webhook_log) - 3} more\")\n\nprint(\"\\\n--- Job 2 (polling only) ---\")\nfor progress, stage, message in stages:\n    tracker.update(job2, progress, stage, message)\n\n# Simulate a client polling at intervals\npoll_points = [0, 20, 50, 80, 100]\nprint(\"  Client polling at intervals:\")\nhistory = tracker.get_full_history(job2)\nfor i, event in enumerate(history):\n    if event[\"progress\"] in poll_points:\n        print(f\"    Poll response: {event['progress']}% - {event['stage']}\")\n\nprint(f\"\\\n  Total events recorded: {len(history)}\")\n\n# Show webhook payload structure\nprint(\"\\\n--- Webhook Payload Structure ---\")\nsample = tracker.webhook_log[4]\nprint(\"  {\")\nfor key, val in sample.items():\n    if key == \"data\":\n        print(f'    \"data\": {{')\n        for dk, dv in val.items():\n            print(f'      \"{dk}\": \"{dv}\",')\n        print(f'    }},')\n    else:\n        print(f'    \"{key}\": \"{val}\",')\nprint(\"  }\")\n\nprint(\"\\\nBoth patterns ready for production use.\")\n</code></pre>\n</div>\n\n<h2>Μέρος 5: Ο server FastAPI</h2>\n\n<p>Αυτή είναι η καρδιά του ClipCraft &mdash; ο server API που συνδέει την ουρά εργασιών, την παρακολούθηση προόδου, την επεξεργασία βίντεο και τη χρέωση. Η βασική διαφορά από έναν server δημιουργίας εικόνων είναι το <strong>ασύγχρονο μοτίβο</strong>: το endpoint <code>/api/generate</code> επιστρέφει αμέσως με ένα ID εργασίας, και οι πελάτες κάνουν polling στο <code>/api/jobs/{id}</code> για την πρόοδο.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/main.py</span></div>\n<pre><code class=\"language-python\">\"\"\"ClipCraft API Server - Video Generation SaaS.\"\"\"\nfrom fastapi import FastAPI, HTTPException, Depends, Header, BackgroundTasks\nfrom fastapi.staticfiles import StaticFiles\nfrom fastapi.responses import JSONResponse\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\nimport uuid\nimport asyncio\nimport time\n\nfrom .config import settings\nfrom .video_service import VideoService\nfrom .job_queue import JobQueue, JobStatus\nfrom .progress import ProgressTracker\nfrom .billing import BillingSystem\n\napp = FastAPI(title=\"ClipCraft API\", version=\"1.0.0\")\napp.mount(\"/static\", StaticFiles(directory=\"static\"), name=\"static\")\n\n# Initialize services\nvideo_service = VideoService(\n    provider=settings.VIDEO_PROVIDER,\n    api_key=settings.VIDEO_API_KEY\n)\njob_queue = JobQueue(max_concurrent=settings.MAX_CONCURRENT_JOBS)\nprogress_tracker = ProgressTracker()\nbilling = BillingSystem()\n\n\n# --- Request/Response Models ---\nclass GenerateRequest(BaseModel):\n    prompt: str = Field(..., min_length=3, max_length=500)\n    style: str = Field(default=\"cinematic\")\n    duration: int = Field(default=5, ge=2, le=15)\n    resolution: str = Field(default=\"1280x720\")\n    webhook_url: Optional[str] = None\n\nclass JobResponse(BaseModel):\n    job_id: str\n    status: str\n    message: str\n    poll_url: str\n\nclass JobStatusResponse(BaseModel):\n    job_id: str\n    status: str\n    progress: int\n    stage: str\n    result_url: Optional[str] = None\n    error: Optional[str] = None\n    estimated_remaining_sec: Optional[int] = None\n\n\n# --- API key authentication ---\nasync def get_user(x_api_key: str = Header(...)):\n    user = billing.get_user_by_key(x_api_key)\n    if not user:\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n    return user\n\n\n# --- Background video generation task ---\nasync def process_video_job(job_id: str):\n    \"\"\"Background task that processes a video generation job.\"\"\"\n    job = job_queue.jobs.get(job_id)\n    if not job:\n        return\n\n    try:\n        progress_tracker.update(job_id, 5, \"initializing\",\n                                \"Loading model and preparing generation\")\n\n        # Call the video generation API\n        result = await video_service.generate(\n            prompt=job.prompt,\n            style=job.style,\n            duration_sec=job.duration_sec,\n            resolution=job.resolution,\n            progress_callback=lambda p, s, m:\n                progress_tracker.update(job_id, p, s, m)\n        )\n\n        progress_tracker.update(job_id, 95, \"uploading\",\n                                \"Uploading to CDN\")\n\n        video_url = result[\"video_url\"]\n        job_queue.complete_job(job_id, video_url)\n\n        progress_tracker.update(job_id, 100, \"completed\",\n                                \"Video ready for download\")\n\n        billing.record_generation(\n            job.user_id, job.prompt,\n            result[\"cost\"], job.resolution\n        )\n\n    except Exception as e:\n        msg = job_queue.fail_job(job_id, str(e))\n        progress_tracker.update(job_id, 0, \"failed\", str(e))\n\n\n# --- Endpoints ---\n@app.post(\"/api/generate\", response_model=JobResponse)\nasync def generate_video(req: GenerateRequest,\n                         background_tasks: BackgroundTasks,\n                         user=Depends(get_user)):\n    \"\"\"Submit a video generation job. Returns immediately with a job ID.\"\"\"\n    # Check billing limits\n    can, reason = billing.can_generate(user[\"id\"])\n    if not can:\n        raise HTTPException(status_code=429, detail=reason)\n\n    # Submit to job queue\n    job_id = job_queue.submit(\n        prompt=req.prompt,\n        style=req.style,\n        duration_sec=req.duration,\n        resolution=req.resolution,\n        user_id=user[\"id\"]\n    )\n\n    # Register progress tracking\n    progress_tracker.register_job(job_id, webhook_url=req.webhook_url)\n\n    # Start processing in the background\n    job = job_queue.process_next()\n    if job:\n        background_tasks.add_task(process_video_job, job.id)\n\n    return JobResponse(\n        job_id=job_id,\n        status=\"pending\",\n        message=\"Video generation job submitted\",\n        poll_url=f\"/api/jobs/{job_id}\"\n    )\n\n\n@app.get(\"/api/jobs/{job_id}\", response_model=JobStatusResponse)\nasync def get_job_status(job_id: str, user=Depends(get_user)):\n    \"\"\"Poll for job status and progress.\"\"\"\n    status = job_queue.get_status(job_id)\n    if not status:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n\n    poll_data = progress_tracker.poll(job_id)\n    estimated = None\n    if poll_data and poll_data.get(\"progress\", 0) > 0:\n        pct = poll_data[\"progress\"]\n        if pct < 100:\n            estimated = int((100 - pct) * 1.5)\n\n    return JobStatusResponse(\n        job_id=job_id,\n        status=status[\"status\"],\n        progress=status.get(\"progress\", 0),\n        stage=poll_data.get(\"stage\", \"unknown\") if poll_data else \"unknown\",\n        result_url=status.get(\"result_url\"),\n        error=status.get(\"error\"),\n        estimated_remaining_sec=estimated\n    )\n\n\n@app.delete(\"/api/jobs/{job_id}\")\nasync def cancel_job(job_id: str, user=Depends(get_user)):\n    \"\"\"Cancel a pending or active job.\"\"\"\n    success = job_queue.cancel_job(job_id)\n    if not success:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n    return {\"message\": \"Job cancelled\", \"job_id\": job_id}\n\n\n@app.get(\"/api/jobs\")\nasync def list_jobs(user=Depends(get_user)):\n    \"\"\"List all jobs for the authenticated user.\"\"\"\n    user_jobs = [\n        job.to_dict() for job in job_queue.jobs.values()\n        if job.user_id == user[\"id\"]\n    ]\n    return {\"jobs\": user_jobs, \"total\": len(user_jobs)}\n\n\n@app.get(\"/api/queue/stats\")\nasync def queue_stats():\n    \"\"\"Get queue statistics (admin endpoint).\"\"\"\n    return job_queue.get_queue_stats()\n\n\n@app.get(\"/api/health\")\nasync def health():\n    return {\n        \"status\": \"ok\",\n        \"provider\": settings.VIDEO_PROVIDER,\n        \"queue\": job_queue.get_queue_stats()\n    }</code></pre>\n</div>\n\n<h2>Μέρος 6: Ρυθμίσεις και ανάπτυξη</h2>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>app/config.py</span></div>\n<pre><code class=\"language-python\">\"\"\"Configuration loaded from environment variables.\"\"\"\nfrom pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    VIDEO_PROVIDER: str = \"runway\"  # \"runway\", \"pika\", or \"stability\"\n    VIDEO_API_KEY: str = \"\"\n    DATABASE_URL: str = \"sqlite:///./clipcraft.db\"\n    MAX_CONCURRENT_JOBS: int = 3\n    MAX_VIDEO_DURATION: int = 15  # seconds\n    STORAGE_BUCKET: str = \"clipcraft-videos\"\n    WEBHOOK_SECRET: str = \"\"\n\n    class Config:\n        env_file = \".env\"\n\nsettings = Settings()</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>.env.example</span></div>\n<pre><code>VIDEO_PROVIDER=runway\nVIDEO_API_KEY=your_api_key_here\nDATABASE_URL=sqlite:///./clipcraft.db\nMAX_CONCURRENT_JOBS=3\nMAX_VIDEO_DURATION=15\nWEBHOOK_SECRET=your_webhook_secret</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>requirements.txt</span></div>\n<pre><code>fastapi==0.109.0\nuvicorn==0.27.0\nhttpx==0.26.0\npydantic==2.5.3\npydantic-settings==2.1.0\npython-multipart==0.0.6\naiofiles==23.2.1</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Dockerfile</span></div>\n<pre><code>FROM python:3.11-slim\n\n# Install FFmpeg for video processing\nRUN apt-get update &amp;&amp; \\\\\n    apt-get install -y --no-install-recommends ffmpeg &amp;&amp; \\\\\n    rm -rf /var/lib/apt/lists/*\n\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n\nEXPOSE 8000\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]</code></pre>\n</div>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>docker-compose.yml</span></div>\n<pre><code>version: \"3.8\"\nservices:\n  clipcraft:\n    build: .\n    ports:\n      - \"8000:8000\"\n    env_file:\n      - .env\n    volumes:\n      - ./videos:/app/videos\n      - ./static:/app/static\n    deploy:\n      resources:\n        limits:\n          memory: 2G</code></pre>\n</div>\n\n<p>Για εκτέλεση τοπικά:</p>\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>Terminal</span></div>\n<pre><code># Development\npip install -r requirements.txt\nuvicorn app.main:app --reload\n\n# Or with Docker\ndocker-compose up --build\n\n# Submit a video generation job\ncurl -X POST http://localhost:8000/api/generate \\\\\n  -H \"X-API-Key: your_key\" \\\\\n  -H \"Content-Type: application/json\" \\\\\n  -d '{\"prompt\": \"sunset over ocean waves\", \"style\": \"cinematic\", \"duration\": 5}'\n\n# Poll for progress\ncurl http://localhost:8000/api/jobs/JOB_ID \\\\\n  -H \"X-API-Key: your_key\"</code></pre>\n</div>\n\n<h2>Μέρος 7: Διεπαφή χρήστη (Frontend)</h2>\n\n<p>Το frontend του ClipCraft χειρίζεται την ασύγχρονη ροή εργασίας: υποβολή ενός prompt, μετά polling για πρόοδο και εμφάνιση του αποτελέσματος όταν είναι έτοιμο. Αποθήκευσέ το ως <code>static/index.html</code>.</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>static/index.html (key parts)</span></div>\n<pre><code class=\"language-html\">&lt;!DOCTYPE html&gt;\n&lt;html&gt;\n&lt;head&gt;\n  &lt;title&gt;ClipCraft - AI Video Generation&lt;/title&gt;\n  &lt;style&gt;\n    body { font-family: system-ui; max-width: 800px; margin: 0 auto; padding: 20px;\n           background: #0f0f0f; color: #e5e5e5; }\n    h1 { background: linear-gradient(135deg, #7c3aed, #a855f7);\n         -webkit-background-clip: text; -webkit-text-fill-color: transparent; }\n    .prompt-input { width: 100%; padding: 14px; font-size: 16px;\n                    border: 2px solid #333; border-radius: 10px;\n                    background: #1a1a1a; color: #e5e5e5; margin: 10px 0; }\n    .controls { display: flex; gap: 12px; margin: 12px 0; align-items: center; }\n    .controls select { padding: 10px; border-radius: 8px; background: #1a1a1a;\n                        color: #e5e5e5; border: 2px solid #333; }\n    .generate-btn { padding: 14px 32px; background: #7c3aed; color: white;\n                    border: none; border-radius: 10px; font-size: 16px;\n                    cursor: pointer; }\n    .generate-btn:disabled { opacity: 0.5; cursor: not-allowed; }\n    .progress-bar { width: 100%; height: 8px; background: #333;\n                    border-radius: 4px; margin: 16px 0; display: none; }\n    .progress-fill { height: 100%; background: linear-gradient(90deg, #7c3aed, #a855f7);\n                     border-radius: 4px; transition: width 0.3s; width: 0%; }\n    .status-text { color: #a78bfa; font-size: 14px; margin: 8px 0; }\n    .result video { width: 100%; border-radius: 12px; margin: 20px 0; }\n  &lt;/style&gt;\n&lt;/head&gt;\n&lt;body&gt;\n  &lt;h1&gt;ClipCraft&lt;/h1&gt;\n  &lt;p&gt;Generate videos from text descriptions&lt;/p&gt;\n  &lt;input class=\"prompt-input\" placeholder=\"Describe the video you want...\" id=\"prompt\"&gt;\n  &lt;div class=\"controls\"&gt;\n    &lt;select id=\"style\"&gt;\n      &lt;option value=\"cinematic\"&gt;Cinematic&lt;/option&gt;\n      &lt;option value=\"anime\"&gt;Anime&lt;/option&gt;\n      &lt;option value=\"drone\"&gt;Drone Shot&lt;/option&gt;\n      &lt;option value=\"macro\"&gt;Macro&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;select id=\"duration\"&gt;\n      &lt;option value=\"3\"&gt;3 seconds&lt;/option&gt;\n      &lt;option value=\"5\" selected&gt;5 seconds&lt;/option&gt;\n      &lt;option value=\"10\"&gt;10 seconds&lt;/option&gt;\n    &lt;/select&gt;\n    &lt;button class=\"generate-btn\" id=\"genBtn\" onclick=\"generate()\"&gt;Generate Video&lt;/button&gt;\n  &lt;/div&gt;\n  &lt;div class=\"progress-bar\" id=\"progressBar\"&gt;\n    &lt;div class=\"progress-fill\" id=\"progressFill\"&gt;&lt;/div&gt;\n  &lt;/div&gt;\n  &lt;p class=\"status-text\" id=\"statusText\"&gt;&lt;/p&gt;\n  &lt;div class=\"result\" id=\"result\"&gt;&lt;/div&gt;\n\n  &lt;script&gt;\n  let pollInterval = null;\n\n  async function generate() {\n    const prompt = document.getElementById('prompt').value;\n    const style = document.getElementById('style').value;\n    const duration = parseInt(document.getElementById('duration').value);\n    const btn = document.getElementById('genBtn');\n    btn.disabled = true;\n    document.getElementById('progressBar').style.display = 'block';\n\n    const res = await fetch('/api/generate', {\n      method: 'POST',\n      headers: {'Content-Type': 'application/json', 'X-API-Key': 'demo'},\n      body: JSON.stringify({prompt, style, duration})\n    });\n    const data = await res.json();\n    pollForResult(data.job_id);\n  }\n\n  function pollForResult(jobId) {\n    pollInterval = setInterval(async () =&gt; {\n      const res = await fetch('/api/jobs/' + jobId,\n        {headers: {'X-API-Key': 'demo'}});\n      const data = await res.json();\n      document.getElementById('progressFill').style.width = data.progress + '%';\n      document.getElementById('statusText').textContent =\n        data.stage + ' (' + data.progress + '%)';\n      if (data.status === 'completed') {\n        clearInterval(pollInterval);\n        document.getElementById('result').innerHTML =\n          '&lt;video controls autoplay src=\"' + data.result_url + '\"&gt;&lt;/video&gt;';\n        document.getElementById('genBtn').disabled = false;\n      } else if (data.status === 'failed') {\n        clearInterval(pollInterval);\n        document.getElementById('statusText').textContent = 'Error: ' + data.error;\n        document.getElementById('genBtn').disabled = false;\n      }\n    }, 2000);\n  }\n  &lt;/script&gt;\n&lt;/body&gt;\n&lt;/html&gt;</code></pre>\n</div>\n\n\n<h2>Μέρος 11: Σύνδεση με το Δικό σας LLM</h2>\n\n<p>Μέχρι τώρα χρησιμοποιήσαμε API τρίτων. Αλλά η πραγματική δύναμη &mdash; και τα πραγματικά περιθώρια &mdash; έρχονται όταν τρέχετε <strong>τα δικά σας μοντέλα</strong>. Αυτό εξαλείφει τα κόστη API ανά αίτημα, σας δίνει πλήρη έλεγχο στην καθυστέρηση και την ιδιωτικότητα, και σας επιτρέπει να ρυθμίσετε τα μοντέλα για τη συγκεκριμένη περίπτωση χρήσης σας.</p>\n\n<p>Τρεις κύριες προσεγγίσεις για αυτο-φιλοξενία:</p>\n<ul>\n<li><strong>vLLM</strong> &mdash; υψηλότερη απόδοση, API συμβατό με OpenAI, ιδανικό για παραγωγή</li>\n<li><strong>Ollama</strong> &mdash; ευκολότερη εγκατάσταση, ιδανικό για ανάπτυξη και μικρής κλίμακας ανάπτυξη</li>\n<li><strong>llama.cpp</strong> &mdash; ελαφρύτερο, τρέχει σε CPU, ιδανικό για edge deployment</li>\n</ul>\n\n<p>Για δημιουργία βίντεο, μπορείτε να αυτο-φιλοξενήσετε μοντέλα όπως <strong>CogVideoX</strong>, <strong>Mochi</strong> ή <strong>AnimateDiff</strong>:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>serve_own_model.py &mdash; Self-hosted Video Generation</span></div>\n<pre><code class=\"language-python\">\"\"\"\nSelf-hosting video generation models for ClipCraft.\nOptions: CogVideoX, Mochi-1, AnimateDiff, SVD (Stable Video Diffusion).\nRequires: GPU with >= 24GB VRAM (A100/A10G or RTX 4090).\n\"\"\"\n\nclass LocalVideoGenerator:\n    \"\"\"Generate videos using locally hosted models.\"\"\"\n\n    def __init__(self, model=\"THUDM/CogVideoX-5b\"):\n        self.model = model\n        self.device = \"cuda\"\n\n    def load_model(self):\n        \"\"\"Load video generation model. Takes 30-60s, needs 24GB+ VRAM.\"\"\"\n        # In production:\n        # import torch\n        # from diffusers import CogVideoXPipeline\n        # self.pipe = CogVideoXPipeline.from_pretrained(\n        #     self.model, torch_dtype=torch.float16\n        # ).to(self.device)\n        # self.pipe.enable_model_cpu_offload()\n        print(f\"Loaded {self.model}\")\n        print(f\"VRAM: ~20GB (fp16), Generation: ~2min per 6s clip\")\n\n    def generate(self, prompt, num_frames=49, fps=8, width=720, height=480):\n        \"\"\"Generate a video clip from text.\"\"\"\n        # In production:\n        # video = self.pipe(\n        #     prompt=prompt,\n        #     num_frames=num_frames,\n        #     guidance_scale=6,\n        #     num_inference_steps=50,\n        # ).frames[0]\n        # from diffusers.utils import export_to_video\n        # export_to_video(video, \"output.mp4\", fps=fps)\n\n        duration = num_frames / fps\n        print(f\"Generating {duration:.1f}s video: \\\\\"{prompt}\\\\\"\")\n        print(f\"  {num_frames} frames at {fps}fps, {width}x{height}\")\n        print(f\"  Estimated time: ~{num_frames * 2.5:.0f}s on A100\")\n        return {\"status\": \"generated\", \"duration\": duration, \"cost\": 0.0}\n\nclass RunwayMLClient:\n    \"\"\"Fallback: Use Runway ML API for production quality.\"\"\"\n\n    def __init__(self, api_key=\"your-runway-key\"):\n        self.api_key = api_key\n        self.base_url = \"https://api.runwayml.com/v1\"\n\n    def generate(self, prompt, duration=4):\n        \"\"\"Generate video via Runway Gen-3 Alpha API.\"\"\"\n        # import requests\n        # r = requests.post(f\"{self.base_url}/generate\",\n        #     headers={\"Authorization\": f\"Bearer {self.api_key}\"},\n        #     json={\"prompt\": prompt, \"duration\": duration})\n        cost = duration * 0.05  # ~$0.05 per second\n        print(f\"Runway API: \\\\\"{prompt}\\\\\" ({duration}s)\")\n        print(f\"  Cost: \\${cost:.2f}\")\n        return {\"status\": \"queued\", \"cost\": cost}\n\n\n# --- Demo ---\nprint(\"=== Self-Hosted Video Generation ===\\\n\")\ngen = LocalVideoGenerator()\ngen.load_model()\nresult = gen.generate(\"A cat walking through a garden, cinematic\", num_frames=49)\nprint(f\"\\\n  Result: {result}\\\n\")\n\n# Cost comparison\nprint(\"=== Cost Comparison ===\")\nprint(\"| Method            | Per 4s clip | 1K clips/mo | GPU Required  |\")\nprint(\"|-------------------|------------|-------------|---------------|\")\nprint(\"| Runway Gen-3      | $0.20      | $200        | None (API)    |\")\nprint(\"| Replicate SVD     | $0.10      | $100        | None (API)    |\")\nprint(\"| Self-host CogVideo| ~$0.02     | ~$20+$300/mo| A100 40GB     |\")\nprint(\"| Self-host AnimDiff| ~$0.01     | ~$10+$150/mo| RTX 4090 24GB |\")\nprint(\"\\\nBreakeven: ~500 clips/month for self-hosting\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Μέρος 12: Εναλλακτικές Ανάπτυξης</h2>\n\n<p>Έχετε πολλαπλές διαδρομές ανάπτυξης ανάλογα με την κλίμακα, τον προϋπολογισμό και τις τεχνικές απαιτήσεις. Εδώ είναι μια πλήρης σύγκριση με πραγματικές εντολές:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>deployment_options.py &mdash; ClipCraft Deployment Guide</span></div>\n<pre><code class=\"language-python\">\"\"\"\nDeployment options for ClipCraft - from hobby to enterprise scale.\nEach option includes: setup commands, estimated costs, and pros/cons.\n\"\"\"\n\nclass DeploymentGuide:\n    \"\"\"Compare deployment platforms for your SaaS.\"\"\"\n\n    def __init__(self, app_name=\"{product_name.lower()}\"):\n        self.app_name = app_name\n\n    def railway(self):\n        \"\"\"Railway.app - Easiest PaaS deployment.\"\"\"\n        print(\"=== Railway.app ===\")\n        print(\"Best for: MVPs, startups, small-to-medium scale\")\n        print(\"Cost: $5/mo hobby, ~$20-50/mo production\")\n        print()\n        print(\"Setup commands:\")\n        print(f\"  npm install -g @railway/cli\")\n        print(f\"  railway login\")\n        print(f\"  railway init\")\n        print(f\"  railway up  # deploys from Dockerfile\")\n        print()\n        print(\"railway.toml:\")\n        print(f'  [build]')\n        print(f'  builder = \"dockerfile\"')\n        print(f'  dockerfilePath = \"Dockerfile\"')\n        print()\n        print(f'  [deploy]')\n        print(f'  healthcheckPath = \"/health\"')\n        print(f'  healthcheckTimeout = 30')\n        print(f'  restartPolicyType = \"on_failure\"')\n        print()\n        print(\"Pros: Auto-scaling, free PostgreSQL, easy env vars\")\n        print(\"Cons: No GPU support, cold starts possible\")\n\n    def render(self):\n        \"\"\"Render.com - Good middle ground.\"\"\"\n        print(\"\\\n=== Render.com ===\")\n        print(\"Best for: Production apps, auto-scaling needed\")\n        print(\"Cost: Free tier available, $7/mo starter, ~$25/mo production\")\n        print()\n        print(\"render.yaml (Infrastructure as Code):\")\n        print(f\"  services:\")\n        print(f\"    - type: web\")\n        print(f\"      name: {self.app_name}\")\n        print(f\"      runtime: docker\")\n        print(f\"      plan: starter\")\n        print(f\"      healthCheckPath: /health\")\n        print(f\"      envVars:\")\n        print(f\"        - key: DATABASE_URL\")\n        print(f\"          fromDatabase:\")\n        print(f\"            name: {self.app_name}-db\")\n        print(f\"            property: connectionString\")\n        print(f\"    - type: worker\")\n        print(f\"      name: {self.app_name}-worker\")\n        print(f\"      runtime: docker\")\n        print(f\"      dockerfilePath: ./Dockerfile.worker\")\n        print()\n        print(\"  databases:\")\n        print(f\"    - name: {self.app_name}-db\")\n        print(f\"      plan: starter\")\n        print()\n        print(\"Pros: Free SSL, managed PostgreSQL, auto-deploy from Git\")\n        print(\"Cons: No GPU, slower builds than Railway\")\n\n    def aws(self):\n        \"\"\"AWS - Full enterprise deployment.\"\"\"\n        print(\"\\\n=== AWS (ECS + Fargate) ===\")\n        print(\"Best for: Enterprise, GPU workloads, full control\")\n        print(\"Cost: ~$50-500/mo depending on scale\")\n        print()\n        print(\"Architecture:\")\n        print(\"  ALB -> ECS Fargate -> RDS PostgreSQL\")\n        print(\"         |-> ElastiCache (Redis)\")\n        print(\"         |-> S3 (file storage)\")\n        print(\"         |-> SQS (job queue)\")\n        print()\n        print(\"Setup (using AWS CDK):\")\n        print(f\"  npm install -g aws-cdk\")\n        print(f\"  cdk init app --language python\")\n        print(f\"  # Define ECS service, RDS, Redis in stack\")\n        print(f\"  cdk deploy\")\n        print()\n        print(\"For GPU (e.g., self-hosted models):\")\n        print(f\"  # Use EC2 with g5.xlarge (A10G GPU, ~$1.00/hr)\")\n        print(f\"  # Or SageMaker endpoints for managed inference\")\n        print()\n        print(\"Pros: GPU support, infinite scale, enterprise features\")\n        print(\"Cons: Complex setup, higher cost, AWS expertise needed\")\n\n    def digitalocean(self):\n        \"\"\"DigitalOcean App Platform.\"\"\"\n        print(\"\\\n=== DigitalOcean App Platform ===\")\n        print(\"Best for: Simple deployments, predictable pricing\")\n        print(\"Cost: $5/mo basic, $12/mo pro, GPU droplets ~$2.50/hr\")\n        print()\n        print(\".do/app.yaml:\")\n        print(f\"  name: {self.app_name}\")\n        print(f\"  services:\")\n        print(f\"    - name: api\")\n        print(f\"      github:\")\n        print(f\"        repo: yourusername/{self.app_name}\")\n        print(f\"        branch: main\")\n        print(f\"        deploy_on_push: true\")\n        print(f\"      dockerfile_path: Dockerfile\")\n        print(f\"      instance_count: 2\")\n        print(f\"      instance_size_slug: professional-xs\")\n        print(f\"      health_check:\")\n        print(f\"        http_path: /health\")\n        print()\n        print(\"Pros: Simple pricing, good docs, GPU droplets available\")\n        print(\"Cons: Smaller ecosystem than AWS, limited auto-scaling\")\n\n    def self_hosted(self):\n        \"\"\"Self-hosted on bare metal or VPS.\"\"\"\n        print(\"\\\n=== Self-Hosted (VPS/Bare Metal) ===\")\n        print(\"Best for: Maximum control, GPU inference, cost optimization\")\n        print(\"Cost: $20-100/mo VPS, $100-300/mo GPU server\")\n        print()\n        print(\"Stack: Nginx + Gunicorn + FastAPI + PostgreSQL + Redis\")\n        print()\n        print(\"docker-compose.yml:\")\n        print(f\"  services:\")\n        print(f\"    app:\")\n        print(f\"      build: .\")\n        print(f\"      ports: [\\\\\"8000:8000\\\\\"]\")\n        print(f\"      environment:\")\n        print(f\"        - DATABASE_URL=postgresql://user:pass@db:5432/{self.app_name}\")\n        print(f\"        - REDIS_URL=redis://redis:6379\")\n        print(f\"      depends_on: [db, redis]\")\n        print(f\"      deploy:\")\n        print(f\"        replicas: 3\")\n        print(f\"    db:\")\n        print(f\"      image: postgres:16-alpine\")\n        print(f\"      volumes: [postgres_data:/var/lib/postgresql/data]\")\n        print(f\"    redis:\")\n        print(f\"      image: redis:7-alpine\")\n        print(f\"    nginx:\")\n        print(f\"      image: nginx:alpine\")\n        print(f\"      ports: [\\\\\"80:80\\\\\", \\\\\"443:443\\\\\"]\")\n        print(f\"      volumes: [./nginx.conf:/etc/nginx/nginx.conf]\")\n        print()\n        print(\"GPU server providers:\")\n        print(\"  - Lambda Labs: A100 ~$1.10/hr, H100 ~$2.49/hr\")\n        print(\"  - RunPod: A100 ~$1.64/hr, community GPUs from $0.20/hr\")\n        print(\"  - Vast.ai: Cheapest GPUs, ~$0.15-0.50/hr\")\n        print(\"  - Hetzner: Dedicated servers from EUR39/mo (no GPU)\")\n\n# --- Demo ---\nguide = DeploymentGuide()\nguide.railway()\nguide.render()\nguide.aws()\nguide.digitalocean()\nguide.self_hosted()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(\"RECOMMENDATION by scale:\")\nprint(\"  Hobby/MVP     -> Railway or Render (free tier)\")\nprint(\"  Startup       -> Railway or DigitalOcean ($20-50/mo)\")\nprint(\"  Growth        -> AWS ECS or DigitalOcean ($100-500/mo)\")\nprint(\"  Enterprise    -> AWS/GCP with GPU instances\")\nprint(\"  Self-hosted AI-> VPS + GPU from Lambda/RunPod/Vast.ai\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Μέρος 13: Περιορισμός Ρυθμού &amp; Προστασία API</h2>\n\n<p>Κάθε SaaS παραγωγής χρειάζεται περιορισμό ρυθμού για να αποτρέψει κατάχρηση, να ελέγξει κόστη και να εξασφαλίσει δίκαιη χρήση. Εδώ είναι μια υλοποίηση επιπέδου παραγωγής:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>rate_limiter.py &mdash; Production Rate Limiting for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nProduction rate limiting for ClipCraft.\nThree tiers: in-memory (development), Redis (production), token bucket (advanced).\n\"\"\"\nimport time\nfrom collections import defaultdict\n\n# === TIER 1: In-Memory Rate Limiter (Development) ===\nclass InMemoryRateLimiter:\n    \"\"\"Simple sliding window rate limiter. Works for single-server setup.\"\"\"\n\n    def __init__(self):\n        self.requests = defaultdict(list)\n\n    def is_allowed(self, key, max_requests, window_seconds):\n        \"\"\"Check if request is allowed under the rate limit.\"\"\"\n        now = time.time()\n        window_start = now - window_seconds\n\n        # Clean old entries\n        self.requests[key] = [t for t in self.requests[key] if t > window_start]\n\n        if len(self.requests[key]) >= max_requests:\n            retry_after = self.requests[key][0] - window_start\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"limit\": max_requests,\n                \"window\": window_seconds,\n                \"retry_after\": round(retry_after, 1),\n                \"current\": len(self.requests[key])\n            }}\n\n        self.requests[key].append(now)\n        return True, {{\n            \"remaining\": max_requests - len(self.requests[key]),\n            \"limit\": max_requests,\n            \"reset\": round(window_start + window_seconds - now, 1)\n        }}\n\n# === TIER 2: Redis Rate Limiter (Production) ===\nclass RedisRateLimiter:\n    \"\"\"Redis-based rate limiter for distributed deployments.\n\n    In production, install: pip install redis\n    And use a real Redis connection.\n    \"\"\"\n\n    def __init__(self):\n        # In production: self.redis = redis.Redis(host='redis', port=6379, db=0)\n        self.memory = defaultdict(list)  # Simulating Redis\n\n    def check_rate_limit(self, user_id, tier=\"free\"):\n        \"\"\"Check rate limit based on user tier.\"\"\"\n        limits = {{\n            \"free\":       {{\"requests_per_minute\": 5,  \"requests_per_day\": 50}},\n            \"starter\":    {{\"requests_per_minute\": 20, \"requests_per_day\": 500}},\n            \"pro\":        {{\"requests_per_minute\": 60, \"requests_per_day\": 5000}},\n            \"enterprise\": {{\"requests_per_minute\": 200, \"requests_per_day\": 50000}},\n        }}\n\n        tier_limits = limits.get(tier, limits[\"free\"])\n        now = time.time()\n\n        # Check per-minute limit\n        minute_key = f\"rate:{{user_id}}:minute\"\n        self.memory[minute_key] = [t for t in self.memory[minute_key] if t > now - 60]\n\n        if len(self.memory[minute_key]) >= tier_limits[\"requests_per_minute\"]:\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"type\": \"per_minute\",\n                \"limit\": tier_limits[\"requests_per_minute\"],\n                \"retry_after_seconds\": 60 - (now - self.memory[minute_key][0])\n            }}\n\n        # Check per-day limit\n        day_key = f\"rate:{{user_id}}:day\"\n        self.memory[day_key] = [t for t in self.memory[day_key] if t > now - 86400]\n\n        if len(self.memory[day_key]) >= tier_limits[\"requests_per_day\"]:\n            return False, {{\n                \"error\": \"daily_limit_exceeded\",\n                \"type\": \"per_day\",\n                \"limit\": tier_limits[\"requests_per_day\"],\n            }}\n\n        # Allow request\n        self.memory[minute_key].append(now)\n        self.memory[day_key].append(now)\n\n        return True, {{\n            \"remaining_minute\": tier_limits[\"requests_per_minute\"] - len(self.memory[minute_key]),\n            \"remaining_day\": tier_limits[\"requests_per_day\"] - len(self.memory[day_key]),\n        }}\n\n# === FastAPI Middleware ===\nclass RateLimitMiddleware:\n    \"\"\"\n    FastAPI middleware for rate limiting.\n\n    Usage in your app:\n        from rate_limiter import RateLimitMiddleware\n        app.add_middleware(RateLimitMiddleware)\n\n    Or as a dependency:\n        @app.get(\"/generate\")\n        async def generate(rate_limit: dict = Depends(check_rate_limit)):\n            ...\n    \"\"\"\n\n    def __init__(self):\n        self.limiter = RedisRateLimiter()\n\n    def get_middleware_code(self):\n        \"\"\"Print the actual FastAPI middleware code.\"\"\"\n        code = \"# Add to your FastAPI app:\\\n\\\n\"\n        code += \"from fastapi import Request, Response\\\n\"\n        code += \"from starlette.middleware.base import BaseHTTPMiddleware\\\n\\\n\"\n        code += \"class RateLimitMiddleware(BaseHTTPMiddleware):\\\n\"\n        code += \"    async def dispatch(self, request, call_next):\\\n\"\n        code += \"        api_key = request.headers.get('X-API-Key', 'anonymous')\\\n\"\n        code += \"        user_id = self.get_user_from_key(api_key)\\\n\"\n        code += \"        tier = self.get_user_tier(user_id)\\\n\"\n        code += \"        allowed, info = self.limiter.check_rate_limit(user_id, tier)\\\n\"\n        code += \"        if not allowed:\\\n\"\n        code += \"            return JSONResponse(status_code=429, content=info)\\\n\"\n        code += \"        response = await call_next(request)\\\n\"\n        code += \"        response.headers['X-RateLimit-Remaining'] = str(info.get('remaining_minute', 0))\\\n\"\n        code += \"        return response\\\n\"\n        print(code)\n\n\n# --- Demo ---\nprint(\"=== Rate Limiting Demo for ClipCraft ===\\\n\")\n\nlimiter = RedisRateLimiter()\n\n# Simulate requests from different users\nusers = [\n    (\"user_free_1\", \"free\"),\n    (\"user_pro_1\", \"pro\"),\n    (\"user_enterprise_1\", \"enterprise\"),\n]\n\nfor user_id, tier in users:\n    print(f\"User: {{user_id}} ({{tier}} tier)\")\n    for i in range(8):\n        allowed, info = limiter.check_rate_limit(user_id, tier)\n        status = \"ALLOWED\" if allowed else \"BLOCKED\"\n        print(f\"  Request {{i+1}}: {{status}} | {{info}}\")\n        if not allowed:\n            break\n    print()\n\n# Show middleware code\nmw = RateLimitMiddleware()\nmw.get_middleware_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Μέρος 14: Ενσωμάτωση Χρέωσης Stripe</h2>\n\n<p>Ένα πραγματικό SaaS χρειάζεται πραγματική χρέωση. Εδώ είναι μια πλήρης ενσωμάτωση Stripe με επίπεδα συνδρομής, χρέωση βάσει χρήσης και διαχείριση webhook:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>billing.py &mdash; Stripe Integration for ClipCraft</span></div>\n<pre><code class=\"language-python\">\"\"\"\nComplete Stripe billing for ClipCraft.\nSupports: subscription tiers, usage-based billing, webhooks.\n\nSetup:\n  pip install stripe\n  # Get keys from https://dashboard.stripe.com/apikeys\n  # Set STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET in .env\n\"\"\"\n\nclass StripeBilling:\n    \"\"\"Full Stripe integration for SaaS billing.\"\"\"\n\n    def __init__(self, secret_key=\"sk_test_...\"):\n        # In production: import stripe; stripe.api_key = secret_key\n        self.plans = {{\n            \"free\":       {{\"price\": 0,    \"name\": \"Free\",       \"stripe_price_id\": None}},\n            \"starter\":    {{\"price\": 19,   \"name\": \"Starter\",    \"stripe_price_id\": \"price_starter_monthly\"}},\n            \"pro\":        {{\"price\": 49,   \"name\": \"Professional\",\"stripe_price_id\": \"price_pro_monthly\"}},\n            \"enterprise\": {{\"price\": 199,  \"name\": \"Enterprise\", \"stripe_price_id\": \"price_enterprise_monthly\"}},\n        }}\n\n    def create_checkout_session(self, user_email, plan_key):\n        \"\"\"Create a Stripe Checkout session for subscription.\"\"\"\n        plan = self.plans[plan_key]\n        if plan[\"price\"] == 0:\n            return {{\"status\": \"free_tier\", \"message\": \"No payment needed\"}}\n\n        # In production:\n        # session = stripe.checkout.Session.create(\n        #     customer_email=user_email,\n        #     payment_method_types=[\"card\"],\n        #     line_items=[{{\n        #         \"price\": plan[\"stripe_price_id\"],\n        #         \"quantity\": 1,\n        #     }}],\n        #     mode=\"subscription\",\n        #     success_url=\"https://{product_name.lower()}.com/billing/success?session_id={{CHECKOUT_SESSION_ID}}\",\n        #     cancel_url=\"https://{product_name.lower()}.com/billing/cancel\",\n        #     metadata={{\"user_email\": user_email, \"plan\": plan_key}},\n        # )\n        # return {{\"checkout_url\": session.url, \"session_id\": session.id}}\n\n        print(f\"Creating checkout for {{user_email}} -> {{plan['name']}} (\\${{plan['price']}}/mo)\")\n        return {{\n            \"checkout_url\": f\"https://checkout.stripe.com/pay/cs_test_...\",\n            \"plan\": plan[\"name\"],\n            \"price\": f\"\\${{plan['price']}}/month\"\n        }}\n\n    def create_usage_record(self, subscription_item_id, quantity, action=\"increment\"):\n        \"\"\"Record usage for metered billing (pay-per-use on top of subscription).\"\"\"\n        # In production:\n        # stripe.SubscriptionItem.create_usage_record(\n        #     subscription_item_id,\n        #     quantity=quantity,\n        #     action=action,\n        #     timestamp=int(time.time())\n        # )\n        print(f\"Usage recorded: {{quantity}} units on {{subscription_item_id}}\")\n        return {{\"status\": \"recorded\", \"quantity\": quantity}}\n\n    def handle_webhook(self, event_type, data):\n        \"\"\"Handle Stripe webhook events.\"\"\"\n        handlers = {{\n            \"checkout.session.completed\": self._on_checkout_complete,\n            \"customer.subscription.updated\": self._on_subscription_update,\n            \"customer.subscription.deleted\": self._on_subscription_cancel,\n            \"invoice.payment_failed\": self._on_payment_failed,\n            \"invoice.paid\": self._on_invoice_paid,\n        }}\n\n        handler = handlers.get(event_type)\n        if handler:\n            return handler(data)\n        return {{\"status\": \"unhandled\", \"event\": event_type}}\n\n    def _on_checkout_complete(self, data):\n        print(f\"  Checkout completed! Activate subscription for user.\")\n        return {{\"action\": \"activate_subscription\"}}\n\n    def _on_subscription_update(self, data):\n        print(f\"  Subscription updated. Sync plan tier in database.\")\n        return {{\"action\": \"sync_tier\"}}\n\n    def _on_subscription_cancel(self, data):\n        print(f\"  Subscription canceled. Downgrade to free tier.\")\n        return {{\"action\": \"downgrade_to_free\"}}\n\n    def _on_payment_failed(self, data):\n        print(f\"  Payment failed! Send dunning email, grace period 7 days.\")\n        return {{\"action\": \"send_dunning_email\", \"grace_days\": 7}}\n\n    def _on_invoice_paid(self, data):\n        print(f\"  Invoice paid. Reset monthly usage counters.\")\n        return {{\"action\": \"reset_usage\"}}\n\n    def get_fastapi_webhook_code(self):\n        \"\"\"Print the FastAPI webhook endpoint code.\"\"\"\n        code = \"# FastAPI webhook endpoint:\\\n\\\n\"\n        code += \"@app.post('/webhooks/stripe')\\\n\"\n        code += \"async def stripe_webhook(request: Request):\\\n\"\n        code += \"    payload = await request.body()\\\n\"\n        code += \"    sig_header = request.headers.get('stripe-signature')\\\n\"\n        code += \"    try:\\\n\"\n        code += \"        event = stripe.Webhook.construct_event(\\\n\"\n        code += \"            payload, sig_header, STRIPE_WEBHOOK_SECRET)\\\n\"\n        code += \"    except stripe.error.SignatureVerificationError:\\\n\"\n        code += \"        raise HTTPException(400, 'Invalid signature')\\\n\"\n        code += \"    billing = StripeBilling()\\\n\"\n        code += \"    result = billing.handle_webhook(event['type'], event['data']['object'])\\\n\"\n        code += \"    return {'status': 'ok', 'result': result}\\\n\"\n        print(code)\n\n    def get_pricing_page_data(self):\n        \"\"\"Generate pricing page data.\"\"\"\n        features = {{\n            \"free\": [\n                \"50 requests/day\",\n                \"Basic quality\",\n                \"Community support\",\n                \"Watermarked output\",\n            ],\n            \"starter\": [\n                \"500 requests/day\",\n                \"Standard quality\",\n                \"Email support\",\n                \"No watermark\",\n                \"API access\",\n            ],\n            \"pro\": [\n                \"5,000 requests/day\",\n                \"Highest quality\",\n                \"Priority support\",\n                \"4K resolution, custom transitions\",\n                \"Batch processing\",\n                \"Webhooks\",\n            ],\n            \"enterprise\": [\n                \"50,000 requests/day\",\n                \"Custom model fine-tuning\",\n                \"Dedicated support\",\n                \"SLA guarantee\",\n                \"SSO/SAML\",\n                \"On-premise option\",\n            ],\n        }}\n        return features\n\n\n# --- Demo ---\nprint(\"=== Stripe Billing for ClipCraft ===\\\n\")\n\nbilling = StripeBilling()\n\n# 1. Show pricing tiers\nprint(\"Pricing Tiers:\")\nfeatures = billing.get_pricing_page_data()\nfor tier, plan in billing.plans.items():\n    print(f\"\\\n  {{plan['name']}} - \\${{plan['price']}}/month\")\n    for f in features[tier]:\n        print(f\"    - {{f}}\")\n\n# 2. Create checkout session\nprint(\"\\\n\\\n--- Creating Checkout ---\")\nresult = billing.create_checkout_session(\"user@example.com\", \"pro\")\nprint(f\"Result: {{result}}\")\n\n# 3. Simulate webhook events\nprint(\"\\\n--- Webhook Events ---\")\nevents = [\n    (\"checkout.session.completed\", {{}}),\n    (\"invoice.paid\", {{}}),\n    (\"customer.subscription.updated\", {{}}),\n    (\"invoice.payment_failed\", {{}}),\n    (\"customer.subscription.deleted\", {{}}),\n]\nfor event_type, data in events:\n    print(f\"\\\nEvent: {{event_type}}\")\n    billing.handle_webhook(event_type, data)\n\n# 4. Usage-based billing\nprint(\"\\\n\\\n--- Usage-Based Billing ---\")\nbilling.create_usage_record(\"si_test_123\", 25, \"increment\")\n\n# 5. Show webhook code\nprint(\"\\\n--- Webhook Endpoint Code ---\")\nbilling.get_fastapi_webhook_code()\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Μέρος 15: Κλιμάκωση σε Μαζικό Προϊόν</h2>\n\n<p>Η κλιμάκωση από MVP σε μαζικό προϊόν απαιτεί αρχιτεκτονικές αλλαγές σε κάθε επίπεδο. Εδώ είναι ο πλήρης οδηγός κλιμάκωσης:</p>\n\n<div class=\"code-block-wrapper\">\n<div class=\"code-block-header\"><span>scaling.py &mdash; ClipCraft Scaling Architecture</span></div>\n<pre><code class=\"language-python\">\"\"\"\nScaling ClipCraft from 100 to 1,000,000 users.\nCovers: horizontal scaling, queue-based processing, caching, monitoring.\n\"\"\"\n\nclass ScalingPlaybook:\n    \"\"\"Complete scaling guide from MVP to enterprise.\"\"\"\n\n    def __init__(self, product=\"ClipCraft\"):\n        self.product = product\n\n    def stage_1_mvp(self):\n        \"\"\"Stage 1: 0-100 users, single server.\"\"\"\n        print(\"=== Stage 1: MVP (0-100 users) ===\")\n        print(\"Architecture: Single server, SQLite/PostgreSQL\")\n        print(\"Cost: $5-20/month\")\n        print()\n        print(\"Stack:\")\n        print(\"  - 1x server (Railway/Render free tier)\")\n        print(\"  - SQLite or managed PostgreSQL\")\n        print(\"  - In-memory caching (Python dict)\")\n        print(\"  - Direct API calls (no queue)\")\n        print()\n        print(\"Focus on:\")\n        print(\"  1. Validate product-market fit\")\n        print(\"  2. Get 10 paying customers\")\n        print(\"  3. Measure: response time, error rate, user retention\")\n\n    def stage_2_growth(self):\n        \"\"\"Stage 2: 100-1K users, add resilience.\"\"\"\n        print(\"\\\n=== Stage 2: Growth (100-1,000 users) ===\")\n        print(\"Architecture: App server + Background workers + Redis\")\n        print(\"Cost: $50-200/month\")\n        print()\n        print(\"Changes from Stage 1:\")\n        print(\"  1. Add Redis for caching and rate limiting\")\n        print(\"  2. Move heavy work to background queues (Celery/ARQ)\")\n        print(\"  3. Add health checks and basic monitoring\")\n        print(\"  4. Set up CI/CD pipeline\")\n        print()\n        print(\"docker-compose additions:\")\n        print(\"  redis:\")\n        print(\"    image: redis:7-alpine\")\n        print(\"  worker:\")\n        print(\"    build: .\")\n        print(\"    command: arq app.worker.WorkerSettings\")\n        print(\"    depends_on: [redis]\")\n        print()\n        print(\"Queue pattern for heavy tasks:\")\n        print(\"  @app.post('/generate')\")\n        print(\"  async def generate(request: GenerateRequest):\")\n        print(\"      job_id = str(uuid.uuid4())\")\n        print(\"      await redis.enqueue_job('process_generation',\")\n        print(\"          job_id=job_id, prompt=request.prompt)\")\n        print(\"      return {{'job_id': job_id, 'status': 'processing'}}\")\n        print()\n        print(\"  @app.get('/jobs/{{job_id}}')\")\n        print(\"  async def get_job(job_id: str):\")\n        print(\"      result = await redis.get(f'job:{{job_id}}')\")\n        print(\"      return result or {{'status': 'processing'}}\")\n\n    def stage_3_scale(self):\n        \"\"\"Stage 3: 1K-10K users, horizontal scaling.\"\"\"\n        print(\"\\\n=== Stage 3: Scale (1,000-10,000 users) ===\")\n        print(\"Architecture: Load balancer + Multiple app servers + Queue\")\n        print(\"Cost: $200-1,000/month\")\n        print()\n        print(\"Architecture diagram:\")\n        print(\"  Users -> CDN (Cloudflare)\")\n        print(\"       -> Load Balancer (nginx/ALB)\")\n        print(\"       -> App Server 1 \\\\ \")\n        print(\"       -> App Server 2  |-> PostgreSQL (managed)\")\n        print(\"       -> App Server 3 /    |-> Redis Cluster\")\n        print(\"       -> Worker 1 \\\\         |-> S3 (file storage)\")\n        print(\"       -> Worker 2  |-> Queue (Redis/SQS)\")\n        print(\"       -> Worker 3 /\")\n        print()\n        print(\"Key changes:\")\n        print(\"  1. Multiple app servers behind load balancer\")\n        print(\"  2. Managed database (RDS/Cloud SQL)\")\n        print(\"  3. Separate worker pool for heavy processing\")\n        print(\"  4. CDN for static assets and cached responses\")\n        print(\"  5. Structured logging + APM (Datadog/Grafana)\")\n        print()\n        print(\"nginx load balancer config:\")\n        print(\"  upstream api {{\")\n        print(\"      least_conn;\")\n        print(\"      server app1:8000;\")\n        print(\"      server app2:8000;\")\n        print(\"      server app3:8000;\")\n        print(\"  }}\")\n        print(\"  server {{\")\n        print(\"      listen 80;\")\n        print(\"      location / {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          proxy_set_header X-Real-IP $remote_addr;\")\n        print(\"      }}\")\n        print(\"      location /health {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          access_log off;\")\n        print(\"      }}\")\n        print(\"  }}\")\n\n    def stage_4_mass(self):\n        \"\"\"Stage 4: 10K+ users, enterprise features.\"\"\"\n        print(\"\\\n=== Stage 4: Mass Product (10,000+ users) ===\")\n        print(\"Architecture: Microservices or modular monolith\")\n        print(\"Cost: $1,000-10,000/month\")\n        print()\n        print(\"Enterprise features to add:\")\n        print(\"  1. Multi-tenant architecture (org-level isolation)\")\n        print(\"  2. SSO/SAML authentication\")\n        print(\"  3. Audit logging\")\n        print(\"  4. Data residency (EU/US regions)\")\n        print(\"  5. Custom SLAs and dedicated support\")\n        print(\"  6. API versioning (v1, v2)\")\n        print(\"  7. Webhook system for integrations\")\n        print(\"  8. Admin dashboard with analytics\")\n        print()\n        print(\"Monitoring stack:\")\n        print(\"  - Prometheus + Grafana (metrics)\")\n        print(\"  - Sentry (error tracking)\")\n        print(\"  - PostHog or Mixpanel (product analytics)\")\n        print(\"  - PagerDuty (alerting)\")\n        print()\n        print(\"SLA targets:\")\n        print(\"  - Uptime: 99.9% (8.7 hours downtime/year)\")\n        print(\"  - API latency p99: < 2 seconds\")\n        print(\"  - Error rate: < 0.1%\")\n\n    def growth_metrics(self):\n        \"\"\"Key metrics to track at each stage.\"\"\"\n        print(\"\\\n=== Key Metrics Dashboard ===\")\n        print()\n        metrics = [\n            (\"MRR (Monthly Recurring Revenue)\", \"$0\", \"$500\", \"$5K\", \"$50K+\"),\n            (\"Active Users (DAU)\", \"10\", \"100\", \"1,000\", \"10,000+\"),\n            (\"API Requests/day\", \"100\", \"5K\", \"100K\", \"1M+\"),\n            (\"Error Rate\", \"<5%\", \"<1%\", \"<0.1%\", \"<0.01%\"),\n            (\"P99 Latency\", \"<5s\", \"<3s\", \"<2s\", \"<1s\"),\n            (\"Churn Rate (monthly)\", \"N/A\", \"<10%\", \"<5%\", \"<3%\"),\n        ]\n        print(f\"| {{'Metric':<30}} | {{'Stage 1':<8}} | {{'Stage 2':<8}} | {{'Stage 3':<8}} | {{'Stage 4':<10}} |\")\n        print(f\"|{{'-'*30}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*10}}-|\")\n        for name, *stages in metrics:\n            print(f\"| {{name:<30}} | {{stages[0]:<8}} | {{stages[1]:<8}} | {{stages[2]:<8}} | {{stages[3]:<10}} |\")\n\n# --- Demo ---\nplaybook = ScalingPlaybook()\nplaybook.stage_1_mvp()\nplaybook.stage_2_growth()\nplaybook.stage_3_scale()\nplaybook.stage_4_mass()\nplaybook.growth_metrics()\n\nprint(\"\\\n\" + \"=\"*60)\nprint(f\"Summary: Build ClipCraft as MVP first.\")\nprint(\"Get 10 paying customers before optimizing.\")\nprint(\"Scale horizontally only when metrics demand it.\")\nprint(\"Monitor everything from day one.\")\n</code></pre>\n<button class=\"run-btn\" onclick=\"runPyodideCode(this)\">&#x25B6; Run</button>\n</div>\n\n<h2>Βασικά συμπεράσματα</h2>\n\n<ul>\n<li><strong>Το ασύγχρονο είναι υποχρεωτικό για βίντεο:</strong> Η δημιουργία βίντεο διαρκεί 30 δευτερόλεπτα έως 5 λεπτά. Πρέπει να χρησιμοποιήσεις ουρά εργασιών με επεξεργασία στο παρασκήνιο &mdash; το σύγχρονο HTTP θα κάνει timeout.</li>\n<li><strong>Η παρακολούθηση προόδου χτίζει εμπιστοσύνη:</strong> Οι χρήστες πρέπει να βλέπουν ότι κάτι συμβαίνει. Πρόσφερε τόσο polling endpoints όσο και webhook callbacks για ευελιξία.</li>\n<li><strong>Η λογική επαναπροσπαθειών είναι κρίσιμη:</strong> Οι μακρόχρονες εργασίες GPU αποτυγχάνουν πιο συχνά από τις γρήγορες κλήσεις API. Αυτόματες επαναπροσπάθειες με εκθετική αναμονή κρατούν την υπηρεσία σου αξιόπιστη.</li>\n<li><strong>Έλεγξε τη ταυτόχρονη εκτέλεση για να ελέγξεις τα κόστη:</strong> Περιόρισε τον αριθμό των ταυτόχρονων εργασιών. Κάθε βίντεο κοστίζει $0.10&ndash;$1.00 σε τέλη API &mdash; ανεξέλεγκτη ταυτόχρονη εκτέλεση μπορεί να εξαντλήσει τον προϋπολογισμό σου σε λεπτά.</li>\n<li><strong>Το βίντεο είναι 10&ndash;50 φορές πιο ακριβό από τις εικόνες:</strong> Τιμολόγησε τις βαθμίδες σου ανάλογα. Ένα πλάνο $10/μήνα που επιτρέπει 100 εικόνες μπορεί να επιτρέπει μόνο 10&ndash;20 βίντεο.</li>\n<li><strong>Το FFmpeg είναι ο καλύτερος σου φίλος:</strong> Χρησιμοποίησέ το για μετακωδικοποίηση, αλλαγές ανάλυσης, υδατογραφήματα και μετατροπή μορφής. Λειτουργεί παντού και χειρίζεται κάθε codec.</li>\n</ul>\n\n<p><strong>Επόμενο:</strong> Στο GenAI-3, κατασκευάζεις ένα <strong>SaaS δημιουργίας φωνής και ήχου</strong> &mdash; χειρίζοντας text-to-speech, κλωνοποίηση φωνής και pipelines επεξεργασίας ήχου.</p>\n\n${navButtons('genai-1','genai-3')}"
  },
  "codeBlocks": [
    {
      "id": "block-0",
      "title": "Project Layout",
      "code": "clipcraft/\n├── app/\n│   ├── __init__.py\n│   ├── main.py            # FastAPI application\n│   ├── config.py          # Settings and API keys\n│   ├── models.py          # Pydantic request/response models\n│   ├── video_service.py   # Video generation API wrapper\n│   ├── job_queue.py       # Async job queue system\n│   ├── progress.py        # Progress tracking and webhooks\n│   ├── processing.py      # Video frame processing\n│   ├── billing.py         # Usage tracking and billing\n│   └── database.py        # Database connection\n├── static/\n│   ├── index.html         # Frontend UI\n│   └── style.css\n├── tests/\n│   └── test_api.py\n├── requirements.txt\n├── Dockerfile\n├── docker-compose.yml\n└── .env.example",
      "language": "text",
      "runnable": false
    },
    {
      "id": "block-1",
      "title": "app/main.py",
      "code": "\"\"\"ClipCraft API Server - Video Generation SaaS.\"\"\"\nfrom fastapi import FastAPI, HTTPException, Depends, Header, BackgroundTasks\nfrom fastapi.staticfiles import StaticFiles\nfrom fastapi.responses import JSONResponse\nfrom pydantic import BaseModel, Field\nfrom typing import Optional\nimport uuid\nimport asyncio\nimport time\n\nfrom .config import settings\nfrom .video_service import VideoService\nfrom .job_queue import JobQueue, JobStatus\nfrom .progress import ProgressTracker\nfrom .billing import BillingSystem\n\napp = FastAPI(title=\"ClipCraft API\", version=\"1.0.0\")\napp.mount(\"/static\", StaticFiles(directory=\"static\"), name=\"static\")\n\n# Initialize services\nvideo_service = VideoService(\n    provider=settings.VIDEO_PROVIDER,\n    api_key=settings.VIDEO_API_KEY\n)\njob_queue = JobQueue(max_concurrent=settings.MAX_CONCURRENT_JOBS)\nprogress_tracker = ProgressTracker()\nbilling = BillingSystem()\n\n\n# --- Request/Response Models ---\nclass GenerateRequest(BaseModel):\n    prompt: str = Field(..., min_length=3, max_length=500)\n    style: str = Field(default=\"cinematic\")\n    duration: int = Field(default=5, ge=2, le=15)\n    resolution: str = Field(default=\"1280x720\")\n    webhook_url: Optional[str] = None\n\nclass JobResponse(BaseModel):\n    job_id: str\n    status: str\n    message: str\n    poll_url: str\n\nclass JobStatusResponse(BaseModel):\n    job_id: str\n    status: str\n    progress: int\n    stage: str\n    result_url: Optional[str] = None\n    error: Optional[str] = None\n    estimated_remaining_sec: Optional[int] = None\n\n\n# --- API key authentication ---\nasync def get_user(x_api_key: str = Header(...)):\n    user = billing.get_user_by_key(x_api_key)\n    if not user:\n        raise HTTPException(status_code=401, detail=\"Invalid API key\")\n    return user\n\n\n# --- Background video generation task ---\nasync def process_video_job(job_id: str):\n    \"\"\"Background task that processes a video generation job.\"\"\"\n    job = job_queue.jobs.get(job_id)\n    if not job:\n        return\n\n    try:\n        progress_tracker.update(job_id, 5, \"initializing\",\n                                \"Loading model and preparing generation\")\n\n        # Call the video generation API\n        result = await video_service.generate(\n            prompt=job.prompt,\n            style=job.style,\n            duration_sec=job.duration_sec,\n            resolution=job.resolution,\n            progress_callback=lambda p, s, m:\n                progress_tracker.update(job_id, p, s, m)\n        )\n\n        progress_tracker.update(job_id, 95, \"uploading\",\n                                \"Uploading to CDN\")\n\n        video_url = result[\"video_url\"]\n        job_queue.complete_job(job_id, video_url)\n\n        progress_tracker.update(job_id, 100, \"completed\",\n                                \"Video ready for download\")\n\n        billing.record_generation(\n            job.user_id, job.prompt,\n            result[\"cost\"], job.resolution\n        )\n\n    except Exception as e:\n        msg = job_queue.fail_job(job_id, str(e))\n        progress_tracker.update(job_id, 0, \"failed\", str(e))\n\n\n# --- Endpoints ---\n@app.post(\"/api/generate\", response_model=JobResponse)\nasync def generate_video(req: GenerateRequest,\n                         background_tasks: BackgroundTasks,\n                         user=Depends(get_user)):\n    \"\"\"Submit a video generation job. Returns immediately with a job ID.\"\"\"\n    # Check billing limits\n    can, reason = billing.can_generate(user[\"id\"])\n    if not can:\n        raise HTTPException(status_code=429, detail=reason)\n\n    # Submit to job queue\n    job_id = job_queue.submit(\n        prompt=req.prompt,\n        style=req.style,\n        duration_sec=req.duration,\n        resolution=req.resolution,\n        user_id=user[\"id\"]\n    )\n\n    # Register progress tracking\n    progress_tracker.register_job(job_id, webhook_url=req.webhook_url)\n\n    # Start processing in the background\n    job = job_queue.process_next()\n    if job:\n        background_tasks.add_task(process_video_job, job.id)\n\n    return JobResponse(\n        job_id=job_id,\n        status=\"pending\",\n        message=\"Video generation job submitted\",\n        poll_url=f\"/api/jobs/{job_id}\"\n    )\n\n\n@app.get(\"/api/jobs/{job_id}\", response_model=JobStatusResponse)\nasync def get_job_status(job_id: str, user=Depends(get_user)):\n    \"\"\"Poll for job status and progress.\"\"\"\n    status = job_queue.get_status(job_id)\n    if not status:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n\n    poll_data = progress_tracker.poll(job_id)\n    estimated = None\n    if poll_data and poll_data.get(\"progress\", 0) > 0:\n        pct = poll_data[\"progress\"]\n        if pct < 100:\n            estimated = int((100 - pct) * 1.5)\n\n    return JobStatusResponse(\n        job_id=job_id,\n        status=status[\"status\"],\n        progress=status.get(\"progress\", 0),\n        stage=poll_data.get(\"stage\", \"unknown\") if poll_data else \"unknown\",\n        result_url=status.get(\"result_url\"),\n        error=status.get(\"error\"),\n        estimated_remaining_sec=estimated\n    )\n\n\n@app.delete(\"/api/jobs/{job_id}\")\nasync def cancel_job(job_id: str, user=Depends(get_user)):\n    \"\"\"Cancel a pending or active job.\"\"\"\n    success = job_queue.cancel_job(job_id)\n    if not success:\n        raise HTTPException(status_code=404, detail=\"Job not found\")\n    return {\"message\": \"Job cancelled\", \"job_id\": job_id}\n\n\n@app.get(\"/api/jobs\")\nasync def list_jobs(user=Depends(get_user)):\n    \"\"\"List all jobs for the authenticated user.\"\"\"\n    user_jobs = [\n        job.to_dict() for job in job_queue.jobs.values()\n        if job.user_id == user[\"id\"]\n    ]\n    return {\"jobs\": user_jobs, \"total\": len(user_jobs)}\n\n\n@app.get(\"/api/queue/stats\")\nasync def queue_stats():\n    \"\"\"Get queue statistics (admin endpoint).\"\"\"\n    return job_queue.get_queue_stats()\n\n\n@app.get(\"/api/health\")\nasync def health():\n    return {\n        \"status\": \"ok\",\n        \"provider\": settings.VIDEO_PROVIDER,\n        \"queue\": job_queue.get_queue_stats()\n    }",
      "language": "python",
      "runnable": false
    },
    {
      "id": "block-1b",
      "title": "app/video_service.py",
      "language": "python",
      "runnable": false,
      "code": "\"\"\"Video generation service - wraps Runway ML / Replicate APIs.\"\"\"\nimport asyncio\nimport httpx\nimport os\nimport json\nimport time\nimport uuid\nimport struct\nimport zlib\nfrom pathlib import Path\nfrom typing import Optional, Callable, Dict\n\n\nclass VideoService:\n    \"\"\"Generates videos via Runway ML or Replicate APIs with demo mode fallback.\"\"\"\n\n    def __init__(self, provider: str = \"runway\", api_key: str = \"\",\n                 cache_dir: str = \"./cache/videos\"):\n        self.provider = provider\n        self.api_key = api_key\n        self.cache_dir = Path(cache_dir)\n        self.cache_dir.mkdir(parents=True, exist_ok=True)\n        self.client = httpx.AsyncClient(timeout=300.0)\n        self.demo_mode = not api_key\n\n    async def generate(self, prompt: str, duration_sec: int = 5,\n                       resolution: str = \"1280x720\", style: str = \"cinematic\",\n                       progress_callback: Optional[Callable] = None) -> Dict:\n        \"\"\"Generate a video from a text prompt.\n\n        Returns dict with video_url, duration, resolution, cost.\n        \"\"\"\n        if self.demo_mode:\n            return await self._generate_demo(prompt, duration_sec,\n                                             resolution, progress_callback)\n\n        if self.provider == \"runway\":\n            return await self._generate_runway(prompt, duration_sec,\n                                               resolution, style,\n                                               progress_callback)\n        elif self.provider == \"replicate\":\n            return await self._generate_replicate(prompt, duration_sec,\n                                                  resolution, style,\n                                                  progress_callback)\n        else:\n            raise ValueError(f\"Unknown provider: {self.provider}\")\n\n    async def _generate_demo(self, prompt, duration_sec, resolution,\n                             progress_callback=None):\n        \"\"\"Create a simple test video file when no API key is set.\"\"\"\n        if progress_callback:\n            progress_callback(10, \"preparing\", \"Demo mode: creating test video\")\n\n        width, height = map(int, resolution.split(\"x\"))\n\n        # Simulate generation time\n        for pct in range(20, 90, 15):\n            await asyncio.sleep(0.5)\n            if progress_callback:\n                progress_callback(pct, \"generating\", f\"Demo rendering frame {pct}%\")\n\n        # Create a minimal valid video placeholder file\n        video_id = uuid.uuid4().hex[:8]\n        video_path = self.cache_dir / f\"demo_{video_id}.mp4\"\n\n        # Write a minimal placeholder file\n        video_path.write_bytes(\n            b\"DEMO_VIDEO_PLACEHOLDER:\"\n            + prompt.encode()[:200]\n            + f\":{duration_sec}s:{resolution}\".encode()\n        )\n\n        if progress_callback:\n            progress_callback(95, \"finalizing\", \"Demo video ready\")\n\n        return {\n            \"video_url\": f\"/cache/videos/{video_path.name}\",\n            \"video_path\": str(video_path),\n            \"duration\": duration_sec,\n            \"resolution\": resolution,\n            \"cost\": 0.0,\n            \"demo\": True,\n        }\n\n    async def _generate_runway(self, prompt, duration_sec, resolution,\n                               style, progress_callback=None):\n        \"\"\"Generate video using Runway ML Gen-3 API.\"\"\"\n        if progress_callback:\n            progress_callback(10, \"submitting\", \"Sending to Runway ML API\")\n\n        response = await self.client.post(\n            \"https://api.runwayml.com/v1/generations\",\n            headers={\n                \"Authorization\": f\"Bearer {self.api_key}\",\n                \"Content-Type\": \"application/json\",\n            },\n            json={\n                \"prompt\": prompt,\n                \"duration\": duration_sec,\n                \"resolution\": resolution,\n                \"style\": style,\n            }\n        )\n        response.raise_for_status()\n        job = response.json()\n        job_id = job[\"id\"]\n\n        # Poll for completion\n        result = await self._poll_job(\n            f\"https://api.runwayml.com/v1/generations/{job_id}\",\n            {\"Authorization\": f\"Bearer {self.api_key}\"},\n            progress_callback\n        )\n        return result\n\n    async def _generate_replicate(self, prompt, duration_sec, resolution,\n                                  style, progress_callback=None):\n        \"\"\"Generate video using Replicate API.\"\"\"\n        if progress_callback:\n            progress_callback(10, \"submitting\", \"Sending to Replicate API\")\n\n        response = await self.client.post(\n            \"https://api.replicate.com/v1/predictions\",\n            headers={\n                \"Authorization\": f\"Token {self.api_key}\",\n                \"Content-Type\": \"application/json\",\n            },\n            json={\n                \"version\": \"latest\",\n                \"input\": {\n                    \"prompt\": f\"{style}: {prompt}\",\n                    \"num_frames\": duration_sec * 24,\n                    \"width\": int(resolution.split(\"x\")[0]),\n                    \"height\": int(resolution.split(\"x\")[1]),\n                }\n            }\n        )\n        response.raise_for_status()\n        prediction = response.json()\n\n        result = await self._poll_job(\n            prediction[\"urls\"][\"get\"],\n            {\"Authorization\": f\"Token {self.api_key}\"},\n            progress_callback\n        )\n        return result\n\n    async def _poll_job(self, url, headers, progress_callback=None,\n                        timeout: int = 600):\n        \"\"\"Poll a generation job until completion.\"\"\"\n        start = time.time()\n        while time.time() - start < timeout:\n            resp = await self.client.get(url, headers=headers)\n            resp.raise_for_status()\n            status = resp.json()\n\n            if status.get(\"status\") == \"succeeded\":\n                output = status.get(\"output\", {})\n                return {\n                    \"video_url\": output.get(\"url\", output.get(\"video\")),\n                    \"duration\": status.get(\"duration\", 5),\n                    \"resolution\": status.get(\"resolution\", \"1280x720\"),\n                    \"cost\": status.get(\"cost\", 0.05),\n                }\n            elif status.get(\"status\") == \"failed\":\n                raise RuntimeError(f\"Generation failed: {status.get('error')}\")\n\n            pct = status.get(\"progress\", 0)\n            if progress_callback and pct:\n                progress_callback(int(pct * 100), \"generating\",\n                                  \"Rendering video frames\")\n\n            await asyncio.sleep(5)\n\n        raise TimeoutError(\"Video generation timed out\")\n\n    def get_status(self, job_id: str) -> Dict:\n        \"\"\"Get the status of a generation job (sync wrapper).\"\"\"\n        return {\"job_id\": job_id, \"status\": \"unknown\",\n                \"message\": \"Use async polling for real-time status\"}\n"
    },
    {
      "id": "block-1c",
      "title": "app/job_queue.py",
      "language": "python",
      "runnable": false,
      "code": "\"\"\"Job queue for managing asynchronous video generation jobs.\"\"\"\nimport sqlite3\nimport time\nimport uuid\nfrom enum import Enum\nfrom typing import Optional, Dict, Any, List\nfrom dataclasses import dataclass, field\n\n\nclass JobStatus(str, Enum):\n    PENDING = \"pending\"\n    PROCESSING = \"processing\"\n    COMPLETED = \"completed\"\n    FAILED = \"failed\"\n    CANCELLED = \"cancelled\"\n\n\n@dataclass\nclass Job:\n    id: str\n    user_id: str\n    prompt: str\n    style: str\n    duration_sec: int\n    resolution: str\n    status: JobStatus = JobStatus.PENDING\n    result_url: Optional[str] = None\n    error: Optional[str] = None\n    created_at: float = field(default_factory=time.time)\n    completed_at: Optional[float] = None\n    webhook_url: Optional[str] = None\n\n\nclass JobQueue:\n    \"\"\"Manages video generation jobs with in-memory queue and SQLite persistence.\"\"\"\n\n    def __init__(self, max_concurrent: int = 3, db_path: str = \"jobs.db\"):\n        self.max_concurrent = max_concurrent\n        self.jobs: Dict[str, Job] = {}\n        self.queue: List[str] = []\n        self.active: List[str] = []\n        self.db_path = db_path\n        self._init_db()\n\n    def _init_db(self):\n        \"\"\"Create SQLite table for job persistence.\"\"\"\n        conn = sqlite3.connect(self.db_path)\n        conn.execute(\"\"\"\n            CREATE TABLE IF NOT EXISTS jobs (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                prompt TEXT NOT NULL,\n                style TEXT,\n                duration_sec INTEGER,\n                resolution TEXT,\n                status TEXT NOT NULL,\n                result_url TEXT,\n                error TEXT,\n                created_at REAL NOT NULL,\n                completed_at REAL,\n                webhook_url TEXT\n            )\n        \"\"\")\n        conn.commit()\n        conn.close()\n\n    def submit(self, job_data: Dict[str, Any]) -> Job:\n        \"\"\"Submit a new job to the queue. Returns the Job object.\"\"\"\n        job_id = str(uuid.uuid4())\n        job = Job(\n            id=job_id,\n            user_id=job_data[\"user_id\"],\n            prompt=job_data[\"prompt\"],\n            style=job_data.get(\"style\", \"cinematic\"),\n            duration_sec=job_data.get(\"duration_sec\", 5),\n            resolution=job_data.get(\"resolution\", \"1280x720\"),\n            webhook_url=job_data.get(\"webhook_url\"),\n        )\n        self.jobs[job_id] = job\n        self.queue.append(job_id)\n        self._persist_job(job)\n        return job\n\n    def process_next(self) -> Optional[Job]:\n        \"\"\"Move the next queued job to active processing.\n\n        Returns the Job if one was available, None otherwise.\n        \"\"\"\n        if len(self.active) >= self.max_concurrent:\n            return None\n        if not self.queue:\n            return None\n\n        job_id = self.queue.pop(0)\n        job = self.jobs[job_id]\n        job.status = JobStatus.PROCESSING\n        self.active.append(job_id)\n        self._persist_job(job)\n        return job\n\n    def complete_job(self, job_id: str, result_url: str) -> str:\n        \"\"\"Mark a job as completed with its result URL.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            raise ValueError(f\"Job {job_id} not found\")\n        job.status = JobStatus.COMPLETED\n        job.result_url = result_url\n        job.completed_at = time.time()\n        if job_id in self.active:\n            self.active.remove(job_id)\n        self._persist_job(job)\n        return f\"Job {job_id} completed\"\n\n    def fail_job(self, job_id: str, error: str) -> str:\n        \"\"\"Mark a job as failed with an error message.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            raise ValueError(f\"Job {job_id} not found\")\n        job.status = JobStatus.FAILED\n        job.error = error\n        job.completed_at = time.time()\n        if job_id in self.active:\n            self.active.remove(job_id)\n        self._persist_job(job)\n        return f\"Job {job_id} failed: {error}\"\n\n    def get_status(self, job_id: str) -> Dict:\n        \"\"\"Get current status of a job.\"\"\"\n        job = self.jobs.get(job_id)\n        if not job:\n            # Try loading from SQLite\n            job = self._load_job(job_id)\n        if not job:\n            return {\"error\": \"Job not found\"}\n        return {\n            \"job_id\": job.id,\n            \"status\": job.status.value,\n            \"result_url\": job.result_url,\n            \"error\": job.error,\n            \"created_at\": job.created_at,\n            \"completed_at\": job.completed_at,\n        }\n\n    def get_stats(self) -> Dict:\n        \"\"\"Return queue statistics.\"\"\"\n        return {\n            \"queued\": len(self.queue),\n            \"active\": len(self.active),\n            \"max_concurrent\": self.max_concurrent,\n            \"total_jobs\": len(self.jobs),\n            \"completed\": sum(1 for j in self.jobs.values()\n                             if j.status == JobStatus.COMPLETED),\n            \"failed\": sum(1 for j in self.jobs.values()\n                          if j.status == JobStatus.FAILED),\n        }\n\n    def _persist_job(self, job: Job):\n        \"\"\"Save job state to SQLite.\"\"\"\n        conn = sqlite3.connect(self.db_path)\n        conn.execute(\"\"\"\n            INSERT OR REPLACE INTO jobs\n            (id, user_id, prompt, style, duration_sec, resolution,\n             status, result_url, error, created_at, completed_at, webhook_url)\n            VALUES (?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?, ?)\n        \"\"\", (job.id, job.user_id, job.prompt, job.style, job.duration_sec,\n              job.resolution, job.status.value, job.result_url, job.error,\n              job.created_at, job.completed_at, job.webhook_url))\n        conn.commit()\n        conn.close()\n\n    def _load_job(self, job_id: str) -> Optional[Job]:\n        \"\"\"Load a job from SQLite.\"\"\"\n        conn = sqlite3.connect(self.db_path)\n        conn.row_factory = sqlite3.Row\n        row = conn.execute(\"SELECT * FROM jobs WHERE id = ?\", (job_id,)).fetchone()\n        conn.close()\n        if not row:\n            return None\n        job = Job(\n            id=row[\"id\"], user_id=row[\"user_id\"], prompt=row[\"prompt\"],\n            style=row[\"style\"], duration_sec=row[\"duration_sec\"],\n            resolution=row[\"resolution\"],\n            status=JobStatus(row[\"status\"]),\n            result_url=row[\"result_url\"], error=row[\"error\"],\n            created_at=row[\"created_at\"], completed_at=row[\"completed_at\"],\n            webhook_url=row[\"webhook_url\"],\n        )\n        self.jobs[job_id] = job\n        return job\n"
    },
    {
      "id": "block-1d",
      "title": "app/progress.py",
      "language": "python",
      "runnable": false,
      "code": "\"\"\"Progress tracking for long-running video generation jobs.\"\"\"\nimport time\nfrom typing import Optional, Dict\nfrom dataclasses import dataclass, field\n\n\n@dataclass\nclass ProgressState:\n    job_id: str\n    percent: int = 0\n    stage: str = \"queued\"\n    message: str = \"\"\n    started_at: float = field(default_factory=time.time)\n    updated_at: float = field(default_factory=time.time)\n    webhook_url: Optional[str] = None\n\n\nclass ProgressTracker:\n    \"\"\"Tracks progress of video generation jobs for polling and webhooks.\"\"\"\n\n    def __init__(self):\n        self.states: Dict[str, ProgressState] = {}\n\n    def register_job(self, job_id: str, webhook_url: Optional[str] = None):\n        \"\"\"Register a new job for progress tracking.\"\"\"\n        self.states[job_id] = ProgressState(\n            job_id=job_id,\n            webhook_url=webhook_url\n        )\n\n    def update(self, job_id: str, percent: int, stage: str,\n               message: str = \"\"):\n        \"\"\"Update the progress of a job.\"\"\"\n        state = self.states.get(job_id)\n        if not state:\n            state = ProgressState(job_id=job_id)\n            self.states[job_id] = state\n        state.percent = min(percent, 100)\n        state.stage = stage\n        state.message = message\n        state.updated_at = time.time()\n\n    def poll(self, job_id: str) -> Dict:\n        \"\"\"Get current progress for a job (used by polling clients).\"\"\"\n        state = self.states.get(job_id)\n        if not state:\n            return {\"job_id\": job_id, \"error\": \"Job not found\"}\n\n        elapsed = time.time() - state.started_at\n        remaining = None\n        if 0 < state.percent < 100:\n            rate = state.percent / elapsed\n            remaining = int((100 - state.percent) / rate) if rate > 0 else None\n\n        return {\n            \"job_id\": job_id,\n            \"percent\": state.percent,\n            \"stage\": state.stage,\n            \"message\": state.message,\n            \"elapsed_sec\": round(elapsed, 1),\n            \"estimated_remaining_sec\": remaining,\n        }\n\n    def get_webhook_url(self, job_id: str) -> Optional[str]:\n        \"\"\"Return the webhook URL for a job, if configured.\"\"\"\n        state = self.states.get(job_id)\n        return state.webhook_url if state else None\n"
    },
    {
      "id": "block-1e",
      "title": "app/billing.py",
      "language": "python",
      "runnable": false,
      "code": "\"\"\"Billing system for ClipCraft video generation SaaS.\"\"\"\nimport sqlite3\nimport uuid\nimport time\nfrom typing import Optional, Tuple, Dict\n\n\nclass BillingSystem:\n    \"\"\"Manages user accounts, API keys, usage tracking, and video tier limits.\"\"\"\n\n    TIERS = {\n        \"free\": {\n            \"monthly_videos\": 3,\n            \"max_resolution\": \"720p\",\n            \"max_duration_sec\": 5,\n            \"price_per_month\": 0.00,\n            \"watermark\": True,\n            \"max_resolution_px\": 720,\n        },\n        \"starter\": {\n            \"monthly_videos\": 30,\n            \"max_resolution\": \"1080p\",\n            \"max_duration_sec\": 10,\n            \"price_per_month\": 19.99,\n            \"watermark\": False,\n            \"max_resolution_px\": 1080,\n        },\n        \"pro\": {\n            \"monthly_videos\": 200,\n            \"max_resolution\": \"4K\",\n            \"max_duration_sec\": 15,\n            \"price_per_month\": 49.99,\n            \"watermark\": False,\n            \"max_resolution_px\": 2160,\n        },\n        \"enterprise\": {\n            \"monthly_videos\": -1,  # unlimited\n            \"max_resolution\": \"4K\",\n            \"max_duration_sec\": 30,\n            \"price_per_month\": 149.99,\n            \"watermark\": False,\n            \"max_resolution_px\": 2160,\n        }\n    }\n\n    def __init__(self, db_path: str = \"billing.db\"):\n        self.db_path = db_path\n        self.conn = sqlite3.connect(db_path)\n        self.conn.row_factory = sqlite3.Row\n        self._init_db()\n\n    def _init_db(self):\n        \"\"\"Create tables if they do not exist.\"\"\"\n        self.conn.executescript(\"\"\"\n            CREATE TABLE IF NOT EXISTS users (\n                id TEXT PRIMARY KEY,\n                email TEXT UNIQUE NOT NULL,\n                api_key TEXT UNIQUE NOT NULL,\n                tier TEXT NOT NULL DEFAULT 'free',\n                created_at REAL NOT NULL\n            );\n            CREATE TABLE IF NOT EXISTS generations (\n                id TEXT PRIMARY KEY,\n                user_id TEXT NOT NULL,\n                prompt TEXT,\n                cost REAL DEFAULT 0.0,\n                resolution TEXT,\n                duration_sec INTEGER,\n                created_at REAL NOT NULL,\n                FOREIGN KEY (user_id) REFERENCES users(id)\n            );\n        \"\"\")\n        self.conn.commit()\n\n    def register_user(self, email: str, tier: str = \"free\") -> Dict:\n        \"\"\"Register a new user and return their account details.\"\"\"\n        if tier not in self.TIERS:\n            raise ValueError(f\"Unknown tier: {tier}\")\n        user_id = str(uuid.uuid4())\n        api_key = f\"ck_{uuid.uuid4().hex}\"\n        self.conn.execute(\n            \"INSERT INTO users (id, email, api_key, tier, created_at) VALUES (?, ?, ?, ?, ?)\",\n            (user_id, email, api_key, tier, time.time())\n        )\n        self.conn.commit()\n        return {\"id\": user_id, \"email\": email, \"api_key\": api_key, \"tier\": tier}\n\n    def get_user_by_key(self, api_key: str) -> Optional[Dict]:\n        \"\"\"Look up a user by API key.\"\"\"\n        row = self.conn.execute(\n            \"SELECT * FROM users WHERE api_key = ?\", (api_key,)\n        ).fetchone()\n        return dict(row) if row else None\n\n    def can_generate(self, user_id: str) -> Tuple[bool, str]:\n        \"\"\"Check whether the user can generate another video this month.\"\"\"\n        user = self.conn.execute(\n            \"SELECT tier FROM users WHERE id = ?\", (user_id,)\n        ).fetchone()\n        if not user:\n            return False, \"User not found\"\n        tier = self.TIERS[user[\"tier\"]]\n        if tier[\"monthly_videos\"] == -1:\n            return True, \"Unlimited\"\n        month_start = time.time() - 30 * 86400\n        count = self.conn.execute(\n            \"SELECT COUNT(*) FROM generations WHERE user_id = ? AND created_at > ?\",\n            (user_id, month_start)\n        ).fetchone()[0]\n        if count >= tier[\"monthly_videos\"]:\n            return False, f\"Monthly limit reached ({tier['monthly_videos']} videos)\"\n        return True, f\"{tier['monthly_videos'] - count} videos remaining\"\n\n    def record_generation(self, user_id: str, prompt: str,\n                          cost: float, resolution: str,\n                          duration_sec: int = 5):\n        \"\"\"Record a video generation event.\"\"\"\n        self.conn.execute(\n            \"INSERT INTO generations (id, user_id, prompt, cost, resolution, duration_sec, created_at) \"\n            \"VALUES (?, ?, ?, ?, ?, ?, ?)\",\n            (str(uuid.uuid4()), user_id, prompt, cost, resolution, duration_sec, time.time())\n        )\n        self.conn.commit()\n\n    def get_user_stats(self, user_id: str) -> Dict:\n        \"\"\"Return usage statistics for the user.\"\"\"\n        user = self.conn.execute(\n            \"SELECT * FROM users WHERE id = ?\", (user_id,)\n        ).fetchone()\n        if not user:\n            return {}\n        tier = self.TIERS[user[\"tier\"]]\n        month_start = time.time() - 30 * 86400\n        row = self.conn.execute(\n            \"SELECT COUNT(*) as count, COALESCE(SUM(cost), 0) as total_cost \"\n            \"FROM generations WHERE user_id = ? AND created_at > ?\",\n            (user_id, month_start)\n        ).fetchone()\n        return {\n            \"user_id\": user_id,\n            \"tier\": user[\"tier\"],\n            \"videos_this_month\": row[\"count\"],\n            \"monthly_limit\": tier[\"monthly_videos\"],\n            \"max_resolution\": tier[\"max_resolution\"],\n            \"max_duration_sec\": tier[\"max_duration_sec\"],\n            \"total_cost\": round(row[\"total_cost\"], 2),\n            \"watermark\": tier[\"watermark\"],\n        }\n"
    },
    {
      "id": "block-2",
      "title": "app/config.py",
      "code": "\"\"\"Configuration loaded from environment variables.\"\"\"\nfrom pydantic_settings import BaseSettings\n\nclass Settings(BaseSettings):\n    VIDEO_PROVIDER: str = \"runway\"  # \"runway\", \"pika\", or \"stability\"\n    VIDEO_API_KEY: str = \"\"\n    DATABASE_URL: str = \"sqlite:///./clipcraft.db\"\n    MAX_CONCURRENT_JOBS: int = 3\n    MAX_VIDEO_DURATION: int = 15  # seconds\n    STORAGE_BUCKET: str = \"clipcraft-videos\"\n    WEBHOOK_SECRET: str = \"\"\n\n    class Config:\n        env_file = \".env\"\n\nsettings = Settings()",
      "language": "python",
      "runnable": false
    },
    {
      "id": "block-3",
      "title": ".env.example",
      "code": "VIDEO_PROVIDER=runway\nVIDEO_API_KEY=your_api_key_here\nDATABASE_URL=sqlite:///./clipcraft.db\nMAX_CONCURRENT_JOBS=3\nMAX_VIDEO_DURATION=15\nWEBHOOK_SECRET=your_webhook_secret",
      "language": "bash",
      "runnable": false
    },
    {
      "id": "block-4",
      "title": "requirements.txt",
      "code": "fastapi==0.109.0\nuvicorn==0.27.0\nhttpx==0.26.0\npydantic==2.5.3\npydantic-settings==2.1.0\npython-multipart==0.0.6\naiofiles==23.2.1",
      "language": "text",
      "runnable": false
    },
    {
      "id": "block-5",
      "title": "Dockerfile",
      "code": "FROM python:3.11-slim\n\n# Install FFmpeg for video processing\nRUN apt-get update && \\\\\n    apt-get install -y --no-install-recommends ffmpeg && \\\\\n    rm -rf /var/lib/apt/lists/*\n\nWORKDIR /app\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nCOPY . .\n\nEXPOSE 8000\nCMD [\"uvicorn\", \"app.main:app\", \"--host\", \"0.0.0.0\", \"--port\", \"8000\"]",
      "language": "dockerfile",
      "runnable": false
    },
    {
      "id": "block-6",
      "title": "docker-compose.yml",
      "code": "version: \"3.8\"\nservices:\n  clipcraft:\n    build: .\n    ports:\n      - \"8000:8000\"\n    env_file:\n      - .env\n    volumes:\n      - ./videos:/app/videos\n      - ./static:/app/static\n    deploy:\n      resources:\n        limits:\n          memory: 2G",
      "language": "yaml",
      "runnable": false
    },
    {
      "id": "block-7",
      "title": "Terminal",
      "code": "# Development\npip install -r requirements.txt\nuvicorn app.main:app --reload\n\n# Or with Docker\ndocker-compose up --build\n\n# Submit a video generation job\ncurl -X POST http://localhost:8000/api/generate \\\\\n  -H \"X-API-Key: your_key\" \\\\\n  -H \"Content-Type: application/json\" \\\\\n  -d '{\"prompt\": \"sunset over ocean waves\", \"style\": \"cinematic\", \"duration\": 5}'\n\n# Poll for progress\ncurl http://localhost:8000/api/jobs/JOB_ID \\\\\n  -H \"X-API-Key: your_key\"",
      "language": "bash",
      "runnable": false
    },
    {
      "id": "block-8",
      "title": "static/index.html (key parts)",
      "code": "<!DOCTYPE html>\n<html>\n<head>\n  <title>ClipCraft - AI Video Generation</title>\n  <style>\n    body { font-family: system-ui; max-width: 800px; margin: 0 auto; padding: 20px;\n           background: #0f0f0f; color: #e5e5e5; }\n    h1 { background: linear-gradient(135deg, #7c3aed, #a855f7);\n         -webkit-background-clip: text; -webkit-text-fill-color: transparent; }\n    .prompt-input { width: 100%; padding: 14px; font-size: 16px;\n                    border: 2px solid #333; border-radius: 10px;\n                    background: #1a1a1a; color: #e5e5e5; margin: 10px 0; }\n    .controls { display: flex; gap: 12px; margin: 12px 0; align-items: center; }\n    .controls select { padding: 10px; border-radius: 8px; background: #1a1a1a;\n                        color: #e5e5e5; border: 2px solid #333; }\n    .generate-btn { padding: 14px 32px; background: #7c3aed; color: white;\n                    border: none; border-radius: 10px; font-size: 16px;\n                    cursor: pointer; }\n    .generate-btn:disabled { opacity: 0.5; cursor: not-allowed; }\n    .progress-bar { width: 100%; height: 8px; background: #333;\n                    border-radius: 4px; margin: 16px 0; display: none; }\n    .progress-fill { height: 100%; background: linear-gradient(90deg, #7c3aed, #a855f7);\n                     border-radius: 4px; transition: width 0.3s; width: 0%; }\n    .status-text { color: #a78bfa; font-size: 14px; margin: 8px 0; }\n    .result video { width: 100%; border-radius: 12px; margin: 20px 0; }\n  </style>\n</head>\n<body>\n  <h1>ClipCraft</h1>\n  <p>Generate videos from text descriptions</p>\n  <input class=\"prompt-input\" placeholder=\"Describe the video you want...\" id=\"prompt\">\n  <div class=\"controls\">\n    <select id=\"style\">\n      <option value=\"cinematic\">Cinematic</option>\n      <option value=\"anime\">Anime</option>\n      <option value=\"drone\">Drone Shot</option>\n      <option value=\"macro\">Macro</option>\n    </select>\n    <select id=\"duration\">\n      <option value=\"3\">3 seconds</option>\n      <option value=\"5\" selected>5 seconds</option>\n      <option value=\"10\">10 seconds</option>\n    </select>\n    <button class=\"generate-btn\" id=\"genBtn\" onclick=\"generate()\">Generate Video</button>\n  </div>\n  <div class=\"progress-bar\" id=\"progressBar\">\n    <div class=\"progress-fill\" id=\"progressFill\"></div>\n  </div>\n  <p class=\"status-text\" id=\"statusText\"></p>\n  <div class=\"result\" id=\"result\"></div>\n\n  <script>\n  let pollInterval = null;\n\n  async function generate() {\n    const prompt = document.getElementById('prompt').value;\n    const style = document.getElementById('style').value;\n    const duration = parseInt(document.getElementById('duration').value);\n    const btn = document.getElementById('genBtn');\n    btn.disabled = true;\n    document.getElementById('progressBar').style.display = 'block';\n\n    const res = await fetch('/api/generate', {\n      method: 'POST',\n      headers: {'Content-Type': 'application/json', 'X-API-Key': 'demo'},\n      body: JSON.stringify({prompt, style, duration})\n    });\n    const data = await res.json();\n    pollForResult(data.job_id);\n  }\n\n  function pollForResult(jobId) {\n    pollInterval = setInterval(async () => {\n      const res = await fetch('/api/jobs/' + jobId,\n        {headers: {'X-API-Key': 'demo'}});\n      const data = await res.json();\n      document.getElementById('progressFill').style.width = data.progress + '%';\n      document.getElementById('statusText').textContent =\n        data.stage + ' (' + data.progress + '%)';\n      if (data.status === 'completed') {\n        clearInterval(pollInterval);\n        document.getElementById('result').innerHTML =\n          '<video controls autoplay src=\"' + data.result_url + '\"></video>';\n        document.getElementById('genBtn').disabled = false;\n      } else if (data.status === 'failed') {\n        clearInterval(pollInterval);\n        document.getElementById('statusText').textContent = 'Error: ' + data.error;\n        document.getElementById('genBtn').disabled = false;\n      }\n    }, 2000);\n  }\n  </script>\n</body>\n</html>",
      "language": "html",
      "runnable": false
    },
    {
      "id": "block-9",
      "title": "serve_own_model.py &mdash; Self-hosted Video Generation",
      "code": "\"\"\"\nSelf-hosting video generation models for ClipCraft.\nOptions: CogVideoX, Mochi-1, AnimateDiff, SVD (Stable Video Diffusion).\nRequires: GPU with >= 24GB VRAM (A100/A10G or RTX 4090).\n\"\"\"\n\nclass LocalVideoGenerator:\n    \"\"\"Generate videos using locally hosted models.\"\"\"\n\n    def __init__(self, model=\"THUDM/CogVideoX-5b\"):\n        self.model = model\n        self.device = \"cuda\"\n\n    def load_model(self):\n        \"\"\"Load video generation model. Takes 30-60s, needs 24GB+ VRAM.\"\"\"\n        # In production:\n        # import torch\n        # from diffusers import CogVideoXPipeline\n        # self.pipe = CogVideoXPipeline.from_pretrained(\n        #     self.model, torch_dtype=torch.float16\n        # ).to(self.device)\n        # self.pipe.enable_model_cpu_offload()\n        print(f\"Loaded {self.model}\")\n        print(f\"VRAM: ~20GB (fp16), Generation: ~2min per 6s clip\")\n\n    def generate(self, prompt, num_frames=49, fps=8, width=720, height=480):\n        \"\"\"Generate a video clip from text.\"\"\"\n        # In production:\n        # video = self.pipe(\n        #     prompt=prompt,\n        #     num_frames=num_frames,\n        #     guidance_scale=6,\n        #     num_inference_steps=50,\n        # ).frames[0]\n        # from diffusers.utils import export_to_video\n        # export_to_video(video, \"output.mp4\", fps=fps)\n\n        duration = num_frames / fps\n        print(f\"Generating {duration:.1f}s video: \\\\\"{prompt}\\\\\"\")\n        print(f\"  {num_frames} frames at {fps}fps, {width}x{height}\")\n        print(f\"  Estimated time: ~{num_frames * 2.5:.0f}s on A100\")\n        return {\"status\": \"generated\", \"duration\": duration, \"cost\": 0.0}\n\nclass RunwayMLClient:\n    \"\"\"Fallback: Use Runway ML API for production quality.\"\"\"\n\n    def __init__(self, api_key=\"your-runway-key\"):\n        self.api_key = api_key\n        self.base_url = \"https://api.runwayml.com/v1\"\n\n    def generate(self, prompt, duration=4):\n        \"\"\"Generate video via Runway Gen-3 Alpha API.\"\"\"\n        # import requests\n        # r = requests.post(f\"{self.base_url}/generate\",\n        #     headers={\"Authorization\": f\"Bearer {self.api_key}\"},\n        #     json={\"prompt\": prompt, \"duration\": duration})\n        cost = duration * 0.05  # ~$0.05 per second\n        print(f\"Runway API: \\\\\"{prompt}\\\\\" ({duration}s)\")\n        print(f\"  Cost: \\${cost:.2f}\")\n        return {\"status\": \"queued\", \"cost\": cost}\n\n\n# --- Demo ---\nprint(\"=== Self-Hosted Video Generation ===\\\\n\")\ngen = LocalVideoGenerator()\ngen.load_model()\nresult = gen.generate(\"A cat walking through a garden, cinematic\", num_frames=49)\nprint(f\"\\\\n  Result: {result}\\\\n\")\n\n# Cost comparison\nprint(\"=== Cost Comparison ===\")\nprint(\"| Method            | Per 4s clip | 1K clips/mo | GPU Required  |\")\nprint(\"|-------------------|------------|-------------|---------------|\")\nprint(\"| Runway Gen-3      | $0.20      | $200        | None (API)    |\")\nprint(\"| Replicate SVD     | $0.10      | $100        | None (API)    |\")\nprint(\"| Self-host CogVideo| ~$0.02     | ~$20+$300/mo| A100 40GB     |\")\nprint(\"| Self-host AnimDiff| ~$0.01     | ~$10+$150/mo| RTX 4090 24GB |\")\nprint(\"\\\\nBreakeven: ~500 clips/month for self-hosting\")",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-10",
      "title": "deployment_options.py &mdash; ClipCraft Deployment Guide",
      "code": "\"\"\"\nDeployment options for ClipCraft - from hobby to enterprise scale.\nEach option includes: setup commands, estimated costs, and pros/cons.\n\"\"\"\n\nclass DeploymentGuide:\n    \"\"\"Compare deployment platforms for your SaaS.\"\"\"\n\n    def __init__(self, app_name=\"{product_name.lower()}\"):\n        self.app_name = app_name\n\n    def railway(self):\n        \"\"\"Railway.app - Easiest PaaS deployment.\"\"\"\n        print(\"=== Railway.app ===\")\n        print(\"Best for: MVPs, startups, small-to-medium scale\")\n        print(\"Cost: $5/mo hobby, ~$20-50/mo production\")\n        print()\n        print(\"Setup commands:\")\n        print(f\"  npm install -g @railway/cli\")\n        print(f\"  railway login\")\n        print(f\"  railway init\")\n        print(f\"  railway up  # deploys from Dockerfile\")\n        print()\n        print(\"railway.toml:\")\n        print(f'  [build]')\n        print(f'  builder = \"dockerfile\"')\n        print(f'  dockerfilePath = \"Dockerfile\"')\n        print()\n        print(f'  [deploy]')\n        print(f'  healthcheckPath = \"/health\"')\n        print(f'  healthcheckTimeout = 30')\n        print(f'  restartPolicyType = \"on_failure\"')\n        print()\n        print(\"Pros: Auto-scaling, free PostgreSQL, easy env vars\")\n        print(\"Cons: No GPU support, cold starts possible\")\n\n    def render(self):\n        \"\"\"Render.com - Good middle ground.\"\"\"\n        print(\"\\\\n=== Render.com ===\")\n        print(\"Best for: Production apps, auto-scaling needed\")\n        print(\"Cost: Free tier available, $7/mo starter, ~$25/mo production\")\n        print()\n        print(\"render.yaml (Infrastructure as Code):\")\n        print(f\"  services:\")\n        print(f\"    - type: web\")\n        print(f\"      name: {self.app_name}\")\n        print(f\"      runtime: docker\")\n        print(f\"      plan: starter\")\n        print(f\"      healthCheckPath: /health\")\n        print(f\"      envVars:\")\n        print(f\"        - key: DATABASE_URL\")\n        print(f\"          fromDatabase:\")\n        print(f\"            name: {self.app_name}-db\")\n        print(f\"            property: connectionString\")\n        print(f\"    - type: worker\")\n        print(f\"      name: {self.app_name}-worker\")\n        print(f\"      runtime: docker\")\n        print(f\"      dockerfilePath: ./Dockerfile.worker\")\n        print()\n        print(\"  databases:\")\n        print(f\"    - name: {self.app_name}-db\")\n        print(f\"      plan: starter\")\n        print()\n        print(\"Pros: Free SSL, managed PostgreSQL, auto-deploy from Git\")\n        print(\"Cons: No GPU, slower builds than Railway\")\n\n    def aws(self):\n        \"\"\"AWS - Full enterprise deployment.\"\"\"\n        print(\"\\\\n=== AWS (ECS + Fargate) ===\")\n        print(\"Best for: Enterprise, GPU workloads, full control\")\n        print(\"Cost: ~$50-500/mo depending on scale\")\n        print()\n        print(\"Architecture:\")\n        print(\"  ALB -> ECS Fargate -> RDS PostgreSQL\")\n        print(\"         |-> ElastiCache (Redis)\")\n        print(\"         |-> S3 (file storage)\")\n        print(\"         |-> SQS (job queue)\")\n        print()\n        print(\"Setup (using AWS CDK):\")\n        print(f\"  npm install -g aws-cdk\")\n        print(f\"  cdk init app --language python\")\n        print(f\"  # Define ECS service, RDS, Redis in stack\")\n        print(f\"  cdk deploy\")\n        print()\n        print(\"For GPU (e.g., self-hosted models):\")\n        print(f\"  # Use EC2 with g5.xlarge (A10G GPU, ~$1.00/hr)\")\n        print(f\"  # Or SageMaker endpoints for managed inference\")\n        print()\n        print(\"Pros: GPU support, infinite scale, enterprise features\")\n        print(\"Cons: Complex setup, higher cost, AWS expertise needed\")\n\n    def digitalocean(self):\n        \"\"\"DigitalOcean App Platform.\"\"\"\n        print(\"\\\\n=== DigitalOcean App Platform ===\")\n        print(\"Best for: Simple deployments, predictable pricing\")\n        print(\"Cost: $5/mo basic, $12/mo pro, GPU droplets ~$2.50/hr\")\n        print()\n        print(\".do/app.yaml:\")\n        print(f\"  name: {self.app_name}\")\n        print(f\"  services:\")\n        print(f\"    - name: api\")\n        print(f\"      github:\")\n        print(f\"        repo: yourusername/{self.app_name}\")\n        print(f\"        branch: main\")\n        print(f\"        deploy_on_push: true\")\n        print(f\"      dockerfile_path: Dockerfile\")\n        print(f\"      instance_count: 2\")\n        print(f\"      instance_size_slug: professional-xs\")\n        print(f\"      health_check:\")\n        print(f\"        http_path: /health\")\n        print()\n        print(\"Pros: Simple pricing, good docs, GPU droplets available\")\n        print(\"Cons: Smaller ecosystem than AWS, limited auto-scaling\")\n\n    def self_hosted(self):\n        \"\"\"Self-hosted on bare metal or VPS.\"\"\"\n        print(\"\\\\n=== Self-Hosted (VPS/Bare Metal) ===\")\n        print(\"Best for: Maximum control, GPU inference, cost optimization\")\n        print(\"Cost: $20-100/mo VPS, $100-300/mo GPU server\")\n        print()\n        print(\"Stack: Nginx + Gunicorn + FastAPI + PostgreSQL + Redis\")\n        print()\n        print(\"docker-compose.yml:\")\n        print(f\"  services:\")\n        print(f\"    app:\")\n        print(f\"      build: .\")\n        print(f\"      ports: [\\\\\"8000:8000\\\\\"]\")\n        print(f\"      environment:\")\n        print(f\"        - DATABASE_URL=postgresql://user:pass@db:5432/{self.app_name}\")\n        print(f\"        - REDIS_URL=redis://redis:6379\")\n        print(f\"      depends_on: [db, redis]\")\n        print(f\"      deploy:\")\n        print(f\"        replicas: 3\")\n        print(f\"    db:\")\n        print(f\"      image: postgres:16-alpine\")\n        print(f\"      volumes: [postgres_data:/var/lib/postgresql/data]\")\n        print(f\"    redis:\")\n        print(f\"      image: redis:7-alpine\")\n        print(f\"    nginx:\")\n        print(f\"      image: nginx:alpine\")\n        print(f\"      ports: [\\\\\"80:80\\\\\", \\\\\"443:443\\\\\"]\")\n        print(f\"      volumes: [./nginx.conf:/etc/nginx/nginx.conf]\")\n        print()\n        print(\"GPU server providers:\")\n        print(\"  - Lambda Labs: A100 ~$1.10/hr, H100 ~$2.49/hr\")\n        print(\"  - RunPod: A100 ~$1.64/hr, community GPUs from $0.20/hr\")\n        print(\"  - Vast.ai: Cheapest GPUs, ~$0.15-0.50/hr\")\n        print(\"  - Hetzner: Dedicated servers from EUR39/mo (no GPU)\")\n\n# --- Demo ---\nguide = DeploymentGuide()\nguide.railway()\nguide.render()\nguide.aws()\nguide.digitalocean()\nguide.self_hosted()\n\nprint(\"\\\\n\" + \"=\"*60)\nprint(\"RECOMMENDATION by scale:\")\nprint(\"  Hobby/MVP     -> Railway or Render (free tier)\")\nprint(\"  Startup       -> Railway or DigitalOcean ($20-50/mo)\")\nprint(\"  Growth        -> AWS ECS or DigitalOcean ($100-500/mo)\")\nprint(\"  Enterprise    -> AWS/GCP with GPU instances\")\nprint(\"  Self-hosted AI-> VPS + GPU from Lambda/RunPod/Vast.ai\")",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-11",
      "title": "rate_limiter.py &mdash; Production Rate Limiting for ClipCraft",
      "code": "\"\"\"\nProduction rate limiting for ClipCraft.\nThree tiers: in-memory (development), Redis (production), token bucket (advanced).\n\"\"\"\nimport time\nfrom collections import defaultdict\n\n# === TIER 1: In-Memory Rate Limiter (Development) ===\nclass InMemoryRateLimiter:\n    \"\"\"Simple sliding window rate limiter. Works for single-server setup.\"\"\"\n\n    def __init__(self):\n        self.requests = defaultdict(list)\n\n    def is_allowed(self, key, max_requests, window_seconds):\n        \"\"\"Check if request is allowed under the rate limit.\"\"\"\n        now = time.time()\n        window_start = now - window_seconds\n\n        # Clean old entries\n        self.requests[key] = [t for t in self.requests[key] if t > window_start]\n\n        if len(self.requests[key]) >= max_requests:\n            retry_after = self.requests[key][0] - window_start\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"limit\": max_requests,\n                \"window\": window_seconds,\n                \"retry_after\": round(retry_after, 1),\n                \"current\": len(self.requests[key])\n            }}\n\n        self.requests[key].append(now)\n        return True, {{\n            \"remaining\": max_requests - len(self.requests[key]),\n            \"limit\": max_requests,\n            \"reset\": round(window_start + window_seconds - now, 1)\n        }}\n\n# === TIER 2: Redis Rate Limiter (Production) ===\nclass RedisRateLimiter:\n    \"\"\"Redis-based rate limiter for distributed deployments.\n\n    In production, install: pip install redis\n    And use a real Redis connection.\n    \"\"\"\n\n    def __init__(self):\n        # In production: self.redis = redis.Redis(host='redis', port=6379, db=0)\n        self.memory = defaultdict(list)  # Simulating Redis\n\n    def check_rate_limit(self, user_id, tier=\"free\"):\n        \"\"\"Check rate limit based on user tier.\"\"\"\n        limits = {{\n            \"free\":       {{\"requests_per_minute\": 5,  \"requests_per_day\": 50}},\n            \"starter\":    {{\"requests_per_minute\": 20, \"requests_per_day\": 500}},\n            \"pro\":        {{\"requests_per_minute\": 60, \"requests_per_day\": 5000}},\n            \"enterprise\": {{\"requests_per_minute\": 200, \"requests_per_day\": 50000}},\n        }}\n\n        tier_limits = limits.get(tier, limits[\"free\"])\n        now = time.time()\n\n        # Check per-minute limit\n        minute_key = f\"rate:{{user_id}}:minute\"\n        self.memory[minute_key] = [t for t in self.memory[minute_key] if t > now - 60]\n\n        if len(self.memory[minute_key]) >= tier_limits[\"requests_per_minute\"]:\n            return False, {{\n                \"error\": \"rate_limit_exceeded\",\n                \"type\": \"per_minute\",\n                \"limit\": tier_limits[\"requests_per_minute\"],\n                \"retry_after_seconds\": 60 - (now - self.memory[minute_key][0])\n            }}\n\n        # Check per-day limit\n        day_key = f\"rate:{{user_id}}:day\"\n        self.memory[day_key] = [t for t in self.memory[day_key] if t > now - 86400]\n\n        if len(self.memory[day_key]) >= tier_limits[\"requests_per_day\"]:\n            return False, {{\n                \"error\": \"daily_limit_exceeded\",\n                \"type\": \"per_day\",\n                \"limit\": tier_limits[\"requests_per_day\"],\n            }}\n\n        # Allow request\n        self.memory[minute_key].append(now)\n        self.memory[day_key].append(now)\n\n        return True, {{\n            \"remaining_minute\": tier_limits[\"requests_per_minute\"] - len(self.memory[minute_key]),\n            \"remaining_day\": tier_limits[\"requests_per_day\"] - len(self.memory[day_key]),\n        }}\n\n# === FastAPI Middleware ===\nclass RateLimitMiddleware:\n    \"\"\"\n    FastAPI middleware for rate limiting.\n\n    Usage in your app:\n        from rate_limiter import RateLimitMiddleware\n        app.add_middleware(RateLimitMiddleware)\n\n    Or as a dependency:\n        @app.get(\"/generate\")\n        async def generate(rate_limit: dict = Depends(check_rate_limit)):\n            ...\n    \"\"\"\n\n    def __init__(self):\n        self.limiter = RedisRateLimiter()\n\n    def get_middleware_code(self):\n        \"\"\"Print the actual FastAPI middleware code.\"\"\"\n        code = \"# Add to your FastAPI app:\\\\n\\\\n\"\n        code += \"from fastapi import Request, Response\\\\n\"\n        code += \"from starlette.middleware.base import BaseHTTPMiddleware\\\\n\\\\n\"\n        code += \"class RateLimitMiddleware(BaseHTTPMiddleware):\\\\n\"\n        code += \"    async def dispatch(self, request, call_next):\\\\n\"\n        code += \"        api_key = request.headers.get('X-API-Key', 'anonymous')\\\\n\"\n        code += \"        user_id = self.get_user_from_key(api_key)\\\\n\"\n        code += \"        tier = self.get_user_tier(user_id)\\\\n\"\n        code += \"        allowed, info = self.limiter.check_rate_limit(user_id, tier)\\\\n\"\n        code += \"        if not allowed:\\\\n\"\n        code += \"            return JSONResponse(status_code=429, content=info)\\\\n\"\n        code += \"        response = await call_next(request)\\\\n\"\n        code += \"        response.headers['X-RateLimit-Remaining'] = str(info.get('remaining_minute', 0))\\\\n\"\n        code += \"        return response\\\\n\"\n        print(code)\n\n\n# --- Demo ---\nprint(\"=== Rate Limiting Demo for ClipCraft ===\\\\n\")\n\nlimiter = RedisRateLimiter()\n\n# Simulate requests from different users\nusers = [\n    (\"user_free_1\", \"free\"),\n    (\"user_pro_1\", \"pro\"),\n    (\"user_enterprise_1\", \"enterprise\"),\n]\n\nfor user_id, tier in users:\n    print(f\"User: {{user_id}} ({{tier}} tier)\")\n    for i in range(8):\n        allowed, info = limiter.check_rate_limit(user_id, tier)\n        status = \"ALLOWED\" if allowed else \"BLOCKED\"\n        print(f\"  Request {{i+1}}: {{status}} | {{info}}\")\n        if not allowed:\n            break\n    print()\n\n# Show middleware code\nmw = RateLimitMiddleware()\nmw.get_middleware_code()",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-12",
      "title": "billing.py &mdash; Stripe Integration for ClipCraft",
      "code": "\"\"\"\nComplete Stripe billing for ClipCraft.\nSupports: subscription tiers, usage-based billing, webhooks.\n\nSetup:\n  pip install stripe\n  # Get keys from https://dashboard.stripe.com/apikeys\n  # Set STRIPE_SECRET_KEY and STRIPE_WEBHOOK_SECRET in .env\n\"\"\"\n\nclass StripeBilling:\n    \"\"\"Full Stripe integration for SaaS billing.\"\"\"\n\n    def __init__(self, secret_key=\"sk_test_...\"):\n        # In production: import stripe; stripe.api_key = secret_key\n        self.plans = {{\n            \"free\":       {{\"price\": 0,    \"name\": \"Free\",       \"stripe_price_id\": None}},\n            \"starter\":    {{\"price\": 19,   \"name\": \"Starter\",    \"stripe_price_id\": \"price_starter_monthly\"}},\n            \"pro\":        {{\"price\": 49,   \"name\": \"Professional\",\"stripe_price_id\": \"price_pro_monthly\"}},\n            \"enterprise\": {{\"price\": 199,  \"name\": \"Enterprise\", \"stripe_price_id\": \"price_enterprise_monthly\"}},\n        }}\n\n    def create_checkout_session(self, user_email, plan_key):\n        \"\"\"Create a Stripe Checkout session for subscription.\"\"\"\n        plan = self.plans[plan_key]\n        if plan[\"price\"] == 0:\n            return {{\"status\": \"free_tier\", \"message\": \"No payment needed\"}}\n\n        # In production:\n        # session = stripe.checkout.Session.create(\n        #     customer_email=user_email,\n        #     payment_method_types=[\"card\"],\n        #     line_items=[{{\n        #         \"price\": plan[\"stripe_price_id\"],\n        #         \"quantity\": 1,\n        #     }}],\n        #     mode=\"subscription\",\n        #     success_url=\"https://{product_name.lower()}.com/billing/success?session_id={{CHECKOUT_SESSION_ID}}\",\n        #     cancel_url=\"https://{product_name.lower()}.com/billing/cancel\",\n        #     metadata={{\"user_email\": user_email, \"plan\": plan_key}},\n        # )\n        # return {{\"checkout_url\": session.url, \"session_id\": session.id}}\n\n        print(f\"Creating checkout for {{user_email}} -> {{plan['name']}} (\\${{plan['price']}}/mo)\")\n        return {{\n            \"checkout_url\": f\"https://checkout.stripe.com/pay/cs_test_...\",\n            \"plan\": plan[\"name\"],\n            \"price\": f\"\\${{plan['price']}}/month\"\n        }}\n\n    def create_usage_record(self, subscription_item_id, quantity, action=\"increment\"):\n        \"\"\"Record usage for metered billing (pay-per-use on top of subscription).\"\"\"\n        # In production:\n        # stripe.SubscriptionItem.create_usage_record(\n        #     subscription_item_id,\n        #     quantity=quantity,\n        #     action=action,\n        #     timestamp=int(time.time())\n        # )\n        print(f\"Usage recorded: {{quantity}} units on {{subscription_item_id}}\")\n        return {{\"status\": \"recorded\", \"quantity\": quantity}}\n\n    def handle_webhook(self, event_type, data):\n        \"\"\"Handle Stripe webhook events.\"\"\"\n        handlers = {{\n            \"checkout.session.completed\": self._on_checkout_complete,\n            \"customer.subscription.updated\": self._on_subscription_update,\n            \"customer.subscription.deleted\": self._on_subscription_cancel,\n            \"invoice.payment_failed\": self._on_payment_failed,\n            \"invoice.paid\": self._on_invoice_paid,\n        }}\n\n        handler = handlers.get(event_type)\n        if handler:\n            return handler(data)\n        return {{\"status\": \"unhandled\", \"event\": event_type}}\n\n    def _on_checkout_complete(self, data):\n        print(f\"  Checkout completed! Activate subscription for user.\")\n        return {{\"action\": \"activate_subscription\"}}\n\n    def _on_subscription_update(self, data):\n        print(f\"  Subscription updated. Sync plan tier in database.\")\n        return {{\"action\": \"sync_tier\"}}\n\n    def _on_subscription_cancel(self, data):\n        print(f\"  Subscription canceled. Downgrade to free tier.\")\n        return {{\"action\": \"downgrade_to_free\"}}\n\n    def _on_payment_failed(self, data):\n        print(f\"  Payment failed! Send dunning email, grace period 7 days.\")\n        return {{\"action\": \"send_dunning_email\", \"grace_days\": 7}}\n\n    def _on_invoice_paid(self, data):\n        print(f\"  Invoice paid. Reset monthly usage counters.\")\n        return {{\"action\": \"reset_usage\"}}\n\n    def get_fastapi_webhook_code(self):\n        \"\"\"Print the FastAPI webhook endpoint code.\"\"\"\n        code = \"# FastAPI webhook endpoint:\\\\n\\\\n\"\n        code += \"@app.post('/webhooks/stripe')\\\\n\"\n        code += \"async def stripe_webhook(request: Request):\\\\n\"\n        code += \"    payload = await request.body()\\\\n\"\n        code += \"    sig_header = request.headers.get('stripe-signature')\\\\n\"\n        code += \"    try:\\\\n\"\n        code += \"        event = stripe.Webhook.construct_event(\\\\n\"\n        code += \"            payload, sig_header, STRIPE_WEBHOOK_SECRET)\\\\n\"\n        code += \"    except stripe.error.SignatureVerificationError:\\\\n\"\n        code += \"        raise HTTPException(400, 'Invalid signature')\\\\n\"\n        code += \"    billing = StripeBilling()\\\\n\"\n        code += \"    result = billing.handle_webhook(event['type'], event['data']['object'])\\\\n\"\n        code += \"    return {'status': 'ok', 'result': result}\\\\n\"\n        print(code)\n\n    def get_pricing_page_data(self):\n        \"\"\"Generate pricing page data.\"\"\"\n        features = {{\n            \"free\": [\n                \"50 requests/day\",\n                \"Basic quality\",\n                \"Community support\",\n                \"Watermarked output\",\n            ],\n            \"starter\": [\n                \"500 requests/day\",\n                \"Standard quality\",\n                \"Email support\",\n                \"No watermark\",\n                \"API access\",\n            ],\n            \"pro\": [\n                \"5,000 requests/day\",\n                \"Highest quality\",\n                \"Priority support\",\n                \"4K resolution, custom transitions\",\n                \"Batch processing\",\n                \"Webhooks\",\n            ],\n            \"enterprise\": [\n                \"50,000 requests/day\",\n                \"Custom model fine-tuning\",\n                \"Dedicated support\",\n                \"SLA guarantee\",\n                \"SSO/SAML\",\n                \"On-premise option\",\n            ],\n        }}\n        return features\n\n\n# --- Demo ---\nprint(\"=== Stripe Billing for ClipCraft ===\\\\n\")\n\nbilling = StripeBilling()\n\n# 1. Show pricing tiers\nprint(\"Pricing Tiers:\")\nfeatures = billing.get_pricing_page_data()\nfor tier, plan in billing.plans.items():\n    print(f\"\\\\n  {{plan['name']}} - \\${{plan['price']}}/month\")\n    for f in features[tier]:\n        print(f\"    - {{f}}\")\n\n# 2. Create checkout session\nprint(\"\\\\n\\\\n--- Creating Checkout ---\")\nresult = billing.create_checkout_session(\"user@example.com\", \"pro\")\nprint(f\"Result: {{result}}\")\n\n# 3. Simulate webhook events\nprint(\"\\\\n--- Webhook Events ---\")\nevents = [\n    (\"checkout.session.completed\", {{}}),\n    (\"invoice.paid\", {{}}),\n    (\"customer.subscription.updated\", {{}}),\n    (\"invoice.payment_failed\", {{}}),\n    (\"customer.subscription.deleted\", {{}}),\n]\nfor event_type, data in events:\n    print(f\"\\\\nEvent: {{event_type}}\")\n    billing.handle_webhook(event_type, data)\n\n# 4. Usage-based billing\nprint(\"\\\\n\\\\n--- Usage-Based Billing ---\")\nbilling.create_usage_record(\"si_test_123\", 25, \"increment\")\n\n# 5. Show webhook code\nprint(\"\\\\n--- Webhook Endpoint Code ---\")\nbilling.get_fastapi_webhook_code()",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-13",
      "title": "scaling.py &mdash; ClipCraft Scaling Architecture",
      "code": "\"\"\"\nScaling ClipCraft from 100 to 1,000,000 users.\nCovers: horizontal scaling, queue-based processing, caching, monitoring.\n\"\"\"\n\nclass ScalingPlaybook:\n    \"\"\"Complete scaling guide from MVP to enterprise.\"\"\"\n\n    def __init__(self, product=\"ClipCraft\"):\n        self.product = product\n\n    def stage_1_mvp(self):\n        \"\"\"Stage 1: 0-100 users, single server.\"\"\"\n        print(\"=== Stage 1: MVP (0-100 users) ===\")\n        print(\"Architecture: Single server, SQLite/PostgreSQL\")\n        print(\"Cost: $5-20/month\")\n        print()\n        print(\"Stack:\")\n        print(\"  - 1x server (Railway/Render free tier)\")\n        print(\"  - SQLite or managed PostgreSQL\")\n        print(\"  - In-memory caching (Python dict)\")\n        print(\"  - Direct API calls (no queue)\")\n        print()\n        print(\"Focus on:\")\n        print(\"  1. Validate product-market fit\")\n        print(\"  2. Get 10 paying customers\")\n        print(\"  3. Measure: response time, error rate, user retention\")\n\n    def stage_2_growth(self):\n        \"\"\"Stage 2: 100-1K users, add resilience.\"\"\"\n        print(\"\\\\n=== Stage 2: Growth (100-1,000 users) ===\")\n        print(\"Architecture: App server + Background workers + Redis\")\n        print(\"Cost: $50-200/month\")\n        print()\n        print(\"Changes from Stage 1:\")\n        print(\"  1. Add Redis for caching and rate limiting\")\n        print(\"  2. Move heavy work to background queues (Celery/ARQ)\")\n        print(\"  3. Add health checks and basic monitoring\")\n        print(\"  4. Set up CI/CD pipeline\")\n        print()\n        print(\"docker-compose additions:\")\n        print(\"  redis:\")\n        print(\"    image: redis:7-alpine\")\n        print(\"  worker:\")\n        print(\"    build: .\")\n        print(\"    command: arq app.worker.WorkerSettings\")\n        print(\"    depends_on: [redis]\")\n        print()\n        print(\"Queue pattern for heavy tasks:\")\n        print(\"  @app.post('/generate')\")\n        print(\"  async def generate(request: GenerateRequest):\")\n        print(\"      job_id = str(uuid.uuid4())\")\n        print(\"      await redis.enqueue_job('process_generation',\")\n        print(\"          job_id=job_id, prompt=request.prompt)\")\n        print(\"      return {{'job_id': job_id, 'status': 'processing'}}\")\n        print()\n        print(\"  @app.get('/jobs/{{job_id}}')\")\n        print(\"  async def get_job(job_id: str):\")\n        print(\"      result = await redis.get(f'job:{{job_id}}')\")\n        print(\"      return result or {{'status': 'processing'}}\")\n\n    def stage_3_scale(self):\n        \"\"\"Stage 3: 1K-10K users, horizontal scaling.\"\"\"\n        print(\"\\\\n=== Stage 3: Scale (1,000-10,000 users) ===\")\n        print(\"Architecture: Load balancer + Multiple app servers + Queue\")\n        print(\"Cost: $200-1,000/month\")\n        print()\n        print(\"Architecture diagram:\")\n        print(\"  Users -> CDN (Cloudflare)\")\n        print(\"       -> Load Balancer (nginx/ALB)\")\n        print(\"       -> App Server 1 \\\\ \")\n        print(\"       -> App Server 2  |-> PostgreSQL (managed)\")\n        print(\"       -> App Server 3 /    |-> Redis Cluster\")\n        print(\"       -> Worker 1 \\\\         |-> S3 (file storage)\")\n        print(\"       -> Worker 2  |-> Queue (Redis/SQS)\")\n        print(\"       -> Worker 3 /\")\n        print()\n        print(\"Key changes:\")\n        print(\"  1. Multiple app servers behind load balancer\")\n        print(\"  2. Managed database (RDS/Cloud SQL)\")\n        print(\"  3. Separate worker pool for heavy processing\")\n        print(\"  4. CDN for static assets and cached responses\")\n        print(\"  5. Structured logging + APM (Datadog/Grafana)\")\n        print()\n        print(\"nginx load balancer config:\")\n        print(\"  upstream api {{\")\n        print(\"      least_conn;\")\n        print(\"      server app1:8000;\")\n        print(\"      server app2:8000;\")\n        print(\"      server app3:8000;\")\n        print(\"  }}\")\n        print(\"  server {{\")\n        print(\"      listen 80;\")\n        print(\"      location / {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          proxy_set_header X-Real-IP $remote_addr;\")\n        print(\"      }}\")\n        print(\"      location /health {{\")\n        print(\"          proxy_pass http://api;\")\n        print(\"          access_log off;\")\n        print(\"      }}\")\n        print(\"  }}\")\n\n    def stage_4_mass(self):\n        \"\"\"Stage 4: 10K+ users, enterprise features.\"\"\"\n        print(\"\\\\n=== Stage 4: Mass Product (10,000+ users) ===\")\n        print(\"Architecture: Microservices or modular monolith\")\n        print(\"Cost: $1,000-10,000/month\")\n        print()\n        print(\"Enterprise features to add:\")\n        print(\"  1. Multi-tenant architecture (org-level isolation)\")\n        print(\"  2. SSO/SAML authentication\")\n        print(\"  3. Audit logging\")\n        print(\"  4. Data residency (EU/US regions)\")\n        print(\"  5. Custom SLAs and dedicated support\")\n        print(\"  6. API versioning (v1, v2)\")\n        print(\"  7. Webhook system for integrations\")\n        print(\"  8. Admin dashboard with analytics\")\n        print()\n        print(\"Monitoring stack:\")\n        print(\"  - Prometheus + Grafana (metrics)\")\n        print(\"  - Sentry (error tracking)\")\n        print(\"  - PostHog or Mixpanel (product analytics)\")\n        print(\"  - PagerDuty (alerting)\")\n        print()\n        print(\"SLA targets:\")\n        print(\"  - Uptime: 99.9% (8.7 hours downtime/year)\")\n        print(\"  - API latency p99: < 2 seconds\")\n        print(\"  - Error rate: < 0.1%\")\n\n    def growth_metrics(self):\n        \"\"\"Key metrics to track at each stage.\"\"\"\n        print(\"\\\\n=== Key Metrics Dashboard ===\")\n        print()\n        metrics = [\n            (\"MRR (Monthly Recurring Revenue)\", \"$0\", \"$500\", \"$5K\", \"$50K+\"),\n            (\"Active Users (DAU)\", \"10\", \"100\", \"1,000\", \"10,000+\"),\n            (\"API Requests/day\", \"100\", \"5K\", \"100K\", \"1M+\"),\n            (\"Error Rate\", \"<5%\", \"<1%\", \"<0.1%\", \"<0.01%\"),\n            (\"P99 Latency\", \"<5s\", \"<3s\", \"<2s\", \"<1s\"),\n            (\"Churn Rate (monthly)\", \"N/A\", \"<10%\", \"<5%\", \"<3%\"),\n        ]\n        print(f\"| {{'Metric':<30}} | {{'Stage 1':<8}} | {{'Stage 2':<8}} | {{'Stage 3':<8}} | {{'Stage 4':<10}} |\")\n        print(f\"|{{'-'*30}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*8}}-|{{'-'*10}}-|\")\n        for name, *stages in metrics:\n            print(f\"| {{name:<30}} | {{stages[0]:<8}} | {{stages[1]:<8}} | {{stages[2]:<8}} | {{stages[3]:<10}} |\")\n\n# --- Demo ---\nplaybook = ScalingPlaybook()\nplaybook.stage_1_mvp()\nplaybook.stage_2_growth()\nplaybook.stage_3_scale()\nplaybook.stage_4_mass()\nplaybook.growth_metrics()\n\nprint(\"\\\\n\" + \"=\"*60)\nprint(f\"Summary: Build ClipCraft as MVP first.\")\nprint(\"Get 10 paying customers before optimizing.\")\nprint(\"Scale horizontally only when metrics demand it.\")\nprint(\"Monitor everything from day one.\")",
      "language": "python",
      "runnable": true
    },
    {
      "id": "block-prod-checklist",
      "title": "Production Deployment Checklist",
      "code": "# ============================================\n# PRODUCTION DEPLOYMENT CHECKLIST\n# ============================================\n# This checklist covers what you need to add\n# before deploying this SaaS to real users.\n# ============================================\n\n# 1. AUTHENTICATION & API KEYS\n# -----------------------------\n# Replace the in-memory billing dict with:\n#   - User registration (email/password or OAuth)\n#   - Password hashing (bcrypt or argon2)\n#   - JWT token generation & validation\n#   - API key generation (uuid4 or secrets.token_urlsafe)\n#   - Rate limiting per user/tier\n#\n# Example auth middleware:\n# from fastapi import Depends, HTTPException, Security\n# from fastapi.security import HTTPBearer, HTTPAuthorizationCredentials\n# security = HTTPBearer()\n# async def verify_token(cred: HTTPAuthorizationCredentials = Security(security)):\n#     payload = jwt.decode(cred.credentials, SECRET_KEY, algorithms=[\"HS256\"])\n#     return payload\n\n# 2. DATABASE PERSISTENCE\n# ------------------------\n# Replace Python dicts with SQLite (minimum) or PostgreSQL:\n#   - Users table: id, email, hashed_password, api_key, tier, created_at\n#   - Usage table: id, user_id, endpoint, tokens_used, timestamp\n#   - Jobs table (if async): id, user_id, status, result_url, created_at\n#\n# Minimum SQLite setup:\n# import sqlite3\n# db = sqlite3.connect(\"app.db\", check_same_thread=False)\n# db.execute(\"\"\"CREATE TABLE IF NOT EXISTS users (\n#     id INTEGER PRIMARY KEY, email TEXT UNIQUE,\n#     api_key TEXT UNIQUE, tier TEXT DEFAULT 'free',\n#     usage_today INTEGER DEFAULT 0, created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP\n# )\"\"\")\n\n# 3. ENVIRONMENT VARIABLES\n# -------------------------\n# Never hardcode secrets. Use .env + python-dotenv:\n#   API_KEY=your-llm-api-key\n#   JWT_SECRET=your-jwt-secret\n#   STRIPE_SECRET_KEY=sk_live_...\n#   DATABASE_URL=postgresql://...\n\n# 4. ERROR HANDLING & LOGGING\n# ----------------------------\n# Add structured logging and error tracking:\n# import logging\n# logging.basicConfig(level=logging.INFO, format=\"%(asctime)s %(levelname)s %(message)s\")\n# Consider: Sentry for error tracking, structured JSON logs for production\n\n# 5. PROJECT-SPECIFIC ITEMS\n# - Use Redis or RabbitMQ for job queue (replace in-memory)\n# - Store videos in S3/R2 with signed URLs (expiring links)\n# - Add webhook notifications for job completion\n# - Implement video length limits per tier\n# - Add progress streaming via WebSocket (replace polling)\n\n# 6. DEPLOYMENT\n# --------------\n# Docker: Use the provided Dockerfile\n# Cloud options: Railway, Render, Fly.io, AWS ECS\n# Always set: --workers 4 --timeout 120\n# Add health check endpoint: GET /health -> {\"status\": \"ok\"}\n# Set up HTTPS (handled by most cloud platforms)\n# Configure CORS for your frontend domain",
      "language": "python",
      "runnable": false
    }
  ]
}